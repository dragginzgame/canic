# 0.56 V1 Release Probe Inventory

0.56 keeps only release probes with a clear packaged or installed v1 question.
This is an inventory, not a new product surface.

## Retained Probes

| Script / target | Release question | Installed CLI? | Packaged crates? | Temp root? | Network? |
| --- | --- | --- | --- | --- | --- |
| `scripts/ci/verify-installed-canic-cli.sh` / `make test-installed-canic-cli` | Does the installed `canic` binary run the maintained source-role, Fleet ensure help, and evidence smoke from a temp workspace? See `docs/operations/0.56-installed-cli-smoke.md`. | Yes | No | Yes | No |
| `scripts/ci/verify-packaged-downstream-cli.sh` / `make test-packaged-downstream-cli` | Can the unpacked packaged CLI and supporting crates inspect downstream source roles and expose Fleet ensure without unpublished local crate dependencies? See `docs/operations/0.56-packaged-downstream-cli.md`. | No | Yes | Yes | No |
| `scripts/ci/verify-packaged-downstream-wasm-store.sh` / `make test-packaged-downstream-wasm-store` | Can the special packaged downstream `wasm_store` bootstrap paths build from packaged Canic crates outside the repository package graph? See `docs/operations/0.56-packaged-wasm-store.md`. | No | Yes | Yes | No |

## Probe Boundaries

The installed CLI proof must exercise the installed binary by default, not
`target/debug/canic`.

It also isolates `HOME`, `CARGO_HOME`, `CARGO_TARGET_DIR`, and `TMPDIR` under
the proof root before running the readiness smoke.

The packaged downstream proofs may use the repository to create package
archives, but downstream execution must resolve through the temporary package
root or installed binary path, not repository-local crate paths.

The packaged downstream CLI proof also checks current Fleet-scoped role
declarations, role inspection, and Fleet ensure help from a downstream
workspace root.

The `wasm_store` probe is intentionally special. It protects the generated
bootstrap wrapper fallback and canonical packaged sibling paths, and is not
ordinary guidance for downstream canister crate dependencies.

The packaged `wasm_store` proof also verifies that generated wrapper metadata
patches packaged Canic sibling crates instead of repository crate paths.
