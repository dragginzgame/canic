# Canic 0.82 Control-Plane Prelude Support Boundary Report

Date: 2026-07-06

## BoundaryHardeningReport

```text
schema_version: 1
slice: 0.82 Control-Plane Prelude Support Boundary
boundary: control-plane bootstrap imports needed value types explicitly instead of wildcard workflow prelude support
status: pass_with_followups
behavior_impact: no_behavior_change
```

## Slice Contract

Boundary:
The hidden control-plane support facade should expose specific owner-shaped
paths, not a broad workflow prelude wildcard, when a sibling crate needs only
one value type.

Current owner:
`canic-core::control_plane_support::workflow::prelude` re-exported the entire
core workflow prelude to the control-plane crate.

Intended owner:
`canic-core::control_plane_support::cdk::types` exposes `Principal` to the
control-plane crate. Other workflow owners stay exposed through their specific
support modules.

Files/modules in scope:

- `crates/canic-core/src/control_plane_support.rs`
- `crates/canic-control-plane/src/workflow/bootstrap/root.rs`

Files/modules explicitly out of scope:

- Core workflow prelude contents.
- Root bootstrap behavior.
- Public endpoint, CLI, JSON, Candid, deployment-truth, or evidence surfaces.

Public surfaces affected:
None intended. The removed prelude support path was a hidden sibling-crate
support shortcut.

Serialized surfaces affected:
None.

Commands/endpoints affected:
None.

Expected behavior change:
None.

Expected non-changes:

- Root bootstrap still uses the same `Principal` type.
- Root bootstrap still imports workflow owners through specific support
  modules.
- Bootstrap logging and topology/pool behavior remain unchanged.

Validation:

- `cargo fmt --all -- --check` passed.
- `cargo check --locked -p canic-core --lib` passed.
- `cargo check --locked -p canic-control-plane --all-targets` passed.

Rollback plan:
Restore `control_plane_support::workflow::prelude` and move root bootstrap back
to the wildcard prelude support import.

Follow-ups:

- Keep hidden support paths narrow and owner-shaped.
- Avoid adding wildcard support facades where explicit support imports are
  enough.

## Findings

### CANIC-082-CONTROL-PLANE-PRELUDE-001

Severity: P3
Category: workflow_ownership
Owner: `canic-core::control_plane_support::workflow::prelude`
Current location: `crates/canic-core/src/control_plane_support.rs`
Intended location: `crates/canic-core::control_plane_support::cdk::types`

Evidence:
Root bootstrap imported `control_plane_support::workflow::prelude::*`, but the
only symbol it needed from that wildcard import was `Principal`.

Risk:
The wildcard support path made it harder to see which core boundary values the
control-plane bootstrap phase actually needed.

Decision:
Fixed with a no-behavior-change import cleanup. Root bootstrap imports
`Principal` through the explicit CDK support path, and the hidden wildcard
workflow prelude support module was removed.

Regression test:
Control-plane all-target compile coverage verifies root bootstrap imports and
workflow support paths after the cleanup.

## Fixes

- Removed the hidden `control_plane_support::workflow::prelude` wildcard
  support module.
- Updated root bootstrap to import `Principal` from
  `control_plane_support::cdk::types`.

## Deferred

- Core workflow prelude remains unchanged.
- Root bootstrap behavior remains unchanged.
