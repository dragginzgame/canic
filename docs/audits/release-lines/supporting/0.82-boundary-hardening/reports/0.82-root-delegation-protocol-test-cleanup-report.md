# Canic 0.82 Root Delegation Protocol Test Cleanup Report

Date: 2026-07-07

## BoundaryHardeningReport

```text
schema_version: 1
slice: 0.82.37 Root Delegation Protocol Test Cleanup
boundary: protocol-surface tests should pin maintained root delegation endpoints only
status: pass
behavior_impact: no_behavior_change
```

## Slice Contract

Boundary:
Root delegation protocol-surface tests should assert the maintained root issuer
policy, renewal, renewal-status, and chain-key lazy-repair endpoint bindings
without embedding retired single-proof endpoint names as absence guards.

Current owner:
`canic::tests::protocol_surface` pinned the maintained root delegation endpoint
bundle but also carried a test-only assertion that retired single-proof
delegation endpoint function names were absent from the source file.

Intended owner:
Protocol-surface tests assert current protocol constants, endpoint guards,
endpoint bindings, and DTO Candid round-trips. Removed endpoint names are not
kept in active tests as anti-resurrection fixtures.

Files/modules in scope:

- `crates/canic/tests/protocol_surface.rs`
- `docs/status/current.md`

Files/modules explicitly out of scope:

- Root delegation endpoint implementations.
- Protocol constants.
- Root delegation DTOs and Candid shape.
- Replay policy.
- Auth verifier behavior.
- CLI command names, flags, or help output.
- JSON, deployment truth, evidence/report, and stable-state schemas.

Public surfaces affected:
None.

Serialized surfaces affected:
None.

Commands/endpoints affected:
None.

Expected behavior change:
None.

Expected non-changes:

- Root issuer policy, renewal-template, renewal-status, and chain-key
  lazy-repair endpoint bindings remain pinned.
- Root delegation endpoint guards remain pinned.
- Root delegation DTO Candid round-trips remain covered.
- No operator, endpoint, Candid, JSON, deployment truth, evidence/report, or
  stable-state surface changed.

Validation:

- `cargo test --locked -p canic --test protocol_surface` passed.
- `cargo clippy --locked -p canic --test protocol_surface -- -D warnings` passed.
- `cargo fmt --all -- --check` passed.
- `cargo test --locked -p canic --test changelog_governance` passed.
- `git diff --check` passed.
- `rg -n "canic_prepare_delegation_proof\\(|canic_get_delegation_proof\\(|legacy single-proof|single-proof root delegation|must stay removed|are_removed_from_manifest|removed_from_manifest|anti-resurrection|resurrection" crates -S` returned no matches.

Rollback plan:
Restore removed endpoint-name absence assertions only if the maintainer
explicitly asks to track removed root delegation surfaces in active tests. Do
not add anti-resurrection tests by default.

Follow-ups:

- Continue scanning active tests for old-surface absence assertions.
- Keep positive protocol tests focused on maintained protocol constants,
  bindings, guards, and DTO shapes.

## Findings

### CANIC-082-ROOT-DELEGATION-PROTOCOL-CLEANUP-001

Severity: P3
Category: test_ownership
Owner: `canic::tests::protocol_surface`
Current location: `crates/canic/tests/protocol_surface.rs`
Intended location: maintained root delegation protocol-surface assertions

Evidence:
The root delegation protocol-surface test asserted that
`canic_prepare_delegation_proof` and `canic_get_delegation_proof` function
names were absent from the root endpoint macro source.

Risk:
Retired endpoint names remained embedded in active tests after the pre-1.0
hard-cut policy ruled out anti-resurrection coverage for removed surfaces.

Decision:
Removed the retired endpoint-name absence assertion while keeping positive
checks for maintained root delegation protocol constants, endpoint guards,
endpoint bindings, and Candid DTO round-trips.

Regression test:
`cargo test --locked -p canic --test protocol_surface`
