# Canic 0.82 Active Source Release Comment Cleanup Report

Date: 2026-07-06

## BoundaryHardeningReport

```text
schema_version: 1
slice: 0.82 Active Source Release Comment Cleanup
boundary: active source comments describe current contracts without stale release-line labels
status: pass_with_followups
behavior_impact: no_behavior_change
```

## Slice Contract

Boundary:
Active source comments and lint-expectation reasons should describe current
contracts directly unless the release number is intentional provenance.

Current owner:
The medic command owns its report categories and exit-code tests. A few active
comments still named the original 0.78 design even though they describe current
medic behavior.

Intended owner:
Active source comments use release-neutral wording for current contracts.
Release numbers remain appropriate in design docs, changelogs, fixture version
values, and code paths that intentionally enforce a named historical hard-cut
contract.

Files/modules in scope:

- `crates/canic-cli/src/medic/mod.rs`
- `crates/canic-cli/src/medic/tests.rs`

Files/modules explicitly out of scope:

- Historical design documents and changelogs.
- Auth hard-cut error strings that intentionally refer to the 0.76 contract.
- Runtime/deploy-plan test fixture version values.
- CLI behavior, report fields, and status derivation.

Public surfaces affected:
None.

Serialized surfaces affected:
None.

Commands/endpoints affected:
None.

Expected behavior change:
None.

Expected non-changes:

- Medic report categories are unchanged.
- Medic exit-code behavior is unchanged.
- No command aliases, compatibility shims, or anti-resurrection tests are
  added.

Validation:

- `cargo fmt --all` passed.
- `cargo test --locked -p canic-cli medic --lib` passed.
- `cargo clippy --locked -p canic-cli --tests -- -D warnings` passed.
- `cargo fmt --all -- --check` passed.
- `rg -n "0\.78 report schema|0\.78 report contract|0\.78 design" crates/canic-cli/src/medic* -S` returned no stale active medic source comments.
- `git diff --check` passed.

Rollback plan:
Restore the previous wording if maintainers want active medic source comments
to name the release line that introduced the contract.

Follow-ups:

- Keep intentionally historical release references in design docs and
  changelogs.

## Findings

### CANIC-082-ACTIVE-SOURCE-RELEASE-COMMENT-001

Severity: P3
Category: source_comment
Owner: medic command source
Current location: active medic comments and lint-expectation reasons
Intended location: release-neutral current-contract comments

Evidence:
Active medic source comments referred to the "0.78 report schema", "0.78
report contract", and "0.78 design" when describing current reserved
categories, aggregate status, and exit-code behavior.

Risk:
Stale release labels in active source comments can make maintained behavior
look historical or tied to a previous release line.

Decision:
Fixed with no-behavior-change comment cleanup. Historical release references
remain outside active source where they are intentional provenance.

Regression test:
Covered by focused medic tests and clippy.
