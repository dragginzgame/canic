# Canic 0.98 Immutable Baseline And Inventory

Date: 2026-07-22

```text
release_anchor: v0.97.3
source_commit_full: 4f4397cd58b648759307b51d98033c7c21538345
source_tree_hash: 0efda05cd46e94c1c45d6a37f6a0270fa8b7bd0c
product_tree_hash: e540970e5aad935a2f4c5aff5ff43c790beb1958d6e33fb5f801ba6c050cc03d
cargo_lock_sha256: bcf041e99d7ead0f1d4419251f4fe5cd24d11604dbb15002330562e37dc547bd
workspace_package_version: 0.97.3
final_receipt_contract: v0.96.8
worktree_at_inventory_start: clean
```

## Verdict

The three 0.98 findings remain present and bounded at the immutable post-0.97
baseline. The 0.97 dependency, macro, public-facade, and root-refill work did
not create a consumer for any proposed deletion and did not alter the final
0.96 receipt-backed conformance fixture. The 0.98 implementation prerequisite
is satisfied.

## Deletion And Reachability Inventory

### Dead project protocol package

`project-protocol-stub` is one workspace member and one lockfile package with
two public role constants. No Cargo manifest depends on it and no Rust source,
fixture, script, example, or active documentation imports its package or
constants. The only live values with those meanings are the private
`PROJECT_HUB_ROLE` and `PROJECT_INSTANCE_ROLE` constants beside their sole
consumer in `delegation_root_stub`.

The complete deletion set is:

- `canisters/test/project_protocol_stub/Cargo.toml`;
- `canisters/test/project_protocol_stub/src/lib.rs`;
- its root workspace-member row; and
- its `Cargo.lock` package row.

No replacement shared package, forwarding module, or compatibility target is
required.

### Obsolete external LocalIntent fixture

The externally uncertain flow is confined to:

- workspace packages `intent_client` and `intent_external`;
- external-principal init/state and the `buy` endpoint in
  `intent_authority`;
- the concurrent `call_buy` setup and assertion in `pic_intent_race`; and
- the test target name in the workspace test script and current operations
  documentation.

No production package calls those test endpoints. Product `LocalIntent` and
`ReceiptBackedIntent` APIs, records, allocations, timers, and cleanup remain
outside the deletion. The installed receipt-backed conformance portion of the
test is unchanged between `v0.96.8` and `v0.97.3`.

### Accepted but unexecuted randomness configuration

The complete active path remains:

```text
CONFIG.md
  -> RandomnessConfig / RandomnessSource schema and defaults
  -> validation
  -> generated compiled CanisterConfig.randomness
  -> host role-detail projection
  -> no runtime consumer
```

No tracked active fleet configuration selects the randomness table. The
management `raw_rand` adapter is reached only by its ops wrapper; no workflow,
lifecycle owner, timer, endpoint, or application path calls the wrapper. Its
invalid-length error and `RawRand` management/system metric variants therefore
also have no reachable producer.

The deletion affects configuration, generated Rust, host role details,
documentation, unreachable management-call code, metric variants, and their
tests. It affects no Candid, stable allocation/schema, backup/restore format,
CLI command/JSON schema, or runtime randomness behavior because no such
behavior exists.

## Frozen Final-0.96 Receipt Assertion Manifest

The surviving real-Wasm conformance must retain these exact observations when
the obsolete race is removed:

| Area | Exact retained proof |
| --- | --- |
| Admission and exact replay | A new operation returns `Created`; an exact retry returns `ExistingPending`; payload, quantity, or one-nanosecond deadline changes return `BindingConflict`. |
| Replay boundary | An absent operation at `deadline == now` returns `ReplayWindowClosed` without a row; a remaining window above 3,600,000,000,000 ns returns `ReplayWindowTooLong` and reports that exact maximum. |
| Capacity | With resource limit 1, a second unit for the same resource returns `CapacityExceeded { current_quantity: 1, requested_quantity: 1, limit: 1 }`; rollback releases that quantity for a later operation. |
| Settlement binding | Missing identity returns `NotFound`; stale revision reports the actual revision; payload mismatch returns `BindingConflict`; each leaves the pending row unchanged. |
| Commit replay | Commit advances revision 1 to 2 with exact source/fingerprint evidence; repeated settlement is `AlreadySettled`; begin is `ExistingCommitted`; contradictory later settlement fails without mutation. |
| Rollback replay | Rollback advances revision 1 to 2 with exact source/fingerprint evidence; repeated settlement is `AlreadySettled`; begin is `ExistingRolledBack`; capacity is released exactly. |
| Upgrade | The pending record, payload, quantity, revision, replay metadata, and resource accounting survive upgrade of the same installed authority Wasm. |
| Reclamation | After the one-hour deadline plus the 15-minute observation rule, eligible committed and rolled-back rows are absent, pending work remains, a reclaimed expired identity closes rather than recreates, and committed resource quantity still constrains admission. |

The focused core proof retained with that journey includes the exact 1,000-row
store limit, terminal eligibility, ordered bounded reclamation, index
reconciliation, corruption rejection, and shared/application capacity
accounting. Slice B must preserve those current tests and the real-Wasm table
above; it must not retain the old external race merely to keep those proofs.

## Contract Boundaries

- Product Candid: unchanged by all three deletions.
- Test Candid: the obsolete `buy`, `call_buy`, and `perform` endpoints and the
  principal-valued authority init disappear in Slice B.
- Product stable state: unchanged. Obsolete test-only LocalIntent rows may
  disappear with the fixture; no fixture-owned stable allocation or decoder
  exists.
- Configuration: the entire randomness subtree becomes an unknown field in
  Slice C; no ignored field or compatibility parser is permitted.
- Cargo packages: one package disappears in Slice A and two in Slice B.
- Root manual refill: unchanged and outside 0.98.

## Slice Authorization

Slice A may delete only the dead project-protocol package and its workspace and
lock rows. Slice B and Slice C remain separate reviewable batches governed by
the frozen inventories above.
