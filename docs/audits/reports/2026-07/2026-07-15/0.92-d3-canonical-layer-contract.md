# 0.92 D3 Canonical Layer Contract

## Verdict

D3 passes. `v0.92.2` fixes `CANIC-092-LAYERING-003` and
`CANIC-092-DOCS-001` by making the strict `AGENTS.md` layer contract the sole
active authority and aligning public `canic-core` documentation with it.

This is a documentation-only slice. It changes no runtime behavior, public or
serialized contract, stable state, dependency, feature, or generated artifact.
The existing product-code layering findings remain open and are not concealed
by this authority correction.

## Slice Contract

```text
finding_ids: CANIC-092-LAYERING-003; CANIC-092-DOCS-001
authority: AGENTS.md strict dependency direction is canonical
behavior_impact: no_behavior_change
serialized_surfaces: none
stable_state: none
deletion_list: wording that permits API-to-ops delegation or assigns
  authoritative state/storage invariants to a competing layer
rerun: layer-boundary; module-structure; publish-surface
non_scope: runtime code movement; layer exceptions; product finding closure
```

## Identity and Comparison

```text
release_parent: v0.92.1
parent_commit: 9d81ee8c229dddad5681e4144a87b827886b8d3a
parent_product_tree_hash: 613d74fee5dd101add830233d0e77aa354c9f53f88c585f8c32cd63c972cb2b2
frozen_baseline_commit: 91736337fc1cfeb891f17d7d62affb5e671348e2
frozen_baseline_product_tree_hash: c2b932cfda4cd3060d8fb171a6005595c8c9e6c8b65d8bfd8ae34a4516e0802e
fix_and_validation_commit: 7e9c12785675781fa935b35ee78735bf2168280d
release_anchor: v0.92.2
release_source_tree: e7bf5705d1d61837779b8b543534b3dc03c27f50
release_product_tree_hash: 4d785d7926c7d62d85ce792f81ef6dfa331dbcb1eaef2257994c1a408722fa8c
```

D2 and D3 were committed together in `v0.92.2`, so D3 causality is established
by its exact documentation and module-header path review rather than a separate
commit. The cumulative comparison remains against the frozen baseline and the
committed `v0.92.1` parent.

## Canonical Authority

- `AGENTS.md` remains normative; `docs/contracts/ARCHITECTURE.md` now states
  that it mirrors that contract and must be corrected on divergence.
- The only documented dependency direction is
  `endpoints -> workflow -> policy -> ops -> model`.
- Endpoints authenticate/marshal and delegate immediately to workflow; active
  API wording no longer authorizes direct delegation to ops.
- Model owns authoritative state and storage invariants.
- Storage is a passive persisted/cached representation; ops owns deterministic
  state access, conversion, and approved single-step platform effects.
- Public core docs, crate docs, module headers, and hygiene guidance use the
  same ownership language.

The implementation still contains the separately indexed API orchestration,
DTO behavior, and 25 ops-to-policy dependency violations. Those require D4 and
later finding-backed product slices; D3 does not move or excuse them.

## Validation

- Active-document/source scans find no remaining wording that permits
  endpoint-to-ops delegation, teaches `model/storage` as a joint owner, or
  uses the superseded domain/decision flow.
- Rust formatting checks pass for all eight changed Rust module-header files.
- `cargo clippy --locked -p canic-core --all-features --all-targets -- -D warnings`:
  pass.
- `cargo package --locked -p canic-core --allow-dirty`: pass, including
  packaged-source verification.
- `scripts/ci/run-layering-guards.sh --self-test`: pass.
- The full layering guard fails as expected on the same 25 pre-existing
  ops-to-policy files, with no D3 regression or newly hidden path.
- Warning-as-error core rustdoc fails only on the already indexed
  `CANIC-092-DOCS-002` private `InternalError` link. That D10 finding is not
  part of D3.
- `git diff --check`: pass.

## Finding Disposition

`CANIC-092-LAYERING-003` and `CANIC-092-DOCS-001` are fixed in
`7e9c12785675781fa935b35ee78735bf2168280d`. Current mandatory-trace reruns
remain eight pass and two fail because D3 changes authority text rather than a
traced product operation.

D4 root-issuer admission ownership is the current candidate.
