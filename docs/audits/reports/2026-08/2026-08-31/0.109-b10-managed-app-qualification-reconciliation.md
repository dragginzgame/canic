# Canic 0.109 B10 Managed-App Qualification Reconciliation

Date: 2026-08-31

## Result

Canic-owned result: `PASS`

Complete B10 result: `PENDING DOWNSTREAM ADOPTION`

The current candidate provides one bounded host-only `canic::testing` facade,
proves that facade through a packaged downstream consumer, and drives both the
managed composed-IcyDB and standalone-local lifecycle boundaries in PocketIC.
It adds no canister endpoint, stable record, timer, lifecycle participant,
policy decision or control-plane effect owner.

B10 is not yet complete because the read-only downstream checkout still uses
its private qualification adapter and direct `canic-core`/`ic-testkit`
development dependencies. Canic cannot remove those files under the repository
boundary. That external adoption is the final B10 evidence gap, not a missing
Canic runtime capability.

## Revision And Worktree

| Item | Evidence |
| --- | --- |
| Published baseline | `v0.109.32`, release commit `1824b6318671aac6f88e83ca154034b5c18407ae` |
| Published validated source | `641f843ac5bc1ddb823bef6b3c32427a5cca70dc` |
| Candidate | dirty development worktree based on `v0.109.32`; no version, commit, tag or publication action was performed |
| Downstream observation | read-only Toko Miner `686b9824c7bb6a51ffbaaff12bf64d027c60648b`, with substantial pre-existing working-tree changes |

The candidate also contains the accepted B9 evidence and independently
authorized 0.110 design edits already present in the shared worktree. This
reconciliation neither attributes those edits to B10 nor changes their
authority.

## Canic-Owned Boundary

| Requirement | Evidence | Result |
| --- | --- | --- |
| One supported downstream surface | `canic::testing::{ManagedAppQualificationInput, ManagedAppFixture, StandaloneAppFixture, install_managed_app, install_standalone_app}` | PASS |
| No private downstream pins required | The packaged consumer depends only on packaged `canic` with `features = ["testing"]`; required PocketIC call traits are re-exported | PASS |
| No runtime authority | The feature is host-only and absent from `wasm32`; its synthetic Principals and operations exist only in a caller-owned PocketIC | PASS |
| Exact managed initialization | The facade compiles checked-in App configuration into the exact managed init, admission projection, Component deployment and Root-issued Directory authority | PASS |
| Managed lifecycle | The published facade proves initial fencing, direct denied ingress, activation/opening, admitted composed-IcyDB ingress, same-release upgrade and successor fencing | PASS |
| Standalone lifecycle | The published facade installs the standalone-local runtime probe, reaches a public application endpoint, upgrades the exact same Wasm and reaches that endpoint again | PASS |
| Packaged usability | The isolated packaged consumer warning-denied compiles managed construction plus standalone same-release upgrade without repository crate paths | PASS |
| Final-runtime Candid ownership | Declaration builds use the maintained Canic declaration environment; generated infrastructure reuses its exact compiled sidecar instead of extracting from the deliberately stripped runtime | PASS |

## Downstream Adoption Gap

Read-only inspection found:

- `apps/toko_miner/app/Cargo.toml` still declares direct qualification-only
  `canic-core` and `ic-testkit` dependencies;
- `apps/toko_miner/app/src/qualification.rs` still owns the private managed
  payload/lifecycle adapter; and
- downstream `CANIC-026` remains `Available`, explicitly recording adoption
  and private-adapter removal as pending.

The downstream must adopt the immutable Canic package containing this
candidate, replace the private adapter with `canic::testing`, remove the two
direct test dependencies, and rerun its exact managed/standalone built-Wasm
qualification. No downstream mutation is authorized or performed here.

## Targeted Validation

Passed on the current development worktree:

```text
cargo test --locked -p canic-host --lib candid
  25 passed; 418 filtered

cargo test --locked -p canic-host --lib declaration_and_runtime_passes_share_exact_cargo_features
  1 passed

cargo test --locked -p canic-host --lib bootstrap_candid
  3 passed

cargo test --locked -p canic-testing-internal --lib published_managed_app_support_drives_composed_lifecycle -- --nocapture
  1 passed; 40 filtered; 136.27s

make test-packaged-downstream-wasm-store
  passed on the final candidate; packaged downstream Canister, managed-App
  testing consumer and Wasm Store probe passed; generated wrapper code section
  2,988,974 bytes and canonical packaged Wasm Store code section 2,986,771
  bytes

cargo clippy --locked -p canic-host --lib --tests -- -D warnings
cargo clippy --locked -p canic --features testing --lib --tests -- -D warnings
cargo clippy --locked -p canic-testing-internal --lib --tests -- -D warnings
  passed

cargo fmt --all -- --check
git diff --check
bash -n scripts/ci/verify-packaged-downstream-wasm-store.sh
shellcheck scripts/ci/verify-packaged-downstream-wasm-store.sh
  passed
```

No broad workspace, release-matrix or full PocketIC gate was run during
development.

## Closeout Boundary

Canic-owned B10 implementation and direct proof are complete, and the current
candidate may enter the maintainer-owned validation and publication workflow.
The 0.109 minor closeout audit remains blocked only on immutable publication
plus the exact downstream adapter adoption evidence above. 0.110 remains
blocked until the human maintainer accepts that closeout.
