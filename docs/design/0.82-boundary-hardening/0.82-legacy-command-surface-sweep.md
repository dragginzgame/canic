# Canic 0.82 Legacy Command Surface Sweep

Date: 2026-07-05

## BoundaryHardeningReport

```text
schema_version: 1
slice: 0.82 Legacy Command Surface Sweep
boundary: rejected operator command forms remain hard-cut and unaliased
status: pass_with_followups
behavior_impact: no_behavior_change
```

## Slice Contract

Boundary:
Recent operator-platform hard cuts for medic, state audit, runtime inspect, and
adjacent topology/runtime aliases.

Current owner:
Command-family parsers and the top-level CLI dispatcher.

Intended owner:
Each command family owns its supported and rejected local command forms. The
top-level dispatcher must reject absent command families instead of introducing
compatibility aliases.

Files/modules in scope:

- `crates/canic-cli/src/tests.rs`
- `crates/canic-cli/src/medic/tests.rs`
- `crates/canic-cli/src/state/tests.rs`
- `crates/canic-cli/src/inspect/mod.rs`
- `crates/canic-cli/src/info.rs`
- `docs/design/0.78-top-level-medic-preflight/0.78-design.md`
- `docs/design/0.80-stable-state-migrations/0.80-design.md`
- `docs/design/0.81-runtime-introspection/0.81-design.md`
- `docs/design/0.82-boundary-hardening/0.82-design.md`

Files/modules explicitly out of scope:

- Removing archived design references.
- Changing active command surfaces.
- Adding new aliases or deprecation shims.
- Reworking command parser architecture.

Public surfaces affected:
None.

Serialized surfaces affected:
None.

Commands/endpoints affected:
None.

Expected behavior change:
None.

Expected non-changes:

- Old medic, state, inspect, topology, and runtime-status shorthand forms remain
  rejected.
- Design documents may mention rejected forms as rejected historical or
  hard-cut surfaces.
- Active help text must not advertise rejected forms as supported commands.

Validation:

- Added a top-level CLI regression test for the sweep candidates that are
  active parser risks.
- `cargo fmt --all -- --check` passed.
- `cargo test --locked -p canic-cli rejected_legacy_operator_surfaces_exit_as_usage_errors`
  passed.
- `cargo test --locked -p canic-cli rejected` passed.

Rollback plan:
Remove this docs-only report and the added dispatcher-level regression test.

Follow-ups:

- Keep old rejected-form references in source-of-truth design docs where they
  describe hard-cut behavior.
- Do not remove archived docs as part of command-surface hardening unless a
  separate docs cleanup slice owns archive pruning.

## Sweep Results

| Command form | Classification | Evidence | Decision |
| --- | --- | --- | --- |
| `canic info medic <deployment>` | rejected active route; design-doc historical reference | `crates/canic-cli/src/info.rs` has no `medic` subcommand; tests reject `info medic demo`; 0.78 design references it as removed. | Keep rejected and tested. |
| `canic medic <deployment>` | rejected active route | Medic parser accepts bare/project/deployment forms only; tests reject `medic demo`. | Keep rejected and tested. |
| `canic medic --blob-storage <target>` | rejected active route | Medic tests reject target options outside deployment scope. | Keep rejected and tested. |
| `canic medic --auth-renewal <issuer>` | rejected active route | Medic tests reject target options outside deployment scope. | Keep rejected and tested. |
| `canic state audit <role>` | rejected active route | State parser requires `--role`; top-level sweep test rejects positional role. | Keep rejected and tested. |
| `canic state manifest <role>` | rejected active route | State parser requires `--role`; top-level sweep test rejects positional role. | Keep rejected and tested. |
| `canic state migrate` | rejected active route | State command exposes only `audit` and `manifest`. | Keep rejected and tested. |
| `canic state repair` | rejected active route | State command exposes only `audit` and `manifest`. | Keep rejected and tested. |
| `canic state explore` | rejected active route | State command exposes only `audit` and `manifest`. | Keep rejected and tested. |
| `canic state dump` | rejected active route | State command exposes only `audit` and `manifest`. | Keep rejected and tested. |
| `canic state audit --format json` | rejected active route | State command uses `--json`, not `--format json`. | Keep rejected and tested. |
| `canic state manifest --format json` | rejected active route | State command uses `--json`, not `--format json`. | Keep rejected and tested. |
| `canic inspect <ambiguous-target>` | rejected active route | Inspect parser requires `canister` or `deployment --role`. | Keep rejected and tested. |
| `canic inspect deployment <deployment>` | rejected active route | Inspect deployment target requires `--role`. | Keep rejected and tested. |
| `canic inspect deployment <deployment> --all` | rejected active route | Inspect does not implement broad deployment fanout. | Keep rejected and tested. |
| `canic topology <deployment>` | absent top-level command | No top-level `topology` command exists. | Keep absent and tested. |
| `canic runtime status <deployment>` | absent top-level command | No top-level `runtime` command exists. | Keep absent and tested. |

## Findings

### CANIC-082-LEGACY-001

Severity: P3
Category: legacy_surface
Owner: top-level CLI dispatcher
Current location: `crates/canic-cli/src/tests.rs`
Intended location: top-level CLI dispatcher tests

Evidence:
Module-level tests covered many hard-cut command forms, but there was no single
dispatcher-level regression covering the 0.82 sweep candidates across medic,
state, inspect, topology, and runtime aliases.

Risk:
Future CLI help or parser cleanup could accidentally reintroduce a shorthand,
alias, or hidden compatibility route while module-level tests still pass.

Decision:
Fixed with a no-behavior-change top-level regression test that asserts rejected
legacy operator surfaces exit as usage errors.

Regression test:
`rejected_legacy_operator_surfaces_exit_as_usage_errors`

## Fixes

- Added a top-level dispatcher test for rejected legacy operator command
  surfaces across medic, state, inspect, topology, and runtime-status aliases.

## Deferred

- Archive and historical design references remain intact.
- No command route, help text, or parser behavior was removed in this slice.
- Broad docs cleanup is deferred until a slice explicitly owns documentation
  pruning.
