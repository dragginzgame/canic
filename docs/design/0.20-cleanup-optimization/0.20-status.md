# 0.20 Status: Cleanup and Optimization

Tracking status for the `0.20.x` audit-led cleanup and optimization line.

This line is status-first rather than design-first: the goals are clear, but
the exact reduction sequence should be driven by recurring audit evidence.

Supporting recurring audit definitions:

- `/home/adam/projects/canic/docs/audits/recurring/system/wasm-footprint.md`
- `/home/adam/projects/canic/docs/audits/recurring/system/instruction-footprint.md`

Supporting runner:

- `/home/adam/projects/canic/scripts/ci/wasm-audit-report.sh`
- `/home/adam/projects/canic/scripts/ci/instruction-audit-report.sh`

Current release notes:

- `/home/adam/projects/canic/docs/changelog/0.20.md`

Upstream handoff:

- `/home/adam/projects/canic/docs/design/0.19-mega-audit/0.19-design.md`

## Metadata

- [ ] `Status owner assigned`
- [x] `Target release confirmed as 0.20`
- [x] `0.20 line opened in changelog`
- [x] `Recurring wasm-footprint audit definition landed`
- [x] `Recurring instruction-footprint audit definition landed`
- [x] `First dated 0.20 wasm-footprint rerun recorded`
- [x] `First dated 0.20 instruction-footprint rerun recorded`

## Core 0.20 Direction

- [ ] Reduce the shared wasm floor for ordinary leaf canisters.
- [ ] Reduce endpoint instruction totals for high-traffic shared surfaces.
- [ ] Add stable `perf!` checkpoints to important flows that currently lack coverage.
- [ ] Keep `root` and `wasm_store` measured as intentional outliers, not as the shared baseline.
- [ ] Remove stale compatibility paths, dormant scripts, and duplicate reference logic when they inflate wasm or maintenance cost.
- [ ] Keep all optimization claims backed by comparable audit artifacts rather than one-off local measurements.
- [ ] Turn regressions into visible audit failures instead of relying on memory or anecdotal observation.

## Active Audit Tracks

### Wasm Track

- [x] Refresh the current `minimal` baseline with the first `0.20` dated rerun.
- [ ] Re-rank the largest shared retained-size hotspots after the `0.19` crate and facade cleanup.
- [ ] Separate shared-floor work from role-specific outliers (`root`, `wasm_store`, coordinator canisters).
- [ ] Record at least one concrete reduction slice with measured before/after deltas.

### Instruction Track

- [x] Record the first endpoint/update-visible instruction baseline for the current shared surface.
- [ ] Record the first comparable flow/checkpoint baseline for important multi-step workflows.
- [ ] Define the first stable scenario set for auth, topology, and root/control-plane heavy flows.
- [ ] Identify the worst instruction outliers by endpoint, flow stage, and caller/auth mode.

### Cleanup Track

- [ ] Keep deleting dead automation and stale docs that no longer match the current runtime.
- [ ] Merge or remove redundant demo/reference code where it reduces maintenance and artifact pressure without weakening coverage.
- [ ] Keep optional lanes out of the ordinary leaf floor unless a dependency is truly shared-runtime critical.

## Baseline Expectations

- `0.20` should treat the latest `0.19` measured floor as the starting point, then refresh it with dated recurring-audit artifacts before claiming new reductions.
- The currently documented `0.19` shared raw release floor is the `minimal`/`app`/`scale`/`shard` lane recorded in the `0.19` notes; the first `0.20` wasm rerun should replace that with a fresh dated baseline instead of relying on changelog memory.
- The first `0.20` instruction baseline should be treated as the canonical reset point for future `perf!` and endpoint regression tracking.
- First dated `0.20` wasm baseline report: `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-31/wasm-footprint.md`
- First clean dated `0.20` instruction baseline report: `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-31/instruction-footprint-3.md`

## Current Notes

- `2026-03-31`: `0.20` opens as an audit-led optimization line, not as one single feature design. The main deliverables are smaller shared artifacts, lower instruction counts, and stronger regression visibility.
- `2026-03-31`: the recurring wasm-footprint audit already exists and has a committed runner. That makes wasm reductions immediately measurable from the start of the line.
- `2026-03-31`: the recurring instruction-footprint audit definition now has a committed runner at `/home/adam/projects/canic/scripts/ci/instruction-audit-report.sh`, so the baseline can be regenerated without rebuilding the method by hand each time.
- `2026-03-31`: the biggest risk to `0.20` comparability is method drift rather than lack of intent. Audit method, scenario identity, and checkpoint naming need to stay stable enough that reductions are real and not just renamed measurements.
- `2026-03-31`: `root` and `wasm_store` should continue to be treated as purposeful outliers. `0.20` should optimize them where it can, but shared-floor wins should be measured first on ordinary leaf/reference canisters such as `minimal`.
- `2026-03-31`: stable `perf!` coverage is still incomplete. Some important flows do not yet have enough checkpoint structure to support meaningful stage-by-stage regression detection, so part of `0.20` is observability cleanup rather than only raw reduction.
- `2026-03-31`: the first dated `0.20` wasm baseline is recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-31/wasm-footprint.md`. Current shrunk leaf floor is `1897556` bytes for `minimal`, `scale`, and `shard`, with `app` effectively identical at `1897571`. `root` remains a separate outlier at `3112916` shrunk bytes.
- `2026-03-31`: the first baseline surfaced one concrete cleanup target immediately: most ordinary leaf canisters get larger after the current `dfx` shrink step than they were as raw Cargo wasm artifacts (`minimal` `1887070 -> 1897556`, `app` `1887070 -> 1897571`, `scale` `1887070 -> 1897556`, `shard` `1887070 -> 1897556`). That shrink-path inversion should be treated as the first real `0.20` reduction slice.
- `2026-03-31`: the current build/audit path still triggers `.did` fallback regeneration during `dfx build` for several canisters. That is not a correctness blocker, but it is audit noise and a likely cleanup target once the first reduction slice is underway.
- `2026-03-31`: the first clean instruction baseline is recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-31/instruction-footprint-3.md`. Under the current method, only the chain-key-free update floor is directly measurable: `test::test` currently records `384` local instructions.
- `2026-03-31`: six successful query scenarios (`canic_time`, `canic_env`, `canic_log`, `canic_subnet_registry`, `canic_subnet_state`, `plan_create_worker`) currently leave no persisted `MetricsKind::Perf` delta. Those rows are method-limited under the current persisted perf transport, not true zero-cost results.
- `2026-03-31`: the repo scan found no real `perf!` checkpoints under `crates/`. That means the first instruction baseline is useful for update-floor regression tracking, but not yet for flow-stage attribution.
- `2026-03-31`: chain-key-dependent update flows such as `scale_hub::create_worker` and sharding assignment updates remain deferred from the baseline until the PocketIC audit mode can provision the expected ECDSA key.

## First 0.20 Completion Boundary

- `0.20.0` should not be considered complete until there is:
  - one dated wasm-footprint rerun,
  - one dated instruction-footprint rerun,
  - one ordered hotspot list for shared wasm pressure,
  - one ordered hotspot list for instruction pressure,
  - and at least one landed reduction slice with comparable before/after evidence.

## Current Restart Point

- Existing foundations:
  - recurring wasm-footprint audit definition
  - wasm audit runner
  - recurring instruction-footprint audit definition
  - cleaner post-`0.19` crate graph and optional-lane separation
- Immediate next slice:
  - investigate the leaf-canister shrink-path inversion from the first dated wasm baseline
  - add first stable `perf!` checkpoints to scaling, sharding, and root-capability flows
  - widen instruction observability so query lanes are measurable or explicitly split into a separate audit method
  - add a key-provisioned PocketIC audit mode for chain-key-dependent update flows
  - improve hotspot attribution so the next wasm rerun yields a useful shared-runtime shortlist
