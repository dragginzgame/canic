# 0.17 Status: Root Wasm Audit and Cutover Requirements

Tracking checklist for implementing `/home/adam/projects/canic/docs/design/0.17-wasm-audit/0.17-design.md`.

Supporting recurring audit definition:

- `/home/adam/projects/canic/docs/audits/recurring/system/wasm-footprint.md`

Supporting runner:

- `/home/adam/projects/canic/scripts/ci/wasm-audit-report.sh`

## Metadata

- [ ] `Status owner assigned`
- [x] `Target release confirmed as 0.17`
- [x] `Recurring wasm audit definition landed`
- [x] `Wasm audit runner committed`

## Core 0.17 Direction

- [x] Preserve intended runtime features while reducing wasm size.
- [x] Record built/shrunk wasm baselines for the deployable canister set.
- [ ] Record `wasm-debug` versus audit-profile comparisons for the deployable canister set.
- [x] Record per-artifact section breakdowns and structure snapshots.
- [ ] Separate `root` runtime bytes from embedded payload bytes.
- [ ] Quantify `root` headroom against the IC wasm cap.
- [ ] Model `root` growth as a function of deployable artifact count and payload growth.
- [ ] Identify the minimum bootstrap/recovery artifact set that remains in `root`.
- [ ] Produce the ordered extraction list that `0.18` will implement.

## Hotspot Attribution

- [x] Capture `twiggy top` for every canister in scope.
- [x] Capture retained-size ownership with `twiggy dominators`.
- [x] Capture generic expansion pressure with `twiggy monos`.
- [x] Distinguish shared-runtime floor pressure from role-specific feature pressure.
- [ ] Distinguish `root` runtime/codegen hotspots from embedded payload hotspots.
- [x] Rank the worst retained-size offenders with concrete evidence.

## 0.18 Handoff Inputs

- [ ] Canonical deployable template inventory recorded.
- [ ] `root` runtime-only baseline or synthetic estimate recorded.
- [ ] Manifest fields required for template-canister cutover recorded.
- [ ] Irreducible bootstrap set recorded.
- [ ] Ordered payload extraction sequence recorded.
- [ ] Cutover inputs are sufficient for `/home/adam/projects/canic/docs/design/0.18-canister-templates/0.18-design.md`.

## Audit Execution

- [x] `bash -n scripts/ci/wasm-audit-report.sh` passes.
- [x] First full `wasm-footprint` audit report recorded under `docs/audits/reports/`.
- [x] Day summary recorded for the first `0.17` wasm audit run.
- [x] Month summary updated for the first `0.17` wasm audit run.
- [x] Baseline comparison path established for same-day reruns.

## Current Audit Notes

- `2026-03-25`: `0.17` starts as a measured audit line, not a feature placeholder. The design and recurring audit definition are both present, and the runner parses cleanly.
- `2026-03-25`: first full wasm-footprint baseline recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint.md`.
- `2026-03-25`: the first real run exposed and fixed a concrete runner bug: `dfx build` does not accept multiple positional canister names, so the runner now builds shrunk artifacts one canister at a time.
- `2026-03-25`: `minimal` no longer carries the test-only provisioning endpoint. Under the standard non-root Canic surface, its release wasm now matches `app` exactly at `3214731` bytes, so `minimal` is restored as the canonical leaf baseline.
- `2026-03-25`: `root` remains the primary structural concern because it combines control-plane runtime growth with embedded child payload accumulation under one IC wasm ceiling.
- `2026-03-25`: the first baseline confirms the shared-runtime floor is heavy: `minimal` shrunk wasm is `3231312` bytes and `app` shrunk wasm is `3214731` bytes.
- `2026-03-25`: the first baseline confirms `root` is already a distinct outlier at `11267613` shrunk bytes, but `0.17` still needs explicit runtime-vs-payload decomposition, cap headroom, and extraction ordering before the audit line can close.
- `2026-03-25`: scaling and sharding are now compile-time optional endpoint groups. `build!` emits `canic_has_scaling` / `canic_has_sharding` from validated role config, and the shared endpoint macro only exports `canic_scaling_registry`, `canic_sharding_registry`, and `canic_sharding_partition_keys` for roles that declare those features.
- `2026-03-25`: ABI verification after the change shows the intended split: `scale_hub` keeps `canic_scaling_registry`; `user_hub` and `shard_hub` keep the sharding endpoints; `minimal`, `app`, `shard`, and `user_shard` no longer export any of those three optional methods.
- `2026-03-25`: current raw release wasm sizes after the first endpoint-surface reduction are `3104849` bytes for `minimal`/`app`/`shard`, `3134775` for `user_shard`, `3162647` for `scale_hub`, `3262469` for `user_hub`, and `3267383` for `shard_hub`. The next `0.17` audit rerun needs to capture the exact shrunk deltas against the earlier baseline.
- `2026-03-25`: same-day rerun recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint-2.md` with `docs/audits/reports/2026-03/2026-03-25/wasm-footprint.md` as the comparable baseline.
- `2026-03-25`: the first optional-endpoint cut reduced shrunk wasm bytes by `109882` for `app`, `scale`, `shard`, and `test`; `109913` for `user_shard`; `76549` for `scale_hub`; `32655` for `user_hub`; `32527` for `shard_hub`; and `260852` for `root`.
- `2026-03-25`: the next compile-time config gates are landed in the shared macro surface: `icrc21_canister_call_consent_message` now follows `[standards].icrc21`, and `canic_delegation_set_signer_proof` / `canic_delegation_set_verifier_proof` now follow `auth.delegated_tokens.enabled`.
- `2026-03-25`: this second slice is structural under the current checked-in config because both `icrc21` and delegated-token auth remain enabled. The immediate win is that disabled builds will no longer pay for those exported surfaces, and `0.17` now has the right config-to-ABI boundary for a later per-role auth split.
- `2026-03-25`: the delegated-auth proof install endpoints are now role-scoped instead of globally present whenever delegated tokens are enabled. `build!` emits `canic_accepts_delegation_signer_proof` / `canic_accepts_delegation_verifier_proof` from validated per-role config, and the shared endpoint macro only exports those methods for roles that declare them.
- `2026-03-25`: the current checked-in role map is explicit. Workspace canisters: `user_shard` accepts signer and verifier proofs; `test` accepts verifier proofs only; all other ordinary non-root canisters omit both proof-install endpoints. Delegation test canisters mirror that split so the auth integration coverage still exercises both roles.
- `2026-03-25`: ABI verification on release wasm confirms the intended boundary. `minimal` exports neither proof-install endpoint, `test` exports only `canic_delegation_set_verifier_proof`, and `user_shard` plus `delegation_signer_stub` export both proof-install methods.
- `2026-03-25`: current raw release wasm sizes for the canisters touched by this auth slice are `3087652` bytes for `minimal`, `3115949` for `test`, `3140010` for `user_shard`, and `3149082` for `delegation_signer_stub`. The next dated audit rerun still needs to capture the comparable shrunk-artifact deltas across the full fleet.
- `2026-03-25`: second same-day rerun recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint-3.md`. Relative to `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint-2.md`, the per-role delegated-auth split reduced shrunk wasm by `17197` bytes for `minimal`, `17148` for `app`, `17197` for `scale`, `17148` for `shard`, `16921` for `scale_hub`, `16835` for `user_hub`, `16906` for `shard_hub`, and `40869` for `root`, while `user_shard` increased by `5235` bytes and `test` increased by `1256` bytes because those roles still carry proof-verification/signing surfaces plus the new role-scoped config machinery.
- `2026-03-25`: after the auth split rerun, the new shared-runtime floor is `3087652` shrunk bytes for `minimal`, `app` is `3087701`, `scale` is `3087652`, `shard` is `3087701`, and `root` is down to `10965892` shrunk bytes. The next `0.17` size slice should target another optional endpoint family or shared runtime fan-in inside that `~3.09 MB` floor.
- `2026-03-25`: the recurring audit and `0.17` design now require a debug/dev versus audit-profile comparison. `wasm-release` remains the shipping metric, but every dated wasm audit should also capture `wasm-debug` artifacts so size drift can be separated into optimization-sensitive codegen effects versus real exported/runtime surface growth.
- `2026-03-25`: `0.17` now has an explicit preservation rule: wasm reductions must not remove intended runtime features or operator-facing signal as an optimization tactic. Feature removal requires a separate deliberate design decision, not an audit-side size shortcut.
- `2026-03-25`: the shared `start!` / `start_root!` fan-in is now structurally split into named lifecycle and capability bundles. `crates/canic/src/macros/start.rs` no longer acts as one indivisible linker-policy block, and `crates/canic/src/macros/endpoints.rs` now exposes explicit bundle macros for lifecycle core, standards, observability, metrics, auth/attestation, topology views, root admin, root auth, non-root sync, and non-root proof-install surfaces.
- `2026-03-25`: default behavior is preserved intentionally. The bundle split is a structural prerequisite for future size work, not a feature change, and `make test-wasm` passes on the refactored macro surface.
- `2026-03-25`: the shared endpoint surface is now split further inside the heavy families. `crates/canic/src/macros/endpoints.rs` now isolates ICRC from ICTS, memory/env/log observability surfaces from each other, and topology state/directory/children/cycle-tracker/placement views into separate bundle macros. The default runtime composition is unchanged, but future wasm cuts can now target those families without reopening the whole shared endpoint block.
- `2026-03-25`: `crates/canic/src/macros/endpoints.rs` and the `canic` build scripts now expose audit-only bundle-disable cfgs for size attribution. Default builds are unchanged, but `0.17` can now build `minimal` with one shared bundle disabled at a time to measure retained byte pressure by capability family.
- `2026-03-25`: first bundle sweep on `canister_minimal` (raw release wasm baseline `3017584` bytes) shows the largest shared families are `auth_attestation` (`2489404`, delta `-528180`), `metrics` (`2905623`, delta `-111961`), and `standards_icts` (`2993164`, delta `-24420`). Secondary costs are `observability_log` (`2995723`, delta `-1861`) and `standards_icrc` (`3012845`, delta `-4739`). The remaining shared topology and env/memory slices are small on `minimal`: `topology_cycles` (`-9895`), `topology_directory` (`-7797`), `topology_children` (`-6826`), `topology_state` (`-6095`), `observability_env` (`-4384`), and `observability_memory` (`-5531`), while `topology_placement` and `nonroot_sync` are effectively zero-cost on `minimal` because config already compiles them out.
- `2026-03-25`: with `metrics` and `ICTS` intentionally kept, the next real shared-runtime optimization target is the auth/attestation stack behind `canic_response_capability_v1`, not another endpoint-family guess.
- `2026-03-25`: auth-bundle attribution confirms that `canic_response_capability_v1` is pulling a large shared stack even on `minimal`. Disabling only `auth_attestation` drops `canister_minimal` from `5189` to `4480` functions and from `226728` to `207124` data bytes, and it removes the `cost_sign_with_ecdsa` and `cost_create_canister` IC imports from the baseline artifact. That points directly at root-capability verification/orchestration and auth crypto linkage as the next implementation target.
- `2026-03-25`: the auth/capability split is now explicit at the API-module level. `crates/canic-core/src/api/rpc/capability/root.rs` owns the full root verifier/orchestrator path, `crates/canic-core/src/api/rpc/capability/nonroot.rs` owns the non-root structural cycles path, and `crates/canic-core/src/workflow/rpc/request/handler/nonroot_cycles.rs` executes the non-root replay/funding flow without pulling the generic root dispatcher into ordinary leaf canisters.
- `2026-03-25`: this corrected split supersedes the earlier intermediate specialization. While implementing it, `0.17` uncovered and fixed an internal regression where the root and non-root capability bodies had been inverted during refactor. The current code restores the intended behavior and keeps the feature set unchanged: same endpoint, same structural proof mode for non-root, same replay semantics, same cycles funding policy, and same root-capability metrics. Verified with `make test-wasm`.
- `2026-03-25`: measured raw release wasm after the corrected capability-module split is `2666209` for `minimal`, `2666209` for `app`, `2666209` for `scale`, `2666209` for `shard`, `2718044` for `test`, `3063093` for `user_shard`, `2728047` for `scale_hub`, `2826640` for `user_hub`, `2831730` for `shard_hub`, and `17571149` for `root`. Versus the prior baseline, that is a further reduction of `348476` bytes on `minimal`, `348525` on `app`, `348476` on `scale`, `348525` on `shard`, `324702` on `test`, `3608` on `user_shard`, `344407` on `scale_hub`, `343969` on `user_hub`, and `343590` on `shard_hub`, while `root` remains effectively flat (`+7384`).
- `2026-03-25`: the corrected split also removed root-only IC imports from the shared leaf floor. `canister_minimal.wasm` no longer imports `cost_sign_with_ecdsa` or `cost_create_canister`; its data section is down to `211688` bytes and its function count is down to `4669`. That confirms the retained non-root auth floor was largely the root capability verifier path, not the endpoint shell itself.

## Completion Boundary

- `0.17` closes only after the audit proves the embedded-root payload model to closure and records the concrete migration inputs required by `0.18`.
- `0.17` does not close on runner availability alone; it closes on dated audit evidence and a complete cutover handoff.
