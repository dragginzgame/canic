# 0.17 Status: Root Wasm Audit and Cutover Requirements

Tracking checklist for implementing `/home/adam/projects/canic/docs/design/0.17-wasm-audit/0.17-design.md`.

Supporting recurring audit definition:

- `/home/adam/projects/canic/docs/audits/recurring/system/wasm-footprint.md`

Supporting runner:

- `/home/adam/projects/canic/scripts/ci/wasm-audit-report.sh`

## Metadata

- [ ] `Status owner assigned`
- [x] `Target release confirmed as 0.17`
- [x] `Recurring wasm audit definition landed`
- [x] `Wasm audit runner committed`

## Core 0.17 Direction

- [x] Record built/shrunk wasm baselines for the deployable canister set.
- [x] Record per-artifact section breakdowns and structure snapshots.
- [ ] Separate `root` runtime bytes from embedded payload bytes.
- [ ] Quantify `root` headroom against the IC wasm cap.
- [ ] Model `root` growth as a function of deployable artifact count and payload growth.
- [ ] Identify the minimum bootstrap/recovery artifact set that remains in `root`.
- [ ] Produce the ordered extraction list that `0.18` will implement.

## Hotspot Attribution

- [x] Capture `twiggy top` for every canister in scope.
- [x] Capture retained-size ownership with `twiggy dominators`.
- [x] Capture generic expansion pressure with `twiggy monos`.
- [x] Distinguish shared-runtime floor pressure from role-specific feature pressure.
- [ ] Distinguish `root` runtime/codegen hotspots from embedded payload hotspots.
- [x] Rank the worst retained-size offenders with concrete evidence.

## 0.18 Handoff Inputs

- [ ] Canonical deployable template inventory recorded.
- [ ] `root` runtime-only baseline or synthetic estimate recorded.
- [ ] Manifest fields required for template-canister cutover recorded.
- [ ] Irreducible bootstrap set recorded.
- [ ] Ordered payload extraction sequence recorded.
- [ ] Cutover inputs are sufficient for `/home/adam/projects/canic/docs/design/0.18-canister-templates/0.18-design.md`.

## Audit Execution

- [x] `bash -n scripts/ci/wasm-audit-report.sh` passes.
- [x] First full `wasm-footprint` audit report recorded under `docs/audits/reports/`.
- [x] Day summary recorded for the first `0.17` wasm audit run.
- [x] Month summary updated for the first `0.17` wasm audit run.
- [x] Baseline comparison path established for same-day reruns.

## Current Audit Notes

- `2026-03-25`: `0.17` starts as a measured audit line, not a feature placeholder. The design and recurring audit definition are both present, and the runner parses cleanly.
- `2026-03-25`: first full wasm-footprint baseline recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint.md`.
- `2026-03-25`: the first real run exposed and fixed a concrete runner bug: `dfx build` does not accept multiple positional canister names, so the runner now builds shrunk artifacts one canister at a time.
- `2026-03-25`: `minimal` no longer carries the test-only provisioning endpoint. Under the standard non-root Canic surface, its release wasm now matches `app` exactly at `3214731` bytes, so `minimal` is restored as the canonical leaf baseline.
- `2026-03-25`: `root` remains the primary structural concern because it combines control-plane runtime growth with embedded child payload accumulation under one IC wasm ceiling.
- `2026-03-25`: the first baseline confirms the shared-runtime floor is heavy: `minimal` shrunk wasm is `3231312` bytes and `app` shrunk wasm is `3214731` bytes.
- `2026-03-25`: the first baseline confirms `root` is already a distinct outlier at `11267613` shrunk bytes, but `0.17` still needs explicit runtime-vs-payload decomposition, cap headroom, and extraction ordering before the audit line can close.
- `2026-03-25`: scaling and sharding are now compile-time optional endpoint groups. `build!` emits `canic_has_scaling` / `canic_has_sharding` from validated role config, and the shared endpoint macro only exports `canic_scaling_registry`, `canic_sharding_registry`, and `canic_sharding_partition_keys` for roles that declare those features.
- `2026-03-25`: ABI verification after the change shows the intended split: `scale_hub` keeps `canic_scaling_registry`; `user_hub` and `shard_hub` keep the sharding endpoints; `minimal`, `app`, `shard`, and `user_shard` no longer export any of those three optional methods.
- `2026-03-25`: current raw release wasm sizes after the first endpoint-surface reduction are `3104849` bytes for `minimal`/`app`/`shard`, `3134775` for `user_shard`, `3162647` for `scale_hub`, `3262469` for `user_hub`, and `3267383` for `shard_hub`. The next `0.17` audit rerun needs to capture the exact shrunk deltas against the earlier baseline.
- `2026-03-25`: same-day rerun recorded at `/home/adam/projects/canic/docs/audits/reports/2026-03/2026-03-25/wasm-footprint-2.md` with `docs/audits/reports/2026-03/2026-03-25/wasm-footprint.md` as the comparable baseline.
- `2026-03-25`: the first optional-endpoint cut reduced shrunk wasm bytes by `109882` for `app`, `scale`, `shard`, and `test`; `109913` for `user_shard`; `76549` for `scale_hub`; `32655` for `user_hub`; `32527` for `shard_hub`; and `260852` for `root`.

## Completion Boundary

- `0.17` closes only after the audit proves the embedded-root payload model to closure and records the concrete migration inputs required by `0.18`.
- `0.17` does not close on runner availability alone; it closes on dated audit evidence and a complete cutover handoff.
