# 0.22 Status: Audits, Wasm Size, and Perf

Tracking status for the `0.22.x` audit, wasm-size, and performance line.

This line is meant to turn the `0.20` cleanup/audit groundwork and the `0.21`
runtime/build cleanup into a repeatable operator-facing measurement lane.

Supporting recurring audit definitions:

- `/home/adam/projects/canic/docs/audits/recurring/system/wasm-footprint.md`
- `/home/adam/projects/canic/docs/audits/recurring/system/instruction-footprint.md`

Supporting runners:

- `/home/adam/projects/canic/scripts/ci/wasm-audit-report.sh`
- `/home/adam/projects/canic/scripts/ci/instruction-audit-report.sh`
- `/home/adam/projects/canic/scripts/ci/run-workspace-tests.sh`

Current release notes:

- `/home/adam/projects/canic/docs/changelog/0.22.md`

Upstream handoff:

- `/home/adam/projects/canic/docs/design/0.20-cleanup-optimization/0.20-status.md`

## Metadata

- [ ] `Status owner assigned`
- [x] `Target release confirmed as 0.22`
- [x] `0.22 line opened in changelog`
- [x] `Artifact freshness now includes build env/profile identity`
- [x] `Shared PocketIC root-baseline plumbing lives in canic-testkit`
- [x] `Three-profile wasm contract aligned across repo and downstream helpers`
- [x] `Cached small-store reconcile baseline added`
- [x] `Workspace timing summary table added`
- [ ] `First dated 0.22 wasm-footprint rerun recorded`
- [x] `First dated 0.22 instruction/perf rerun recorded`
- [x] `First per-suite workspace timing summary recorded in release notes`
- [x] `Shared tagged setup script covers Rust, Cargo tools, and dfx`
- [x] `Local thin-root install builds only config-selected canisters`
- [x] `Stale duplicate setup/update path removed`
- [x] `0.22 line concluded with remaining optional work deferred to 0.23`

## Core 0.22 Direction

- [ ] Make wasm-size regressions visible as explicit measured deltas, not just one-off local observations.
- [ ] Keep instruction and `perf!` baselines comparable across reruns and across the important root/auth/replay flows.
- [ ] Keep PocketIC-heavy test/setup time attributable with reusable baselines, explicit timings, and reproducible artifact inputs.
- [ ] Reduce shared-floor wasm size before spending time on intentional outliers.
- [ ] Treat special test/build variants as first-class reproducible inputs rather than letting them silently reuse stale `.dfx` artifacts.
- [ ] Keep the explicit `debug` / `fast` / `release` wasm contract aligned across repo-local and downstream tooling.

## Active Tracks

### Wasm Track

- [ ] Record the first dated `0.22` wasm-footprint rerun.
- [ ] Add simple per-canister wasm budgets or deltas for the main reference roles.
- [ ] Re-rank the shared retained-size hotspots after the current root/build cleanup settles.
- [ ] Keep `root` and other control-plane canisters measured as purposeful outliers rather than the shared floor.

### Perf Track

- [x] Record the first dated `0.22` instruction/perf rerun.
- [ ] Expand comparable `perf!` coverage across root bootstrap, publication, auth, replay, and scaling flows.
- [x] Make end-of-run timing summaries short and operator-readable.
- [ ] Separate “real slowdown” from “first baseline build” in PocketIC-heavy suites.

### Test/Artifact Track

- [x] Make `.dfx` artifact freshness sensitive to build env and profile identity.
- [x] Route special root-baseline build variants through shared `canic-testkit` root setup instead of ad hoc prebuild glue.
- [x] Cache the expensive reconcile small-store baseline the same way `root_suite` now caches topology/scaling baselines.
- [ ] Push more custom auth/root fixture setup down into `canic-testkit` so downstream projects can reuse it directly.

## Baseline Expectations

- `0.22` should treat artifact reproducibility as part of performance work, not as separate housekeeping.
- Any suite or audit runner that depends on non-default build flags should make that identity visible and reusable through shared test/build infrastructure.
- Wasm-size and instruction claims should stay tied to dated audit artifacts, not changelog memory.
- The first `0.22` timing story should include both build/setup cost and steady-state cached restore cost for the heavy PocketIC suites.

## Current Notes

- `2026-04-03`: `0.22` is now treated as concluded. The remaining optional follow-through work is deferred to `/home/adam/projects/canic/docs/design/0.23-deferred-follow-through/0.23-design.md` instead of keeping the `0.22` line open-ended.
- `2026-04-02`: `0.22` opens after the `0.21` build/install cleanup line, so the immediate goal is not another topology redesign. It is measurement, comparability, and cost visibility.
- `2026-04-02`: `.dfx` artifact reuse is now build-env-aware, which closes one of the main hidden risks for audit comparability and special PocketIC build variants.
- `2026-04-02`: Canic now exposes one explicit three-profile wasm contract: `debug` for raw large wasm, `fast` for local/test/demo installs, and `release` for shipping/install artifacts.
- `2026-04-02`: the generated hidden `wasm_store` wrapper now carries both Canic `fast` and `release` profile definitions, so packaged downstreams do not need a repo-local helper just to keep their hidden bootstrap path aligned.
- `2026-04-02`: the reconcile suite no longer owns its own custom small-store prebuild path. That work now goes through the shared root harness contract, which is the right base for later caching and timing work.
- `2026-04-02`: the next obvious runtime win is a cached small-store reconcile baseline in `canic-testkit`, because that suite is still the largest remaining fresh-root cost.
- `2026-04-02`: the next wasm/perf win is not just another one-off reduction. It is getting comparable recurring reports and small readable per-suite timing summaries into the normal operator path.
- `2026-04-03`: `root_wasm_store_reconcile` now restores a cached debug small-store baseline after its first expensive setup, so the later reconcile cases no longer pay for a fresh small-store root install each time.
- `2026-04-03`: `scripts/ci/run-workspace-tests.sh` now prints a compact end-of-run timing summary table, which makes the heavy PocketIC suites attributable without reading the full phase-by-phase logs.
- `2026-04-03`: the first dated `0.22` instruction report is now recorded at `/home/adam/projects/canic/docs/audits/reports/2026-04/2026-04-03/instruction-footprint.md`.
- `2026-04-03`: the first dated `0.22` wasm rerun is still blocked on a reachable local `dfx` replica. `scripts/ci/wasm-audit-report.sh` now fails immediately when local `dfx` is unavailable or `dfx canister create --all` fails, instead of ignoring setup failure and dying later in `dfx build`.
- `2026-04-03`: managed `wasm_store` publication no longer recomputes full-store byte accounting on every chunk publish. The hot chunk path now does an incremental occupied-byte check, which directly addresses the downstream `IC0522` failure mode on large staged release sets.
- `2026-04-03`: root-side publication now caches one store's `stored_chunks` hash set for the duration of a publication pass instead of re-fetching the full management-canister list for every template routed into that same store.
- `2026-04-03`: the public visible-canister builder now applies the same `ic-wasm shrink` pass as the hidden bootstrap `wasm_store` builder, and a full reference rebuild brought every visible role in `root.release-set.json` back down to one staged chunk.
- `2026-04-03`: the visible reference canister `.did` files were removed from `canisters/`, so live interface truth now stays in generated `.dfx/local/canisters/**/*.did` outputs while `crates/canic-wasm-store/wasm_store.did` remains the one checked-in canonical exception for the hidden bootstrap path.
- `2026-04-03`: the local thin-root install path now resolves its build set from `canic.toml`, builds one quiet `dfx build -qq <canister>` target at a time, and no longer relies on `dfx.json` dependency edges to decide what needs to be built.
- `2026-04-03`: the normal local-install wait loop is quieter now. It still reports phase and subnet-role progress, but it no longer dumps recent root logs every few seconds unless bootstrap actually fails or times out.
- `2026-04-03`: `canic-build-canister-artifact` now owns the readable downstream build output directly, including the selected `debug|fast|release` profile, one-time context output across a build session, per-canister elapsed time, and visible spacing between canister builds.
- `2026-04-03`: the shared tagged `scripts/install.sh` path now bootstraps Rust when needed, installs the pinned Canic toolchain plus `dfx`, and replaces the stale duplicate `scripts/env/update.sh` setup lane.

## Handoff Snapshot

- `0.22` itself no longer has an active restart point.
- Deferred follow-through now lives in `/home/adam/projects/canic/docs/design/0.23-deferred-follow-through/0.23-design.md`.

- Existing foundations:
  - recurring wasm-footprint audit definition
  - recurring instruction-footprint audit definition
  - workspace test runner with suite sequencing and timings
  - shared root-baseline plumbing in `canic-testkit`
  - build-env-aware `.dfx` artifact freshness
  - config-driven local thin-root build selection instead of `dfx build --all`
  - shared tagged setup/install script for Rust/Cargo/Canic/dfx bootstrap
- Deferred next slice for `0.23`:
  - rerun the next dated wasm-footprint audit once local `dfx` is reachable
  - push more custom auth/root fixture setup into reusable `canic-testkit` helpers
  - record one cold-machine validation pass for the tagged setup script and thin-root local install path
  - decide whether the next `0.23` work should focus on more reusable testkit plumbing or on another measurable wasm/perf reduction
