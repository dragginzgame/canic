# Canic 0.99 Pre-Implementation Semantic Inventory

Date: 2026-07-23

## Status and Gate

- Status: terminology and surface inventory complete; implementation is
  approved under a reinstall-only release boundary.
- Inventory baseline: immutable `v0.98.24` at
  `9bc0f016dadf99fb88c3f2fa28855eb666f5660c`.
- Inventory method: direct source inspection, active endpoint/config/storage
  tracing and repository-wide stale-term searches.
- Product changes in this preparation pass: none.
- Release-boundary verdict: fresh install/reinstall only. No 0.98 host state,
  stable state or Canister is an input to 0.99.

The inventory is authoritative for semantic destinations. The
[0.99 design](0.99-design.md) remains authoritative for behavior, recovery,
evidence encoding and acceptance criteria. A later implementation discovery
that contradicts this inventory requires a design correction before code is
changed; it is not permission to add a compatibility path.

## Canonical Authority Map

| Concern | Sole 0.99 authority |
| --- | --- |
| Source definition | checked-in App config and package declarations |
| Live identity | immutable protected `FleetBinding` plus host-only `FleetName` |
| Network identity | resolved `CanonicalNetworkId`; never an environment label |
| Current Fleet-wide coordination | current one-member Fleet root |
| Fleet membership Registry | absent in 0.99 |
| Local Canister membership | existing root-owned `SubnetRegistry` |
| Fleet consumer lookup | root-derived `FleetDirectory` |
| Local consumer lookup | root-derived `SubnetDirectory` |
| Keyed placement | `PlacementBindingRegistry` |
| Local activation and binding | new protected ID-21 Fleet-activation cell |
| Delegated-auth audience | `DelegationAudience::Fleet(FleetKey)` |
| Fresh-install activation | one `FleetInstallActivationJournal` |
| Release-set integrity | host-owned post-build manifest digest |
| Canister build identity | one durable `ReleaseBuildPlanRecord`, journalled random `ReleaseBuildNonce` and embedded derived `ReleaseBuildId` |

0.99 does not expose a Fleet Registry, Coordinator Subnet selector or Fleet
Coordinator. A fresh 0.100 installation creates the first Fleet Registry from
empty state; it does not consume this 0.99 installation.

## Network and Release Identity

Environment profiles are routing/configuration inputs. Each resolves one
`CanonicalNetworkId`:

- the compiled pinned public-IC trust root resolves the public IC;
- a PocketIC or other harness created and owned by Canic resolves from its
  pre-created journalled seed plus root-key digest; and
- a pre-existing local replica or other live network requires explicit
  enrollment of its root trust anchor.

Two profiles such as staging and production may resolve to the same public IC
and therefore share one canonical network/Fleet namespace. Profile labels,
gateway URLs and `BuildNetwork::{Local, Ic}` are never identity inputs. A
pre-creation instance seed cannot be invented for a replica Canic did not
create.

Release evidence has two non-interchangeable identities:

| Identity | Time and owner | Inputs |
| --- | --- | --- |
| `ReleaseBuildNonce` | host generates and journals before Wasm build | 32 cryptographically random bytes |
| `ReleaseBuildId` | host derives before Wasm build; exact value embedded in each Wasm | `SHA-256("canic:release-build-id\0" || u64_be(32) || ReleaseBuildNonce)` |
| `release_set_manifest_digest` | host computes after final Wasms exist | canonical manifest containing the exact final Wasm hashes |

The canonical pre-/post-build owner is:

~~~rust
pub enum ReleaseBuildPlanState {
    Planned,
    Finalized {
        release_set_manifest_digest: [u8; 32],
    },
}

pub struct ReleaseBuildPlanRecord {
    pub nonce: ReleaseBuildNonce,
    pub release_build_id: ReleaseBuildId,
    pub state: ReleaseBuildPlanState,
}
~~~

It is durably created at
`.canic/release-builds/<release-build-id>/plan.cbor` in `Planned` before
building, advances once to `Finalized` after exact Wasm hashes exist, and is
then immutable. Fresh-install recovery starts only from the finalized record
and stores its canonical hash, build ID and manifest digest.
There is no placeholder digest or second active nonce owner.

`ReleaseBuildId` excludes Wasm bytes, Wasm hashes, manifest bytes and the
manifest digest. There is no source/toolchain-derived alternative. A retry
reuses the journalled nonce and ID; a new build plan creates a new nonce. A
Canister compares only its embedded `ReleaseBuildId` with the installation
argument. The host validates the complete post-build release set. The manifest
digest is never embedded in a Wasm whose hash feeds that manifest.

## Terminology Dispositions

| Active meaning on the baseline | 0.99 disposition |
| --- | --- |
| config/source “Fleet” | App |
| one live installed “deployment target” | Fleet |
| deployment plan, action, comparison or receipt | retain Deployment |
| passive artifact-promotion and external-lifecycle report families | remove; no maintained operator consumer or execution authority survived the recursive CLI review |
| root-cascaded Fleet-wide `AppIndex` | `FleetDirectory` |
| root-cascaded local `SubnetIndex` | `SubnetDirectory` |
| complete root-owned local membership | retain `SubnetRegistry` |
| keyed-placement `DirectoryRegistry` | `PlacementBindingRegistry` |
| internal B-tree/hash lookup index | retain ordinary implementation “index” |
| Cargo/ICP repository root “project” | retain ordinary workspace “project” |
| application-domain role names containing “project” | retain; not Canic topology |
| immutable-object-storage `ProjectCycles` protocol names | retain exact external protocol contract |
| auth audience `Project`, `CanicSubnet` or `Canister` | remove; sole Fleet audience |
| `ApplicationReceipt` and application-owned intent terminology | retain; not App identity |

The hard cut is semantic, not a blind word replacement. In particular,
`DeploymentPlanV1`, `DeploymentReceiptV1`, `DeploymentCheckV1`, promotion and
comparison types remain because they describe deployment operations or
evidence. Fields inside them that identify a long-lived target change to Fleet
identity.

## Prime Surface Inventory

| Baseline surface | Frozen destination |
| --- | --- |
| `SubnetRole` | `SubnetSlotId` |
| `SubnetRole::PRIME` and serialized `prime` | `SubnetSlotId::DEFAULT` and `default` |
| `is_prime()` | `is_default()` |
| config `subnets.prime` | `subnets.default` |
| `SubnetIdentity` | `CurrentRootInstallIdentity` |
| `SubnetIdentity::Prime` | current-root identity with no expected module hash |
| `SubnetIdentity::PrimeWithModuleHash` | current-root identity with an exact expected hash |
| `SubnetIdentity::Standard` and `SubnetContextParams` | remove; a fresh 0.100 installation defines its own Tree contract |
| `SubnetIdentity::Manual` | remove; tests use the current-root contract |
| `prime_root_pid` | `fleet_root_pid` |
| `is_prime_root` | `is_fleet_root` |
| `is_prime_subnet` | `is_default_subnet` |
| `PrimeRoot*` errors/diagnostics | corresponding `FleetRoot*` term |
| future distinguished-Subnet behavior | absent in 0.99; a fresh 0.100 installation introduces Coordinator placement |

`default` identifies an App-declared workload slot.
`fleet_root_pid` identifies the current root of the one-member Fleet.
`Coordinator Subnet` identifies neither of those and has no selector or
persisted identity in 0.99.

## Configuration and Source Layout

The exact top-level config hard cut is:

~~~toml
[app]
name = "toko"
init_mode = "enabled"

[services.fleet]
roles = ["project_hub"]

[subnets.default]
~~~

The schema changes are:

| Baseline | 0.99 |
| --- | --- |
| `ConfigModel.fleet: Option<FleetConfig>` | removed |
| `FleetConfig` | removed |
| `AppConfig` without identity | required `AppConfig.name: AppId` |
| top-level `app_index` | `services.fleet.roles` |
| `BTreeMap<SubnetRole, SubnetConfig>` | `BTreeMap<SubnetSlotId, SubnetConfig>` |
| `CanisterConfig.directory` | `CanisterConfig.binding` |
| `DirectoryConfig` | `BindingConfig` |
| `fleet_name()` / `FleetRoleRef` for source config | `app_id()` / `AppRoleRef` |
| `package.metadata.canic.fleet` | `package.metadata.canic.app` |
| `fleets/<name>/canic.toml` | `apps/<app>/canic.toml` |
| workspace members under `fleets/` | corresponding members under `apps/` |

`controllers`, standards, logging, auth, whitelist, declared `roles`, canister
kinds, scaling, sharding, funding, diagnostics and metrics keep their behavior.
`services.fleet.roles` is authoring input, not a runtime Registry and not a
second Directory authority.

Tracked configs, Cargo workspace members, build scripts, packaged-downstream
fixtures, scaffolds, examples and active documentation move in the same slice.
No discovery fallback searches both `fleets/` and `apps/`.

## Candid and Lifecycle Inventory

### Endpoint changes

| Baseline endpoint | 0.99 endpoint | Access and exact result |
| --- | --- | --- |
| `canic_app(AppCommand)` | `canic_fleet_admin(FleetCommand)` | internal controller update; `Result<FleetCommandResponse, Error>` |
| `canic_app_state()` | `canic_fleet_state()` | controller query; `Result<FleetStateResponse, Error>` |
| `canic_app_index(PageRequest)` | `canic_fleet_directory(PageRequest)` | public query; `Result<FleetDirectoryPageResponse, Error>` |
| `canic_subnet_index(PageRequest)` | `canic_subnet_directory(PageRequest)` | public query; `Result<SubnetDirectoryPageResponse, Error>` |

`FleetCommand` keeps the existing `SetStatus` and
`SetCyclesFundingEnabled` variants. `FleetStateInput`,
`FleetStateResponse`, `FleetMode` and `FleetStatus` keep the current fields and
variant semantics; their Rust type-name changes do not create extra Candid
labels.

### Endpoint names retained

| Endpoint | Frozen treatment |
| --- | --- |
| `canic_subnet_registry()` | exact name, public access and response shape retained for recovery |
| `canic_sync_state(StateSnapshotInput)` | exact name and parent-only access retained; input labels break as listed below |
| `canic_sync_topology(TopologySnapshotInput)` | exact name, access and full shape retained |
| `canic_env()` | exact name/public access retained; two response labels change |
| delegated-token, issuer-policy, renewal and proof endpoints | names/access retained; Fleet-only audience and proof-delivery shapes apply |
| unrelated pool, scaling, sharding, observability, ICRC and Wasm-store endpoints | retain unless another explicit 0.99 contract changes their nested identity |

The new activation/recovery inventory is:

| Endpoint | Access | Result/role |
| --- | --- | --- |
| `canic_fleet_activation_status()` | controller query on every managed Canister | exact phase, identity and evidence; root also returns complete bounded cascade- and credential-manifest bytes when present |
| `canic_prepare_fleet_activation()` | root-only controller update | idempotently persists cascade/credential preparation but does not deliver credentials or activate |
| `canic_resume_fleet_activation(FleetActivationResumeRequest)` | root-only controller update | requires journalled operation/generation identity, delivers credentials/references and activates non-roots then root |
| `canic_prepare_fleet_credential_generation(FleetCredentialGenerationRequest)` | internal non-root update from protected current root | installs or advances the exact prepared credential-generation reference without activating |
| `canic_activate_fleet(FleetActivationRequest)` | internal non-root update from protected current root | requires the stored generation and commits only exact prepared evidence |

These are the only new activation endpoints. There is no public unguarded
status, separate install/upgrade status path or compatibility endpoint.
While the root is `Prepared`, its existing controller-only template
stage/prepare/publish updates remain admitted solely to assemble the exact
release inventory consumed by `canic_prepare_fleet_activation`. The former
`canic_wasm_store_bootstrap_resume_root_admin` endpoint is removed rather than
retained as a second activation path.

### Changed records and variants

| Record | Exact 0.99 labels |
| --- | --- |
| `StateSnapshotInput` | `fleet_state`, `fleet_directory`, `subnet_directory` |
| `CanisterInitPayload` | `fleet`, `install_id`, `release_build_id`, `env`, `fleet_directory`, `subnet_directory` |
| `EnvBootstrapArgs` | `fleet_root_pid`, `subnet_slot`, `subnet_pid`, `root_pid`, `canister_role`, `parent_pid` |
| `EnvSnapshotResponse` | same labels as `EnvBootstrapArgs` |
| `DelegationAudience` | sole `fleet : FleetKey` variant |
| `InstallActiveDelegationProofRequest` | `credential`, `bundle` |

`TopologySnapshotInput { parents, children_map }` and its nested records remain
unchanged. `Error { code, message }`, all existing error-code labels,
`PageRequest { limit, offset }` and `SubnetRegistryResponse` remain unchanged.

The exact lifecycle arguments are:

~~~rust
pub struct CurrentRootInstallIdentity {
    pub fleet: FleetBinding,
    pub install_id: [u8; 32],
    pub release_build_id: ReleaseBuildId,
    pub expected_module_hash: Option<[u8; 32]>,
}

pub struct CanisterInitPayload {
    pub fleet: FleetBinding,
    pub install_id: [u8; 32],
    pub release_build_id: ReleaseBuildId,
    pub env: EnvBootstrapArgs,
    pub fleet_directory: FleetDirectoryInput,
    pub subnet_directory: SubnetDirectoryInput,
}

pub struct FleetActivationIdentity {
    pub fleet: FleetBinding,
    pub operation_id: [u8; 32],
    pub release_build_id: ReleaseBuildId,
}

pub enum FleetActivationPhase {
    Prepared,
    Active,
}

pub struct FleetCascadeManifestEntry {
    pub principal: Principal,
    pub state_snapshot_hash: [u8; 32],
    pub topology_snapshot_hash: [u8; 32],
}

pub struct FleetCredentialGenerationRef {
    pub generation: u64,
    pub manifest_hash: [u8; 32],
}

pub struct FleetActivationStatusResponse {
    pub phase: FleetActivationPhase,
    pub identity: FleetActivationIdentity,
    pub cascade: Option<FleetCascadeActivationEvidence>,
    pub cascade_manifest: Option<Vec<FleetCascadeManifestEntry>>,
    pub credential: Option<FleetCredentialGenerationRef>,
    pub credential_manifest: Option<FleetCredentialManifest>,
    pub activated_at_ns: Option<u64>,
}

pub struct FleetActivationResumeRequest {
    pub operation_id: [u8; 32],
    pub credential: FleetCredentialGenerationRef,
}

pub struct FleetCredentialGenerationRequest {
    pub operation_id: [u8; 32],
    pub credential: FleetCredentialGenerationRef,
}

pub struct FleetActivationRequest {
    pub operation_id: [u8; 32],
    pub credential: FleetCredentialGenerationRef,
    pub activation_evidence_hash: [u8; 32],
}
~~~

- fresh root `init` accepts only `CurrentRootInstallIdentity`;
- fresh managed non-root `init` accepts `CanisterInitPayload` followed by the
  existing optional application bytes;
- managed root and non-root 0.98-to-0.99 `post_upgrade` is unsupported; and
- `start_local!` remains a local-only standalone lifecycle and cannot qualify
  an installed managed Fleet Canister.

The fresh lifecycle records normalize into the one internal
`FleetActivationIdentity` by mapping `install_id` to `operation_id`. The
protected state stores that identity inside `Prepared`/`Active`; it does not
store parallel install and upgrade states.

The activation bounds and durable ownership are frozen:

| Object | Exact bound and owner |
| --- | --- |
| activation Canisters/cascade entries | 4,096 entries; ID 21 on the root, mirrored in the sole host activation record |
| canonical cascade manifest | 1,048,576 bytes |
| credential manifest | 64 entries and 262,144 bytes; ID 21 on the root |
| credential bundle | 65,536 canonical bytes; existing auth allocation on the root |
| proof-install raw Candid payload | 131,072 bytes before decoding |
| retained bundle-generation set | 4,194,304 bytes; existing auth allocation |
| retained prepared credential generations | current plus immediately previous, exactly 2 maximum |
| complete ID-21 activation record | 2,097,152 canonical bytes |
| complete host activation record | 2,097,152 canonical bytes |

Credential generation starts at `1` and advances only by checked `n + 1` for
the same activation identity. Every issuer and non-issuer must record the same
`FleetCredentialGenerationRef`; issuers additionally install their exact
bundle. The first activation call is forbidden until that convergence is
durable. `Active` freezes the generation. If a previously verified proof
expires after activation calls begin, it remains non-authorizing; exact
activation resumes from stored evidence and ordinary post-`Active` renewal
restores availability. This is the sole bounded expiry recovery path, not a
second activation or credential authority.

### Directory shapes

`DirectoryProvenance { fleet: FleetBinding, source_root: Principal }`,
`FleetDirectoryInput` and `SubnetDirectoryInput` are the only cascade
envelopes. Each input contains `provenance` and bounded `entries`.
`FleetDirectoryPageResponse` and `SubnetDirectoryPageResponse` each contain
`provenance`, bounded `entries` and `total`. `DirectoryEntryInput` and
`DirectoryEntryResponse` contain only `role` and `pid`.

There is no provenance-free parallel DTO. The fixed bounds are 16,384 encoded
bytes, 1,000 cascade entries and 1,000 page entries.
The root authors the provenance once. Direct children authenticate that root
as parent; deeper receivers authenticate their exact parent relay and require
the unchanged `source_root` to equal their protected Fleet root.

### Managed endpoint qualification

Direct source inspection found one raw production Canic query export:
`canic_memory_ledger` in
`crates/canic/src/macros/endpoints/shared.rs`. It must move to
`canic_query(requires(caller::is_controller()))` without changing its Candid
name, arguments, result or access.

The remaining raw query/update exports are test-only support Canisters:

- `canisters/test/sharding_root_stub/src/lib.rs`; and
- `canisters/test/blob_storage_cashier_mock/src/lib.rs`; and
- `canisters/test/intent_authority/src/lib.rs`.

The first two simulate external surfaces; `intent_authority` is an isolated
receipt-backed-intent PocketIC fixture. None calls `canic::start!` or qualifies
as a managed Fleet application Canister. They may retain their direct test
exports. Every endpoint in an installed managed Fleet Canister, including
Canic-provided endpoints, must pass through
`canic_query`/`canic_update`. Lifecycle and `canister_inspect_message` hooks are
not query/update exceptions and do not provide the Prepared fence.

### Reinstall-only release boundary

0.99 accepts no pre-0.99 host state, stable state, Canister or recovery
evidence. The supported release operation is a fresh installation using the
0.99 configuration, artifacts, activation journal and protected runtime
identity. Existing deployments are reinstalled.

No qualification probe, quiescence sequence, operator assertion or inferred
idle state can turn an earlier installation into a supported 0.99 input.

## CLI Inventory

The inventory was completed recursively against the immutable `v0.98.24`
predecessor and rechecked against the post-`v0.99.30` command tree. Every
accepted leaf has one disposition below. A family row enumerates all of its
leaves explicitly; it is not a disposition for an unreviewed wildcard.

### Source and diagnostic hard cuts

The source/config family changed atomically:

~~~text
canic fleet check                 -> canic app check
canic fleet create                -> canic app create
canic fleet list                  -> canic app list
canic fleet config                -> canic app config
canic fleet adoption report       -> canic app adoption report
canic fleet role declare          -> canic app role declare
canic fleet role attach           -> canic app role attach
canic fleet role rename           -> canic app role rename
canic fleet role list             -> canic app role list
canic fleet role inspect          -> canic app role inspect
canic fleet delete                -> canic app delete
canic build <fleet> <role>        -> canic build <app> <role>
scaffold canister --fleet         -> scaffold canister --app
~~~

All corresponding positional value names, help, errors, output headings and
internal option types changed from source `fleet` to `app`. The role-attach
`--subnet prime` value becomes `--subnet default`.

| Predecessor leaf | Surviving leaf | Disposition | Current owner | Current consumer and non-overlapping purpose |
| --- | --- | --- | --- | --- |
| `fleet check` | `app check` | `rename` | App configuration diagnostics | operators validate one source App |
| `fleet create` | `app create` | `rename` | App scaffolding | operators create one complete minimal App |
| `fleet list` | `app list` | `rename` | App discovery | operators enumerate checked-in Apps |
| `fleet config` | `app config` | `rename` | App config projection | operators inspect one source App contract |
| `fleet adoption report` | `app adoption report` | `rename` | passive adoption analysis | operators review onboarding evidence without mutation |
| `fleet role declare` | `app role declare` | `rename` | App role lifecycle | operators declare a source role |
| `fleet role attach` | `app role attach` | `rename` | App role lifecycle | operators attach a role to one workload slot |
| `fleet role rename` | `app role rename` | `rename` | App role lifecycle | operators rename a source role and matching package metadata |
| `fleet role list` | `app role list` | `rename` | App role projection | operators enumerate declared App roles |
| `fleet role inspect` | `app role inspect` | `rename` | App role projection | operators inspect one role contract |
| `fleet delete` | `app delete` | `rename` | App filesystem lifecycle | operators delete one explicitly confirmed source App |
| `build <fleet> <role>` | `build <app> <role>` | `rename` | Canister build workflow | operators build one App role artifact |
| `scaffold canister --fleet` | `scaffold canister <app> <role>` | `rename` | declared-role scaffolding | operators add one declared-only role to an existing App |
| `inspect canister <principal>` | unchanged | `keep` | runtime inspection | operators inspect one exact principal without Fleet lookup |
| `inspect deployment <name> --role <role>` | `inspect fleet <fleet> --role <role>` | `rename` | runtime inspection plus canonical installed-Fleet resolution | operators inspect one exact role selected through Fleet authority |
| `medic project` | unchanged | `keep` | passive project diagnostics | operators and CI validate local source/config readiness without selecting a Fleet |
| `medic deployment <name>` | `medic fleet <fleet>` | `rename` | passive Fleet diagnostics plus canonical installed-Fleet resolution | operators and CI validate one installed Fleet and optional auth/blob-storage capabilities |

The renamed leaves have no predecessor parser, alias, help text, test or active
documentation path.

### Complete surviving command tree

Every row is `keep`. The owner column names the sole implementation authority;
the final column identifies the current consumer and distinct purpose.

| Leaf | Owner | Current consumer and non-overlapping purpose |
| --- | --- | --- |
| `status` | project/Fleet status projection | operators view independently discovered source Apps and installed Fleets |
| `medic project` | project preflight diagnostics | operators and CI validate local source, config and package readiness |
| `medic fleet` | Fleet preflight diagnostics | operators and CI validate one installed Fleet and optional targeted capabilities |
| `state audit` | state-contract audit | operators validate declared stable-state allocation and metadata |
| `state manifest` | state-manifest projection | operators and CI inspect the derived state contract |
| `app check` | App configuration diagnostics | operators validate one source App |
| `app create` | App scaffolding | operators create a complete minimal App |
| `app list` | App discovery | operators enumerate checked-in Apps |
| `app config` | App config projection | operators inspect one source App contract |
| `app adoption report` | passive adoption analysis | operators review onboarding evidence without mutation |
| `app role declare` | App role lifecycle | operators declare one role |
| `app role attach` | App role lifecycle | operators attach a role to one workload slot |
| `app role rename` | App role lifecycle | operators rename one role and matching package metadata |
| `app role list` | App role projection | operators enumerate declared roles |
| `app role inspect` | App role projection | operators inspect one role contract |
| `app delete` | App filesystem lifecycle | operators delete one explicitly confirmed source App |
| `scaffold canister` | declared-role scaffolding | operators add one declared-only canister role to an existing App |
| `replica start` | local replica lifecycle | developers start the project-owned local ICP process |
| `replica status` | local replica lifecycle | developers inspect the project-owned replica process |
| `replica stop` | local replica lifecycle | developers stop the project-owned replica process |
| `network enroll` | canonical-network trust enrollment | operators bind a non-public environment profile to an explicitly verified root key |
| `install` | fresh Fleet installation journal | operators install and activate one App as one Fleet; this is the sole install mutation |
| `inspect canister` | runtime inspection | operators inspect one exact principal without Fleet lookup |
| `inspect fleet` | runtime inspection plus installed-Fleet resolution | operators inspect one exact role selected through Fleet authority |
| `blob-storage status` | blob billing workflow | operators inspect one selected Fleet role's billing state |
| `blob-storage sync-gateways` | blob billing workflow | operators explicitly synchronize gateway billing state |
| `blob-storage fund` | blob billing workflow | operators explicitly fund one billing cashier |
| `auth renewal status` | delegated-auth diagnostics | operators inspect issuer policy, proof and renewal state in one Fleet |
| `build` | Canister build workflow | operators build one App role artifact |
| `deploy plan` | deployment-truth planner | operators explain the deterministic plan without mutation |
| `deploy check` | deployment-truth check | operators and CI evaluate current local/observed deployment evidence |
| `deploy inspect plan` | deployment-truth projection | operators inspect the raw plan JSON |
| `deploy inspect inventory` | deployment-truth projection | operators inspect observed inventory JSON |
| `deploy inspect diff` | deployment-truth projection | operators inspect plan-versus-inventory differences |
| `deploy inspect report` | deployment-truth projection | operators inspect the complete passive report |
| `deploy inspect compare` | deployment comparison | operators compare two validated check artifacts |
| `deploy inspect catalog list` | canonical Fleet catalog projection | operators list durable Fleets for one resolved network |
| `deploy inspect catalog inspect` | canonical Fleet catalog projection | operators inspect one durable Fleet row |
| `deploy inspect resume-report` | install receipt reconciliation | operators inspect restart/resume safety from an exact receipt |
| `evidence gate` | stable evidence-envelope policy | CI and operators evaluate one maintained evidence artifact |
| `evidence compare` | stable evidence-envelope comparison | CI and operators compare two maintained evidence artifacts |
| `cycles balance` | ICP CLI wallet adapter | operators inspect the selected identity's cycles balance |
| `cycles convert` | manual root ICP-to-cycles adapter | operators explicitly convert ICP already held by one Fleet root |
| `cycles mint` | ICP CLI wallet adapter | operators explicitly mint cycles from ICP |
| `cycles transfer` | ICP CLI wallet adapter plus Fleet target resolution | operators transfer cycles to one exact principal or Fleet role |
| `cycles topup` | ICP CLI wallet adapter plus Fleet target resolution | operators top up one installed Fleet canister |
| `token balance` | ICP CLI token adapter | operators inspect a token balance |
| `token transfer` | ICP CLI token adapter plus Fleet target resolution | operators transfer tokens to one exact principal or Fleet role |
| `info list` | installed-Fleet information projection | operators list one Fleet's registered canisters |
| `info cycles` | installed-Fleet information projection | operators inspect one Fleet's cycles distribution |
| `info metrics` | runtime metrics projection | operators inspect maintained metrics for one Fleet target |
| `info endpoints` | runtime endpoint projection | operators inspect the exported Canic endpoint inventory |
| `info env` | runtime environment projection | operators inspect one Canister's protected Fleet/environment identity |
| `backup create` | backup execution journal | operators capture one Fleet topology and durable snapshots |
| `backup list` | backup layout projection | operators enumerate backup directories |
| `backup inspect` | backup layout projection | operators inspect one backup or dry-run plan |
| `backup manifest validate` | backup manifest validator | operators validate one manifest without live calls |
| `backup prune` | backup cleanup policy | operators explicitly remove selected, non-protected backup directories |
| `backup verify` | backup verifier | operators verify journal agreement and artifact checksums |
| `backup status` | backup recovery projection | operators inspect resumable journal state |
| `restore plan` | restore planner | operators derive a restore plan without mutation |
| `restore prepare` | restore document authority | operators atomically create or adopt the canonical plan and journal |
| `restore apply` | restore dry-run builder | operators validate and materialize the restore execution document |
| `restore run` | restore execution journal | operators execute or resume the bounded restore state machine |
| `restore status` | restore recovery projection | operators inspect durable restore progress and required attention |

The apparent overlaps are intentional and bounded:

- `status` is a quick project/Fleet inventory; `medic` is readiness
  diagnostics; `state` audits declared persistence; `info` projects maintained
  live endpoints; and `inspect` reads runtime status. None owns another's
  authority.
- `deploy inspect` reads deployment-truth and recovery artifacts; top-level
  `inspect` calls live runtime-status endpoints.
- `evidence` evaluates the maintained generic evidence envelope and does not
  rebuild the deleted lifecycle/promotion DTO families.

### Deleted command and report families

| Predecessor leaf | Disposition | Reason and surviving authority |
| --- | --- | --- |
| `deploy register` | `delete` | fresh `install` atomically publishes the canonical-network Fleet catalog |
| `deploy root verify` | `delete` | activation/install receipts own root verification |
| `deploy inspect root` | `delete` | no consumer remained after manual root verification was removed |
| `deploy install` | `move` | top-level `install <app> <fleet>` is the sole mutating install entrypoint |
| `deploy authority check` | `delete` | `deploy check` already contains the controller reconciliation plan used by install preflight |
| `deploy authority evidence` | `delete` | orphan dry-run wrapper with no operator or execution consumer |
| `deploy authority report` | `delete` | orphan dry-run aggregate with no operator or execution consumer |
| `deploy authority receipt` | `delete` | evidence-only receipt recorded zero actions and owned no recovery transition |
| `deploy external plan` | `delete` | passive archived external-lifecycle DTO builder with no execution owner |
| `deploy external check` | `delete` | passive archived external-lifecycle DTO builder with no execution owner |
| `deploy external handoff` | `delete` | passive archived external-lifecycle DTO builder with no execution owner |
| `deploy external proposals` | `delete` | passive archived external-upgrade DTO builder with no execution owner |
| `deploy external pending` | `delete` | passive archived external-lifecycle DTO builder with no execution owner |
| `deploy external critical-fix` | `delete` | passive archived report with no execution owner |
| `deploy external verify` | `delete` | supplied-evidence wrapper with no live verification owner |
| `deploy external inspect consent` | `delete` | archived request-to-DTO transformer with no consent authority |
| `deploy external inspect verification-policy` | `delete` | archived request-to-DTO transformer with no policy consumer |
| `deploy external inspect verification-check` | `delete` | archived request-to-DTO transformer with no live verification owner |
| `deploy external inspect completion` | `delete` | archived request-to-DTO transformer with no completion authority |
| `deploy promote plan` | `delete` | passive artifact-promotion transformer with no maintained install consumer |
| `deploy promote check` | `delete` | passive artifact-promotion transformer with no maintained install consumer |
| `deploy promote diff` | `delete` | passive artifact-promotion transformer with no maintained install consumer |
| `deploy promote inspect readiness` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect artifact-identity` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect transform` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect transform-evidence` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect target-lineage` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect provenance` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect wasm-store-identity` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect catalog-verification` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect execution-receipt` | `delete` | evidence-only wrapper with no execution or recovery owner |
| `deploy promote inspect policy` | `delete` | archived request-to-DTO transformer |
| `deploy promote inspect materialization-identity` | `delete` | archived request-to-DTO transformer |

Deletion includes CLI dispatch, parsing, help, examples, tests and the orphan
public host DTO/build/validation/digest/text stacks. The current authority
reconciliation plan remains because deployment preflight and installation use
it directly. Current Wasm-template publication lifecycle and current
install-artifact materialization also remain; they are live execution
behaviors, not the removed passive promotion command family.

The public host boundary is `canic_host::installed_fleet` with
`InstalledFleetRequest`, `InstalledFleetResolution`,
`InstalledFleetRegistry`, `InstalledFleetSource`, `ResolvedFleetTopology`,
`InstalledFleetError`, `resolve_installed_fleet_from_root` and
`read_installed_fleet_from_root`. There is no parallel
`installed_deployment` module or compatibility alias.

The local pending ICP-conversion log stores `fleet` and binds that value into
its replay key and generated operation identity. Pre-cut pending files
containing `deployment` fail schema decoding; there is no dual reader. Backup
manifests and reports retain `deployment` only where the value names the
captured deployment operation/topology artifact rather than the live selector.

`deploy register` is deleted. Fresh `canic install <app> <fleet>` owns Fleet
registration through the canonical-network Fleet catalog and activation
journal; no manual environment-scoped registration authority remains.
The `deploy root verify` and `deploy inspect root` families are also deleted
because their root-verification transition and passive evidence artifacts had
no maintained consumer after manual registration was removed.

Fresh installation is exactly:

~~~text
canic install <app> <fleet>
~~~

Both values are mandatory. The command never infers the App from the Fleet
name and never requires the two names to match. Plan-driven installation is:

~~~text
canic deploy plan <fleet> --app <app>
~~~

The plan is passive deployment evidence. It is not accepted through a second
installation command.

The new network-enrollment command is:

~~~text
canic network enroll <environment>
  --root-key <der-file>
  --fingerprint <64-lowercase-hex>
~~~

There is no automatic trust-on-first-use or alias for another network command.

`canic status` renders independently discovered App and Fleet sections:

~~~text
APPS:   APP | CONFIG | CANISTERS
FLEETS: FLEET | APP | NETWORK | DEPLOYED | ROOT
~~~

It never joins an App config to a Fleet because their display names happen to
be equal.

The implementation authority is exact:

- the App table is projected only from checked-in App config discovery;
- the Fleet table is projected only from the catalog selected by canonical
  network identity; and
- an optional live-role comparison may use only the Fleet catalog row's
  explicit `app` identity to select an App config.

An App without a Fleet and a Fleet without a locally available App are both
valid status rows. Equal App and Fleet display names do not create a
relationship.

The deployment planning form is
`canic deploy plan <fleet> --app <app>`. There is no `--fleet-template`,
`--deployment-name`, old `canic fleet` source command or command alias.

## Host JSON Inventory

Serialized deployment-operation type names remain. Every current serialized
field is classified by meaning:

| Baseline JSON meaning/label | 0.99 label |
| --- | --- |
| source `fleet`, `fleet_template`, `expected_fleet_template`, `observed_fleet_template`, `package_metadata_fleet` | `app`, `app`, `expected_app`, `observed_app`, `package_metadata_app` |
| live `deployment_name`, `deployment`, `expected_deployment_name`, `observed_deployment_name` | `fleet_name`, `fleet`, `expected_fleet_name`, `observed_fleet_name` |
| ambient target identity expressed only by `environment` plus a name | add exact `canonical_network_id` and generated `fleet_id`; retain `environment` only as provenance |
| release build evidence | `release_build_plan_hash`, `release_build_id`, `release_set_manifest_digest`; the nonce remains only in `ReleaseBuildPlanRecord` |
| deployment workflow/evidence terms such as `plan_id`, `deployment_manifest_digest`, `DeploymentPlanV1` and `DeploymentReceiptV1` | retain |

The existing `DeploymentIdentityV1` evolves in place and is the standard JSON
projection:

~~~text
{
  "canonical_network_id": "<64 lowercase hex>",
  "fleet_id": "<64 lowercase hex>",
  "fleet_name": "<canonical name>",
  "app": "<canonical app>",
  "environment": "<non-authoritative profile>",
  ...existing deployment evidence fields...
}
~~~

Fleet catalog rows, activation journals, plans, checks, inventories, execution
receipts, policy/evidence envelopes and App-adoption inputs use those exact
identity labels when they carry the identity. They do not retain a second
`deployment_name`, `fleet_template` or ambiguous `fleet` source field.
The root-verification request/report/receipt family is deleted with its
command paths and has no replacement DTO.

Backup/restore `BackupPlan.fleet` and equivalent live-target fields remain
Fleet terms but must bind the same canonical network and Fleet ID. The manifest
section named `deployment` remains because it records a backup of a deployment
operation/topology snapshot. Schema/version numbers change only where the
existing serialized contract's normal versioning rules require the hard-cut
shape; no legacy reader remains.

## Host Path Inventory

| Baseline path | 0.99 path/treatment |
| --- | --- |
| `fleets/<source>/...` | `apps/<app>/...` |
| `.canic/<environment>/deployments/<name>.json` | unsupported legacy state; never read or migrated |
| `.canic/<environment>/deployment-receipts/<name>/...` | unsupported legacy state; never read or migrated |
| canonical deployment receipts | `.canic/networks/<network-id>/fleets/<fleet-id>/deployment-receipts/...` |
| environment-scoped installed catalog | network-scoped Fleet catalog mapping immutable `FleetName` to `FleetId` |
| enrolled non-public trust root | `.canic/networks/<network-id>/trust/root-key.der` plus `enrollment.json`; profile lookup at `.canic/environment-profiles/<environment>/network.json` |
| fresh-install activation recovery | `.canic/recovery/fleet-install-activations/<network-id>/<fleet-id>/<operation-id>/journal.cbor` |

The fresh-install journal has exactly `Planned`, `RootInstalled`,
`CanistersPrepared`, `CanistersActivated` and `HostAuthorityCommitted` phases.
It binds the finalized release-build plan hash and manifest digest, the exact
`FleetActivationHostRecord`, the root-install receipt when present and the
committed Fleet-catalog hash when terminal. It is the sole fresh-install
recovery authority. An environment alias that resolves to the same network
uses the same Fleet catalog and cannot create a second host authority.

## Stable-State Inventory

| ID | Key | Frozen 0.99 treatment |
| --- | --- | --- |
| 12 | `canic.core.app_index.v1` | rename owner to `FleetDirectory`; preserve bytes/key |
| 13 | `canic.core.subnet_index.v1` | rename owner to `SubnetDirectory`; preserve bytes/key |
| 14 | retired | never reuse |
| 15 | `canic.core.subnet_registry.v1` | retain exact `SubnetRegistry` |
| 16 | `canic.core.env.v1` | initialize the current `fleet_root_pid`/`subnet_slot` shape with `default` |
| 17 | retired | never reuse |
| 18 | `canic.core.app_state.v1` | rename owner to `FleetState`; preserve bytes/key |
| 19 | `canic.core.auth_state.v1` | initialize the Fleet-only shape; later own exact prepared credential bundles keyed by the ID-21 activation identity |
| 21 | `canic.core.fleet_activation.v1` | new sole bounded `FleetActivationRecord` owning `FleetBinding`, local state, non-root Prepared application init bytes, the root cascade manifest and retained credential manifests |
| 55 | `canic.core.directory_registry.v1` | rename owner to `PlacementBindingRegistry`; preserve bytes/key |

No other allocation changes solely for terminology. There is no old ID-19
decoder or runtime dual reader. ID 21 is not mirrored into Env or Directory
storage. ID 19 owns only the exact proof bundles and retry records; it does
not duplicate the Fleet binding, cascade manifest or credential-manifest
bytes.

## Delegated-Auth Cut

The removal set is complete:

- DTO `DelegationAudience::{Canister, CanicSubnet, Project}`;
- model `RootDelegationAudiencePolicy` equivalents;
- stable `DelegationAudienceRecord` equivalents;
- canonical encoder/decoder tags and comparison rules for all old variants;
- ambient `local_canic_subnet` and `local_project` audience acceptance;
- issuer policies and renewal templates containing any old variant;
- delegated sessions, prepared/retained tokens, active proofs, chain-key
  batches, positive caches and replay material whose meaning depends on them;
  and
- tests, benches, examples and docs that construct an old audience.

The sole post-cut audience is `Fleet(FleetKey)`. Canister and role restriction
remain available through existing signed subject/grant data rather than through
an alternative audience.

## Search Scope and Implementation Gate

The baseline semantic search found 2,247 matching lines across 325 active
source/test/script files for the combined old families. The largest ownership
families include config/build discovery, Candid topology DTOs, stable
directories/state, auth canonicalization, deployment truth, installed state,
backup/restore and fixtures. Counts are scope evidence, not proof of
completion.

Slice closeout must search active code, tests, scripts, examples, generated
interfaces and active docs for:

- source/config uses of `fleet`, `fleets/`, `FleetConfig` and `FleetRoleRef`;
- live-identity uses of `deployment_name`, `fleet_template` and environment as
  identity;
- `AppIndex`, `SubnetIndex`, public topology `Index`, `AppState` and
  `DirectoryRegistry`;
- `SubnetRole`, `Prime`, `prime`, `prime_root_pid` and prime predicates;
- every old delegation-audience variant and canonical tag;
- raw query/update exports in managed Fleet packages; and
- any 0.99 `canic_fleet_registry`, Authority-Subnet selector or Coordinator
  writer.

Matches may survive only where this inventory explicitly retains the ordinary
English/external/stable-key meaning. No alias, deprecated endpoint, fallback
path, dual schema, compatibility decoder or anti-resurrection test is
permitted.

## Preparation Verdict

`TERMINOLOGY COMPLETE — REINSTALL-ONLY IMPLEMENTATION APPROVED`.

The semantic destinations and named boundaries are frozen. The first
mutating batch is **Implementation Slice 1 — Minimum Config Hard Cut**.
