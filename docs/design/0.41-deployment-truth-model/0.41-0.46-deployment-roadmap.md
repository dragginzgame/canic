# Deployment Roadmap

## Status

Tentative cross-line roadmap for deployment work after the 0.40 protected
internal-call line.

This file is the compact contract across the approved 0.41 through 0.46 design
docs. Post-46 topics are backlog notes, not approved numbered follow-on lines.

---

## Line Shape

```text
0.41 tells truth and refuses unsafe installs.
0.42 reconciles authority.
0.43 abstracts execution and records partial outcomes.
0.44 promotes artifact identity, not authority.
0.45 coordinates lifecycle without pretending Canic has authority.
0.46 compares deployments using stable truth artifacts.
```

The order matters:

```text
observe truth
-> refuse unsafe states
-> reconcile authority
-> abstract execution
-> promote artifacts
-> coordinate external lifecycle
-> compare deployments
```

Canic should not become more flexible before it becomes more honest.

---

## Deployment Terminology

Use these terms consistently:

```text
FleetTemplate:
  reusable desired topology:
  roles, build and discovery rules, config defaults, pool policy shape,
  scaffold profile, and adoption profile.

DeploymentTarget:
  concrete live deployment:
  prod, staging, v2, tenant-a, destructive-rehearsal,
  with root, network, controllers, canister IDs, artifact set, receipts,
  and inventory.

DeploymentGroup:
  operational collection of deployment targets:
  prod + staging + v2, promotion lanes, and comparison policy.
```

Naming rule:

```text
fleet template = reusable desired topology
deployment target = concrete root/network/canister/artifact state
deployment group = related deployment targets
```

`FleetTemplate` is the model term. `canic fleet ...` is the template/adoption
command namespace for reusable project shape, distinct from
`canic deploy ...`.

New deployment-truth docs and new `canic deploy ...` command sketches should
use `deployment` when referring to live state. CLI placeholders should use
`<deployment>` for a concrete `DeploymentTarget`.

0.41 should use deployment-target command names for live state. Obsolete
fleet-named live state is invalid input, not a source for automatic
conversion into deployment-target state.

---

## Hard Cut Policy

Deployment lines do not carry backward-compatibility obligations for old live
deployment commands or old live deployment state.

Each minor line or patch may hard-cut unsafe deployment behavior when the new
truth model requires it.

Rules:

- no old live-deployment CLI names kept as aliases;
- no compatibility shims for obsolete live deployment state;
- no automatic conversion from legacy fleet-named live state;
- no fallback planner that rediscovers deployment intent outside
  `DeploymentPlanV1`;
- no promise that a command or state file that worked before a line still works
  after that line.

If an obsolete path is encountered, Canic should fail with a clear error that
points to the new deployment-target model.

Hard-cut failures should be typed safety or report errors where possible, not
panics.

---

## Install Safety Ladder

0.41 observes, diffs, reports, validates local/materialized artifacts, emits
lightweight current-install phase receipts, and may refuse known unsafe
continuation through current installer gates.

0.41 does not replace the executor.

0.42 explains authority reconciliation and external actions. The minimum
release is report-first and dry-run: it produces authority reconciliation
reports, not controller mutation.

0.43 establishes plan-driven execution behind `DeploymentExecutor`. Full
deploy-install semantics belong there unless an earlier line explicitly ships a
thin preflight wrapper. Its result is an operation receipt with partial
application semantics, not a boolean success/failure flag.

Receipts never replace live inventory.

---

## Minimum Releasable Slices

These are minimum safe release bars for each line. They are not the full design
scope and they do not require every later command surface to exist before a line
can ship.

```text
0.41 minimum release:
  read-only inventory
  role artifact manifest
  safety report
  post-build materialization checks
  lightweight current-install phase receipts with verified postconditions
  receipt status vocabulary that can express partial execution evidence

0.42 minimum release:
  dry-run authority reconciliation
  exact external-action report

0.43 minimum release:
  current CLI backend behind executor boundary
  no new backend required
  artifact root decoupling from plan semantics
  structured execution receipts with partial-application status
  at least one test or harness path validates the same DeploymentPlanV1 shape
    used by real deployments

0.44 minimum release:
  digest-pinned artifact override plan
  promotion readiness report
  role-scoped promotion artifact level: sealed wasm vs source/build
  target canonical embedded config check

0.45 minimum release:
  LifecycleAuthorityV1 projection from existing CanisterControlClassV1 classes
  external-upgrade proposal and verification shape

0.46 minimum release:
  plan/inventory/receipt comparison
  drift report over the 0.41 diff categories
```

---

## Adoption Coverage Semantics

The 0.41 through 0.46 roadmap is limited to deployment needs directly tied to
truth, authority, execution, promotion, external lifecycle, and comparison.

Items below use three labels:

- **Release bar**: required before the line is complete.
- **Design hook**: the model must not block this later work, but UX or stable
  public command surfaces may wait.
- **Deferred backlog**: named backlog outside 0.41 through 0.46.

This distinction keeps the active roadmap from becoming another monolithic
deployment platform rewrite.

A line is complete when its release bar ships. Design hooks must not be
described in changelogs or status docs as shipped user-facing features until
their owning line implements them.

### 0.41 Deployment Truth

Release bar:

- read-only inventory;
- role artifact manifest;
- safety report;
- post-build materialization checks;
- lightweight current-install phase receipts with verified postconditions for
  phases the current installer already executes.

Design hook:

- deployment identity stored by deployment target, not only fleet template
  name;
- inventory-only reads against `ic`;
- host-side config validation before build;
- deployment catalog fields usable by 0.46.

### 0.42 Authority Reconciliation

Release bar:

- dry-run authority reconciliation;
- exact external-action report.

Design hook:

- destructive pool import classified explicitly and requiring confirmation
  later;
- observe/adopt planning for existing canisters without destructive reset;
- imported, jointly controlled, user-controlled, and unsafe canister classes;
- per-spawn controller policy as a design object;
- CLI profile and identity-binding semantics as authority design.

### 0.43 Backend-Agnostic Execution

Release bar:

- current CLI backend behind the executor boundary;
- no new backend required;
- artifact root decoupling from plan semantics;
- at least one test or harness path validates the same `DeploymentPlanV1` shape
  used by real deployments.

Design hook:

- workspace root, ICP root, and artifact root fields recorded so project
  manifests can bind them later;
- executor result shapes are structured enough for future JSON;
- `wasm_store` staging through `DeploymentExecutor::stage_artifact`;
- optional project manifest shape is anticipated, but the stable public
  contract is deferred to the post-46 CI/GitOps backlog.

### 0.44 Artifact Overrides And Promotion

Release bar:

- digest-pinned artifact override plan;
- promotion readiness report;
- promotion artifact level: sealed wasm vs source/build;
- target canonical embedded config check.

Design hook:

- external wasm and pinned wasm sources;
- explicit config-independent role flags;
- verifier, role epoch, and cascade prerequisites in promotion readiness;
- role-scoped artifact override planning;
- store-backed receipt artifact locators.

### 0.45 External And User-Owned Lifecycle

Release bar:

- `LifecycleAuthorityV1` projection from existing `CanisterControlClassV1`
  classifications;
- external-upgrade proposal and verification shape.

Design hook:

- partial deployment upgrade semantics for user-controlled roles;
- security patch playbook shape;
- SDK or frontend consent requirements without implementing a frontend
  contract;
- externally completed upgrade receipts.

### 0.46 Multi-Deployment Operations

Release bar:

- deployment-target local state hard cut;
- exact deployment target identity for plan-mediated install;
- explicit deployment registration with minimal state;
- old fleet-named live state refusal with recovery guidance;
- passive two-target comparison over existing deployment-truth artifacts.

Design hook:

- comparison model can preserve enough normalized fields for later deployment
  groups, catalog, teardown, verification, and adoption work, but those
  workflows are post-46 backlog topics.

### Deferred Backlog

These are important adoption needs, but they should not expand the 0.41 through
0.46 release bars:

- full brownfield adoption suite;
- standalone or leaf-only production profile;
- frontend declaration pipeline;
- official GitHub or GitLab action;
- signed plans and Sigstore-style receipts;
- OpenTelemetry or external monitoring integration;
- full `wasm_store` artifact registry and retention;
- deployment clone and trust-domain migration command;
- reproducible build attestation;
- cost attribution per deployment;
- memory migration tooling;
- complete non-Rust or hybrid fleet build orchestration.

### Current Sealed Artifact Policy

While `CANIC_CONFIG_PATH` embeds deployment-specific config into wasm, Canic
must assume per-deployment rebuilds whenever canonical embedded config differs.

`build once, deploy many` is safe only for roles whose metadata proves they are
config-independent or for targets with identical canonical embedded config.

Until a later split-config design exists, promotion must not imply sealed wasm
byte identity across roots, networks, authority profiles, or runtime variants.

---

## Cross-Line Invariants

### Receipts Are Not Truth

Receipts are evidence of attempted work and verified postconditions.

Live inventory wins over local state.

### Deployment Execution Is Not Atomic

Canic must not model deployment execution as all-or-nothing.

A failed deployment may leave some roles changed, some unchanged, and some not
attempted. Receipts must preserve per-phase and per-role outcomes.

The first recovery target is re-inventory plus resume from verified live
postconditions, not automatic rollback. Rollback or restore is an explicit
later plan type.

### Plans Do Not Execute Alone

Canic installs only after comparing:

```text
intended plan
vs
last receipt
vs
current live inventory
```

### Root Trust Anchor Defines Trust Domain

Same deployment means same root trust anchor.

Different root trust anchor means a new trust domain or an explicit migration.

### Config Digests Are Canonical

Safety decisions use canonical embedded config digests, not raw config file
bytes.

The first 0.41 implementation slice should lock canonical digest behavior with
tests for:

- reordered TOML;
- resolved includes;
- explicit defaults;
- normalized principals;
- changed root or trust-domain values.

### Promotion Never Copies Authority

Promotion may carry sealed wasm bytes or source/build identity.

It must not copy source root identity, controllers, authority profile, network,
pool canister IDs, or operation epoch into the target by default.

### Promotion Mode Is Explicit

Promotion never implies both byte identity and source/build identity.

Each promoted role chooses exactly one level:

```text
sealed wasm:
  exact source bytes are reused

source/build:
  the same source/build recipe is materialized for the target config
```

When `CANIC_CONFIG_PATH` or another target config input changes embedded
deployment identity, source/build promotion may intentionally produce a
different target wasm digest.

For source/build promotion, Canic preserves the source/build recipe identity
but intentionally rematerializes the artifact with the target deployment's
canonical embedded config. The target artifact digest and target config digest
are new evidence, not proof of sealed byte identity.

### Executor Backends Do Not Change Plan Meaning

`DeploymentPlanV1` means the same thing whether execution uses the current CLI
backend, PocketIC, or a future direct-agent backend.

### Unknown Control Classification Blocks Mutation

Unknown or unsafe canister control classification blocks mutation until
inventory or authority reconciliation proves a safer state.

### `wasm_store` Is A Role And A Possible Transport

`wasm_store` is modeled as a normal role artifact.

It may also be used as an artifact transport after bootstrap, but those concepts
must stay separate.

### `wasm_store` Availability Is Evidence

`wasm_store` catalog and staging state are artifact availability evidence.

Live inventory remains deployment truth.

---

## `wasm_store` Enablement Thread

0.41 through 0.46 should make `wasm_store` visible, typed, digest-checked,
receipted, transport-capable, diffable, and safe to resume against only with
live proof.

They should not turn `wasm_store` into the artifact registry, rollback
database, retention authority, source of deployment truth, or cross-deployment
promotion brain.

```text
0.41 observes wasm_store as a role artifact and artifact-transport endpoint.
0.43 routes wasm_store staging through DeploymentExecutor::stage_artifact.
0.44 permits receipt-backed and wasm_store-backed artifact locators in plans.
0.46 compares wasm_store artifact availability as evidence, not truth.
```

Post-0.46 work may promote `wasm_store` into a provenance-rich artifact
registry with pinning, rollback selection, cross-deployment cache reuse, and
deployment-aware retention.

### Automated Resume Requires Live Proof

A receipt can skip work only after live inventory proves the phase
postcondition still holds.

Automated resume is optional until the safety report model has been exercised.

---

## Design Docs

- [0.41 Deployment Truth Model](0.41-design.md)
- [0.42 Authority Reconciliation](../0.42-authority-reconciliation/0.42-design.md)
- [0.43 Backend-Agnostic Execution](../0.43-backend-agnostic-execution/0.43-design.md)
- [0.44 Artifact Overrides And Promotion](../0.44-artifact-promotion/0.44-design.md)
- [0.45 External And User-Owned Lifecycle](../0.45-external-lifecycle/0.45-design.md)
- [0.46 Multi-Deployment Operations](../0.46-multi-deployment-operations/0.46-design.md)

## Post-46 Backlog

These are intentionally outside the 0.41 through 0.46 deployment truth
roadmap. They exist so adoption needs are named without expanding the active
design scope and must not be treated as approved numbered follow-on lines.

For the product-gap source material that informed this split, see
[Deployment Adoption Gap Inventory](../deployment-adoption-gap-inventory.md).

- [Deferred From 0.46](../post-46-backlog/deferred-from-0.46.md)
- [Post-46 Backlog Index](../post-46-backlog/README.md)
- [Wasm Store Artifact Registry And Retention](../post-46-backlog/wasm-store-artifact-registry/design.md)
- [Adoption Profiles](../post-46-backlog/adoption-profiles/design.md)
- [CI, GitOps, And Provenance](../post-46-backlog/ci-gitops-provenance/design.md)
- [DR, Clone, And Operational Verification](../post-46-backlog/dr-clone-verification/design.md)
