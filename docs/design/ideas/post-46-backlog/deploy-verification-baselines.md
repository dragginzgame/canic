# Deploy Verification Baselines

Status: optional idea.

This idea includes verification profiles, runtime checks, protected-call
probes, cycle floor checks, metrics snapshots, digest comparison, and a full
`canic deploy verify` surface.

Reason deferred: passive comparison already exists. Active verification
profiles can be useful, but they are a feature line, not a closeout
requirement.

Constraint if revived: verification profiles must remain separate from passive
comparison unless explicitly promoted.
