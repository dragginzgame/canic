# Canic 0.98 Design: Architectural Sediment Hard Cuts

Date: 2026-07-21; amended 2026-07-23

## Status

- Status: closed at immutable `v0.98.2`. Slice A and the corrected 0.97
  predecessor are published in `v0.98.0`; Slice B is published in `v0.98.1`;
  Slice C and the maintainer-approved consolidation amendment are published in
  `v0.98.2`.
- Post-closeout amendment: fifteen additional hard-cut tails were accepted into
  this design beginning on 2026-07-22 after source-and-history passes found
  evidence that was absent from the original 42-candidate closeout. Slices D
  and E are published in `v0.98.10` and `v0.98.11`; Slices F through J are
  published in `v0.98.12`; Slice K is published in `v0.98.13`; Slice L is
  published in `v0.98.14`; Slice M is published in `v0.98.15`; Slice N is
  published in `v0.98.16`; Slice O is published in `v0.98.17`; Slice P is
  published in `v0.98.19`; Slice Q is published in `v0.98.20`; Slice R is in
  implementation for `0.98.21`. This amendment does not alter the
  immutable `v0.98.2` release claim or retroactively add the new findings to
  its candidate totals.
- Immutable release anchor: commit
  `73973fc24c407b1732de1a142d4990b5cb6becf6`, source tree
  `a53cc20e4533f7c7277e2fda3c594ecba8eb99ac`, product-tree hash
  `961b30a138f55e4644b34372b51bd595abbca933234ed39b07b598113d90c0d3`,
  and Cargo.lock SHA-256
  `dc6355881a2dc3856cb8a991b03b5b368e73dc5398a9da599fcb68be63721458`.
- Release line: 0.98, after closed 0.96 receipt and released 0.97 role-owned
  dependency/CDK work. Rigorous 0.97 closeout corrections remain a predecessor
  release requirement.
- Immutable implementation baseline: released `v0.97.3` at
  `4f4397cd58b648759307b51d98033c7c21538345`, source tree
  `0efda05cd46e94c1c45d6a37f6a0270fa8b7bd0c`, product-tree hash
  `e540970e5aad935a2f4c5aff5ff43c790beb1958d6e33fb5f801ba6c050cc03d`,
  and Cargo.lock SHA-256
  `bcf041e99d7ead0f1d4419251f4fe5cd24d11604dbb15002330562e37dc547bd`.
- Canonical evidence: the
  [architectural sediment audit](../../audits/reports/2026-07-21/architectural-sediment.md).
- Frozen post-0.97 evidence: the
  [immutable baseline and inventory](../../audits/reports/2026-07/2026-07-22/0.98-immutable-baseline-and-inventory.md).
- Canonical closeout evidence:
  [0.98-closeout-audit.md](0.98-closeout-audit.md).
- Approved 0.98.2 scope amendment and supporting evidence:
  [consolidation design](consolidation-design-amendment.md),
  [tracker](consolidation-implementation-tracker.md),
  [ledger](consolidation-ledger.md), and
  [validation](consolidation-validation-evidence.md).
- Compatibility posture: pre-1.0 hard cut. Deleted config, Rust, test package,
  test endpoint, metric, and fixture paths receive no alias, deprecated
  wrapper, fallback parser, ignored field, forwarding package, or dual test.
- Downstream scope: repositories outside Canic remain read-only. 0.98 documents
  the configuration migration but performs no downstream edit.

## Decision

0.98 removes three evidence-proven pieces of architectural sediment:

1. the accepted but unexecuted per-role randomness configuration and its
   unreachable raw_rand runtime adapter;
2. the dead project-protocol-stub package left behind by the protected
   descriptor/client hard cut; and
3. the obsolete LocalIntent external-call race fixture, including
   intent_client, intent_external, and the buy/call_buy/perform path.

The surviving receipt-backed intent conformance remains in intent_authority,
but the PocketIC test is renamed and narrowed so it describes only the current
receipt-backed contract.

This is a hard-cut line, not a general cleanup line. The original scope's only
non-deletion representation requirement is structured unknown-field evidence
inside the existing ConfigError::CannotParseToml category so the config cut is
proved semantically. The accepted amendment adds only the bounded truth and
authority corrections listed below. It introduces no new runtime abstraction,
entropy service, shared protocol crate, transaction framework, recovery
engine, compatibility parser, or compatibility layer.

## Maintainer-Approved 0.98.2 Amendment

Before 0.98.2 was published, a repository-wide consolidation audit found one
P1, 11 P2 findings, and 30 non-finding candidates. The maintainer explicitly
approved folding that bounded work into the existing patch instead of opening
another release line. The immutable comparison baseline for the complete patch
is therefore `v0.98.1`; no intermediate dirty-worktree fingerprint is a release
authority.

The amendment is limited to:

- syntax-aware role build-macro validation and single-parse config
  projection;
- correction and executable validation of current configuration guidance;
- truthful CycleTracker state-contract metadata;
- removal of dead host duration, runtime snapshot, capability-verifier,
  singleton auth-kind, and orphan ICP helper surfaces;
- narrowing public visibility to proved consumers; and
- exact audit/dependency evidence for the surviving authorities.

It adds no new product feature or replacement abstraction. The 42-candidate
ledger is supporting evidence for this amendment; it does not re-count the
three original 0.98 findings. The canonical closeout audit combines both
scopes and records their contract impact.

## Why This Is A Separate Release Line

0.96 changes durable receipt retention and reclamation. 0.97 changes Cargo
dependency ownership, macro plumbing, and a public Rust facade. Mixing the
sediment deletions into either line would obscure their separate contract and
validation boundaries:

| Line | Sole concern |
| --- | --- |
| 0.96 | Receipt replay horizon, eligibility, bounded terminal reclamation, and downstream conformance |
| 0.97 | Role-owned Canic dependency graph, exact features, and canic::cdk removal |
| 0.98 | Delete proven architectural sediment and consolidate the surviving authorities |

0.98 is technically independent of most 0.97 internals, but it follows 0.97
in release order. Its implementation must use the post-0.97 module/import
layout and must not restore canic::cdk or a forbidden Canic dependency merely
to preserve an old fixture.

## Implementation Baseline And Closeout Dependency

The final 0.96 receipt/reclamation contract is immutable at `v0.96.8`, and the
released 0.97 dependency/CDK baseline is immutable at `v0.97.3`. The complete
deletion, reachability, Cargo-package, stable-allocation, fixture, example, and
documentation inventories were rerun against that exact 0.97 tree. The frozen
evidence report records the final receipt assertion manifest and confirms that
the post-0.97 layout changes none of the three deletion decisions.

The rigorous 0.97 closeout audit subsequently found five P1 defects. Those
findings do not change the 0.98 deletion set. Their bounded corrections and
cumulative validation are published at `v0.98.0`, which is the corrected
immutable predecessor for the remaining 0.98 slices; `v0.97.3` itself is not
retroactively described as passing.

Any later evidence that changes reachability, storage, package layout, or the
deletion set stops the affected slice until this design is amended.

## Central Invariants

After 0.98:

1. Every accepted Canic configuration field has one current build, host,
   validation, or runtime consumer.
2. Every workspace package has a current consumer or a necessary build,
   runtime, host, procedural-macro, external-contract, or test boundary.
3. Canic's maintained code, fixtures, examples, and documentation use
   LocalIntent only for locally decidable work. Its public contract explicitly
   excludes rollback based solely on an uncertain external-call result: a
   transport error, callback error, decode error, or missing response never
   proves that an external effect did not occur.
4. Tests demonstrate the supported architecture. A passing test cannot retain
   a call shape that the maintained operational contract rejects.
5. Role constants and fixture-only protocol values live with their current
   owner. A shared package is not created for two constants.
6. Removed pre-1.0 surfaces fail directly. They do not parse and ignore,
   forward, warn, or continue under a legacy mode.

## Release Boundary

### In scope

- CanisterConfig.randomness and the RandomnessConfig/RandomnessSource schema.
- Randomness validation, defaults, compiled-model rendering, bootstrap
  exports, host role-detail projection, and active config documentation.
- Structured logical-path and unknown-field evidence inside the existing
  ConfigError::CannotParseToml category.
- MgmtInfra::raw_rand, its ops wrapper, invalid-length error, and unreachable
  management/system metric variants.
- Randomness-only tests and test-fixture construction fields.
- The project-protocol-stub workspace member/package/lock entry.
- intent_client and intent_external workspace members/packages/lock entries.
- intent_authority external principal state/init parameter and the buy
  LocalIntent external-call endpoint.
- The old capacity race in pic_intent_race.
- Renaming the surviving test to pic_receipt_backed_intent and updating current
  scripts/operations documentation to that name.
- Exact contract accounting for TOML, generated Rust, host output, metrics,
  test Candid, Cargo packages, and test artifact names.
- The bounded items listed in the accepted 0.98.2 consolidation amendment,
  with one disposition ledger and no unrelated redesign.

### Out of scope

- 0.96 replay deadlines, terminal grace, eligibility, reclamation, timers,
  capacity, storage schema, or downstream Toko conformance.
- 0.97 protected package catalog, role dependency shape/features,
  `canic::cdk` removal, macro plumbing, or downstream dependency migration;
  the amendment changes only build-macro syntax detection and exact current
  dependency evidence.
- ReplayReceipt controller observation, RecoveryRequired reconciliation, or
  operator recovery commands.
- icp-refill deletion, redesign, or consumer proof.
- LocalIntent API removal or redesign.
- ReceiptBackedIntent API, stable records, replay metadata, eligibility,
  settlement, or cleanup changes.
- New randomness, PRNG, entropy, nonce, security, timer, or reseed
  functionality.
- A replacement shared project protocol package.
- Broad package pruning, naming normalization, or test framework redesign
  beyond the accepted consolidation findings.
- A repository-wide configuration or CLI error-code taxonomy.
- Editing archived designs, historical changelogs, immutable audit reports, or
  released validation commands merely because they name a removed package or
  test.

## Current Implementation Reality

### Randomness is a complete configuration story without execution

The schema currently defaults every role to:

~~~text
enabled = true
reseed_interval_secs = 3600
source = "ic"
~~~

The active path is:

~~~text
canic.toml
  -> RandomnessConfig / RandomnessSource
  -> validation
  -> generated compiled CanisterConfig.randomness
  -> host role-detail projection
  -> no runtime consumer
~~~

Exact current owners are:

| Surface | Current location | Current effect |
| --- | --- | --- |
| User documentation | CONFIG.md | Promises seeding and periodic reseeding |
| Parsed schema/default | canic-core config/schema/subnet | Accepts and defaults enabled/ic/3600 |
| Validation | canic-core config/validation/subnet | Rejects an enabled zero interval |
| Generated model | canic-core bootstrap/render | Emits the complete subtree into compiled config |
| Host projection | canic-host release_set/config/projection | Emits randomness=off or source/reseed detail |
| Raw IC effect | canic-core infra/ic/mgmt/randomness | Can call management raw_rand |
| Ops/metrics | canic-core ops/ic/mgmt and metric enums | Wraps and labels raw_rand |
| Runtime workflow | none | Never reads the configuration or calls the adapter |

Repository-wide reachability finds no current call to the ops raw_rand
function and no runtime read of CanisterConfig.randomness. No tracked active
canic.toml sets the randomness table. Tests cover defaults, parsing,
validation, rendering, and fixture construction, but no seed, PRNG, timer,
reseed, or randomness-dependent behaviour.

The original implementation was introduced in the 0.6 era with an actual PRNG
and reseed story. The consuming runtime disappeared while its schema and
adapter survived. Historical origin does not justify retaining a false current
contract.

### project-protocol-stub has no consumer

The package contains only:

~~~text
PROJECT_HUB: CanisterRole
PROJECT_INSTANCE: CanisterRole
~~~

It was created for the 0.40 shared protected-endpoint descriptor and generated
client architecture. 0.65.17 hard-cut that architecture:

- canic_protected_endpoint was removed;
- ProtectedInternalEndpoint and internal-call DTOs were removed;
- descriptor-based generated clients were removed; and
- project fixtures moved to delegated-token endpoints.

The package was emptied to two constants but remained in the workspace.
Current source, manifests, tests, build scripts, and active documentation have
no dependency on it. The current delegation root already owns the two role
constants it uses.

### The intent test contains two incompatible architectures

The current pic_intent_race test first runs this old path:

~~~text
two intent_client canisters
  -> intent_authority.buy
  -> LocalIntent reservation
  -> await intent_external.perform
  -> commit on Ok
  -> rollback on any call error
~~~

It then runs the current receipt-backed conformance in the same
intent_authority canister:

~~~text
begin_or_load deterministic operation
  -> Created / retained / conflict / capacity decision
  -> domain-supplied TerminalEvidence
  -> compare-and-set settlement
  -> exact replay and upgrade persistence
~~~

The first path contradicts the maintained adapter contract. LocalIntent is
locally decidable; an IC transport/callback/decode failure after an await
cannot prove that an external effect did not happen. The second path is the
surviving contract and is also the only current in-repository public
application receipt consumer established by the active 0.96 line.

The correct hard cut removes only the first path. LocalIntent remains supported
for runtime_probe and other purely local reservations.

## Supported Architecture After 0.98

### Configuration

CanisterConfig contains only current runtime inputs:

~~~text
role kind
initial cycles
top-up and cycles-funding policy
scaling / sharding / directory
authentication
standards
diagnostics
metrics
~~~

There is no Canic randomness key and no implied default entropy service.

Applications that need randomness make an explicit role-owned decision:

- depend directly on the relevant upstream IC API after 0.97;
- define the security and replay requirements in the application/domain
  boundary;
- own any seed expansion, persistence, scheduling, and reseeding; and
- test the behaviour that consumes the random value.

0.98 does not recommend time as entropy and does not replace the removed
Canic contract with a convenience wrapper.

### Project fixture protocol

Project role values remain next to the fixture that consumes them:

~~~text
delegation_root_stub
  -> PROJECT_HUB_ROLE
  -> PROJECT_INSTANCE_ROLE
  -> embedded test Wasm release set
~~~

No shared package exists without a shared current protocol.

### Receipt-backed intent conformance

The maintained test shape becomes:

~~~text
pic_receipt_backed_intent
  -> install intent_authority with unit init
  -> begin/load receipt-backed operation
  -> assert exact pending and conflict decisions
  -> settle from deterministic test-supplied opaque TerminalEvidence
  -> assert committed and rolled-back replay
  -> upgrade the same canister
  -> assert durable state and deadline behaviour
  -> exercise the frozen final-0.96 receipt assertion manifest
~~~

The deterministic evidence source may be the installed authority principal or
another explicitly constructed principal. The fixture does not pretend that
Canic validates a domain receipt or that an external effect occurred. Canic
tests binding, state, revision, capacity, persistence, replay, and reclamation;
the downstream domain adapter owns actual external receipt validation.

The final `v0.96.8` assertion manifest is frozen in the
[immutable baseline and inventory](../../audits/reports/2026-07/2026-07-22/0.98-immutable-baseline-and-inventory.md).
The surviving real-Wasm test and focused core tests must preserve these exact
areas and decisions:

| Receipt area | Frozen final-0.96 assertion |
| --- | --- |
| Admission | New valid work is `Created`; exact retained work loads before temporal or capacity admission; payload, quantity, and replay-deadline disagreement is `BindingConflict`. |
| Replay window | Equality is closed; a remaining window above 3,600,000,000,000 ns reports `ReplayWindowTooLong` with that exact maximum. |
| Capacity | The installed fixture reports exact current/requested/limit quantity values; focused core proof retains the exact 1,000-row record boundary. |
| Settlement | Missing identity, expected revision, payload binding, decision, source canister, and evidence fingerprint are checked before transition. |
| Terminal replay | Committed and rolled-back states replay exactly; repeated settlement is `AlreadySettled`; contradictory settlement does not mutate state. |
| Upgrade | Pending state, bindings, revision, replay metadata, and capacity accounting survive same-Wasm upgrade. |
| Reclamation | Exact one-hour replay and 15-minute observation boundaries, ordered bounded removal, preserved totals, replay after removal, and fail-closed corruption remain proved. |

`TerminalEvidence` in this fixture remains opaque domain evidence supplied by
the test. Canic proves only evidence binding and receipt state-machine
behaviour; it does not prove that the represented external effect is true.

## Hard-Cut Contract

### Randomness configuration

Before 0.98, this parses:

~~~toml
[subnets.prime.canisters.app.randomness]
enabled = true
reseed_interval_secs = 3600
source = "ic"
~~~

After 0.98, the entire table is an unknown field under the existing
deny_unknown_fields schema. Configuration parsing fails through this existing
typed category chain:

~~~text
FleetConfigError::ConfigInvalid { path: <canic.toml>, ... }
  -> FleetConfigError::CoreConfig {
       operation: FleetConfigOperation::Project,
       ...
     }
  -> ConfigError::CannotParseToml { ... }
~~~

`ConfigError::CannotParseToml` is the existing core category; Canic currently
defines no separate stable error code for that category. 0.98 does not invent
a repository-wide error-code system. It does require the category's typed
cause to expose an unknown-field issue with these semantic evidence fields:

~~~text
logical_path = subnets.<name>.canisters.<role>.randomness
unknown_field = randomness
~~~

The focused test matches the typed category and those fields. It does not
match, parse, or freeze raw Serde/TOML wording. If the final parser cannot
retain this evidence without a randomness-specific compatibility scanner or a
broad parsing framework, Slice C stops and this design is amended. The parser
must also prove that the field is neither ignored nor downgraded to a warning.

The only migration is:

~~~text
Delete the complete randomness table.
~~~

There is no accepted-but-ignored key, serde alias, legacy parser, warning-only
mode, or automatic transfer to an application feature.

Omitted randomness configuration requires no source edit because the old field
was defaulted. Rebuilding regenerates compiled configuration without the
removed field.

### Rust and generated model

The following Rust surfaces are removed:

- RandomnessConfig;
- RandomnessSource;
- CanisterConfig.randomness;
- bootstrap re-exports and generated render functions for those types;
- MgmtInfra::raw_rand and the ops raw_rand wrapper;
- RawRandInvalidLength;
- ManagementCallMetricOperation::RawRand; and
- SystemMetricKind::RawRand.

No replacement Rust type or function is added. Downstream source matching
SystemMetricKind::RawRand or constructing CanisterConfig/RandomnessConfig must
remove that branch/field. Generated role config is rebuilt rather than read
through a compatibility type.

### Host and metrics output

Configured-role details no longer emit:

- randomness=off; or
- randomness=<source> reseed=<seconds>s.

Any structured CLI output that carries the role details list therefore loses
only these impossible-behaviour entries. The rest of the list and its schema
remain unchanged.

The raw_rand system/management metric dimensions are deleted. Because no
supported runtime reaches the adapter, there is no valid current emitted
series to migrate. Dashboards or source code that statically enumerate the
label must remove it.

Accurate negative security documentation stating that authentication does not
use raw_rand remains. Historical changelogs/designs also remain immutable.

### Cargo packages and test artifacts

The following package identities disappear:

- project-protocol-stub;
- intent_client; and
- intent_external.

They are unpublished test packages. Their Cargo.lock rows, workspace member
entries, Wasm build targets, and current test-runner references are deleted.
No renamed or empty placeholder package remains.

### Test Candid

These test-only endpoints disappear:

- intent_authority.buy;
- intent_client.call_buy; and
- intent_external.perform.

intent_authority no longer accepts an external principal as its init argument.
Its maintained unit init and receipt endpoints remain test-only. Product
Candid, the checked-in Wasm-store DID, and generated root Candid do not change.

### Stable state boundary

0.98 distinguishes product and generic stable contracts from state created
only by the obsolete test flow:

- product stable-memory IDs and record schemas do not change;
- generic LocalIntent and ReceiptBackedIntent IDs, records, encodings, and
  upgrade formats do not change;
- obsolete intent_authority test-only LocalIntent state may disappear with the
  removed buy path; and
- the surviving receipt-backed records must remain compatible across the
  PocketIC test's upgrade boundary.

At the provisional anchor, the obsolete flow writes terminal LocalIntent
records and resource totals in Canic's generic LocalIntent format. The
intent_authority fixture declares no fixture-owned MemoryId or persisted
record type; it bootstraps Canic's generic memory registry. The post-0.97
inventory must prove both facts again. If it discovers a fixture-owned ID,
record, or upgrade input, that test-only contract is recorded explicitly
before deletion rather than being folded into a claim that all stable state is
unchanged.

## Contract-Risk Register

| Surface | Current contract | 0.98 result | Risk and required proof |
| --- | --- | --- | --- |
| Product stable memory | Randomness and the retired fixture path own no product memory ID or record schema | Unchanged | Product allocation/state-contract guard passes |
| Generic intent stable formats | LocalIntent and ReceiptBackedIntent own maintained generic IDs, records, and encodings | Unchanged | Focused round-trip/state-contract tests and the frozen final-0.96 assertions pass |
| intent_authority test stable state | Obsolete buy writes generic LocalIntent terminal records/totals; no fixture-owned ID or record is visible at the provisional anchor | Obsolete test state may disappear; no product migration | Final inventory records the exact generic IDs/records exercised and reconfirms no fixture-owned schema; surviving receipt state upgrades compatibly |
| Product Candid | Randomness and the retired fixture endpoints are not product endpoints | Unchanged | Generated root and checked-in Wasm-store interface comparison |
| canic.toml | Randomness table is accepted and defaulted | Explicit table becomes a typed unknown-field failure; omission remains valid | Breaking config migration must be documented and tested |
| Typed config error | ConfigError::CannotParseToml is the core category but currently retains only rendered text | Category retained with structured logical-path and unknown-field evidence; no stable code invented | Typed cause assertion; no parsing or freezing of Serde text |
| Generated config Rust | CanisterConfig includes randomness | Field/types/rendering removed; roles rebuild | Build at least minimal and root profiles |
| Public Rust randomness | Randomness types and CanisterConfig.randomness are reachable | Removed without alias | Reference-surface and downstream migration accounting |
| Public Rust metric enum | SystemMetricKind::RawRand is public | Variant removed without alias | Record the Rust exhaustive-match break independently of runtime metric output |
| Host role details | Can emit randomness status text | Entries disappear | Text/JSON detail assertions updated without changing surrounding schema |
| Emitted metrics | RawRand exists in system/management taxonomy but has no runtime producer | Unobservable label path removed | Metric inventory proves no current emitted series or caller; runtime metric compatibility risk is zero |
| Cargo workspace | Three unpublished test packages remain | Packages/member/lock rows removed | Locked metadata and reverse-dependency proof |
| Test Candid/Wasm | buy, call_buy, perform and two auxiliary Wasm artifacts exist | Removed | Product interfaces unchanged; current receipt fixture passes |
| Current scripts/docs | pic_intent_race names both old and current test shapes | Current target renamed; history retained | CI runner and operations docs name one supported test |
| Backup/restore and deployment artifacts | No dependency on the three findings | Unchanged | No source/schema diff in owning crates |

The randomness cut improves security truthfulness: Canic stops claiming that a
role receives IC or time-derived entropy when no such runtime behaviour exists.
It does not assert that direct application use of raw_rand is automatically
secure. That decision remains application-owned and must account for replay,
authorization, persistence, and the value's actual security purpose.

## Deletion Manifest

Implementation must start from a fresh post-0.97 inventory, but the current
minimum deletion set is:

### Randomness

- CONFIG.md randomness bullets, table, and field descriptions.
- canic-core config schema field/types/defaults and implicit Wasm-store
  construction.
- canic-core config validation branch.
- canic-core bootstrap imports/re-exports/render functions.
- canic-core infra randomness module and module registration.
- canic-core management ops raw_rand wrapper and error.
- canic-core management/system metric enum variants.
- canic-host randomness default constant, label helper, imports, and role
  detail projection.
- Randomness-only schema tests.
- Randomness fields/imports in current test helpers, policy seams, and fixture
  builders.

### Dead project package

- canisters/test/project_protocol_stub/Cargo.toml.
- canisters/test/project_protocol_stub/src/lib.rs.
- Root workspace member entry.
- Cargo.lock package entry.

### Obsolete intent fixture

- canisters/test/intent_client.
- canisters/test/intent_external.
- Their workspace members and Cargo.lock entries.
- intent_authority LocalIntent/Call imports, EXTERNAL state, external-principal
  init, external_principal helper, and buy endpoint.
- Old client/external installation and concurrent call assertions in
  pic_intent_race.
- Old three-canister build roster.
- Principal-valued intent_authority init payload in lifecycle test support.
- Current CI/operations references to the obsolete pic_intent_race name.

### Rename/move

- crates/canic-tests/tests/pic_intent_race.rs becomes
  pic_receipt_backed_intent.rs.
- scripts/ci/run-workspace-tests.sh invokes the new target.
- docs/operations/receipt-backed-intent-adapter.md names the new focused
  conformance path.

Released changelogs, archived designs, immutable audits, and old validation
commands are historical evidence. They are not edited to pretend the removed
surface never existed.

## Slice Plan

### Slice A — Delete the dead project protocol package

#### Invariant established

Every workspace package has a current consumer or necessary boundary.

#### Canonical authority retained

The role/root fixture that actually uses PROJECT_HUB_ROLE and
PROJECT_INSTANCE_ROLE.

#### Obsolete authority deleted

The entire project-protocol-stub package.

#### Affected areas

- workspace Cargo.toml;
- Cargo.lock;
- canisters/test/project_protocol_stub.

#### Contract impact

Unpublished test Cargo package only. No product Rust, Candid, stable state,
config, CLI, JSON, metric, or artifact-format contract.

#### Validation

- locked Cargo metadata has no package/member;
- reverse dependency analysis has no missing edge;
- project hub/instance/delegation fixture builds still resolve their role
  values; and
- workspace manifest governance passes.

#### Explicit exclusions

No replacement common/shared/protocol package and no anti-resurrection test.

### Slice B — Narrow intent conformance to the receipt-backed architecture

#### Invariant established

Canic's maintained code, fixtures, examples, and documentation do not use
LocalIntent across an externally uncertain await, and the public contract
forbids treating an uncertain call result as proof for rollback.

#### Canonical authority retained

intent_authority receipt-backed endpoints and the exact final-0.96
ReceiptBackedIntent assertion manifest frozen at the implementation baseline.

#### Obsolete authority deleted

intent_client, intent_external, buy/call_buy/perform, and the LocalIntent
capacity-race half of the test.

#### Affected areas

- three test canister directories;
- canic-tests PocketIC test;
- canic-testing-internal lifecycle/build roster;
- workspace manifest/lock;
- CI test runner;
- current receipt adapter operations documentation.

#### Contract impact

Test package names, test Wasm artifacts, test-only Candid, test target name,
and any obsolete intent_authority test-only LocalIntent state. Product stable
IDs/schemas and generic LocalIntent/ReceiptBackedIntent formats do not change;
surviving receipt-backed upgrade state remains compatible.

#### Frozen final-0.96 assertion manifest

This subsection is intentionally baseline-blocked. Before Slice B can start,
it must be replaced with the exact released 0.96 source locations, test names,
boundary inputs, typed decisions, persisted fields, and reclamation outcomes
for every row in the provisional receipt inventory above. A general reference
to "post-0.96 behaviour" does not satisfy the gate.

#### Validation

- new pic_receipt_backed_intent target passes;
- intent_authority install and upgrade use unit init;
- every admission, binding, replay-window, capacity, settlement, terminal
  replay, upgrade, and reclamation assertion in the frozen final-0.96 manifest
  passes with its exact typed decision and boundary evidence;
- lifecycle negative-upgrade fixture still uses intent_authority successfully;
- receipt consumer inventory still identifies intent_authority exactly; and
- maintained source, fixtures, examples, and docs contain no LocalIntent
  rollback around an externally uncertain await; and
- runtime_probe/local intent unit tests continue proving legitimate LocalIntent
  use.

#### Explicit exclusions

Do not delete LocalIntent. Do not add a generic external-call simulator or
claim that Canic validates domain receipts.

### Slice C — Delete the no-op randomness contract

#### Invariant established

Every accepted configuration field and every management adapter has a current
runtime consumer.

#### Canonical authority retained

The typed CanisterConfig parser/model for current runtime features and direct
application ownership of any future randomness.

#### Obsolete authority deleted

The complete randomness schema/render/projection/adapter/metrics/test/docs
chain.

#### Affected areas

- canic-core config/bootstrap/infra/ops/metrics/tests;
- canic-host config projection;
- active CONFIG.md;
- any current public/reference surface tests that enumerate the removed Rust
  types or metric variants.

#### Contract impact

Breaking TOML, Rust construction/matching, generated ConfigModel, role-detail
output, metric taxonomy, and the public shape of
ConfigError::CannotParseToml. Its existing category remains; its typed cause
gains logical-path and unknown-field evidence. No stable state, product
Candid, backup/restore, or durable artifact change.

#### Validation

- configs that omit randomness continue parsing;
- a config containing randomness reaches ConfigError::CannotParseToml through
  the existing ConfigInvalid/CoreConfig(Project) chain and exposes logical
  path subnets.<name>.canisters.<role>.randomness plus unknown field
  randomness as typed evidence;
- the unknown-field test does not inspect rendered Serde/TOML text and proves
  there is no fallback, warning, or ignored value;
- all tracked active canic.toml files parse;
- focused core config/render and host projection tests pass;
- maintained role packages rebuild generated config;
- active source inventory contains no randomness type/field/render/projection
  and no callable raw_rand adapter;
- SystemMetricKind public/reference inventory is updated; and
- generated root and checked-in Wasm-store Candid remain unchanged.

#### Explicit exclusions

No replacement PRNG, no time-derived entropy, no reseed timer, and no
compatibility parser.

### Closeout — Contract accounting

#### Invariant established

Every removed path has one explicit source/operator migration and every
surviving current surface is proven.

#### Canonical authority retained

The 0.98 design, current config parser, workspace metadata, receipt-backed
conformance, and ordinary release validation owners.

#### Obsolete authority deleted

Any stale active docs, scripts, fixture rosters, or public-reference
expectations found by the final inventory.

#### Affected areas

Documentation, current validation scripts, closeout report, changelog/status
for the implemented 0.98 patch.

#### Contract impact

No additional contract beyond Slices A through C.

#### Validation

- focused tests from every prior slice;
- source/package/public/config/metric/test-target inventories;
- diff hygiene and formatting;
- one contract-accounting report distinguishing product, test-only, and
  historical surfaces.

#### Explicit exclusions

No broad workspace/release/deployment gate unless the maintainer requests it.
Do not edit history to achieve a repository-wide zero string match.

## Required Tests

### Configuration hard-cut tests

1. A role with no randomness table parses and compiles exactly as a current
   non-randomness role.
2. A role with a randomness table fails as
   ConfigInvalid(path) -> CoreConfig(Project) ->
   ConfigError::CannotParseToml, with typed logical path
   subnets.<name>.canisters.<role>.randomness and unknown field randomness.
3. The implicit Wasm-store role constructs without a deleted field.
4. Generated config contains no randomness type or value.
5. Host role details cannot emit randomness text.
6. Public/reference tests no longer enumerate RawRand.

These tests prove the maintained parser and surface. They must not be named as
legacy/randomness anti-resurrection tests; the unknown-field case is a current
strict-schema proof.

### Workspace/package tests

1. Cargo metadata contains no deleted package.
2. No current member, script, or build roster references a deleted target.
3. Project hub, project instance, and delegation root build without a shared
   protocol package.

### Receipt-backed conformance tests

1. intent_authority installs with unit init.
2. An exact retained operation loads before temporal/capacity rejection.
3. Pending, committed, and rolled-back state survives upgrade.
4. Binding and revision conflicts remain typed.
5. TerminalEvidence remains test-supplied opaque domain evidence; the test
   proves exact binding and state-machine replay, not truth of an external
   effect.
6. Every exact assertion in the frozen final-0.96 manifest remains covered,
   including its released eligibility/reclamation boundaries.
7. A repository inventory proves that maintained production code, test
   fixtures, examples, and current documentation contain no LocalIntent
   rollback based solely on an externally uncertain await result.

### Contract comparison

- Product root Candid: unchanged.
- Checked-in Wasm-store Candid: unchanged.
- Product stable-memory IDs and record schemas: unchanged.
- Generic LocalIntent and ReceiptBackedIntent IDs, records, and encodings:
  unchanged.
- intent_authority obsolete test-only LocalIntent state: permitted to
  disappear; the baseline inventory records the exact generic IDs and records
  the old flow exercises.
- intent_authority surviving receipt-backed state: compatible across the
  PocketIC upgrade boundary.
- Backup/restore JSON and artifact layouts: unchanged.
- CLI command/help/exit schema: unchanged, except configured role-detail
  values can no longer contain randomness entries.
- Test-only Candid/artifacts: the three retired endpoints and two auxiliary
  Wasm packages are absent.

## Acceptance Criteria

0.98 is implementation-complete only when all of the following are true:

1. RandomnessConfig, RandomnessSource, and CanisterConfig.randomness are absent
   from active source.
2. The raw_rand infra/ops function and its unreachable error/metric variants
   are absent.
3. CONFIG.md contains no supported Canic randomness table or promise.
4. Current configs omitting the old default continue to parse and build.
5. Explicit old randomness input fails through the existing typed
   ConfigInvalid/CoreConfig(Project)/CannotParseToml category chain, reporting
   the logical path and unknown field as structured evidence without matching
   raw Serde text, fallback, warning, or ignored-value behaviour.
6. project-protocol-stub is absent from source, metadata, and lock data.
7. intent_client and intent_external are absent from source, metadata, lock
   data, and current build rosters.
8. intent_authority has no external principal state/init requirement and no
   LocalIntent external-call endpoint.
9. The surviving PocketIC test is named for receipt-backed intent, installs
   one authority fixture, and covers every exact assertion frozen from the
   final released 0.96 contract.
10. Legitimate LocalIntent runtime/API tests still pass, and the maintained
    code, fixture, example, and documentation inventory contains no rollback
    based solely on an externally uncertain await result.
11. Product Candid, stable state, backup/restore, and deployment contracts have
    no unclassified change.
12. No alias, shim, ignored field, warning-only parser, placeholder package,
    deprecated endpoint, or compatibility test is added.
13. Historical evidence remains historically accurate.
14. Focused package/config/host/PocketIC/layering checks pass.
15. The 0.98 closeout report records exact deletion and contract evidence.
16. The implementation evidence names the exact released 0.97 baseline and
    the final released 0.96 receipt assertion manifest.
17. All 42 consolidation candidates have a final disposition, with no
    deferred or unresolved finding.
18. All 37 workspace packages and every major subsystem have a proved current
    owner or an evidence-backed rejection of the suspected sediment.
19. The amendment's dead runtime snapshot, host duration/ICP, capability,
    singleton auth-kind, and excess visibility surfaces are removed or
    narrowed without a compatibility path; CycleTracker metadata names its
    active persisted owner.
20. The combined TOML, public Rust, dependency, stable, Candid, JSON, CLI, and
    package-version impact is explicit, and both focused and cumulative
    amendment validation are recorded.

## Validation Matrix

The implementation plan must run targeted commands equivalent to:

| Area | Required evidence |
| --- | --- |
| Workspace deletion | cargo metadata --locked and focused workspace manifest tests |
| Core config | focused canic-core config/schema/validation/render tests, including typed unknown-field path/key evidence without rendered-text matching |
| Host projection | focused canic-host release_set config projection tests proving ConfigInvalid(path) -> CoreConfig(Project) -> CannotParseToml |
| Minimal roles | targeted wasm checks for minimal and one configured root role |
| Receipt conformance | canic-tests pic_receipt_backed_intent PocketIC target |
| Lifecycle fixture | targeted lifecycle boundary test using intent_authority |
| LocalIntent survival | focused LocalIntent API/workflow/storage tests and runtime_probe build |
| Public surface | canic reference/workspace manifest tests and source inventory |
| Contracts | generated root/checked-in Wasm-store Candid comparison, product/generic stable allocation guards, test-only state inventory, and receipt-backed upgrade proof |
| Layering | scripts/ci/run-layering-guards.sh |
| Hygiene | cargo fmt check for changed Rust and git diff --check |

Repository policy still forbids an automated broad workspace, release matrix,
deployment, or publish run unless the maintainer explicitly requests it.

## Failure Handling

### Config removal exposes a hidden consumer

If implementation finds a current runtime read that the audit missed:

1. stop the randomness slice;
2. trace the consumer to a production entry point;
3. determine whether it provides real behaviour and tests;
4. amend the design before choosing retention or deletion.

Do not keep half the schema or add an ignored compatibility field.

### Deleted package has a real consumer

If post-0.97 metadata finds a current consumer of project-protocol-stub,
intent_client, or intent_external:

1. classify the exact consumer;
2. distinguish current product/test need from historical build residue; and
3. amend the affected slice.

Do not replace the deleted package with an alias or copy its obsolete
architecture into a new common crate.

### Receipt conformance depends on the old external fixture

If a maintained 0.96 assertion currently obtains only through
intent_external/client:

1. express the assertion directly through the receipt-backed authority;
2. use deterministic typed evidence without claiming an external effect; and
3. retain the current receipt invariant.

If that cannot be done without changing receipt semantics, stop and return the
issue to the 0.96 owner. Do not preserve unsafe LocalIntent behaviour.

## Alternatives Rejected

### Implement the promised randomness system

Rejected. No current consumer justifies a PRNG/reseed/timer framework. Building
one solely to validate old configuration would turn sediment into new
architecture.

### Parse randomness and ignore it with a warning

Rejected. This preserves a false contract and makes configuration validation
less truthful.

### Deprecate RandomnessConfig and raw_rand

Rejected. Pre-1.0 has no proven compatibility obligation, and deprecation
retains both the public promise and maintenance cost.

### Keep an empty project-protocol-stub package

Rejected. Package identity is not a compatibility contract here. An empty
placeholder would encode the deleted descriptor architecture into the current
workspace.

### Move the two role constants into a new shared crate

Rejected. The values have one current fixture owner and do not justify a
boundary.

### Keep intent_client/external as generic inter-canister test utilities

Rejected. They have one obsolete consumer and encode the wrong rollback
lesson. Current inter-canister infrastructure is tested elsewhere.

### Delete LocalIntent

Rejected. LocalIntent has current legitimate consumers and owns a distinct
locally decidable reservation boundary.

### Add a generic receipt/recovery adapter framework

Rejected. Canic owns generic reservation state; downstream domains own actual
external receipt validation and recovery. 0.98 only removes a misleading
fixture.

### Fold replay operator recovery into 0.98

Rejected. RecoveryRequired observation/reconciliation is safety-sensitive,
potentially additive Candid/CLI work with external-evidence rules. It needs its
own design line.

### Delete icp-refill because no workspace role enables it

Rejected. The audit found an explicit current downstream capability contract.
Lack of an in-workspace role is a missing proof, not deletion evidence.

## Migration Guide

### Canic configuration users

Remove any complete per-role randomness table:

~~~toml
# Delete this entire table.
[subnets.<subnet>.canisters.<role>.randomness]
enabled = true
reseed_interval_secs = 3600
source = "ic"
~~~

If the application genuinely uses randomness, add and own an explicit
application-level implementation. Do not translate source = "time" into a
security-sensitive entropy source.

### Rust users

- Remove RandomnessConfig and RandomnessSource construction/imports.
- Remove the randomness field from any direct CanisterConfig construction.
- Remove SystemMetricKind::RawRand matches.
- Depend on upstream IC APIs directly for application-owned generic IC calls,
  following the 0.97 dependency rules.

### Test and maintainer scripts

- Remove deleted package targets.
- Replace the current pic_intent_race target with
  pic_receipt_backed_intent.
- Install intent_authority with unit init.
- Do not retain an old test-name alias.

## Documentation Rules

Implementation updates only active promises and current commands:

- CONFIG.md;
- current receipt-backed intent operations documentation;
- current CI/test runner labels;
- current 0.98 changelog/status/audit evidence.

It does not rewrite:

- archived designs;
- released changelogs;
- immutable prior audit reports;
- historical status archives; or
- accurate auth documents that say auth does not call raw_rand.

The goal is truthful current architecture, not deletion of historical evidence.

## Final Recommendation

Accept 0.98 as one bounded hard-cut line with the original three implementation
slices, the approved 0.98.2 consolidation amendment, and one closeout:

1. delete the dead project protocol package;
2. narrow intent testing to the current receipt-backed authority;
3. delete the false randomness contract and unreachable adapter; and
4. consolidate the repository authorities identified by the 42-candidate
   amendment ledger; and
5. close with explicit contract accounting after the implementation slices.

At the immutable `v0.98.2` closeout, the line ended when the three original
findings and all accepted 42-candidate amendment findings were removed and
proven. Replay recovery, ICP-refill proof, and unrelated cleanup remain outside
0.98. The dated post-closeout amendment below governs fifteen later, separately
proved tails without rewriting that historical closeout.

## Post-Closeout Amendment: Additional Hard-Cut Tails

Date: 2026-07-22; extended 2026-07-23

Status: accepted design scope fully implemented; Slices D and E published in
`v0.98.10` and `v0.98.11`; Slices F through J published in `v0.98.12`; Slice K
published in `v0.98.13`; Slice L published in `v0.98.14`; Slice M published in
`v0.98.15`; Slice N published in `v0.98.16`; Slice O published in `v0.98.17`.
Slice P is published in `v0.98.19`; Slice Q is published in `v0.98.20`; Slice
R is in implementation for `0.98.21`.

This amendment records fifteen findings from later current-source and git-
history pass. Each finding has an exact producer/consumer or deletion-history
proof. They are not inferred from naming, raw lint volume, or the mere presence
of a one-variant type.

The original 42-candidate ledger remains the immutable accounting for the
published `v0.98.2` consolidation. The
[post-closeout ledger supplement](consolidation-ledger.md#post-closeout-supplement)
records these additions without changing those totals. Implementation must
resolve its clean source baseline at slice start; a dirty-worktree fingerprint
is never a release authority.

### Decision

The following tails are accepted for hard-cut implementation:

| ID | Finding | Required disposition |
| --- | --- | --- |
| `CANIC-098-POST-STATE-001` | Core persists, maps, queries, and cascades an empty subnet-auth state whose final field disappeared in `0.65.17` | PUBLISHED in `v0.98.10` — complete active core subnet-state surface removed; memory ID 17 permanently retired |
| `CANIC-098-POST-AUTH-001` | Delegated auth accepts only `chain_key_batch`, while `RootProofMode`, its validation, and its canonical tag still model a removed alternative | PUBLISHED in `v0.98.11` — selector and three canonical bytes removed from V1; durable counters and restored proofs honor configured epoch floors |
| `CANIC-098-POST-AUTH-002` | Endpoint call kind is forwarded through delegated-token access after update-token consumption was removed in `0.61.0` | PUBLISHED in `v0.98.12` — delegated verification no longer receives call kind; endpoint identity, performance, and metrics retain it |
| `CANIC-098-POST-LIFECYCLE-001` | Non-root init bytes are cloned and captured by an internal bootstrap workflow that does not consume them | PUBLISHED in `v0.98.12` — internal bootstrap is argument-free; both application hooks retain original init bytes |
| `CANIC-098-POST-COST-001` | `CostGuardPermit` stores four private fields that never participate in settlement or capability proof | PUBLISHED in `v0.98.12` — permit retains only its two durable intent identities and private construction boundary |
| `CANIC-098-POST-ADOPTION-001` | The top-level adoption-report warning array has had no producer since its introduction | PUBLISHED in `v0.98.12` — dead top-level JSON field and envelope projection removed; finding-local warnings retained |
| `CANIC-098-POST-CLI-001` | Auth CLI parsing returns a one-variant private dispatch enum | PUBLISHED in `v0.98.12` — parser returns renewal-status options directly with unchanged CLI behavior |
| `CANIC-098-POST-CDK-001` | Internal XRC wire bindings have no consumer after Canic stopped wrapping that protocol; four bounded-string aliases are likewise unused | PUBLISHED in `v0.98.13` — XRC module/export and unused 8-, 16-, 32-, and 256-byte aliases removed; generic enforcement plus active 64-/128-byte aliases retained |
| `CANIC-098-POST-TEST-001` | Repo-only test support retains a never-consumed upgrade wrapper, an attestation verifier-cache alternative whose final test disappeared in `0.65.0`, unused fixture projections, and a role constant kept alive only by its own test | PUBLISHED in `v0.98.14` — orphan helpers and alternative removed; sole issuer baseline and all behaviorally consumed role constants retained |
| `CANIC-098-POST-ICRC21-001` | The ICRC-21 dispatcher exposes a static-message convenience with no repository consumer or active documentation beside the maintained typed registration contract | PUBLISHED in `v0.98.15` — convenience removed; typed registration and dispatch retained |
| `CANIC-098-POST-ICRC2-001` | A generic ICRC-2 allowance and transfer stack is reachable only from its own API-to-workflow-to-ops-to-infra chain, with no facade, endpoint, canister, CLI, test, or script caller | PUBLISHED in `v0.98.16` — complete orphan chain, wire bindings, ckUSDC/ckUSDT metadata, and dead metric surface removed; independent ICP refill retained |
| `CANIC-098-POST-IC-HELPERS-001` | The facade grouped a useful instrumented call builder with a consumerless HTTP stack and a forwarding-only build-network facade after 0.97 removed generic CDK ownership; repository-only caller counts did not establish whether the builder itself was intended downstream developer experience | CORRECTED in open `0.98.21` — HTTP/build-network facades and generic CDK relays remain deleted; one canonical `canic::api::call` builder is restored over the single retained call transport and metrics authority |
| `CANIC-098-POST-CDK-RELAY-001` | `canic-core::cdk` still relays upstream IC runtime APIs into Canic runtime and macro code; its clock module only forwards into `IcOps`, and its stable-structures inventory exports types with no repository consumer | PUBLISHED in `v0.98.19` — runtime and hidden macro plumbing use upstream `ic-cdk` directly, time is owned by `IcOps`, and unused stable exports are removed without an alias |
| `CANIC-098-POST-ICP-ACCOUNT-001` | Generic CDK `Account`/`Subaccount` helpers are consumed only by manual ICP refill; custom ordering/hash/default-subaccount semantics have no production caller, and resume rebuilds the CMC account despite persisting its identity | PUBLISHED in `v0.98.20` — the ICRC-1 wire account is adapter-local, unused semantics/aliases are removed, and resume consumes the persisted CMC owner/subaccount |
| `CANIC-098-POST-ICRC103-001` | Global configuration can advertise ICRC-103 even though Canic implements neither its allowance-list endpoint nor its required ICRC-1 ledger metadata, and the generic ICRC-2 authority was already removed | FIXED in open `0.98.21` — the unsupported flag and advertisement are removed; ICRC-10 reports only itself and configured ICRC-21 |

### Amendment Invariants

Implementation establishes all of the following:

1. Every active stable state domain contains authoritative restorable data.
2. Every accepted configuration selector chooses between at least two current
   behaviors; fixed cryptographic policy is direct behavior with no selector
   or reserved transcript slot.
3. Internal lifecycle and authorization arguments have current consumers.
4. Capability objects store only the identities needed to prove or settle the
   capability.
5. Every maintained JSON collection has a current producer or current semantic
   meaning.
6. Private command dispatch models only actual branching.
7. Structural Candid, stable, and proof-payload discriminants are retained only
   where they carry current boundary data. The root-proof-mode selector and its
   canonical byte carry no current choice and are removed together.
8. Application-owned generic IC calls may use upstream `ic-cdk` or Canic's one
   canonical instrumented call builder. The Canic builder adds typed public
   errors and metrics over the single internal transport; no generic CDK relay,
   HTTP-outcall facade, or build-network convenience facade survives.
9. Canic runtime code imports upstream IC runtime APIs directly. The hidden
   macro compiler namespace may remain, but it does not relay those APIs
   through Canic-core.
10. Protocol account wire shapes live with their adapter. Durable refill
    identity is the input to retry/resume rather than a value reconstructed
    beside the record.
11. Supported-standards output names only protocols backed by a maintained
    implementation. Configuration cannot advertise an absent endpoint.

### Slice D — Delete Empty Core Subnet State

Status: published in `v0.98.10`.

#### Evidence and obsolete authority

`SubnetAuthStateRecord` and `SubnetAuthStateInput` are empty. The surrounding
`SubnetStateRecord`, `SubnetStateData`, stable cell, DTO input/response,
mapper, ops wrapper, query workflow, root endpoint, protocol constant, audit
probe, cascade slot, snapshot-builder step, checked-in Wasm-store Candid, and
endpoint tests preserve only that empty shape.

The conversion functions explicitly discard the record and DTO values. The
state-contract manifest nevertheless says the domain
`subnet_state_restores_auth_state`. Git history anchors the residue to
`6d8310ef0` (`0.65.17`), where the final
`delegated_root_public_key` field and its mappings were deleted but their
containers remained.

#### Canonical authorities retained

- `AppStateRecord` remains the core runtime application-state authority.
- `canic-control-plane`'s distinct subnet state remains the publication-store
  authority. Its Wasm-store data and memory ID 84 are unrelated to this cut.
- Stable-memory allocation history remains the authority preventing ID reuse.

#### Required hard cut

- Delete the core subnet stable cell, record/data types, storage ops, mapper,
  query workflow, API re-export, DTOs, root endpoint, protocol constant, root
  probe endpoint, and presence-only endpoint assertions.
- Delete `StateSnapshotInput.subnet_state`, the internal snapshot slot,
  snapshot adapter/import branch, debug marker, builder method, all
  `with_subnet_state()` calls, and their tests.
- Remove the domain from the active core state-contract manifest and runtime-
  environment allocation set.
- Update every maintained generated or checked-in Candid surface, including
  the Wasm-store DID, so no empty subnet-state field or method remains.
- Mark memory ID 17 permanently retired in the canonical allocation authority.
  Do not renumber another ID, reuse 17, decode the old empty record into a new
  owner, or leave an active placeholder cell.

#### Contract and migration impact

This is a breaking product Candid and public Rust hard cut. Existing bytes in
memory ID 17 become abandoned retired storage; no data migration is needed
because the current record carries no information. Supported upgrades must
prove that removal does not initialize another structure over ID 17 and that
all surviving active allocation IDs remain unchanged.

#### Validation

- focused core state/allocation/state-contract tests pass;
- targeted Candid generation/comparison for affected roles passes;
- affected root and cascade PocketIC tests prove the surviving app/index
  propagation and endpoint surface;
- maintained source and DIDs contain no core `SubnetAuthState*`, core
  `SubnetState*`, `canic_subnet_state`, or `subnet_state` cascade slot;
- allocation evidence names ID 17 as retired and ID 84 as the active,
  unrelated control-plane subnet-state allocation; and
- no compatibility endpoint, DTO alias, empty record, or fallback decoder is
  added.

### Slice E — Hard-Cut Delegated Root-Proof Mode and Canonical Format

Status: published in `v0.98.11`.

The maintainer has selected chain-key batch as Canic's sole root cryptographic
protocol on the IC. A future alternative requires a new explicit design and
trust-format change; the current runtime carries no dormant protocol selector.

#### Evidence and obsolete authority

`auth.delegated_tokens.root_proof_mode` accepts only
`"chain_key_batch"`. `RootProofMode` contains only `ChainKeyBatch`; verifier
construction can produce no other value; token verification still rejects the
impossible alternative; and canonical encoding ignores the value and always
writes tag byte 2.

Git history anchors the singleton tail to `a2f5d08b3` (`0.76.6`), where the
canister-signature root-proof variant was removed. The 0.98.2 capability-
verifier and auth payload-kind cuts concern different types and did not remove
this delegated-auth config/DTO selector.

#### Canonical authorities retained

- Chain-key batch root proof remains the sole delegated root-proof policy.
- `RootProof::IcChainKeyBatchSignatureV1` remains the structural proof
  payload boundary because it carries the actual signed proof through Candid
  and stable records. Retention of that payload shape does not retain a
  selectable protocol.
- Chain-key algorithm, issuer proof, and stable proof discriminants remain at
  their Candid or persistence boundaries where they carry actual proof data.
- The existing V1 root-policy, delegated-registry, batch-header, and issuer-
  leaf domains remain the sole current trust transcript.

#### Required hard cut

- Remove the TOML field, default, schema guidance, validation, bootstrap
  rendering, and runtime parsing for `root_proof_mode`.
- Remove `RootProofMode`, `proof_mode`, `preferred_proof_mode`, verifier-config
  fields, constructors, conversions, and impossible validation branches.
- Delete `encode_root_proof_mode` and all three mode-byte emissions. Do not
  replace them with a constant byte, reserved slot, padding field, protocol
  enum, or compatibility marker.
- Edit the sole current V1 root-policy and delegated-registry encoders in
  place. `ROOT_KEY_POLICY_DOMAIN`, `DELEGATED_AUTH_REGISTRY_DOMAIN`,
  `CHAIN_KEY_BATCH_HEADER_DOMAIN`, and `CHAIN_KEY_DELEGATION_CERT_DOMAIN`
  retain their existing V1 values. The delegated-auth registry snapshot and
  chain-key batch schema versions remain 1.
- Before new material is issued, raise `min_accepted_proof_epoch` and
  `min_accepted_registry_epoch` above the highest pre-cut epochs accepted by
  the deployment. Advance the root's durable proof and registry counters to at
  least those floors. These are monotonic revocation counters, not protocol or
  schema versions.
- Reject old signed material through the existing typed
  `ProofEpochTooOld`/`RegistryEpochTooOld` policy boundary. Do not create a new
  canonical generation merely to distinguish the removed byte.
- Make explicit legacy TOML input fail through the existing strict unknown-
  field configuration error. Do not accept and ignore it.

The canonical domains remain fixed at their current V1 values:

| Constant | Required value |
| --- | --- |
| `ROOT_KEY_POLICY_DOMAIN` | `CANIC_ROOT_KEY_POLICY_V1` |
| `DELEGATED_AUTH_REGISTRY_DOMAIN` | `CANIC_DELEGATED_AUTH_REGISTRY_SNAPSHOT_V1` |
| `CHAIN_KEY_BATCH_HEADER_DOMAIN` | `CANIC_ROOT_DELEGATION_CHAIN_KEY_BATCH_V1` |
| `CHAIN_KEY_DELEGATION_CERT_DOMAIN` | `CANIC_ROOT_DELEGATION_CHAIN_KEY_ISSUER_LEAF_V1` |

Do not introduce V2 types, constants, domain labels, schema values, readers,
writers, or negotiation. Before 1.0, the maintained shape is V1 and hard cuts
change that sole shape directly.

#### Contract and migration impact

This is a breaking TOML, public Rust, canonical-hash, and delegated-auth trust-
format cut. Configuration users delete the explicit `root_proof_mode` line;
omission selects the sole maintained chain-key batch behavior.

Root-key-policy and delegated-auth registry hashes derived from the old V1
shape are intentionally invalid after upgrade. Old batch signatures,
issuer-leaf hashes, installed delegation proofs, and tokens are rejected by the
raised proof/registry epoch floors. No old hash, signature, proof, certificate,
or token receives a compatibility verification path.

The upgrade boundary must fail closed and re-establish trust in this order:

1. quiesce or accept a bounded delegated-auth outage during the mixed-version
   deployment window;
2. record the highest pre-cut proof and registry epochs, then configure both
   minimum accepted floors strictly above them across the fleet;
3. upgrade the root authority and advance its durable counters to at least the
   new floors;
4. upgrade every configured issuer to the byte-free V1 verifier before
   installing new material;
5. construct the current V1 registry and discard or mark stale every stored
   active proof, batch, and renewal result below the new floors or bound to the
   old registry hash;
6. create and sign new root delegation batches at the raised epochs;
7. install new active proofs on every configured issuer; and
8. upgrade or re-enable all remaining verifier roles only against newly issued
   material.

Issuer canisters must treat a restored active proof from the old transcript as
unavailable until the root installs a new proof. Root renewal bookkeeping must
be forced due when its last-installed material does not match the current
registry hash/domain; an old expiry deadline must not postpone reprovisioning.
Externally held old tokens cannot be migrated and are rejected.

This invalidation is the migration. Do not add a legacy canonical decoder,
dual hasher, second schema generation, grace window, alias, or fallback. Stable
records that remain structurally decodable exist only so current lifecycle code
can reject, clear, prune, or replace them; they do not authorize old proofs.

#### Validation

- focused schema tests prove omission succeeds and explicit legacy input is a
  typed unknown field;
- delegated-auth provisioning, renewal, signing, verification, and canonical-
  hash tests pass;
- new fixed vectors cover the sole V1 root-policy and registry encodings with
  no mode byte and the unchanged V1 batch-header/issuer-leaf domains;
- old fixed vectors, stored active proofs, signed batches, certificates, and
  tokens fail closed at the raised proof/registry epoch policy;
- a focused upgrade journey proves old material becomes unavailable, root
  renewal is immediately due, new material is signed and installed, and
  delegated auth resumes only with the new transcript;
- maintained source/config guidance contains no `RootProofMode` or
  `root_proof_mode`, no `encode_root_proof_mode`, and no constant replacement
  for its removed byte; and
- maintained delegated-auth source contains no V2 protocol/schema/domain
  generation and no compatibility verification branch.

### Slice F — Remove Delegated-Token Call-Kind Plumbing

Status: published in `v0.98.12`.

#### Evidence

The access expression evaluator forwards `ctx.call.kind` through two
`delegated_token_verified` functions into `verify_token`, where the parameter
is named `_call_kind` and is unused. Git history shows that
`consume_update_token_once` was removed in `701631cc7` (`0.61.0`) while its
call-kind plumbing survived.

#### Required hard cut

Remove the argument and imports only from the delegated-token access chain.
Do not remove `EndpointCallKind` from endpoint identity, macro expansion,
performance accounting, or metrics; all three current call kinds remain active
there.

#### Contract impact and validation

There is no Candid, config, stable, JSON, CLI, or behavior change. Focused
access/auth tests must prove token decode, cryptographic verification, subject
binding, and required-scope enforcement. Source inventory must show no
call-kind parameter in delegated-token verification and continued call-kind
use in endpoint performance/metrics.

### Slice G — Remove Unused Internal Non-Root Init Arguments

Status: published in `v0.98.12`.

#### Evidence

Both non-root start paths clone init bytes into
`schedule_init_nonroot_bootstrap`, which captures them in the zero-delay timer
and passes them to `bootstrap_init_nonroot_canister`. That workflow explicitly
documents the argument as unused. The application hook separately receives the
same bytes through `canic_install(args)`.

#### Required hard cut

- Make the core API adapter, lifecycle scheduler, and workflow bootstrap
  argument-free.
- Remove the bootstrap-only clone and async capture in both generated start
  paths.
- Continue passing the original init arguments to the application-owned
  `canic_install(args)` hook.
- Preserve synchronous restore, zero-delay scheduling, hook order, metrics,
  bootstrap status, placement acknowledgement, and feature-gated sharding
  startup.

#### Contract impact and validation

This removes only internal Rust plumbing. Generated lifecycle behavior and
application init Candid remain unchanged. Focused lifecycle boundary and macro
tests must prove that internal bootstrap is argument-free while the user hook
still receives init bytes after Canic invariants are restored.

### Slice H — Minimize `CostGuardPermit`

Status: published in `v0.98.12`.

#### Evidence

`CostGuardPermit` stores `_cost_class`, `_quota_key`, `_payer`, and `_private`
at reservation time. No settlement, recovery, abort, workflow, ops, or test
reads those fields. Only `quota_intent_id` and `reservation_id` identify the
durable reservation pair.

#### Required hard cut

Remove the four dead fields and their initialization. Retain the permit type,
its non-public construction boundary, both intent identities, and every
permit-typed parameter that proves an expensive external effect crossed the
cost guard. The cut must not replace the permit with raw IDs or remove
proof-carrying parameters merely because an ops boundary names one `_permit`.

#### Contract impact and validation

This is a private Rust layout reduction with no runtime, stable, Candid,
config, JSON, or CLI change. Focused cost-guard and affected workflow tests
must continue proving reserve, complete, recover, abort, rollback, and durable
settlement behavior.

### Slice I — Delete Unproduced Adoption-Report Warnings

Status: published in `v0.98.12`.

#### Evidence

`AdoptionReportV1.warnings` has been initialized as `Vec::new()` by its sole
production constructor since `e0c6b6bbb` (`0.67.32`). No top-level warning
producer exists. The CLI still maps that permanently empty vector into the
evidence-envelope summary, while meaningful role and observed-canister warning
collections have real producers.

#### Required hard cut

- Remove the top-level `warnings` field, constructor initialization, CLI
  evidence-summary projection, serialized fixtures, and tests for its
  presence.
- Retain role-finding warnings, observed-canister warnings, blocked actions,
  missing/stale evidence, and conflict messages.
- Retain the current report schema version, following the established pre-1.0
  rule for deleting permanently empty report fields. Add no `null`, empty
  fallback, alias, or legacy report reader.

#### Contract impact and validation

This is a breaking JSON hard cut. Text output and actual warning/conflict
semantics remain unchanged. Focused host adoption, CLI rendering, JSON, and
evidence-envelope tests must prove that the top-level key is absent and every
surviving warning source still reaches its maintained output.

### Slice J — Collapse Private Auth CLI Dispatch

Status: published in `v0.98.12`.

#### Evidence

Private `AuthCommand` has only `RenewalStatus`. `AuthOptions::parse` wraps
`RenewalStatusOptions` in that enum, and `run_command` immediately unwraps it
through a single match arm. The nested `auth renewal status` parser already
owns rejection of every unsupported command form.

#### Required hard cut

Return `RenewalStatusOptions` directly from parsing and invoke the renewal
status runner without a one-variant dispatch layer. Remove the enum,
single-arm runner, irrefutable test destructuring, and no-longer-needed imports
or derives.

#### Contract impact and validation

This is private CLI implementation cleanup. Command spelling, help/version,
arguments, exit codes, Candid validation, text, and JSON remain unchanged.
Focused auth CLI parser and output tests must cover the sole maintained command
and typed usage rejection.

### Slice K — Delete Orphan XRC Bindings and Unused Bounded-String Aliases

Status: published in `v0.98.13`.

#### Evidence

The internal XRC module defines request, response, metadata, asset, and error
wire types, but no runtime, facade, canister, host, CLI, test, macro, build, or
configuration path imports them. This is residue from Canic's earlier role as
an IC protocol-wrapper facade. The 8-, 16-, 32-, and 256-byte bounded-string
aliases likewise have no consumer; only their defining module referred to the
16-byte alias in generic implementation tests.

The generic `BoundedString<N>` is not obsolete. It validates construction and
Serde/Candid decoding, declares a finite stable-structures bound, and rejects
oversized or invalid UTF-8 stable bytes. Its 64- and 128-byte aliases remain
active in pool identity, directory, sharding, intent, partition-key, and blob-
storage state.

#### Required hard cut

- Delete the complete XRC module and its module export. Add no replacement
  wrapper, alias, fallback, or compatibility surface.
- Delete only the unused 8-, 16-, 32-, and 256-byte aliases.
- Retain the generic implementation and active 64- and 128-byte aliases. Test
  the generic implementation directly rather than retaining a test-only alias.

#### Contract impact and validation

This removes hidden internal Rust surface only. There is no Candid service,
stable encoding, JSON, CLI, config, dependency, package-version, or runtime
behavior change. Focused bounded-string tests and strict affected-crate checks
must pass, and a maintained-source inventory must find no removed XRC symbol or
alias.

### Slice L — Delete Orphan Repo-Only Test Fixtures

Status: published in `v0.98.14`.

#### Evidence

`UpgradeScenario` and its one-variant error were introduced in `0.80.10`, but
no workspace test ever constructed the wrapper. The role-attestation fixture
still maintained two cache kinds even though the last
`install_test_root_with_verifier_cached` consumer disappeared with the
protected internal-client test in `0.65.0`. Its second cache, verifier identity,
conditional snapshot membership, readiness branch, and public lookup wrappers
therefore had no behaviorally reachable consumer.

The test-role `MINIMAL` constant likewise appeared only in the fixture crate's
own constant-name test. Every other retained role constant is used by a current
PocketIC harness or scenario.

#### Required hard cut

- Delete the unused upgrade module and re-exports.
- Reduce role-attestation caching to one direct `root + issuer` baseline.
  Remove the verifier cache kind, metadata, result projection, lookup/readiness
  branches, and unused public wrappers.
- Delete the self-sustaining `MINIMAL` constant and test entry.
- Retain all current issuer-baseline build, snapshot, restore, and readiness
  behavior, plus every role constant with a real fixture consumer.

#### Contract impact and validation

This changes only unpublished workspace test support. Production Wasm, Candid,
stable state, JSON, CLI, config, dependencies, package versions, and runtime
behavior remain unchanged. The complete test-support suite and the maintained
role-attestation PocketIC target must pass through the reduced path; strict
affected-crate checks and a removed-symbol inventory must also pass.

### Slice M — Delete Unused ICRC-21 Registration Convenience

Status: published in `v0.98.15`.

#### Evidence

`Icrc21Dispatcher::register_static_with` has no repository caller, active
documentation, macro expansion, canister use, test use, or script consumer.
The complete production public-function token scan identifies it as the sole
definition-only function. Git history traces it to a 2025 static-message
shortcut; it never acquired a consumer or distinct protocol responsibility
beside the typed registration route.

The downstream-facing `Icrc21Dispatcher::register` remains intentional: it
accepts the complete typed ICRC-21 request and response contract and is the
documented application registration surface. `consent_message` remains the
sole dispatch owner used by the generated endpoint workflow.

#### Required hard cut

- Delete `register_static_with` and its fixed English/generic-display response
  construction.
- Retain `register` and `consent_message` without an alias, replacement
  convenience, fallback route, or parallel registry.
- Prove the maintained facade registration and dispatch behavior with typed
  observable response state.

#### Contract impact and validation

This removes one unused public Rust convenience method before 1.0. The
maintained ICRC-21 endpoint, Candid request/response DTOs, handler behavior,
configuration, stable state, JSON, CLI, dependencies, package versions, and
runtime behavior remain unchanged. The focused facade protocol suite, affected
strict Clippy, formatting, and exact removed-symbol search must pass.

### Slice N — Delete Orphan Generic ICRC-2 Ledger Stack

Status: published in `v0.98.16`.

#### Evidence

`LedgerApi::transfer_from` is the only entry to the generic ledger workflow,
but no facade, endpoint, macro, canister, CLI, test, or script calls it. The
workflow calls only its local allowance rule and `LedgerOps`; `LedgerOps`
calls only `LedgerInfra`; and `LedgerInfra` alone consumes the internal
ICRC-2 bindings and ckUSDC/ckUSDT metadata. The chain therefore sustains its
own layers without a product consumer.

The 0.97 CDK-surface inventory already classified ICRC-2 as internal-only with
no maintained facade consumer. Current source and full-history tracing find no
later adoption. The manual root ICP-to-cycles workflow is independent: it owns
its ICP ledger and CMC bindings under `icp_refill` and does not call this
generic adapter.

#### Required hard cut

- Delete the generic ledger API, workflow, ops, and infra modules plus their
  module exports and typed wrapper branches.
- Delete the now-empty internal `cdk::spec` hierarchy and its ICRC-2 allowance,
  transfer arguments, result, and error bindings.
- Delete ckUSDC/ckUSDT constants used only for removed diagnostic labels.
- Delete the unreachable `ledger` platform-call metric surface and keep metric
  coverage on active generic, HTTP, and management surfaces.
- Add no facade, replacement adapter, compatibility alias, upstream wrapper,
  or fallback call path.

#### Contract impact and validation

This removes hidden `canic-core` Rust surface only. No maintained endpoint,
public facade, Candid service, stable record, JSON, CLI, configuration,
dependency, package version, or runtime behavior changes. Focused all-feature
core check and Clippy, active platform-metric tests, ICP-refill tests, facade
protocol tests, formatting, and exact retired-symbol searches must pass.

### Slice O — Move Generic IC Helpers To Upstream Ownership

Status: published in `v0.98.17`; instrumented-call portion corrected in open
`0.98.21`.

#### Evidence

The released 0.97 ownership rule removed the human-facing `canic::cdk`
surface and directs application-owned generic IC code to `candid` and
`ic-cdk`. The facade nevertheless continued to export `Call`,
`CallBuilder`, `CallResult`, `HttpApi`, and `BuildNetworkApi`; the prelude also
re-exported `Call`.

At the `v0.98.17` review, repository tracing found no maintained consumer of
those facade helpers. That evidence was sufficient for the consumerless HTTP
stack and forwarding-only build-network facade, but not for a public developer
convenience whose consumers live in downstream application repositories. The
call API and workflow wrapped the separately active internal ops/infra
transport. The complete HTTP API-through-infra stack called only itself and
owned metrics that no other operation could emit. `BuildNetworkApi` wrapped a
workflow wrapper that existed only to forward ICP refill to the active
`BuildNetworkOps` authority.

Canic itself still needs the internal call transport for management, NNS,
authentication, RPC, and manual ICP refill. It also still needs
`BuildNetworkOps` and `BuildNetworkInfra` for runtime admission,
control-plane bootstrap, status, pool policy, and refill network selection.
Those are implementation authorities, not a replacement application facade.

#### Required hard cut

- Delete the public facade `api::ic` module and the `Call` prelude export.
- Delete the API/workflow call wrappers while retaining the shared ops/infra
  transport used by Canic runtime behavior.
- Delete the consumerless HTTP API, workflow, ops, infra, DTO, domain, and
  metric stack.
- Delete `BuildNetworkApi` and its one-call workflow forwarding layer while
  retaining the active build-network ops/infra authority.
- Delete the empty `api::ic::canic` module.
- Update active downstream contract guidance to use upstream
  `ic_cdk::call::Call`.
- Add no alias, compatibility module, replacement Canic helper, fallback
  transport, or dormant metric branch.

The open `0.98.21` maintainer correction supersedes only the call-builder
deletion. All other requirements above remain in force.

#### Contract impact and validation

This is a breaking pre-1.0 public Rust hard cut. Downstream application code
using these helpers imports `ic-cdk` directly and owns its application HTTP
policy and metrics. The removed HTTP metric family and HTTP-only platform
dimensions disappear with their sole producer.

Open `0.98.21` restores one canonical instrumented call surface after
maintainer review established it as intentional Canic developer experience.
It does not restore `api::ic`, generic CDK relays, HTTP, build-network, or
historical protected-client surfaces.

Candid services, stable records, JSON, CLI, configuration, dependencies,
package versions, internal Canic call behavior, and manual ICP refill remain
unchanged. Focused all-feature checks and strict Clippy for `canic-core` and
`canic`, runtime-metric tests, internal call-transport tests, facade tests,
the direct-`ic-cdk` sharding fixture check, formatting, layering, and exact
retired-symbol searches must pass.

### Slice P — Remove The Internal IC Runtime Relay

Status: published in `v0.98.19`.

#### Evidence

Slice O removed the public application helper surfaces, but Canic runtime and
the hidden macro compiler namespace still imported IC runtime APIs through
`canic-core::cdk`. Canic-core itself had no policy or adapter behavior in
those re-exports: it forwarded upstream call builders, endpoint attributes,
ambient APIs, task spawning, logging, and traps unchanged. The later
instrumented-call correction does not restore this runtime relay.

The separate `cdk::utils::time` module was another one-owner relay. Every
runtime precision entered through `IcOps`, apart from one bootstrap diagnostic
that bypassed the ops owner. The stable-structures module also re-exported
sets, logs, heaps, file-memory helpers, readers/writers, and aliases with no
repository consumer.

#### Required hard cut

- Make Canic's hidden compiler namespace import `candid` and `ic-cdk`
  directly while retaining the namespace required by generated macros.
- Make Canic runtime, management, ingress, lifecycle, auth-certificate,
  logging, task, and call code import `ic-cdk` directly.
- Move host/Canister clock behavior into `IcOps` and delete the duplicate
  utility module.
- Remove stable-structures exports that have no current repository consumer.
- Add no alias, compatibility relay, replacement facade, or duplicate clock.

#### Contract impact and validation

This is a breaking pre-1.0 hidden/public `canic-core` Rust hard cut. Direct
consumers of removed runtime re-exports use upstream `ic-cdk`. The hidden
macro path remains compiler plumbing and preserves generated endpoint
behavior.

Candid services, stable records and memory IDs, JSON, CLI, configuration,
package versions, internal call semantics, and build-network behavior remain
unchanged. `canic` and `canic-control-plane` declare direct dependencies on
the already locked `ic-cdk` version. All-feature checks, strict all-target
Clippy, focused clock/bootstrap/macro/template tests, dependency hygiene,
formatting, layering, changelog governance, diff hygiene, and exact relay
searches must pass.

### Slice Q — Localize ICP Refill Account Identity

Status: published in `v0.98.20`.

#### Evidence

`canic-core::cdk::types::Account` and `Subaccount` appear generic, but their
only production consumer is the root-only manual ICP-refill ledger adapter.
The account type also implements ordering, hashing, `Principal` conversion,
and semantic equality between an absent subaccount and an explicit zero
subaccount. No production path consumes those behaviors.

The refill record already persists `cmc_to_account_owner` and
`cmc_to_account_subaccount` as part of the recovery identity. Transfer resume
nevertheless reconstructs the same account from the stored CMC Canister and
target Canister rather than using the persisted effect arguments.

#### Required hard cut

- Remove the generic CDK account and subaccount surfaces without an alias.
- Define the structurally identical ICRC-1 account only beside the raw ledger
  transfer argument.
- Persist CMC account identity once at operation creation.
- Project that exact identity into the recovery operation and consume it for
  every transfer attempt.
- Remove unused ordering, hashing, conversion, and default-subaccount
  semantics.
- Do not change the stable refill record, public endpoint DTO, outbound
  ICRC-1 Candid shape, or refill policy.

#### Contract impact and validation

This is a breaking pre-1.0 hidden `canic-core` Rust hard cut. The removed
generic helpers have no maintained facade or external repository consumer.
The ICRC-1 account record remains structurally identical on the wire.

Stable record fields and memory IDs, public Candid, JSON, CLI, configuration,
dependencies, package versions, and normal refill behavior remain unchanged.
Focused raw-adapter, storage, workflow, replay, retry, and recovery tests,
strict Clippy, host/Wasm checks, stable-memory guard, formatting, layering,
changelog governance, and diff hygiene must pass.

### Slice R — Remove False ICRC-103 Support

Status: fixed in open `0.98.21`.

#### Evidence

The global `standards.icrc103` flag is rendered into every compiled config and
can add ICRC-103 to `icrc10_supported_standards`. Canic has no
`icrc103_get_allowances` endpoint, no ICRC-103 request/response model, and no
required `icrc103:public_allowances` or `icrc103:max_take_value` ICRC-1 ledger
metadata. The generic ICRC-2 allowance and transfer stack was removed in
Slice N, so no underlying allowance authority exists.

The flag therefore changes only an advertisement. It can make a Canic
canister claim support for a protocol it cannot execute.

#### Required hard cut

- Remove `standards.icrc103` from schema, generated config, active
  documentation, and supported-standards projection.
- Remove the unused ICRC-103 name, URL, and capacity branch.
- Replace the zero-state ICRC-10 registry wrapper with one pure projection
  function for the surviving protocols.
- Retain ICRC-10 and configured ICRC-21 behavior.
- Add no ignored field, alias, compatibility parser, or replacement protocol.

#### Contract impact and validation

This is an intentional pre-1.0 TOML and observable query-result hard cut.
Explicit `standards.icrc103` input is rejected by the existing strict schema,
and the ICRC-10 query no longer returns a false ICRC-103 row.

The `icrc10_supported_standards` Candid method/result shape, ICRC-21 endpoint,
stable memory, public DTOs, JSON, CLI, dependencies, and package versions
remain unchanged. Focused standards projection, checked-in config, facade
protocol, strict Clippy, Wasm, formatting, layering, changelog-governance, and
diff-hygiene checks must pass.

### 0.98.21 Maintainer Correction — Restore The Instrumented Call Builder

Status: implemented in open `0.98.21`.

#### Evidence

The Slice O audit proved that Canic had no repository-internal consumer of its
public call facade. It did not prove that downstream application authors had no
use for a simpler call builder. The maintainer confirms that generic IC calls
are painful enough to justify a supported Canic developer surface.

The `CallOps` and infra call authorities survived Slice O because management,
NNS, authentication, capability RPC, and ICP-refill actively use their typed
errors and bounded metrics. Restoring a public boundary over that path does not
create another transport or compete with protected Canic capability RPC.

#### Required canonical surface

- Expose `Call`, `CallBuilder`, and `CallResult` only under
  `canic::api::call`; the prelude may re-export the `Call` entrypoint.
- Support bounded/unbounded waits, single/tuple/raw Candid arguments, cycle
  attachment, execution, and single/tuple Candid response decoding.
- Preserve public typed errors at the API boundary.
- Route `api -> workflow -> CallOps -> infra -> ic-cdk`, retaining the existing
  target/method and bounded platform-call metrics.
- Do not restore `canic::api::ic`, `canic::cdk`, HTTP, build-network, generic
  management, historical `CanicCall`, or compatibility aliases.
- Keep capability RPC as the sole protected Canic-to-Canic operation flow.

#### Contract impact and validation

This intentionally restores a public Rust convenience, without changing
Candid, stable state, JSON, CLI, configuration, dependencies, or package
versions. Focused core tests must prove that the public builder reaches the
existing metrics authority. Facade tests must pin the maintained construction,
prelude, and typed decode surface without introducing production test behavior.
Strict Clippy, Wasm, formatting, layering, changelog governance, and diff
hygiene must pass.

### Combined Contract Accounting

| Surface | Amendment result |
| --- | --- |
| Stable memory | Core subnet-state cell removed and ID 17 permanently retired; restored old auth proofs/batches/renewal results are rejected, cleared, pruned, or replaced without reusing an allocation ID |
| Candid | Core `canic_subnet_state` method and empty cascade field removed; unrelated control-plane subnet state and the data-carrying structural root-proof payload remain; the ICRC-10 query shape is unchanged while its false ICRC-103 result row is removed |
| Config TOML | Explicit delegated `root_proof_mode` and unsupported `standards.icrc103` become strict unknown fields; omission preserves chain-key batch and implemented standards behavior |
| Cryptographic encoding | Root-proof-mode byte removed from the sole V1 root-policy/registry shape; all domain and schema labels remain V1; raised proof/registry epochs reject old material before reissue |
| JSON | Permanently empty top-level adoption `warnings` key removed; current schema version and meaningful warnings retained |
| CLI | Auth command syntax/output unchanged; private dispatch only removed |
| Public Rust | Empty subnet state, delegated root-proof-mode construction/matching surfaces, unused ICRC-21 static-message registration, generic CDK/HTTP/build-network facades, the Canic-core IC runtime relay, and generic refill-only account helpers removed; one canonical instrumented `canic::api::call` builder is restored over the retained transport |
| Internal Rust | Dead call-kind/init-arg plumbing, permit ballast, orphan XRC/ICRC-2/HTTP stacks, redundant build-network/time/account/ICRC-10 registry wrappers, unused account semantics, bounded-string/stable-structure exports, and orphan repo-only test fixtures removed; the call API/workflow adapters are intentional boundaries over the single active CallOps/infra authority |
| Dependencies | `canic` and `canic-control-plane` own direct edges to the already locked `ic-cdk`; no dependency version changes |
| Package versions | Unchanged by automated implementation policy |

### Implementation Order and Boundaries

1. Slice D was implemented alone and published in `v0.98.10` because it
   crosses stable-allocation, Candid, cascade, facade, audit-probe, and
   PocketIC boundaries.
2. Slice E was implemented alone and published in `v0.98.11` because it
   deliberately invalidates delegated-auth material through an ordered epoch-
   floor and reprovisioning journey.
3. Slice I was implemented as a separately validated operator-contract cut and
   published in `v0.98.12` because it alters JSON.
4. Slices F, G, H, and J form the focused internal cleanup batch published in
   `v0.98.12`; each retains its own validation evidence.
5. Slice K is a separate hidden-Rust-surface hard cut published in `v0.98.13`;
   it preserves the bounded stable-state invariant.
6. Slice L is a repo-only test-support hard cut published in `v0.98.14`;
   it preserves the sole exercised cached attestation baseline.
7. Slice M is a public Rust hard cut published in `v0.98.15`; it
   preserves typed ICRC-21 registration and endpoint dispatch.
8. Slice N is a hidden Rust hard cut published in `v0.98.16`; it
   preserves the independent root-only manual ICP-to-cycles workflow.
9. Slice O is a public Rust hard cut published in `v0.98.17`; open `0.98.21`
   corrects only its call-builder decision. Generic CDK, HTTP, and
   build-network facades remain removed while the restored builder uses
   Canic's active internal call authority.
10. Slice P is a Canic-core Rust hard cut published in `v0.98.19`; it
    removes the redundant IC runtime relay without changing generated macro
    behavior or maintained platform operations.
11. Slice Q is a hidden Canic-core Rust hard cut published in `v0.98.20`;
    it keeps the ICRC-1 wire shape in its sole adapter and makes persisted
    recovery identity authoritative.
12. Slice R is a TOML and observable-query hard cut targeted at open
    `0.98.21`; it prevents Canic from advertising an unimplemented protocol.
13. The `0.98.21` maintainer correction restores one canonical instrumented
    call builder without reviving the generic IC facade family.

Do not combine these cuts with replay recovery, ICP-refill redesign, additive
auth algorithms, a new subnet-state replacement, or a new CLI command. Do not
assign a patch number or change Cargo package versions from this design; the
maintainer-owned release flow determines the implementation batch target.

### Amendment Acceptance Criteria

The amendment is complete only when:

1. all fifteen ledger rows have implementation and validation evidence;
2. the removed core subnet-state/Candid/cascade surface, delegated
   root-proof-mode config/type, top-level adoption warning key, and private
   auth dispatch wrapper are absent from maintained source;
3. memory ID 17 is retired without renumbering or reuse;
4. the sole V1 canonical fixed vectors contain no mode byte, no V2 generation
   exists, old material fails the raised epoch floors, and ordered
   reprovisioning restores delegated auth;
5. application init hooks still receive their original arguments;
6. the cost permit still gates every current expensive side effect;
7. meaningful adoption warnings and conflicts still reach their outputs;
8. orphan XRC bindings and unused bounded-string aliases are absent while
   active boundary and stable-state length enforcement remains;
9. repo-only test support contains no unused upgrade wrapper, verifier-cache
   alternative, fixture projection, or self-sustaining role constant;
10. ICRC-21 exposes one typed handler-registration route with no static-message
    convenience or parallel registry;
11. no generic ICRC-2 wrapper stack, internal binding hierarchy, orphan
    ckUSDC/ckUSDT metadata, or unreachable ledger metric surface remains;
12. no public generic CDK relay, HTTP-outcall, or build-network helper remains;
    the canonical `canic::api::call` builder and prelude entrypoint share the
    active internal call transport and metrics authority;
13. `canic-core::cdk` no longer relays IC runtime APIs, the hidden compiler
    namespace is backed directly by upstream crates, `IcOps` owns time, and
    every retained stable-structures export has a current repository consumer;
14. no generic CDK account/subaccount helper survives; the adapter-local
    ICRC-1 account preserves its wire shape and transfer resume consumes the
    persisted CMC account identity;
15. no ICRC-103 configuration or advertisement survives; the maintained
    supported-standards response contains only ICRC-10 and configured ICRC-21;
16. the restored Call builder supports the maintained construction and decode
    contract without restoring `api::ic`, `canic::cdk`, HTTP, build-network, or
    historical protected-client surfaces;
17. targeted checks required by each slice pass; and
18. changelog, status, ledger disposition, and contract evidence are updated as
    part of the implemented behavior batch, not by this design-only amendment.
