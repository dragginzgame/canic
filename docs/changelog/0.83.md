# 0.83 Changelog

## 0.83.24 - 2026-07-09 - Deployment-truth status label ownership

### Summary

- Fixes `CANIC-083-DEBT-030` by moving deployment-truth status labels onto the
  deployment-truth model enum owners. Safety status, deployment execution
  preflight status, deployment execution status, promotion readiness status,
  external lifecycle plan status, external upgrade completion status, and
  external upgrade verification requirement status now expose stable `label()`
  methods.

- Replaces duplicate status-label helper functions in deployment-truth text
  renderers and medic receipt summaries with calls to those model-owned labels.

- Keeps operator text output labels, medic text, command behavior, endpoint
  surfaces, Candid, JSON schemas, deployment truth schema, evidence/report
  schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo fmt --all
cargo check --locked -p canic-host -p canic-cli
cargo test --locked -p canic-host deployment_truth --lib
cargo test --locked -p canic-cli medic
cargo clippy --locked -p canic-host -p canic-cli --all-targets -- -D warnings
cargo test --locked -p canic --test changelog_governance
cargo fmt --all -- --check
git diff --check
```

## 0.83.23 - 2026-07-09 - Runtime introspection enum label ownership

### Summary

- Fixes `CANIC-083-DEBT-029` by moving runtime introspection enum labels onto
  the `domain::runtime` enum owners. Runtime status, timer status, state-domain
  status, failure severity, health status, readiness status, check status,
  diagnostic severity, and field-visibility enums now expose stable `label()`
  methods.

- Replaces `canic inspect` text-renderer label helpers for runtime status,
  timer status, state-domain status, and recent-failure severity with calls to
  the runtime domain enum labels.

- Updates runtime DTO serde-label tests to compare every runtime enum variant's
  serialized label against the domain-owned label, keeping Candid and serde
  contracts pinned without a second copy of the expected strings in the DTO
  tests.

- Keeps inspect text output, runtime JSON labels, runtime Candid labels,
  command behavior, endpoint surfaces, deployment truth, evidence/report
  schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo fmt --all
cargo check --locked -p canic-core -p canic-cli
cargo test --locked -p canic-core runtime --lib
cargo test --locked -p canic-cli inspect
cargo test --locked -p canic --test protocol_surface
cargo clippy --locked -p canic-core -p canic-cli --all-targets -- -D warnings
cargo test --locked -p canic --test changelog_governance
```

## 0.83.22 - 2026-07-09 - State manifest and audit label ownership

### Summary

- Fixes `CANIC-083-DEBT-028` by moving state manifest storage and
  migration-policy labels onto the state contract owner. `StateStorage` and
  `MigrationPolicy` now expose stable `as_str()` labels for their schema values.

- Replaces duplicate local label match blocks in `canic state manifest` text
  rendering and runtime state-summary building with the model-owned labels.

- Moves state-audit status labels onto `StateAuditStatus`, so `canic state
  audit` text rendering and medic's state-audit summary consume report-owned
  status labels instead of owning local match blocks.

- Keeps state manifest JSON labels, text output, runtime state summary strings,
  command behavior, endpoint surfaces, Candid, deployment truth,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo fmt --all
cargo check --locked -p canic-core -p canic-cli -p canic-host
cargo test --locked -p canic-core state_contract --lib
cargo test --locked -p canic-core runtime --lib
cargo test --locked -p canic-cli state
cargo test --locked -p canic-cli medic
cargo test --locked -p canic-host state_manifest --lib
cargo clippy --locked -p canic-core -p canic-cli -p canic-host --all-targets -- -D warnings
cargo test --locked -p canic --test changelog_governance
```

## 0.83.21 - 2026-07-09 - Local metadata Candid surface hard cut

### Summary

- Fixes `CANIC-083-DEBT-027` by hard-cutting `ids::BuildNetwork` out of the
  Candid trait surface. After the delegated-auth policy metadata cleanup,
  `BuildNetwork` has no active Candid DTO or `.did` consumer.

- Also removes `CandidType` from `ValidationReport` and `ValidationIssue`;
  those types are root bootstrap validation metadata and are not exposed as
  endpoint DTOs or checked-in `.did` declarations.

- Keeps `BuildNetwork` as local runtime/config/policy data with `serde`
  support and stable `as_str()` labels.

- Keeps endpoint surfaces, Candid payloads, JSON schemas, deployment truth,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo fmt --all
cargo check --locked -p canic-core -p canic -p canic-control-plane
cargo test --locked -p canic-core auth --lib
cargo test --locked -p canic-control-plane --lib
cargo test --locked -p canic --test protocol_surface
cargo clippy --locked -p canic-core -p canic-control-plane --all-targets -- -D warnings
cargo test --locked -p canic --test changelog_governance
```

## 0.83.20 - 2026-07-09 - Delegated-auth Candid surface hard cut

### Summary

- Fixes `CANIC-083-DEBT-026` by hard-cutting delegated-auth verifier policy
  and registry snapshot metadata out of the Candid trait surface.
  `RootProofMode`, `RootKeyPolicyV1`, `DelegatedAuthRegistrySnapshotV1`, and
  `DelegatedAuthIssuerPolicySnapshotV1` no longer derive `CandidType`.

- Removes stale protocol-surface Candid round-trip assertions for those
  internal canonical-hash metadata shapes.

- Keeps active delegated token, root proof, issuer proof, proof install, proof
  status, endpoint, JSON, deployment truth, evidence/report, and stable-state
  surfaces unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo fmt --all
cargo check --locked -p canic-core -p canic
cargo test --locked -p canic --test protocol_surface
cargo test --locked -p canic-core auth --lib
cargo clippy --locked -p canic-core --all-targets -- -D warnings
cargo fmt --all -- --check
cargo test --locked -p canic --test changelog_governance
```

## 0.83.19 - 2026-07-09 - Authority report text label typing

### Summary

- Fixes `CANIC-083-DEBT-025` by tightening deployment-truth authority report
  text rendering so report title, field, section, count, fallback,
  hard-failure, observation-gap, blocker, next-action, automatic-action, and
  external-action labels use typed labels internally.

- Keeps operator text output labels, command behavior, endpoint surfaces,
  Candid, JSON schemas, deployment truth schema, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo check --locked -p canic-host
cargo test --locked -p canic-host authority
cargo clippy --locked -p canic-host --all-targets -- -D warnings
```

## 0.83.18 - 2026-07-09 - Comparison report label typing

### Summary

- Fixes `CANIC-083-DEBT-024` by tightening deployment-truth comparison report
  validation and text rendering so validation field names, target-side/field
  labels, and text field/section/count/target/fallback labels use typed labels
  internally.

- Keeps validation error field strings, operator text output labels, command
  behavior, endpoint surfaces, Candid, JSON schemas, deployment truth schema,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo check --locked -p canic-host
cargo test --locked -p canic-host comparison
cargo clippy --locked -p canic-host --all-targets -- -D warnings
```

## 0.83.17 - 2026-07-09 - Execution-preflight validation/text label typing

### Summary

- Fixes `CANIC-083-DEBT-023` by tightening deployment-truth
  execution-preflight validation and text rendering so validation field names
  and text field/section/status labels use typed labels internally.

- Also tightens execution-preflight blocker construction so maintained
  safety-finding codes and the static authority fallback subject use typed
  labels internally.

- Keeps validation error field strings, safety-finding code strings, fallback
  subject string, operator text output labels, command behavior, endpoint
  surfaces, Candid, JSON schemas, deployment truth schema, evidence/report
  schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo check --locked -p canic-host
cargo test --locked -p canic-host execution_preflight
cargo clippy --locked -p canic-host --all-targets -- -D warnings
```

## 0.83.16 - 2026-07-09 - Execution-preflight receipt label typing

### Summary

- Fixes `CANIC-083-DEBT-022` by tightening host install-root
  execution-preflight receipt construction so the receipt phase, operation ID,
  failure command-result code, and evidence keys use typed labels internally.

- Tightens deployment-truth execution-preflight planned phases so the
  current-install planned-phase list uses typed labels internally.

- Keeps receipt JSON phase strings, receipt operation IDs, command-result
  codes, evidence strings, planned-phase strings, command behavior, endpoint
  surfaces, Candid, JSON schemas, deployment truth schema, evidence/report
  schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo check --locked -p canic-host
cargo test --locked -p canic-host execution_preflight
cargo clippy --locked -p canic-host --all-targets -- -D warnings
```

## 0.83.15 - 2026-07-09 - Install-root label typing

### Summary

- Fixes `CANIC-083-DEBT-020` by tightening host install-root deployment-truth
  receipt construction so operation phases, completed-phase receipts,
  artifact-promotion install receipts, and deployment-truth gate operation IDs
  use `InstallPhaseLabel` instead of raw strings internally.

- Fixes `CANIC-083-DEBT-021` by tightening host install-root timing summary
  rendering so timing rows use `InstallTimingLabel` instead of raw strings
  internally.

- Keeps receipt JSON phase strings, receipt operation IDs, command behavior,
  timing table labels, endpoint surfaces, Candid, JSON schemas, deployment
  truth schema, evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo check --locked -p canic-host
cargo test --locked -p canic-host install_truth
cargo test --locked -p canic-host install_timing_summary
cargo clippy --locked -p canic-host --all-targets -- -D warnings
```

## 0.83.14 - 2026-07-08 - Runtime bootstrap phase label typing

### Summary

- Fixes `CANIC-083-DEBT-019` by tightening runtime bootstrap status so
  process-local phase labels are represented by `BootstrapPhaseLabel` instead
  of raw strings.

- Keeps `BootstrapStatusResponse.phase`, `canic_bootstrap_status`, runtime
  introspection recent-failure metadata, root/nonroot lifecycle scheduling
  behavior, endpoint surfaces, Candid, JSON, deployment truth,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo test --locked -p canic-core bootstrap --lib
cargo check --locked -p canic-control-plane
cargo clippy --locked -p canic-core --all-targets -- -D warnings
```

## 0.83.13 - 2026-07-08 - Replay-policy constructor label typing

### Summary

- Fixes `CANIC-083-DEBT-018` by tightening replay-policy manifest constructors
  so static command-kind, command-manifest, quota-policy, and cycle-reserve
  labels are typed at manifest call sites instead of being accepted or stored
  as loose `&'static str` labels.

- Keeps endpoint names, command-manifest string values, quota/reserve policy
  IDs, reasons, tests, runtime replay storage, replay guards, operation IDs,
  and persisted receipts unchanged.

- Keeps endpoint surfaces, command behavior, Candid, JSON, deployment truth,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo test --locked -p canic-core replay_policy --lib
cargo clippy --locked -p canic-core --all-targets -- -D warnings
```

## 0.83.12 - 2026-07-08 - Replay-policy command-kind label typing

### Summary

- Fixes `CANIC-083-DEBT-017` by tightening replay-policy manifests so static
  command-kind labels are represented by a typed manifest value instead of raw
  `&'static str` fields inside `ReplayPolicy` variants.

- Keeps runtime replay storage, replay guards, operation IDs, and persisted
  receipts on the existing `model::replay::CommandKind` boundary.

- Treats the Rust replay-policy manifest model change as a pre-1.0 hard cut.

- Keeps endpoint surfaces, command behavior, Candid, JSON, deployment truth,
  evidence/report schemas, and stable-state layout unchanged.

### CLI

- No command forms changed.

### Testing

Validated with:

```bash
cargo test --locked -p canic-core replay_policy --lib
cargo clippy --locked -p canic-core --all-targets -- -D warnings
```

## 0.83.11 - 2026-07-08 - Host report label typing

### Summary

- Fixes `CANIC-083-DEBT-015` by tightening `canic state audit` reports so
  scope, category, and source labels are typed internally instead of owned as
  raw strings.

- Fixes `CANIC-083-DEBT-016` by tightening deployment-root verification
  reports so identity/evidence check-row names are typed internally instead of
  repeated as raw strings across the builder and validator.

- Keeps audit codes, subjects, details, next actions, command strings, and
  embedded manifest data in their existing report shapes. Keeps serialized
  `DeploymentRootVerificationCheckV1.name` labels unchanged.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic state audit` still emits the same scope, category, and source labels,
  including `project`, `role`, `manifest`, `schema_version`, `memory_id`,
  `migration`, `removed_state`, `snapshot`, `naming`, `lifecycle`,
  `invariant`, `test_coverage`, and `state_manifest`.

- No command forms changed.

```bash
canic state audit --json
canic state audit --role <role> --json
canic deploy inspect root --request <file>
canic deploy root verify <deployment> --from-check <file>
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-host state_manifest --lib
cargo test --locked -p canic-cli state
cargo test --locked -p canic-host root_verification --lib
cargo test --locked -p canic-cli deploy_root
```

## 0.83.10 - 2026-07-08 - Deploy-plan diagnostic label typing

### Summary

- Fixes `CANIC-083-DEBT-014` by tightening `canic deploy plan` diagnostics so
  category, severity, and source labels are typed internally instead of owned
  as raw strings.

- Keeps deploy-plan diagnostic codes, subjects, details, next actions, and
  embedded `DeploymentPlanV1` data in their existing report shapes.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic deploy plan` still emits the same diagnostic labels, including
  `artifact`, `authority`, `config`, `deployment_identity`, `inventory`,
  `observation`, `topology`, `trust_domain`, `unsupported_shape`,
  `verifier_readiness`, `info`, `warning`, `blocked`, `unsupported`,
  `cli_arg`, `build_profile`, `deployment_config`,
  `deployment_plan_builder`, `fleet_config`, `installed_deployment`, and
  `local_observation`.

- No command forms changed.

```bash
canic deploy plan <deployment> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli deploy_plan
```

## 0.83.9 - 2026-07-08 - Replica and deploy-plan label typing

### Summary

- Fixes `CANIC-083-DEBT-012` by tightening `canic replica status --json`
  report ownership so the closed `status_source` labels are typed internally
  instead of owned as string literals in the status builder.

- Fixes `CANIC-083-DEBT-013` by tightening `canic deploy plan`
  future-apply preview rows so phase, operation, and status labels are typed
  internally instead of owned as raw strings.

- Keeps delegated ICP CLI command/error strings and embedded ICP status
  payloads as runtime data, and keeps deploy-plan diagnostic codes, subjects,
  details, next actions, and embedded `DeploymentPlanV1` data in their existing
  report shapes.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic replica status --json` still emits the same `status_source` labels:
  `icp_cli`, `icp_cli_stale`, `http_status`, and `none`.

- `canic deploy plan` still emits the same proposed-operation labels,
  including `future_apply_preview`, `create_canister`, `install_wasm`,
  `upgrade_wasm`, `set_controllers`, `register_child`, `register_root`,
  `upload_artifact`, `apply_policy`, `verify_topology`, `verify_readiness`,
  and `not_executed`.

- No command forms changed.

```bash
canic replica status --json
canic deploy plan <deployment> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli replica
cargo test --locked -p canic-cli deploy_plan
```

## 0.83.8 - 2026-07-08 - Blob-storage method-mode typing

### Summary

- Fixes `CANIC-083-DEBT-011` by tightening `canic blob-storage` action report
  ownership so the closed `query`/`update` method-mode labels are typed
  internally instead of converted to raw strings before report construction.

- Keeps response-derived gateway `sync_action`, next-action guidance labels,
  delegated command strings, error text, and canister-derived data as strings.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic blob-storage sync-gateways` and `canic blob-storage fund` still emit
  the same action `mode` labels: `query` and `update`.

- No command forms changed.

```bash
canic blob-storage sync-gateways <deployment> <canister-or-role> --dry-run --json
canic blob-storage fund <deployment> <canister-or-role> --cycles <cycles> --dry-run --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli blob_storage
```

## 0.83.7 - 2026-07-08 - Wallet command parser typing

### Summary

- Fixes `CANIC-083-DEBT-010` by tightening `canic token` and `canic cycles`
  wallet parsing so maintained subcommand sets are represented by typed
  internal command kinds instead of raw strings.

- Keeps caller-provided token symbols, receivers, cycles pending-operation
  command strings, and delegated ICP CLI command/error strings as strings.

- Keeps CLI behavior, help text, command forms, endpoint surfaces, Candid,
  JSON, deployment truth, evidence/report schemas, and stable-state layout
  unchanged.

### CLI

- `canic token` and `canic cycles` still accept the same maintained command
  forms.

- No command forms changed.

```bash
canic token balance
canic token icp balance
canic token transfer 1.25 <principal-or-account>
canic token icp transfer 1.25 <deployment>/<role-or-canister>
canic cycles balance
canic cycles transfer 4T <principal>
canic cycles transfer 4T <deployment>/<role-or-canister>
canic cycles topup <deployment> <role-or-canister> 4T
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli token
cargo test --locked -p canic-cli cycles
```

## 0.83.6 - 2026-07-08 - Backup report status typing

### Summary

- Fixes `CANIC-083-DEBT-009` by tightening `canic backup` report ownership so
  create mode/layout/status, list status, prune status/action, and
  status/inspect layout-status labels are typed internally instead of owned as
  raw strings.

- Keeps dynamic backup scope labels, paths, operation kind/state labels,
  errors, and canister-derived data as strings.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic backup` still emits the same labels, including `dry-run`, `execute`,
  `existing`, `new`, `planned`, `running`, `complete`, `paused`, `failed`,
  `invalid-manifest`, `invalid-plan`, `invalid-plan-journal`, `ok`,
  `would-remove`, and `removed`.

- No command forms changed.

```bash
canic backup create <deployment> --dry-run
canic backup status <backup-ref>
canic backup inspect <backup-ref> --json
canic backup list
canic backup prune --failed --dry-run
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli backup
```

## 0.83.5 - 2026-07-08 - Blob-storage report label typing

### Summary

- Fixes `CANIC-083-DEBT-008` by tightening `canic blob-storage` report
  ownership so closed report kind, Candid source, action, funding status, and
  readiness state labels are typed internally instead of owned as raw strings.

- Keeps free-form command strings, error messages, blocker/warning code arrays,
  and canister-derived text values as strings.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic blob-storage` still emits the same JSON labels, including
  `blob_storage_status`, `blob_storage_error`,
  `blob_storage_sync_gateways_result`, `blob_storage_fund_result`,
  `installed_deployment`, `sync_gateways`, `fund`, `ready`, `warning`,
  `blocked`, `funding_needed`, `not_configured`, `not_needed`, and `unknown`.

- No command forms changed.

```bash
canic blob-storage status <deployment> <canister-or-role> --json
canic blob-storage sync-gateways <deployment> <canister-or-role> --json
canic blob-storage fund <deployment> <canister-or-role> --cycles <cycles> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli blob_storage
```

## 0.83.4 - 2026-07-08 - Metrics and cycles report status typing

### Summary

- Fixes `CANIC-083-DEBT-006` by tightening `canic info metrics` canister row
  status ownership so the closed `ok`, `empty`, `unavailable`, and `error`
  status labels are typed internally instead of owned as raw strings.

- Fixes `CANIC-083-DEBT-007` by tightening `canic info cycles` canister row
  status and coverage ownership so `ok`, `empty`, `error`, `covered`,
  `partial`, and `none` are typed internally instead of owned as raw strings.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and
  stable-state layout unchanged.

### CLI

- `canic info metrics` still emits the same row status labels: `ok`, `empty`,
  `unavailable`, and `error`.

- `canic info cycles` still emits the same row status and coverage labels:
  `ok`, `empty`, `error`, `covered`, `partial`, and `none`.

- No command forms changed.

```bash
canic info metrics <deployment> --json
canic info cycles <deployment> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli metrics
cargo test --locked -p canic-cli cycles
```

## 0.83.3 - 2026-07-08 - Auth renewal report typing

### Summary

- Fixes `CANIC-083-DEBT-005` by tightening the
  `canic auth renewal status` report wrapper so CLI-owned report kind, local
  Candid source, and aggregate renewal status labels are typed internally
  instead of owned as raw strings.

- Keeps decoded canister response statuses as response data, and keeps CLI
  behavior, JSON fields, JSON labels, text output meaning, endpoint surfaces,
  Candid, deployment truth, evidence/report schemas, and stable-state layout
  unchanged.

### CLI

- `canic auth renewal status` still emits the same JSON labels:
  `auth_renewal_status`, `installed_deployment`, `active_attempt`,
  `configured`, `disabled`, `missing`, `unavailable`, and `drift_detected`.

- No command forms changed.

```bash
canic auth renewal status <deployment> --issuer <principal> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli auth
```

## 0.83.2 - 2026-07-08 - Runtime inspect report typing

### Summary

- Fixes `CANIC-083-DEBT-004` by tightening the `canic inspect` report wrapper
  so command, endpoint, health/readiness slots, source attribution, response
  format, and aggregate status are typed internally instead of owned as loose
  JSON/raw strings.

- Keeps CLI behavior, JSON fields, JSON labels, text output meaning, endpoint
  surfaces, Candid, deployment truth, evidence/report schemas, and stable-state
  layout unchanged.

### CLI

- `canic inspect` still emits the same source labels and response-format labels:
  `cli_arg`, `deployment_record`, `runtime_observed`, and `candid`.

- No command forms changed.

```bash
canic inspect canister <principal> --json
canic inspect deployment <deployment> --role <role> --json
```

### Testing

Validated with:

```bash
cargo test --locked -p canic-cli inspect
```

## 0.83.1 - 2026-07-08 - Advanced deploy text-output flag hard cut

### Summary

- Fixes `CANIC-083-DEBT-002` by hard-cutting default-JSON advanced deploy
  report families from `--format json|text` to JSON by default plus `--text`
  for human-readable output.

- Keeps JSON payload schemas, deployment truth, evidence/report schemas,
  endpoint surfaces, Candid, and stable-state layout unchanged.

- Removes an active `canic state manifest` help breadcrumb for the removed
  `--format json` spelling without changing state command behavior.

### CLI

- `canic deploy inspect compare` now uses `--text` for human-readable
  comparison summaries.

- `canic deploy inspect root` and `canic deploy root verify` now use `--text`
  for human-readable root verification output.

- `canic deploy authority check|evidence|report|receipt` now use `--text` for
  human-readable dry-run authority summaries.

- `canic deploy external` report builders now use `--text` for human-readable
  external lifecycle summaries.

- `canic deploy promote` report builders now use `--text` for human-readable
  promotion summaries.

- `canic state manifest` help now describes only the maintained `--json`
  surface.

```bash
canic deploy inspect compare --left <file> --right <file> --text
canic deploy inspect root --request <file> --text
canic deploy root verify <deployment> --from-check <file> --text
canic deploy authority report <deployment> --text
canic deploy external plan <deployment> --text
canic deploy external verify --request <file> --text
canic deploy promote plan --request <file> --text
canic deploy promote inspect readiness --request <file> --text
```

### Breaking

- Hard-cuts the old `--format json|text` forms for the affected advanced deploy
  report families. No aliases, shims, compatibility routes, or
  anti-resurrection tests were added.

### Testing

Validated with:

```bash
cargo fmt --all
cargo test --locked -p canic-cli deploy
cargo fmt --all -- --check
git diff --check
cargo test --locked -p canic --test changelog_governance
cargo clippy --locked -p canic-cli --all-targets -- -D warnings
cargo test --locked -p canic-cli state
```

## 0.83.0 - 2026-07-08 - Technical debt audit and report JSON flag hard cut

### Summary

- Starts the 0.83 technical debt audit line with docs-only audit artifacts.

- Makes `ledger.md` the canonical first-pass debt ledger and records the first
  accepted finding.

- Fixes `CANIC-083-DEBT-001` by standardizing the affected active
  report-command families on `--json` for raw JSON output.

- Keeps stable CI/GitOps evidence-envelope output as a distinct current
  surface through `--evidence-envelope`.

### Audit

- Added `docs/audits/0.83-technical-debt/README.md`.

- Added `docs/audits/0.83-technical-debt/ledger.md`.

- Added `docs/audits/0.83-technical-debt/recommended-slices.md`.

- Added `docs/audits/0.83-technical-debt/deferred.md`.

- Added `docs/audits/0.83-technical-debt/rejected.md`.

- Recorded and fixed `CANIC-083-DEBT-001`, a P2 finding for mixed CLI report
  JSON flag conventions across active report families.

- Recorded `CANIC-083-DEBT-002` for remaining advanced deploy report families
  that needed a separate convention slice.

- Recorded rejected/non-finding candidates for pure-policy imports,
  TODO/FIXME/HACK markers, and remaining semantic helper type aliases.

### CLI

- `canic deploy inspect catalog list|inspect` now use `--json` for catalog
  JSON reports.

- `canic deploy check <deployment>` now prints text by default, uses `--json`
  for raw `DeploymentCheckV1`, and uses `--evidence-envelope` for
  `EvidenceEnvelopeV1` output.

- `canic fleet adoption report <fleet>` now prints text by default, uses
  `--json` for raw adoption-report JSON, and uses `--evidence-envelope` for
  `EvidenceEnvelopeV1` output.

- `canic evidence gate` now uses `--json` for raw gate reports and
  `--evidence-envelope` for envelope output.

- `canic evidence compare` now uses `--json` for structured comparison output.

```bash
canic deploy inspect catalog list --json
canic deploy inspect catalog inspect <deployment> --json
canic deploy check <deployment> --json
canic deploy check <deployment> --evidence-envelope
canic fleet adoption report <fleet> --profile <profile> --json
canic fleet adoption report <fleet> --profile <profile> --evidence-envelope
canic evidence gate --policy <path> --envelope <path> --json
canic evidence gate --policy <path> --envelope <path> --evidence-envelope
canic evidence compare --left <path> --right <path> --json
```

### Breaking

- Hard-cuts the old `--format json` / `--format envelope-json` report-selection
  forms for the affected command families. No aliases or compatibility routes
  were added.

### Docs

- Updated active architecture, operation, and CLI docs to use `--json` and
  `--evidence-envelope` for the affected report families.

### Migration Notes

- This patch adds no new command families.

### Testing

Validated with:

```bash
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo test --locked -p canic-cli
cargo test --locked -p canic --test protocol_surface
cargo test --locked -p canic-host deployment_truth --lib
cargo fmt --all
cargo test --locked -p canic-cli deploy
cargo test --locked -p canic-cli fleet
cargo test --locked -p canic-cli evidence
cargo test --locked -p canic --test changelog_governance
git diff --check
cargo clippy --locked -p canic-cli --all-targets -- -D warnings
```
