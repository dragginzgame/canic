# 0.43 Changelog

## 0.43.2 - 2026-05-24 - execution preflight evidence hardening

### Added

- Added `validate_deployment_execution_preflight(...)` for standalone
  `DeploymentExecutionPreflightV1` artifacts.
- Added `validate_deployment_execution_preflight_for_check(...)` for
  source-check-bound preflight artifacts, keeping passive executor readiness
  tied to the `DeploymentCheckV1` that produced it.
- Added host tests pinning the `DeploymentExecutionPreflightV1` JSON field
  names and enum strings before any public CLI surface is promoted.

### Changed

- Current-install preflight paths now validate generated execution-preflight
  evidence before returning read-only readiness or writing the
  `execution_preflight` receipt.
- Execution-preflight validation now rejects schema drift, blank provenance
  IDs, status/blocker mismatches, duplicate capability entries,
  missing-capability inconsistencies, and mixed check/preflight identity.

### Notes

- This remains passive executor-readiness evidence. It does not add a public
  CLI command, controller mutation, or a new executor backend.

### Testing

- Added deployment-truth unit coverage for valid check-derived preflight
  evidence.
- Added deployment-truth unit coverage rejecting mutated status, mismatched
  source-check identity, and inconsistent capability metadata.
- Added deployment-truth JSON shape coverage for
  `DeploymentExecutionPreflightV1`.
- Re-ran focused host execution-preflight, install-truth, clippy, changelog
  governance, and diff checks during the slice.

## 0.43.1 - 2026-05-24 - passive execution preflight integration

### Added

- Added `deployment_execution_preflight_from_check(...)`, which derives
  authority reconciliation from `DeploymentCheckV1` and emits the same passive
  `DeploymentExecutionPreflightV1` readiness model.
- Added `check_install_execution_preflight(...)`, a read-only host API for
  deriving current-install execution readiness from install inputs without
  writing receipts or entering the mutating install path.
- Added host-owned text rendering for `DeploymentExecutionPreflightV1`, keeping
  the executor-readiness summary in the deployment-truth presentation layer
  without adding a new CLI command.

### Changed

- Current install now writes a dedicated `execution_preflight`
  deployment-truth receipt after the materialized safety gate and before later
  install phases continue.
- Blocked execution preflight state is persisted as a
  `FailedBeforeMutation` receipt and stops the install before later phases run.
- Deployment truth and execution preflight now share current-install input
  resolution for workspace root, ICP root, config path, and fleet name.

### Notes

- `DeploymentExecutionPreflightV1` remains passive readiness evidence. This
  patch does not add a public CLI command, controller mutation, or a new
  executor backend.

### Testing

- Added deployment-truth unit coverage proving check-based execution preflight
  matches the explicit plan/report/authority path.
- Added deployment-truth unit coverage for passive preflight text rendering.
- Added install-truth unit coverage for ready and blocked execution-preflight
  receipt paths, including blocked receipt persistence before returning an
  error.
- Extended current-install preflight coverage to prove the read-only execution
  preflight API does not write `.canic` state.

## 0.43.0 - 2026-05-24 - executor boundary scaffold

### Added

- Added the first backend-agnostic execution context model:
  `DeploymentExecutionContextV1`, `DeploymentExecutorBackendV1`, and
  `DeploymentExecutorCapabilityV1`.
- Added a minimal `DeploymentExecutor` trait and current-CLI capability helper
  so later installer extraction can reason about backend capabilities without
  rediscovering deployment intent.
- Added a concrete current-CLI executor wrapper and routed current-install
  execution context construction through that executor object.
- `DeploymentReceiptV1` can now carry optional execution context metadata,
  letting receipts record the backend, workspace root, ICP root, artifact
  roots, and supported executor capabilities without changing current install
  behavior.
- Added passive `DeploymentExecutionPreflightV1` readiness output over
  `DeploymentPlanV1`, `SafetyReportV1`, `AuthorityReconciliationPlanV1`, and
  executor capabilities, so 0.43 has a plan-shaped execution gate before
  backend operations run.

### Changed

- Current-install deployment truth receipts now attach current-CLI execution
  context metadata when they are written by the install flow. This records the
  workspace root, ICP root, build artifact roots, backend, and backend
  capabilities as receipt evidence without moving mutation behind the executor
  yet.
- Current install now checks that the selected backend advertises the
  capabilities required by the existing install phases before those phases
  begin mutating deployment state.

### Documentation

```bash
canic deploy plan <fleet>
canic deploy check <fleet>
canic deploy authority check <fleet>
```

### Notes

- This opening slice does not move current install mechanics behind the
  executor yet. It establishes the receipt/context vocabulary that later 0.43
  slices will use.
- The current CLI backend remains the only implemented backend.
- `DeploymentExecutionPreflightV1` is passive readiness evidence. It does not
  execute controller changes or replace deployment truth and authority checks.

### Testing

- Added deployment-truth unit coverage for current-CLI execution context JSON
  round-tripping and deterministic missing-capability reporting.
- Added deployment-truth unit coverage for ready and blocked execution
  preflight results using the same `DeploymentPlanV1` shape as deployment truth.
- Added install-truth unit coverage proving receipted current-install phases
  include current-CLI execution context metadata.
- Added install-truth unit coverage for current-install required capability
  reporting.
