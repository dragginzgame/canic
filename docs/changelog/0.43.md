# 0.43 Changelog

## 0.43.5 - 2026-05-24 - deployment receipt status hardening

### Added

- Added shared deployment receipt status classification for generic receipt
  construction.
- Added focused coverage for failed execution status classification:
  `FailedBeforeMutation`, `FailedAfterMutation`, and `PartiallyApplied`.
- Added resume-safety coverage proving receipts with contradictory execution
  status and role-phase evidence are blocked.

### Changed

- `deployment_receipt_from_check(...)` now derives failed execution status
  from the command result and role-phase evidence instead of treating every
  failed command as post-mutation.
- Failed receipts now classify as:
  - `FailedBeforeMutation` when no role-phase evidence shows applied work;
  - `FailedAfterMutation` when role evidence shows applied work but no failed
    role;
  - `PartiallyApplied` when role evidence shows both applied and failed role
    work.
- Receipt-aware resume checks now reject persisted receipts whose claimed
  `operation_status` contradicts the command result and role-phase evidence.

### Notes

- Current-install code can still supply an explicit status for phases whose
  mutation boundary is not fully represented by role-phase receipts yet.
- This is receipt semantics hardening only. It does not add a new executor
  backend, controller mutation, or a public execution-receipt JSON contract.

### Testing

- Added deployment-truth unit coverage for the shared execution-status
  classifier.
- Added deployment-truth unit coverage proving generic deployment receipt
  construction derives `PartiallyApplied` from mixed role outcomes.
- Added receipt-aware resume-safety coverage rejecting mismatched persisted
  execution status.
- Ran `cargo fmt --all --check`.
- Ran `cargo test -p canic-host execution_status_classifier --lib --offline`.
- Ran `cargo test -p canic-host deployment_receipt_from_check_derives_partial_status_from_role_receipts --lib --offline`.
- Ran `cargo test -p canic-host receipt_aware_diff_blocks_execution_status_mismatch --lib --offline`.
- Ran `cargo test -p canic-host deployment_truth --lib --offline`.
- Ran `cargo clippy -p canic-host --all-targets --offline -- -D warnings`.
- Ran `cargo test -p canic --test changelog_governance --locked`.
- Ran `git diff --check`.

## 0.43.4 - 2026-05-24 - artifact staging receipt evidence

### Added

- Added `ArtifactTransportV1` for typed artifact movement evidence across
  local CLI, `wasm_store`, and future direct-agent transports.
- Added `StagingReceiptV1` as the executor-facing artifact staging evidence
  shape, recording role, artifact identity, transport, optional
  `wasm_store` locator, prepared chunk hashes, published chunk count, and
  verified postcondition.
- Added `staging_receipt_evidence(...)` to convert typed staging receipts into
  compact phase evidence labels while current install still stores phase
  evidence in the existing deployment receipt envelope.

### Changed

- Current install now enriches the `stage_release_set` phase receipt with
  release-set manifest-derived staging evidence instead of only recording the
  root canister and manifest path.
- The `stage_release_set` evidence now includes release version, staged role,
  artifact identity, `wasm_store` transport, prepared/published chunk counts,
  `wasm_store` bootstrap locator, and observed staging postcondition.
- The 0.43 status handoff now records that typed staging receipts have landed
  as the first artifact-staging evidence slice.

### Notes

- This does not replace the current installer executor, change mutation
  behavior, or promote execution receipt JSON into a stable public automation
  contract.
- Typed staging receipts are the source shape for later executor work; the
  current install path maps them into existing phase evidence for this slice.

### Testing

- Added JSON-shape coverage for `StagingReceiptV1`.
- Added unit coverage proving staging receipt evidence preserves transport,
  chunk, locator, and postcondition facts.
- Added current-install coverage proving `stage_release_set` evidence is
  derived from the release-set manifest.
- Ran `cargo fmt --all --check`.
- Ran `cargo test -p canic-host staging_receipt --lib --offline`.
- Ran `cargo test -p canic-host current_install_staging_evidence --lib --offline`.
- Ran `cargo check -p canic-host --offline`.
- Ran `cargo test -p canic-host deployment_truth --lib --offline`.
- Ran `cargo clippy -p canic-host --all-targets --offline -- -D warnings`.
- Ran `cargo test -p canic --test changelog_governance --locked`.
- Ran `git diff --check`.

## 0.43.3 - 2026-05-24 - remove standalone canic-cdk crate

### Changed

- Removed the standalone `canic-cdk` workspace crate from the workspace,
  publish order, package verification scripts, README layout, workspace
  dependency declarations, and lockfile.
- Preserved the public `canic::cdk` import path by moving the remaining active
  CDK facade surface into `canic-core::cdk` and re-exporting it from `canic`.
- Re-exported `impl_storable_bounded!` and `impl_storable_unbounded!` from the
  `canic` facade now that the macro implementation lives in `canic-core`.
- Removed direct `canic-cdk` dependencies from `canic`, `canic-core`,
  `canic-control-plane`, and `canic-backup`.

### Removed

- Removed stale copied CDK surfaces that no active caller used:
  `cdk::env`, `cdk::spec::governance`, ICRC-1/ICRC-10 spec wrappers,
  `cdk::spec::system`, `cdk::types::wasm`, and `cdk::utils::wasm`.
- Removed the old static CDK token/catalog-style surface instead of carrying it
  forward into the new core-owned facade.

### Internal

- Moved ckUSDC/ckUSDT ledger principal constants beside the IC infra adapters
  that use them.
- Moved the NNS `get_subnet_for_canister` request/response DTOs beside the NNS
  registry infra adapter.
- Added a narrow local hash helper inside `canic-backup` so backup packaging no
  longer depends on a broad CDK support crate.

### Notes

- This is a package-boundary cleanup. It does not change the public
  `canic::cdk` facade path for ordinary downstream users.
- Active manifests, scripts, and source no longer reference `canic-cdk` or
  `canic_cdk`.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo check --workspace --offline`.
- Ran `cargo check -p canic-core --offline`.
- Ran `cargo check -p canic --offline`.
- Ran `cargo check -p canic-backup --offline`.
- Ran `cargo check -p canic-host --offline`.
- Ran `cargo clippy -p canic-core --all-targets --offline -- -D warnings`.
- Ran `cargo clippy -p canic-control-plane --all-targets --offline -- -D warnings`.
- Ran `cargo clippy -p canic-backup --all-targets --offline -- -D warnings`.
- Ran `cargo clippy -p canic-host --all-targets --offline -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets --offline -- -D warnings`.
- Ran `cargo test -p canic-core cdk --lib --offline`.
- Ran `cargo test -p canic-core --test stable_memory_abi_guard --locked`.
- Ran `cargo test -p canic --test workspace_manifest --locked`.
- Ran `cargo test -p canic --test changelog_governance --locked`.
- Ran `git diff --check`.

## 0.43.2 - 2026-05-24 - execution preflight evidence hardening

### Added

- Added `validate_deployment_execution_preflight(...)` for standalone
  `DeploymentExecutionPreflightV1` artifacts.
- Added `validate_deployment_execution_preflight_for_check(...)` for
  source-check-bound preflight artifacts, keeping passive executor readiness
  tied to the `DeploymentCheckV1` that produced it.
- Added host tests pinning the `DeploymentExecutionPreflightV1` JSON field
  names and enum strings before any public CLI surface is promoted.

### Changed

- Current-install preflight paths now validate generated execution-preflight
  evidence before returning read-only readiness or writing the
  `execution_preflight` receipt.
- Execution-preflight validation now rejects schema drift, blank provenance
  IDs, status/blocker mismatches, duplicate capability entries,
  missing-capability inconsistencies, and mixed check/preflight identity.

### Notes

- This remains passive executor-readiness evidence. It does not add a public
  CLI command, controller mutation, or a new executor backend.

### Testing

- Added deployment-truth unit coverage for valid check-derived preflight
  evidence.
- Added deployment-truth unit coverage rejecting mutated status, mismatched
  source-check identity, and inconsistent capability metadata.
- Added deployment-truth JSON shape coverage for
  `DeploymentExecutionPreflightV1`.
- Re-ran focused host execution-preflight, install-truth, clippy, changelog
  governance, and diff checks during the slice.

## 0.43.1 - 2026-05-24 - passive execution preflight integration

### Added

- Added `deployment_execution_preflight_from_check(...)`, which derives
  authority reconciliation from `DeploymentCheckV1` and emits the same passive
  `DeploymentExecutionPreflightV1` readiness model.
- Added `check_install_execution_preflight(...)`, a read-only host API for
  deriving current-install execution readiness from install inputs without
  writing receipts or entering the mutating install path.
- Added host-owned text rendering for `DeploymentExecutionPreflightV1`, keeping
  the executor-readiness summary in the deployment-truth presentation layer
  without adding a new CLI command.

### Changed

- Current install now writes a dedicated `execution_preflight`
  deployment-truth receipt after the materialized safety gate and before later
  install phases continue.
- Blocked execution preflight state is persisted as a
  `FailedBeforeMutation` receipt and stops the install before later phases run.
- Deployment truth and execution preflight now share current-install input
  resolution for workspace root, ICP root, config path, and fleet name.

### Notes

- `DeploymentExecutionPreflightV1` remains passive readiness evidence. This
  patch does not add a public CLI command, controller mutation, or a new
  executor backend.

### Testing

- Added deployment-truth unit coverage proving check-based execution preflight
  matches the explicit plan/report/authority path.
- Added deployment-truth unit coverage for passive preflight text rendering.
- Added install-truth unit coverage for ready and blocked execution-preflight
  receipt paths, including blocked receipt persistence before returning an
  error.
- Extended current-install preflight coverage to prove the read-only execution
  preflight API does not write `.canic` state.

## 0.43.0 - 2026-05-24 - executor boundary scaffold

### Added

- Added the first backend-agnostic execution context model:
  `DeploymentExecutionContextV1`, `DeploymentExecutorBackendV1`, and
  `DeploymentExecutorCapabilityV1`.
- Added a minimal `DeploymentExecutor` trait and current-CLI capability helper
  so later installer extraction can reason about backend capabilities without
  rediscovering deployment intent.
- Added a concrete current-CLI executor wrapper and routed current-install
  execution context construction through that executor object.
- `DeploymentReceiptV1` can now carry optional execution context metadata,
  letting receipts record the backend, workspace root, ICP root, artifact
  roots, and supported executor capabilities without changing current install
  behavior.
- Added passive `DeploymentExecutionPreflightV1` readiness output over
  `DeploymentPlanV1`, `SafetyReportV1`, `AuthorityReconciliationPlanV1`, and
  executor capabilities, so 0.43 has a plan-shaped execution gate before
  backend operations run.

### Changed

- Current-install deployment truth receipts now attach current-CLI execution
  context metadata when they are written by the install flow. This records the
  workspace root, ICP root, build artifact roots, backend, and backend
  capabilities as receipt evidence without moving mutation behind the executor
  yet.
- Current install now checks that the selected backend advertises the
  capabilities required by the existing install phases before those phases
  begin mutating deployment state.

### Documentation

```bash
canic deploy plan <fleet>
canic deploy check <fleet>
canic deploy authority check <fleet>
```

### Notes

- This opening slice does not move current install mechanics behind the
  executor yet. It establishes the receipt/context vocabulary that later 0.43
  slices will use.
- The current CLI backend remains the only implemented backend.
- `DeploymentExecutionPreflightV1` is passive readiness evidence. It does not
  execute controller changes or replace deployment truth and authority checks.

### Testing

- Added deployment-truth unit coverage for current-CLI execution context JSON
  round-tripping and deterministic missing-capability reporting.
- Added deployment-truth unit coverage for ready and blocked execution
  preflight results using the same `DeploymentPlanV1` shape as deployment truth.
- Added install-truth unit coverage proving receipted current-install phases
  include current-CLI execution context metadata.
- Added install-truth unit coverage for current-install required capability
  reporting.
