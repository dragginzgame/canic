# Canic 0.40

## 0.40.8 - 2026-05-20 - shared protected endpoint descriptors

### Added

- Added `canic_protected_endpoint!`, a facade macro for defining shared
  `ProtectedInternalEndpoint` descriptors in protocol modules. This covers the
  cross-canister client case where a caller crate should consume stable method
  and role metadata without depending on the target canister implementation
  crate that owns the endpoint macro expansion.
- `canic_protected_endpoint!` supports both single-role descriptors and
  multi-role descriptors, and can declare a small descriptor table in one macro
  invocation. Its role-selection behavior matches `canic_internal_client!`.
- Extended the internal-client macro integration test so one generated client
  consumes both endpoint-macro-generated descriptors and shared protocol-module
  descriptors.

### Changed

- The generated protected-client story now has two descriptor sources:
  endpoint implementation crates get
  `canic_internal_endpoint_<endpoint>()` from the endpoint macro, while shared
  protocol crates can define the same descriptor shape with
  `canic_protected_endpoint!`.
- `ProtectedInternalEndpoint::new(...)` now rejects empty method names, empty
  accepted-role sets, empty caller roles, and duplicate caller roles at
  construction time. The shared descriptor macro also rejects `roles = []` at
  compile time, so protected client metadata cannot describe an uncallable
  endpoint.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo test -p canic --test internal_client_macro`.
- Ran `cargo clippy -p canic --test internal_client_macro -- -D warnings`.
- Ran `cargo test -p canic-core api::ic::call --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.

## 0.40.7 - 2026-05-20 - typed internal client macro

### Added

- Added `canic_internal_client!`, a facade macro for generating typed protected
  internal update clients from `ProtectedInternalEndpoint` descriptor functions.
  The generated methods call through `CanicInternalClient`, so callers use the
  protected envelope/proof path by construction.
- `canic_internal_client!` supports both single-role descriptors and explicit
  `role = ...` method clauses for multi-role protected endpoints.
- Added `CanicInternalCallOptions` and `CanicInternalWaitMode`. Generated
  protected clients now expose `with_options(...)`, `with_bounded_wait()`,
  `with_unbounded_wait()`, `with_cycles(...)`, and
  `with_proof_ttl_secs(...)` so typed clients can configure transport behavior
  without dropping down to raw `CanicCall`.
- Added compile coverage proving the macro generates typed methods for
  single-argument, zero-argument, and explicit-role protected update calls.
  The same test defines a real protected `#[canic_update(...)]` endpoint and
  verifies that its generated `canic_internal_endpoint_<endpoint>()` descriptor
  is compatible with `canic_internal_client!`.

### Changed

- Protected endpoint macro expansion now emits descriptor accessors using the
  stable generated name `canic_internal_endpoint_<endpoint>()` instead of a
  hidden `__canic_internal_endpoint_<endpoint>()` implementation detail. Typed
  client generators can rely on that symbol shape.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo test -p canic --test internal_client_macro`.
- Ran `cargo clippy -p canic --test internal_client_macro -- -D warnings`.
- Ran `cargo test -p canic-core api::ic::call --lib`.
- Ran `cargo test -p canic-macros endpoint::expand --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.

## 0.40.6 - 2026-05-20 - generated protected endpoint descriptors

### Added

- Added `ProtectedInternalEndpoint` descriptors for protected Canic internal
  endpoints. Endpoint macro expansion now emits a descriptor function
  next to every `caller::has_role(...)` / `caller::has_any_role(...)` protected
  internal endpoint.
- Added a generic `CanicInternalClient` that consumes those descriptors and
  dispatches through `CanicCall`, so generated or hand-written typed clients can
  avoid repeating protected method names and accepted-role metadata by hand.
- Added `ProtectedInternalEndpoint::required_single_role()` for generated
  clients whose protected endpoint descriptor has exactly one accepted caller
  role.
- Added protocol-owned protected endpoint descriptors for the built-in
  wasm-store protected update methods.
- Re-exported `CanicInternalClient` and `ProtectedInternalEndpoint` through
  `canic::api::ic`, and included `CanicInternalClient` in the prelude.

### Changed

- Protected endpoint metadata now preserves the exported wire method name,
  including `name = "..."` overrides, so client descriptors bind the same method
  string used by wrapper envelope verification.
- `WasmStoreInternalClient` now calls protected updates through
  `CanicInternalClient` and protocol-owned `ProtectedInternalEndpoint`
  descriptors instead of constructing `CanicCall` directly. Single-role
  protected calls now derive the root role from descriptor metadata instead of
  repeating it at the call site.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic-core`.
- Ran `cargo check -p canic`.
- Ran `cargo test -p canic-macros endpoint::expand --lib`.
- Ran `cargo test -p canic-macros --lib`.
- Ran `cargo test -p canic-core api::ic::call --lib`.
- Ran `cargo test -p canic-control-plane typed_client_endpoint_table_matches_protocol_manifests --lib`.
- Ran `cargo test -p canic-control-plane protected_endpoint_table_uses_generated_descriptors --lib`.
- Ran `cargo test -p canic-control-plane workflow::runtime::template --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-control-plane --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.

## 0.40.5 - 2026-05-20 - remove AppIndex-only sibling predicate

### Removed

- Removed the transitional `caller::has_app_role(...)` macro predicate. It was
  verifier-local AppIndex authorization and is not part of the final 0.40
  protected internal RPC model.
- Removed the matching runtime access expression, evaluator branch, and
  `access::auth::has_app_role(...)` predicate.

### Changed

- `caller::has_app_role(...)` now fails macro parsing with an explicit 0.40
  hard-cut message directing protected internal endpoints to
  `caller::has_role(...)`.
- Updated the access architecture contract so sibling Canic RPC authorization is
  described in terms of root-signed `caller::has_role(...)` and
  `caller::has_any_role(...)`, not verifier-local AppIndex state.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic-core`.
- Ran `cargo check -p canic-macros`.
- Ran `cargo test -p canic-macros endpoint::parse --lib`.
- Ran `cargo test -p canic-macros endpoint::validate --lib`.
- Ran `cargo test -p canic-macros endpoint::expand --lib`.
- Ran `cargo test -p canic-macros --lib`.
- Ran `cargo test -p canic-core access:: --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.

## 0.40.4 - 2026-05-20 - wasm-store internal client

### Added

- Added a typed `WasmStoreInternalClient` for the root control-plane path. The
  client owns the built-in wasm-store method mapping, uses `CanicCall` for
  protected update methods, and keeps structural query exceptions on raw
  `CallOps`.
- Added an explicit endpoint table behind the wasm-store client, with coverage
  proving the typed methods exactly match the canonical protected-update and
  structural-query manifests.
- Added a private `RootAuthMaterialClient` for structural root auth material
  requests: delegation proofs, role attestations, method-scoped internal
  invocation proofs, and verifier key refreshes.
- Added an explicit endpoint table behind the root auth material client so root
  proof/key bootstrap endpoints stay classified together.
- Re-exported the wasm-store protected-update and structural-query manifests
  through the public `canic::protocol` facade for downstream tooling and tests.

### Changed

- Routed template-source resolution, publication prepare/chunk/stage calls, and
  store-local GC calls through the typed client instead of a generic
  `call_store_result(...)` helper spread through the publication workflow.
- Moved the borrowed chunk publish input into the client module so store
  publication code no longer needs to know the wire DTO shape for the protected
  chunk endpoint.
- Routed `AuthApi` remote root auth material requests and verifier key refresh
  through the private root auth material client instead of embedding root
  endpoint constants at each call site.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic-control-plane`.
- Ran `cargo check -p canic-core`.
- Ran `cargo test -p canic-control-plane typed_client_endpoint_table_matches_protocol_manifests --lib`.
- Ran `cargo test -p canic-core root_auth_material_client_endpoint_table_is_structural_bootstrap_only --lib`.
- Ran `cargo test -p canic --test protocol_surface public_protocol_reexports_wasm_store_protection_manifest`.
- Ran `cargo test -p canic-core api::auth --lib`.
- Ran `cargo test -p canic --test protocol_surface`.
- Ran `cargo test -p canic-control-plane workflow::runtime::template::tests::wasm_store_protected_method_classifier_matches_protocol_list --lib`.
- Ran `cargo test -p canic-control-plane workflow::runtime::template --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-control-plane --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.

## 0.40.3 - 2026-05-20 - protected call guardrails

### Added

- Added a canonical `canic-core::protocol` list for wasm-store update methods
  that require a protected internal invocation proof.
- Added a canonical `canic-core::protocol` list for wasm-store structural query
  exceptions while protected query endpoints remain out of scope.
- Added a first-party source guard test that rejects raw `Call`, `CallOps`, or
  `ic_cdk::call::Call` use with known protected wasm-store method names.
- The same guard now verifies that the wasm-store macro declarations and
  checked-in `wasm_store.did` agree with the protected-method and query-exception
  manifests.
- The macro and `.did` checks are bidirectional: extra protected envelope
  methods or structural query exceptions must be added to the manifest instead
  of appearing only in generated endpoint shape.
- Added a built-in internal endpoint classification manifest for the 0.40
  model. First-party macro-emitted internal endpoints must now be classified as
  protected internal RPC, structural bootstrap, structural query exception,
  existing capability RPC, discovery/standards, or operator/controller raw path.
- Added macro coverage that protected internal endpoint wrappers verify
  invocation proofs against the exported wire method name, including
  `name = "..."` overrides.

### Changed

- Routed the control-plane wasm-store method classifier through the canonical
  protected-method list instead of maintaining a local duplicate match.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo test -p canic-core --test protected_internal_call_guard`.
- Ran `cargo test -p canic-macros protected_internal_role_endpoint_verifies_exported_method_name --lib`.
- Ran `cargo test -p canic-control-plane workflow::runtime::template::tests::wasm_store_protected_method_classifier_matches_protocol_list --lib`.

## 0.40.2 - 2026-05-20 - protected updates and ic-memory runtime

### Added

- Migrated the local wasm-store update endpoints to the 0.40 protected
  internal-call ABI. The update methods now require a root-signed
  `caller::has_role("root")` internal invocation proof and export
  `CanicInternalCallEnvelopeV1` instead of their former raw argument tuples.
- Routed root control-plane wasm-store update calls through `CanicCall`, so
  prepare, manifest stage, chunk publish, chunk/info reads used by installation,
  and store-local GC updates obtain method-scoped root proofs.
- Aligned the direct root auth RPC decode path with the direct endpoint ABI for
  role attestations and internal invocation proofs. The local root workflow still
  returns the root capability response envelope, while remote direct endpoint
  calls now decode the signed proof payload they actually request.
- Refreshed the canonical `crates/canic-wasm-store/wasm_store.did` to reflect
  the envelope ABI for protected update methods and the new 0.40 auth error
  codes.
- Added wasm-store reconcile coverage for the hard-cut boundary: raw legacy
  update calls no longer dispatch against protected update methods.

### Boundary

- Wasm-store catalog/status query methods remain structural root-query
  exceptions in V1 because protected attested queries are intentionally out of
  scope for 0.40.
- The existing root capability RPC and bootstrap/debug root endpoints are not
  double-wrapped in this slice.
- Canic now consumes the generic `ic-memory 0.5.1` static range/declaration
  registry. `canic-core` and `canic-control-plane` declare their framework
  ranges through `ic-memory`, while Canic keeps only the eager TLS touch helper
  for framework storage wrappers.
- The compatibility `canic_core::ic_memory_key!` macro now registers
  declarations and opens memory through `ic-memory`, with a Canic bootstrap
  readiness guard kept in front so host tests and canister lifecycle paths
  still initialize memory before stable stores are touched.
- Removed the remaining Canic open wrapper/error mapping and the shadow
  `canic-tests` policy adapter, leaving Canic-specific namespace policy tests
  next to the production policy adapter.
- Split the host-side test harness boundary so published `canic-testkit` no
  longer depends on any Canic runtime crate. Generic PocketIC/artifact helpers
  stay in `canic-testkit`, while Canic-specific role/init/readiness helpers now
  live in unpublished `canic-testing-internal`.

### Testing

- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic-control-plane`.
- Ran `cargo check -p canic-wasm-store`.
- Ran `cargo test -p canic --test protocol_surface`.
- Ran `cargo test -p canic-tests --test root_wasm_store_reconcile`.
- Ran `cargo test -p canic-tests --test root_wasm_store_reconcile root_raw_wasm_store_update_call_is_rejected_before_dispatch`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-control-plane --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-wasm-store --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-tests --test root_wasm_store_reconcile -- -D warnings`.
- Ran `cargo check --workspace`.
- Ran `git diff --check`.
- Ran `cargo check -p canic-core -p canic-control-plane -p canic`.
- Ran `cargo check -p canic-testkit`.
- Ran `cargo tree -p canic-testkit -e normal --depth 2`.
- Ran `cargo test -p canic-testkit`.
- Ran `cargo check -p canic-testing-internal`.
- Ran `cargo check -p canic-tests --tests`.
- Ran `cargo clippy -p canic-testkit --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-testing-internal --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-tests --tests -- -D warnings`.
- Ran `cargo test -p canic-core --lib -- --nocapture`.
- Ran `cargo test -p canic-control-plane --lib -- --nocapture`.
- Ran `cargo test -p canic-core --test stable_memory_abi_guard -- --nocapture`.
- Ran `cargo test -p canic-core memory::policy -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic-control-plane -p canic -p canic-tests --all-targets -- -D warnings`.

## 0.40.1 - 2026-05-20 - outgoing internal-proof cache

### Added

- Added a heap-only outgoing cache for `CanicCall` method-scoped internal
  invocation proofs. The cache is keyed by the exact root, configured
  attestation key name, subject canister, caller role, subnet, audience
  canister, exported method, and requested TTL.
- Cached proofs are reused only while they still bind to the requested call
  edge, remain outside the refresh margin, and meet the local minimum accepted
  role epoch floor. Stale or mismatched entries are evicted and the caller
  requests a fresh root-signed proof.
- Added coarse internal-call auth error codes for retry classification:
  `AuthMaterialStale`, `AuthKeyUnknown`, `AuthProofExpired`, and
  `InternalRpcMalformed`.
- `CanicCall` now inspects protected endpoint `Result` responses, invalidates
  the cached proof, requests fresh proof material, and retries once when the
  callee reports stale role-epoch material or an unknown attestation key.

### Boundary

- The cache is an optimization only. Callee-side wrapper verification remains
  authoritative for signature, caller, role, audience, method, subnet, TTL, and
  epoch checks.
- Retry is intentionally narrow. `CanicCall` does not retry malformed
  envelopes, expired proofs, authorization failures, role/method/audience
  mismatches, or domain handler errors.

### Testing

- Ran `cargo test -p canic-core api::ic::call --lib`.

## 0.40.0 - 2026-05-19 - internal invocation proof ABI

### ➕ Added

- Added passive Candid DTOs for the 0.40 protected internal-call wire ABI:
  `CanicInternalCallEnvelopeV1`, `CanicInternalCallHeaderV1`,
  `InternalInvocationProofRequest`, `InternalInvocationProofPayloadV1`, and
  `SignedInternalInvocationProofV1`.
- Added a separate internal invocation proof signing domain,
  `CANIC_INTERNAL_INVOCATION_PROOF_V1`, so method-scoped invocation proofs
  cannot be confused with generic role attestations.
- Added hash coverage proving method-scoped internal invocation proofs use a
  distinct signing domain from existing role attestations.
- Added root capability issuance for `InternalInvocationProofRequest`, including
  replay binding, root capability metrics, direct root endpoint routing, and
  signed `InternalInvocationProofPayloadV1` responses.
- Added verifier-side internal invocation proof validation that checks
  signature material with the internal proof domain plus caller, audience,
  exported method, accepted role, subnet, TTL, and epoch bindings.
- Added the `caller::has_role(...)` and `caller::has_any_role([...])` endpoint
  macro predicates for 0.40 protected update endpoints. The generated wrapper
  now exports the `CanicInternalCallEnvelopeV1` ABI, verifies the method-scoped
  proof before dispatch, and decodes the original Candid arguments only after
  proof validation succeeds.
- Protected internal wrappers reject mixed access predicates in this slice, so
  an attested-role endpoint cannot silently drop an extra `requires(...)`
  condition while switching to the envelope verifier path.
- Added the low-level `CanicCall` public API. It Candid-encodes the original
  endpoint arguments, requests a method-scoped internal invocation proof from
  root for the configured caller role, builds a `CanicInternalCallEnvelopeV1`,
  and dispatches through the raw call layer. `Call` remains unchanged for
  external services.

### 📝 Boundary

- This slice only establishes the 0.40 protocol data model and signing domain.
  Existing internal endpoints are not mechanically migrated; only endpoints that
  opt into `caller::has_role(...)` use the protected envelope wrapper path.
- Application handlers keep their natural Rust signatures. `CanicCall` is the
  first low-level caller primitive; generated clients, outgoing proof caching,
  and retry-on-stale-material behavior are still staged follow-up work.
- Root internal-proof issuance authorizes the requesting subject through either
  Root AppIndex role ownership or the subnet registry role. This deliberately
  avoids the old `caller::is_registered_to_subnet()` prefilter so AppIndex
  canisters can request method-scoped proofs.
- Root chooses the signed proof epoch from its current role-attestation epoch
  floor instead of accepting a caller-provided epoch in the internal-proof
  request.

### 🧪 Testing

- Ran `cargo test -p canic-core ops::auth::crypto::tests::internal_invocation_proofs_use_distinct_signing_domain`.
- Ran `cargo test -p canic-core workflow::rpc::request::handler --lib`.
- Ran `cargo test -p canic --test protocol_surface`.
- Ran `cargo test -p canic-core ops::auth --lib`.
- Ran `cargo test -p canic-macros endpoint --lib`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core api::ic::call --lib`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo check --workspace`.
