# Canic 0.38

## 0.38.4 - 2026-05-17 - Managed runtime ABI guard coverage

### 🔧 Changed

- Extended the stable-memory ABI source guard from the initial framework crates
  to the full Canic-managed runtime surface, including the facade, CDK bridge,
  macro crate, core runtime, control plane, and canonical `wasm_store` crate.
- Refreshed the `canic-memory` runtime-selected slot guidance so declaration,
  bootstrap validation, and post-bootstrap memory opening are shown as separate
  phases.

### 🧪 Testing

- Ran `cargo test -p canic-core --test stable_memory_abi_guard --
  --nocapture`.
- Ran `cargo fmt --all --check`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.

## 0.38.3 - 2026-05-17 - Default ABI ledger recovery endpoint

### 🔧 Changed

- Moved `canic_memory_ledger` into the default Canic runtime endpoint bundles
  so the minimal controller-only ABI ledger recovery diagnostic is available
  on ordinary Canic-managed canisters, including the canonical `wasm_store`
  surface.
- Kept `canic_memory_registry` opt-in through
  `canic_emit_memory_observability_endpoints!()` because it is a live
  post-bootstrap registry diagnostic, not the fail-closed recovery path.
- Split the memory endpoint emitters so the raw ledger diagnostic can be
  bundled by default without duplicating the opt-in registry diagnostic used by
  test/probe canisters.
- Updated the checked-in `wasm_store.did` surface to include
  `canic_memory_ledger` while still excluding `canic_memory_registry`.

### 🧪 Testing

- Updated protocol-surface coverage so `wasm_store` must expose
  `canic_memory_ledger` and must not expose `canic_memory_registry`.
- Added source-level protocol coverage that the default shared and `wasm_store`
  runtime bundles emit the ledger diagnostic, while the opt-in memory
  observability macro emits only the live registry diagnostic.
- Added a Candid parser guard for the checked-in canonical `wasm_store.did` so
  the manual ABI-surface update cannot leave an invalid interface file behind.
- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic -p canic-tests --tests`.
- Ran `cargo test -p canic --test protocol_surface -- --nocapture`.
- Ran `cargo check -p canic --target wasm32-unknown-unknown`.
- Ran `cargo check -p canic-wasm-store --target wasm32-unknown-unknown`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-wasm-store --all-targets -- -D warnings`.

## 0.38.2 - 2026-05-17 - ABI ledger recovery diagnostics

### 🔧 Changed

- Added a controller-only `canic_memory_ledger` diagnostic query for canisters
  that opt into `canic_emit_memory_observability_endpoints!()`.
- The new ledger diagnostic bypasses normal Canic endpoint dispatch and reads
  the committed ID `0` ABI ledger through the restricted diagnostic path, so
  operators can inspect ledger state even when ordinary runtime bootstrap
  blocks endpoint dispatch.
- Added a dedicated `MemoryLedgerResponse` DTO that exposes ledger header
  fields, the authoritative committed generation, committed range authorities,
  historical owner ranges, and memory ID records separately from the live
  runtime registry view.
- Documented `canic_memory_ledger` as the concrete recovery diagnostic endpoint
  in the 0.38 stable-memory ABI design.

### 🧪 Testing

- Extended root diagnostic coverage so controllers can read the ABI ledger
  self-record and non-controllers are denied.
- Added protocol-surface coverage that the default `wasm_store` canister still
  does not expose memory diagnostics.
- Ran `cargo check -p canic-core -p canic -p canic-tests --tests`.
- Ran `cargo check -p canic-memory -p canic-core -p canic -p canic-tests
  --tests`.
- Ran `cargo test -p canic-memory ledger_snapshot -- --nocapture`.
- Ran `cargo test -p canic-memory diagnostic_snapshot -- --nocapture`.
- Ran `cargo test -p canic-memory
  declare_with_key_metadata_records_schema_metadata -- --nocapture`.
- Ran `cargo test -p canic --test protocol_surface
  memory_ledger_diagnostic_bypasses_normal_dispatch -- --nocapture`.
- Ran `cargo test -p canic --test protocol_surface -- --nocapture`.
- Ran `cargo test -p canic-tests --test root_suite hierarchy --
  --test-threads=1 --nocapture`.
- Ran `cargo clippy -p canic-core -p canic -p canic-tests --all-targets --
  -D warnings`.
- Ran `cargo clippy -p canic-memory -p canic-core -p canic --all-targets --
  -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic --target wasm32-unknown-unknown`.
- Ran `cargo test -p canic-core -p canic`.
- Ran `git diff --check`.

## 0.38.1 - 2026-05-17 - Stable memory schema metadata

### 🔧 Changed

- Added optional `schema_version` and `schema_fingerprint` metadata to
  explicit stable-memory declarations, registered-memory diagnostics, and the
  core memory registry DTO.
- Added `MemoryApi::declare_with_key_metadata(...)` for startup declaration
  snapshots that want schema metadata while keeping ordinary
  `declare_with_key(...)` declarations defaulted to no schema metadata.
- Stores schema metadata in the ABI ledger as diagnostic declaration metadata,
  not allocation identity: changes for the same stable key and memory ID are
  preserved as declaration history and do not create allocation conflicts.
- Records the canonical `0-99` Canic framework and `100-254` application
  allocation authority ranges as ledger metadata, separate from normal owner
  range reservations, and exposes them through ledger diagnostics.
- Tightens ledger physical-header validation so invalid magic, physical format,
  schema version, header length, or committed slot values fail closed instead
  of being repaired during bootstrap.
- Adds raw stable-memory preflight before declaration-snapshot mutation so
  brand-new memory can initialize a genesis ledger, but foreign or corrupt raw
  memory and existing `MemoryManager` state without a valid ID `0` Canic ABI
  ledger fail closed.
- Makes the wasm `MemoryApi::ledger_snapshot()` diagnostic path decode only the
  ID `0` ABI ledger from raw stable memory instead of depending on normal
  runtime registry reconstruction.
- Validates schema metadata before bootstrap mutation: present schema versions
  must be greater than zero, and present fingerprints must be non-empty ASCII
  strings without control characters and no longer than 256 bytes.

### 🧪 Testing

- Added `canic-memory` coverage for schema metadata declaration, ledger export,
  schema metadata drift history, and invalid schema metadata rejection.
- Added ledger coverage for canonical range authority records and fail-closed
  rejection of corrupted authority metadata.
- Added ledger coverage for invalid physical-header fields.
- Added manager and ledger coverage for raw stable-memory classification,
  existing-ledger bootstrap preflight, missing ID `0` ledgers, non-ledger ID
  `0` cells, and corrupt ledger-cell payloads.
- Added ledger coverage for restricted diagnostic snapshot decoding from an
  existing ID `0` stable-cell payload.
- Ran `cargo check -p canic-memory --target wasm32-unknown-unknown`.
- Ran `cargo test -p canic-memory`.
- Ran `cargo test -p canic-core -p canic-control-plane`.
- Ran `cargo clippy -p canic-memory --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-core -p canic-control-plane --all-targets -- -D warnings`.

## 0.38.0 - 2026-05-17 - Stable memory ABI hard cut

### ⚠️ Breaking

- Treats stable-memory allocation as durable ABI: the stable key, not owner
  crate, Rust type, or local label, is the persistent memory identity.
- Reserves the Canic framework range as `0-99`, reserves downstream
  application memory as `100-254`, and makes ID `255` permanently invalid as
  the unallocated-bucket sentinel.
- Makes ID `0` the canonical `canic.memory.abi_ledger.v1` ledger self-record.
  IDs `1-99` are framework expansion budget unless later allocated by explicit
  Canic framework declarations.

### 🔧 Changed

- Widened `canic-core`'s bootstrap range to `11-99` while keeping
  `canic-control-plane` in the `5-10` planning subrange.
- Added current runtime declaration-snapshot validation so duplicate memory
  IDs, duplicate stable keys, and exact duplicate declarations fail before user
  ledger records are committed during bootstrap.
- Added historical-ledger preflight for pending bootstrap declarations so a
  later stable-key or ID conflict cannot leave earlier user claims written into
  the ledger during a failed bootstrap.
- Reworked the persisted layout ledger into a generation-framed store with two
  committed slots, generation checksums, header metadata, and highest-valid
  generation selection so corrupted newer slots are ignored.
- Made ledger mutation, validation, and diagnostic snapshot reads fail closed
  when no committed generation validates.
- Tightened stable-key namespace enforcement so non-Canic crates cannot claim
  `canic.*` keys even if they choose IDs inside the framework range.
- Split startup declaration from memory opening in the public `MemoryApi`:
  `declare_with_key(...)` queues metadata before bootstrap, while
  `register_with_key(...)` only opens already-validated slots after bootstrap.
- Split `ic_memory_key!` macro declaration from opening: constructor-time
  declaration descriptors are collected before registry validation, while
  eager stable-store initialization opens virtual memory only after bootstrap
  publishes the validated runtime registry.
- Made macro-backed stable-memory opening enforce bootstrap readiness on host
  and wasm targets, backed by a host-test bootstrap hook and process-wide
  registry initialized state so tests cannot bypass the validation-before-open
  rule.
- Added a `MemoryApi::ledger_snapshot()` diagnostic read path for persisted
  ABI ledger history that does not depend on current runtime registry
  reconstruction.
- Updated `canic-memory` guidance to document the `0-99`/`100-254` split and
  the corrected ID `255` sentinel semantics.

### 🧪 Testing

- Added `canic-memory` coverage for duplicate current declarations, duplicate
  stable keys, exact duplicate declarations, the ID `0` self-record, ID `255`
  rejection, non-Canic attempts to claim the `canic.*` namespace, declaration
  before opening, opening before bootstrap rejection, late declaration rejection
  after bootstrap, and failed bootstrap behavior that leaves non-conflicting
  sibling declarations out of historical ledger state.
- Added host-test bootstrap hooks for `canic-core` and `canic-control-plane`
  so unit tests validate the stable-memory registry before first macro-backed
  store access instead of opening handles directly.
- Added direct ledger recovery tests for highest-valid-generation selection and
  corrupted-newer-slot recovery, plus fail-closed behavior when no committed
  slot validates.
- Added a framework source guard that rejects implicit `ic_memory!` and
  `MemoryApi::register(...)` use in framework-owned crates.
- Extended the framework source guard to reject direct raw stable-memory APIs,
  independent `MemoryManager` access, and `RestrictedMemory` carve-outs in
  framework-owned crates.
- Ran `cargo test -p canic-memory`.
- Ran `cargo test -p canic-core -p canic-control-plane`.
