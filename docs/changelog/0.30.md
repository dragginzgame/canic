## 0.30.35 - 2026-05-05 - Restore Runner State Markers

### 📝 Summary

- `0.30.35` lets `canic restore run` accept, echo, and require `--updated-at <text>` markers on runner summaries and receipts so native runner transitions can carry operator-supplied comparable state markers instead of always using `unknown`.

### 🔧 Changed

- Added `--updated-at <text>` to `canic restore run` so `--execute` can stamp pending, completed, and failed journal states with the caller-provided marker.
- Reused the same marker for `--unclaim-pending` recovery so recovered operations and pending-recovered receipts carry the operator-supplied state marker.
- Added `requested_state_updated_at` to `canic restore run` output when a caller supplies `--updated-at <text>`, keeping the requested marker visible even when no operation receipt is emitted.
- Added `--require-receipt-updated-at <text>` so automation can fail closed unless every emitted runner receipt carries the expected marker.
- Added `--require-state-updated-at <text>` so automation can fail closed on the echoed `requested_state_updated_at` marker even when no receipts are emitted.
- Kept the existing default of `unknown` when no marker is supplied, preserving existing runner behavior.

### 📚 Documentation

- Documented `restore run --updated-at <text>`, `requested_state_updated_at`, `--require-receipt-updated-at <text>`, and `--require-state-updated-at <text>` in [crates/canic-cli/README.md](../../crates/canic-cli/README.md), including the expected comparable timestamp-style usage.
- Updated top-level and restore-specific usage text so the new runner marker flag is visible from help output.

### 🧪 Testing

- Added restore-runner parsing coverage for `--updated-at <text>`.
- Added restore-runner dry-run, execution, and recovery coverage proving supplied state markers are echoed in runner output and written into journal state and operation receipts.
- Added restore-runner fail-closed coverage for receipt marker mismatches.
- Added restore-runner fail-closed coverage for response-level marker mismatches on receipt-free dry-runs.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-updated-at cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-usage cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-updated-at-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-marker-gate cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-usage-marker-gate cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-marker-gate-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-marker-echo cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-usage-marker-echo cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-marker-echo-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-state-marker-gate cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-usage-state-marker-gate cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-run-state-marker-gate-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-package cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0335-package2 cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.

---

## 0.30.34 - 2026-05-05 - Restore Runner Receipts and Pending Guards

### 📝 Summary

- `0.30.34` adds restore pending-work summaries, runner operation receipts/summaries, and fail-closed progress/stale-pending/receipt gates so automation can require claimed-work freshness and execution audit events without recomputing counters.

### 🔧 Changed

- Added `--require-remaining-count <n>`, `--require-attention-count <n>`, and `--require-completion-basis-points <n>` to `canic restore apply-status`, `canic restore apply-report`, and `canic restore run`.
- Added `pending_summary` to restore apply status, reports, and runner output so automation can see the claimed operation sequence, operation kind, update marker, and whether that marker is known.
- Added `operation_receipts` to `canic restore run` output so command completion, command failure, and pending recovery events share one compact audit stream.
- Added `operation_receipt_summary` to `canic restore run` output so scripts can read total, completed-command, failed-command, and recovered-pending receipt counts without walking the detailed audit stream.
- Added `operation_receipt_count` and `--require-receipt-count <n>` to `canic restore run` so CI can fail closed when the runner emits more or fewer audit events than expected.
- Added `--require-completed-receipt-count <n>`, `--require-failed-receipt-count <n>`, and `--require-recovered-receipt-count <n>` so restore automation can fail closed on the specific receipt class it expects.
- Added `--require-no-pending-before <text>` to `canic restore apply-status`, `canic restore apply-report`, and `canic restore run` so scripts can fail closed when pending work is older than a comparable cutoff marker or has no known marker.

### 📚 Documentation

- Documented pending summaries, runner receipts/summaries, progress gates, stale-pending gates, and receipt-kind gates in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Updated top-level `canic` usage text so restore apply and runner guard flags are visible from the root help path.

### 🧹 Cleanup

- Added a restore CLI test fixture for journal-backed commands so new fail-closed status, report, and runner tests share the same setup, output reading, and temp-directory cleanup path.

### 🧪 Testing

- Added CLI coverage proving progress gates write status, reports, or runner summaries before failing on mismatched expectations.
- Added restore apply status/report/runner coverage for pending summaries and stale-pending gates.
- Added restore runner coverage for command-completed, command-failed, pending-recovered operation receipts, receipt summaries, total receipt-count gating, and receipt-kind gating.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-progress-gates cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-pending-summary cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-pending-summary cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-test-fixture cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipts cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-gate cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-summary cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-kind-gates cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-usage cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-progress-gates-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-pending-summary-clippy cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-pending-summary-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-test-fixture-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipts-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-gate-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-summary-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-receipt-kind-gates-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.

---

## 0.30.33 - 2026-05-05 - Restore Apply Progress Summaries

### 📝 Summary

- `0.30.33` adds restore apply progress summaries to status, report, and runner output so automation can read remaining, transitionable, attention-needed, and integer completion progress without recomputing counters.

### 🔧 Changed

- Added `progress` to restore apply status and reports with total, completed, remaining, transitionable, attention-needed, and completion basis-point counts.
- Added the same `progress` summary to `canic restore run` output because runner summaries are built from apply reports.
- Kept progress values integer-only by using `completion_basis_points`, avoiding floating-point JSON drift in scripts.

### 📚 Documentation

- Documented restore apply progress summaries in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).

### 🧪 Testing

- Added restore apply status/report coverage for progress summary counts.
- Added restore runner dry-run coverage for progress summary JSON.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-progress cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-progress cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-backup cargo test -p canic-cli preflight -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-progress-clippy cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0333-cli-progress-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.

---

## 0.30.32 - 2026-05-05 - Journal Operation Count Receipts

### 📝 Summary

- `0.30.32` persists restore apply journal operation-kind counts and validates supplied counts against concrete journal operations, while keeping older journals readable.

### 🔧 Changed

- Added `operation_counts` to restore apply journals so the persisted journal carries the same operation-kind receipt shown by dry-runs, status, reports, and runner summaries.
- Added `operation_counts_supplied` to restore apply status, reports, and runner summaries so automation can tell persisted receipts from recomputed older-journal counts.
- Refreshed journal `operation_counts` whenever an operation changes state, keeping resumable journals internally consistent after claim, unclaim, completion, or failure transitions.
- Hardened apply journal validation so supplied operation-kind counts must match concrete operation rows.
- Kept compatibility for older journals that do not include `operation_counts`; missing counts decode as empty and are recomputed for status/report output.

### 📚 Documentation

- Documented that restore apply journals persist the same operation-kind counts shown by dry-run output and that status/report output flags whether the journal supplied them.

### 🧪 Testing

- Added restore apply journal coverage for persisted operation-kind counts.
- Added restore apply status, report, and runner coverage for the `operation_counts_supplied` flag.
- Added validation coverage for operation-kind count mismatches.
- Added compatibility coverage for older restore apply journals that lack `operation_counts`.
- Ran `cargo fmt --all --check`.
- Ran `cargo check -p canic-core -p canic-macros -p canic`.
- Ran `cargo test -p canic-macros -- --nocapture`.
- Ran `cargo check -p canister_minimal -p canister_app -p canister_root -p canister_playground`.
- Ran `cargo test -p canic --test reference_surface -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic-macros -p canic --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0332-journal-counts cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0332-cli-journal-counts cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0332-cli-backup cargo test -p canic-cli preflight -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0332-journal-counts-clippy cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0332-cli-journal-counts-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.

---

## 0.30.31 - 2026-05-05 - Concrete Member Verification Checks

### 📝 Summary

- `0.30.31` makes restore planning expand role-level member verification checks into concrete member operations, honors verification role filters before dry-runs or runner previews are generated, and carries operation-kind counts through dry-runs, apply journals, and runner summaries.

### 🔧 Changed

- Changed restore planning so `verification.member_checks` entries are copied onto every matching `RestorePlanMember` instead of only being counted in `verification_summary`.
- Changed member verification role filters so checks with `roles = [...]` only apply to matching member roles; empty role filters still apply broadly.
- Changed restore verification summaries to count the concrete per-member checks that will actually render as apply operations.
- Hardened restore apply journal validation so upload, load, and verification operations must carry the fields required to render their runner commands.
- Added explicit planned snapshot upload counts alongside load counts in restore plans, status output, apply dry-runs, and backup preflight summaries.
- Added explicit total planned operation counts in restore plans, status output, apply dry-runs, and backup preflight summaries.
- Added apply operation-kind counts so dry-runs, status, reports, and runner summaries show uploads, loads, reinstalls, member verifications, fleet verifications, and total verification operations.
- Derived upload and total operation counts from older restore-plan JSON when those newer fields are missing.

### 📚 Documentation

- Documented concrete member verification expansion and role-filter behavior in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Documented that restore operation summaries now distinguish planned snapshot uploads from loads and include total planned operations.
- Documented that restore apply dry-runs, reports, and runner summaries include operation-kind counts for operator inspection.

### 🧪 Testing

- Added restore planner coverage for role-level member check expansion and role-filtered check exclusion.
- Added restore apply journal coverage for missing operation-specific runner fields.
- Added restore apply dry-run, status, report, and runner coverage for operation-kind count output.
- Added restore compatibility coverage for older restore-plan JSON that lacks upload and total operation counts.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0331-kind-counts cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0331-cli-kind-counts cargo test -p canic-cli restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0331-cli-backup cargo test -p canic-cli preflight -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0331-kind-counts-clippy cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0331-cli-kind-counts-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.30 - 2026-05-05 - Restore Fleet Verification Operations

### 📝 Summary

- `0.30.30` makes restore apply dry-runs render declared fleet-level verification checks as final `verify-fleet` operations, keeping restore readiness counts, dry-run output, and runner command previews aligned.

### ➕ Added

- Added `fleet_verification_checks` to restore plans so fleet-level verification declarations are preserved after planning instead of only being counted in summaries.
- Added `verify-fleet` restore apply operations that run after member upload, load, reinstall, and member verification work in the final restore phase.
- Added command previews for fleet verification checks, using `dfx canister status` for status checks and `dfx canister call <target-root> <method>` for method checks.

### 🔧 Changed

- Changed restore apply dry-runs so `rendered_operations` now includes fleet-level verification checks when the manifest declares them.
- Kept older restore-plan JSON readable by defaulting missing `fleet_verification_checks` to an empty list.
- Kept fleet verification operations non-mutating and targeted at the restored fleet root canister.

### 📚 Documentation

- Documented restore-plan fleet verification checks and final `verify-fleet` apply operations in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).

### 🧪 Testing

- Ran `CARGO_TARGET_DIR=/tmp/canic-0330-restore-fleet-verify cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0330-cli-restore cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0330-backup-clippy cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0330-cli-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `rustfmt --edition 2024 --check crates/canic-backup/src/restore/mod.rs`.

---

## 0.30.29 - 2026-05-05 - Restore Runner Constants, Ingress Limits, And Local Playground

### 📝 Summary

- `0.30.29` centralizes native restore-runner mode, stopped-reason, next-action, and executed-operation state strings behind named constants while preserving the existing JSON output shape, adds generated ingress payload limits for `canic_update` endpoints, and adds a local playground canister for quick manual experiments.

### ➕ Added

- Added a default `16 KiB` update ingress limit for Canic-managed canisters.
- Added `#[canic_update(payload(max_bytes = ...))]` for explicit per-endpoint payload limits.
- Added a generated `inspect_message` hook through `start!`, `start_root!`, and `start_wasm_store!` so canisters do not have to hardcode a new endpoint name.
- Added ctor-backed update endpoint metadata registration so exported method names, including `name = "..."` overrides, are used for payload-limit lookup.
- Added `canic::CANIC_DEFAULT_UPDATE_INGRESS_MAX_BYTES` for consumers that need to reference the framework default.
- Added a PocketIC probe canister and integration test for default, explicit, and exported-name payload limits.
- Added `canic::start_local!()` for dev-only standalone canisters that need a synthesized local CANIC environment instead of hand-written init args.
- Added `canisters/playground` as a manual local sandbox that uses `start_local!()` and stays out of the reference release set and test fixtures.

### 🔧 Changed

- Replaced scattered restore-runner string literals with private constants for `run_mode`, `stopped_reason`, `next_action`, executed-operation states, and runner command failure reasons.
- Added small constructors for restore-runner response modes and executed-operation rows so mode flags and row states are assembled through one path.
- Added a shared `RestoreRunResponseMode` constructor so each mode uses one internal flag assembly path.
- Moved shared restore-run response construction onto `RestoreRunResponse` and centralized the response version constant.
- Centralized runner command-preview configuration and max-step stop checks so dry-run and execute paths share the same helper logic.
- Shared command-preview configuration and command-unavailable error construction between the native runner and `restore apply-command`.
- Kept the serialized restore-runner response values unchanged so existing scripts and CI gates continue to match the same strings.

```rust
#[canic_update(payload(max_bytes = 32 * 1024))]
fn import(payload: String) -> Result<usize, Error> {
    Ok(payload.len())
}
```

### 🧪 Testing

- Ran `cargo test -p canic-macros -- --nocapture`.
- Ran `cargo test -p canic-core ingress::payload -- --nocapture`.
- Ran `cargo check -p canic -p canister_minimal -p canister_app -p canister_root`.
- Ran `cargo test -p canic-tests --test pic_ingress_payload_limits -- --nocapture`.
- Ran `cargo check -p payload_limit_probe -p canic-tests`.
- Ran `cargo clippy -p payload_limit_probe -p canic-tests --test pic_ingress_payload_limits -- -D warnings`.
- Ran `cargo clippy -p canic-macros -p canic-core -p canic --all-targets -- -D warnings`.
- Ran `cargo check -p canic -p canister_playground`.
- Ran `cargo clippy -p canic -p canister_playground --all-targets -- -D warnings`.
- Ran `cargo check -p canister_playground --target wasm32-unknown-unknown`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0329-restore-runner-strings cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0329-apply-command cargo test -p canic-cli apply_command -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0329-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `rustfmt --edition 2024 --check crates/canic-cli/src/restore/mod.rs`.

---

## 0.30.28 - 2026-05-05 - Typed Restore Runner Summaries

### 📝 Summary

- `0.30.28` starts runner cleanup by moving native restore runner summaries from ad hoc JSON assembly to typed response structs, adds explicit runner mode, state, action, and count gates for automation, and reduces the shell restore runner to a mode-aware compatibility wrapper.

### 🔧 Changed

- Added a typed `RestoreRunResponse` for `canic restore run` output so dry-run, execute, recovery, and failure summaries share one schema-backed response path.
- Added `run_mode` to `canic restore run` summaries so scripts can branch on one stable mode field instead of inferring mode from compatibility booleans.
- Added `--require-run-mode <text>`, `--require-stopped-reason <text>`, `--require-next-action <text>`, and `--require-executed-count <n>` to `canic restore run` so CI can write the run summary and then fail when the runner stops in an unexpected state.
- Added typed executed-operation rows for native runner summaries instead of constructing those rows with loose JSON values.
- Changed runner requirement enforcement to read typed summary fields directly instead of reparsing internally generated JSON.
- Updated top-level CLI usage text so the native runner gates are visible from `canic --help`.
- Changed `scripts/restore/apply_journal.sh` to delegate to `canic restore run`, keeping older output flags accepted while removing the duplicate bash claim/mark loop.
- Added `--dry-run`, `--execute`, and `--unclaim-pending` pass-through modes to `scripts/restore/apply_journal.sh`; the script still defaults to execute for existing callers.
- Added pass-through support for the native runner mode, stopped-reason, next-action, and executed-count gates to `scripts/restore/apply_journal.sh`.
- Added a direct `serde` dependency to `canic-cli` for the typed summary serialization path.

```bash
canic restore run \
  --journal restore-apply-journal.json \
  --execute \
  --network local \
  --max-steps 1 \
  --out restore-run.json \
  --require-run-mode execute \
  --require-stopped-reason max-steps-reached \
  --require-next-action rerun \
  --require-executed-count 1 \
  --require-no-attention
```

### 🧪 Testing

- Ran `CARGO_TARGET_DIR=/tmp/canic-0328-typed-runner cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0328-usage cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0328-cli-full cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0328-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `bash -n scripts/restore/apply_journal.sh`.
- Ran `scripts/restore/apply_journal.sh --help`.
- Ran `scripts/restore/apply_journal.sh --journal fake.json --dry-run --execute`.
- Ran `scripts/restore/apply_journal.sh --journal fake.json --dry-run --canic /bin/echo --require-run-mode dry-run --require-stopped-reason preview --require-next-action rerun --require-executed-count 0`.
- Ran `git diff --check`.

---

## 0.30.27 - 2026-05-05 - Restore Runner Entry Points

### 📝 Summary

- `0.30.27` moves guarded restore apply journal execution into the `canic` binary, keeps native dry-run previews, adds pending-operation recovery, writes operator-friendly run summaries, adds CI-friendly run gates, and keeps the shell runner as a compatibility wrapper.

### ➕ Added

- Added `canic restore run --journal <file> --dry-run`, which previews the native runner path by reporting apply status, attention state, the next transition, and the next generated `dfx` command without mutating the journal.
- Added `canic restore run --journal <file> --execute`, which checks readiness, claims the next sequence, runs the generated `dfx` command, marks success or failure, and persists the journal after each transition.
- Added `canic restore run --journal <file> --unclaim-pending`, which recovers an interrupted runner by moving the current pending operation back to ready and writing a recovery summary.
- Added `stopped_reason` and `next_action` to native runner summaries so operators and CI can tell whether to rerun, inspect a failed operation, unclaim pending work, fix blocked inputs, or stop.
- Added `--max-steps <n>` for `canic restore run` so operators can execute cautious incremental restore batches.
- Added `--require-complete` and `--require-no-attention` for `canic restore run` so CI can write the run summary and then fail closed when the journal is incomplete or still needs review.
- Changed command failure handling so `canic restore run --execute --out <file>` writes the run summary and failed journal state before returning the command-failed error.
- Added `scripts/restore/apply_journal.sh`, which wraps guarded status checks, command preview, sequence-checked claim, `dfx` execution, completion/failure marking, and final apply reporting.

```bash
canic restore run \
  --journal restore-apply-journal.json \
  --execute \
  --network local \
  --max-steps 1 \
  --out restore-run.json \
  --require-no-attention
```

### 📚 Documentation

- Documented the native runner dry-run, execute mode, summary action fields, pending-operation recovery, run gates, and maintained runner script in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).

### 🧪 Testing

- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-cli cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-run-exec cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-run-gates cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-run-failure-summary cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-run-recovery cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-restore-run-summary-fields cargo test -p canic-cli restore_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-cli-full6 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0327-clippy6 cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `bash -n scripts/restore/apply_journal.sh`.
- Ran `scripts/restore/apply_journal.sh --help`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.26 - 2026-05-05 - Restore Apply Reports

### 📝 Summary

- `0.30.26` adds a restore apply report command and fail-closed attention gate so operators and CI can inspect restore runner progress without parsing the full journal.

### ➕ Added

- Added `RestoreApplyJournal::report` plus `RestoreApplyJournalReport`, `RestoreApplyReportOutcome`, and compact operation report rows for pending, failed, and blocked work.
- Added `canic restore apply-report --journal <file> [--out <file>]`, which validates the journal and writes a focused outcome report.
- Added `canic restore apply-report --require-no-attention`, which writes the report and then exits non-zero when pending, failed, or blocked work needs review.

```bash
canic restore apply-report \
  --journal restore-apply-journal.json \
  --out restore-apply-report.json \
  --require-no-attention
```

### 📚 Documentation

- Documented restore apply reports in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).

### 🧪 Testing

- Added CLI coverage proving `apply-report` writes a failed/pending attention summary from a valid journal.
- Added CLI coverage proving `apply-report --require-no-attention` writes the report before failing on pending work.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0326-restore-cli2 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0326-cli-full2 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0326-clippy2 cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.25 - 2026-05-05 - Restore Apply Command Guard

### 📝 Summary

- `0.30.25` adds fail-closed restore runner guards for scripts that need ready work, a real next `dfx` command, matching claim/unclaim sequences, and a claimed operation before marking progress.

### ➕ Added

- Added `canic restore apply-status --require-ready`, which writes status JSON and then exits non-zero if restore work is still blocked.
- Added `canic restore apply-command --require-command`, which writes the preview JSON and then exits non-zero if no executable command is available.
- Added `canic restore apply-claim --sequence <n>`, which refuses to claim work if the next transitionable operation is no longer the operation a runner previewed.
- Added `canic restore apply-unclaim --sequence <n>`, which refuses to release pending work if the pending operation no longer matches the operation a recovery script inspected.
- Added `canic restore apply-mark --require-pending`, which refuses to mark an operation completed or failed unless it was claimed first.
- Added a typed CLI error for unready restore apply journals so automation can surface blocked reasons directly.
- Added a typed CLI error for unavailable restore runner commands so automation can distinguish missing commands from journal parsing failures.
- Added a typed CLI error for stale `apply-claim` sequence attempts so parallel or resumed runners can detect journal movement.
- Added a typed CLI error for stale `apply-unclaim` sequence attempts so recovery scripts do not release different pending work.
- Added a typed CLI error for unclaimed `apply-mark` attempts so runners can distinguish claim-order mistakes from journal validation failures.

```bash
canic restore apply-status \
  --journal restore-apply-journal.json \
  --out restore-apply-status.json \
  --require-ready \
  --require-no-pending \
  --require-no-failed
```

```bash
canic restore apply-command \
  --journal restore-apply-journal.json \
  --network local \
  --out restore-apply-command.json \
  --require-command
```

```bash
canic restore apply-claim \
  --journal restore-apply-journal.json \
  --sequence 0 \
  --out restore-apply-journal.json
```

```bash
canic restore apply-unclaim \
  --journal restore-apply-journal.json \
  --sequence 0 \
  --out restore-apply-journal.json
```

```bash
canic restore apply-mark \
  --journal restore-apply-journal.json \
  --sequence 0 \
  --state completed \
  --out restore-apply-journal.json \
  --require-pending
```

### 🔧 Changed

- Restore runner examples now guard readiness before command rendering, guard `apply-command` before claim/execute/mark steps, claim the previewed sequence, document sequence-checked unclaim recovery, and guard `apply-mark` after claiming work.

### 📚 Documentation

- Documented `--require-ready`, `--require-command`, `apply-claim --sequence`, `apply-unclaim --sequence`, `--require-pending`, and the updated external restore runner loop in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).

### 🧪 Testing

- Added CLI coverage proving `apply-status --require-ready` writes status before failing when restore work is blocked.
- Added CLI coverage proving `apply-command --require-command` writes a preview before failing when the journal is complete.
- Added CLI coverage proving `apply-claim --sequence` rejects stale preview sequence numbers.
- Added CLI coverage proving `apply-unclaim --sequence` rejects stale pending sequence numbers.
- Added CLI coverage proving `apply-mark --require-pending` rejects unclaimed ready operations.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0325-restore-cli5 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0325-cli-full5 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0325-clippy5 cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.24 - 2026-05-04 - Restore Apply Claim State

### 📝 Summary

- `0.30.24` adds no-execute restore apply claim and unclaim steps so external runners can mark the next operation `pending` before running a `dfx` command and release it back to `ready` if execution never started.
- Pending operations now remain the next transitionable operation until they are marked completed or failed, which makes interrupted restore runners easier to resume from the journal alone.

### ➕ Added

- Added `RestoreApplyJournal::mark_next_operation_pending`, `RestoreApplyJournal::mark_operation_pending`, `RestoreApplyJournal::mark_next_operation_ready`, `RestoreApplyJournal::mark_operation_ready`, and `RestoreApplyJournal::next_transition_operation` for runner claim/resume state.
- Added `canic restore apply-claim --journal <file> [--out <file>]`, which validates the journal and marks the next transitionable operation pending without executing restore mutations.
- Added `canic restore apply-unclaim --journal <file> [--out <file>]`, which releases the current pending operation back to ready when a runner stopped before executing it.
- Added `canic restore apply-status --require-no-pending`, which writes status and then fails closed when a journal already has claimed work.
- Added `canic restore apply-status --require-no-failed`, which writes status and then fails closed when a journal has failed operations.
- Added `canic restore apply-status --require-complete`, which writes status and then fails closed until all apply operations are completed.
- Added `state_updated_at` metadata on restore apply journal operations, plus `--updated-at <text>` on `apply-claim`, `apply-unclaim`, and `apply-mark`, so runners can record when they changed local journal state.
- Added transition metadata to `RestoreApplyJournalStatus` so automation can distinguish the next ready operation from the next ready-or-pending operation and see its latest update marker.

```bash
canic restore apply-claim \
  --journal restore-apply-journal.json \
  --updated-at 2026-05-04T12:00:00Z \
  --out restore-apply-journal.json
```

```bash
canic restore apply-unclaim \
  --journal restore-apply-journal.json \
  --updated-at 2026-05-04T12:01:00Z \
  --out restore-apply-journal.json
```

```bash
canic restore apply-status \
  --journal restore-apply-journal.json \
  --out restore-apply-status.json \
  --require-no-pending \
  --require-no-failed \
  --require-complete
```

### 🔧 Changed

- `canic restore apply-next` and `canic restore apply-command` now return the earliest pending operation before later ready operations.
- Pending claims are idempotent for the same operation but still reject attempts to claim, unclaim, or complete later operations first.

### 📚 Documentation

- Documented `canic restore apply-claim`, `canic restore apply-unclaim`, and pending-operation resume behavior in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Added a copyable external restore runner loop example showing status guards, command preview, claim, command execution, and completion marking.

### 🧪 Testing

- Added backup coverage for pending claims, unclaim recovery, no-pending unclaim rejection, transition status metadata, state update marker validation, pending next-operation output, command previews for pending work, and out-of-order pending rejection.
- Added CLI coverage for `apply-claim`, `apply-unclaim`, `apply-mark` update markers, `apply-status --require-no-pending`, `apply-status --require-no-failed`, and `apply-status --require-complete`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-restore-backup3 cargo test -p canic-backup apply_journal -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-restore-cli6 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-backup-full3 cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-cli-full6 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-clippy6 cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0324-doc-cli cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.23 - 2026-05-04 - Ordered Restore Apply Advancement

### 📝 Summary

- `0.30.23` makes restore apply journal advancement ordered and adds no-execute command previews so external restore runners cannot skip ahead of the next ready operation and can see the `dfx` command they are about to run.
- `0.30.23` also adds `ManagementCall` metrics so operators can identify which management-canister operation is failing without adding target canister, snapshot, module, or chunk identifiers as labels.

### ➕ Added

- Added `RestoreApplyJournal::next_command_preview`, `RestoreApplyJournal::next_command_preview_with_config`, `RestoreApplyCommandConfig`, `RestoreApplyCommandPreview`, and `RestoreApplyRunnerCommand` for no-execute runner command previews.
- Added `canic restore apply-command --journal <file> [--dfx <path>] [--network <name>] [--out <file>]`, which renders the next ready operation as a configured `dfx` command preview with mutation and stopped-canister flags.
- Added `ManagementCall` metrics with fixed `operation`, `outcome`, and `reason` labels for management-canister call outcome visibility.

```bash
canic restore apply-command \
  --journal restore-apply-journal.json \
  --network local \
  --out restore-apply-command.json
```

### 🔧 Changed

- `RestoreApplyJournal::mark_operation_completed` and `RestoreApplyJournal::mark_operation_failed` now reject out-of-order transitions when an earlier ready or pending operation still needs to advance.
- Existing completed and failed operations remain idempotent to re-mark, so retrying the same journal update is still safe.
- `canic restore apply-mark` now fails closed before writing an output journal when the requested sequence is not the next transitionable operation.

### 📚 Documentation

- Documented `canic restore apply-command` and that `canic restore apply-mark` refuses to skip earlier ready operations in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Documented the `ManagementCall` metric family in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added backup coverage for upload, load, reinstall, status verification, method verification, and unsupported verification command previews, including configured `dfx`, network, mutation, and stopped-canister flags.
- Added backup coverage for out-of-order restore apply journal transition rejection.
- Added CLI coverage for `apply-command` parsing and command-preview writes.
- Added CLI coverage proving `apply-mark` rejects out-of-order operation updates without writing an output journal.
- Added metrics coverage for `ManagementCall` row projection, public facade access, pagination sorting, reset coverage, and metrics documentation coverage.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0323-restore-backup5 cargo test -p canic-backup apply_journal -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0323-restore-cli4 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0323-backup-full4 cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0323-cli-full4 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0323-clippy4 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-test cargo test -p canic-core management_call -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-query cargo test -p canic-core page_sorts_new_multi_label_metric_families_before_paginating -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-docs cargo test -p canic-core metrics_docs_cover_all_metric_families -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-reset cargo test -p canic-core reset_for_tests_clears_all_metric_families -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-management-metrics-allfeatures cargo clippy -p canic-core --all-targets --all-features -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.22 - 2026-05-04 - Restore Apply Journal Resume State

### 📝 Summary

- `0.30.22` adds deterministic restore apply journal state transitions and next-operation output so external restore runners can resume from a validated operation journal instead of maintaining separate progress files.
- `0.30.22` also tightens metrics documentation and facade coverage so every metric family stays visible and documented as the metrics surface grows.

### ➕ Added

- Added `RestoreApplyJournal::mark_operation_completed` and `RestoreApplyJournal::mark_operation_failed` so completed and failed operation states refresh journal counts through one validated path.
- Added `RestoreApplyJournal::next_ready_operation`, `RestoreApplyJournal::next_operation`, and `RestoreApplyNextOperation` so runners can fetch the full next ready operation row.
- Added transition errors for missing operations, invalid state changes, and failed operations without a reason.
- Added `canic restore apply-next --journal <file> [--out <file>]` for no-mutation next-operation output.
- Added `canic restore apply-mark --journal <file> --sequence <n> --state completed|failed [--reason <text>] [--out <file>]` for no-mutation journal updates.

```bash
canic restore apply-next \
  --journal restore-apply-journal.json \
  --out restore-apply-next.json
```

```bash
canic restore apply-mark \
  --journal restore-apply-journal.json \
  --sequence 0 \
  --state completed \
  --out restore-apply-journal.json
```

### 📚 Documentation

- Documented the apply journal next-operation and marking workflow in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Updated top-level and restore-specific CLI usage text with `canic restore apply-next` and `canic restore apply-mark`.
- Added missing `InterCanisterCall`, `RootCapability`, and `System` family details to [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added backup coverage for completed journal transitions, failed journal transitions with reasons, and blocked-operation transition rejection.
- Added backup coverage for full next-operation output and blocked next-operation output.
- Added CLI coverage for `apply-next` parsing and next-operation writes.
- Added CLI coverage for `apply-mark` parsing, completed-operation journal writes, and failed-operation reason enforcement.
- Added metrics audit coverage proving every public `MetricsKind` has a table row and family detail section in [docs/metrics.md](../metrics.md).
- Expanded public facade coverage so every metrics family can be queried through `canic::api::metrics::MetricsQuery`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0322-restore-backup3 cargo test -p canic-backup apply_journal -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0322-restore-cli2 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0322-backup-full2 cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0322-cli-full2 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0322-clippy4 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-test cargo test -p canic-core metrics_docs_cover_all_metric_families -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-metrics-audit-allfeatures cargo clippy -p canic-core --all-targets --all-features -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.21 - 2026-05-04 - Restore Apply Journal

### 📝 Summary

- `0.30.21` adds a no-mutation restore apply journal and status summary so dry-runs can emit and inspect operation states before real upload, load, reinstall, or verification execution is enabled.
- `0.30.21` also adds provisioning metrics so operators can see create, install, propagation, and upgrade workflow progress without adding canister principal, module hash, chunk hash, or parent-principal cardinality.

### ➕ Added

- Added `RestoreApplyJournal`, `RestoreApplyJournalOperation`, and `RestoreApplyOperationState` for the initial restore apply operation journal.
- Added `RestoreApplyJournal::from_dry_run`, which marks operations `ready` only when the plan is restore-ready and artifact validation is present and complete.
- Added `RestoreApplyJournal::validate` and `RestoreApplyJournalError` so status reporting rejects malformed journals before summarizing them.
- Added `RestoreApplyJournalStatus` and `RestoreApplyJournal::status` so automation can inspect journal readiness, operation counts, and the next ready operation without re-reading the plan.
- Added `canic restore apply --journal-out <file>` so operators can write the initial apply journal next to the dry-run operation plan.
- Added `canic restore apply-status --journal <file> [--out <file>]` for compact apply journal status output.
- Added `MetricsKind::Provisioning` rows with `[operation, role, outcome, reason]` labels for workflow-level create, install, propagation, and upgrade visibility.

```bash
canic restore apply \
  --plan restore-plan.json \
  --status restore-status.json \
  --backup-dir backups/<run-id> \
  --dry-run \
  --out restore-apply-dry-run.json \
  --journal-out restore-apply-journal.json
```

```bash
canic restore apply-status \
  --journal restore-apply-journal.json \
  --out restore-apply-status.json
```

### 🧪 Testing

- Added restore apply journal coverage for validated ready operations and blocked operations when artifact validation is missing.
- Added CLI coverage proving `--journal-out` writes operation state counts and per-operation states.
- Added restore apply journal validation coverage for count mismatches and duplicate operation sequences.
- Added restore apply status coverage for next ready operation reporting.
- Added CLI coverage proving `apply-status` reads a valid journal, writes compact status JSON, and rejects inconsistent journal counts.
- Added provisioning metric coverage for stable `[operation, role, outcome, reason]` labels, count accumulation, sorted pagination, and public facade access.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0321-restore-backup4 cargo test -p canic-backup apply_journal -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0321-restore-cli4 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0321-backup-full3 cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0321-cli-full3 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0321-clippy3 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-test cargo test -p canic-core provisioning_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-provisioning-metrics-allfeatures cargo clippy -p canic-core --all-targets --all-features -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.20 - 2026-05-04 - Restore Artifact Validation

### 📝 Summary

- `0.30.20` lets restore apply dry-runs bind to a backup directory and fail before execution if planned snapshot artifacts are missing, outside the backup root, or checksum-invalid.
- `0.30.20` also adds intent metrics so operators can see call, pool, and cleanup reservation lifecycle outcomes without adding resource-key or intent-id cardinality.
- `0.30.20` also adds platform-call metrics so operators can see generic, management, ledger, ECDSA, HTTP, and XRC call outcomes without adding target, method, URL, ledger, key, or asset cardinality.

### ➕ Added

- Added `RestoreApplyArtifactValidation` and `RestoreApplyArtifactCheck` so apply dry-run output can include artifact existence and checksum verification results.
- Added `RestoreApplyDryRun::try_from_plan_with_artifacts`, which validates every planned artifact path under a caller-provided backup root.
- Added `canic restore apply --backup-dir <dir> --dry-run`, which validates restore artifact inputs without running DFX or mutating canisters.
- Added `MetricsKind::Intent` rows with `[surface, operation, outcome, reason]` labels for call, pool, and cleanup reservation visibility.
- Added `MetricsKind::PlatformCall` rows with `[surface, mode, outcome, reason]` labels for low-cardinality platform-call outcome visibility.

```bash
canic restore apply \
  --plan restore-plan.json \
  --status restore-status.json \
  --backup-dir backups/<run-id> \
  --dry-run \
  --out restore-apply-dry-run.json
```

### 📚 Documentation

- Documented the intent metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the platform-call metric row shape and fixed label set in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added restore apply dry-run coverage for successful artifact validation, missing artifacts, and backup-root path traversal rejection.
- Added CLI coverage proving `--backup-dir` produces artifact validation details in the dry-run JSON.
- Added intent metric coverage for stable `[surface, operation, outcome, reason]` labels, count accumulation, and sorted pagination.
- Added public facade coverage for querying the new `Intent` metric family through `canic::api::metrics::MetricsQuery`.
- Added platform-call metric coverage for stable `[surface, mode, outcome, reason]` labels, count accumulation, and sorted pagination.
- Added public facade coverage for querying the new `PlatformCall` metric family through `canic::api::metrics::MetricsQuery`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0320-restore-backup2 cargo test -p canic-backup apply_dry_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0320-restore-cli2 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0320-backup-full cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0320-cli-full cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0320-clippy2 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `cargo fmt --all`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-intent-metrics-test cargo test -p canic-core intent_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-intent-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-intent-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-intent-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-intent-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-platformcall-metrics-test cargo test -p canic-core platform_call_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-platformcall-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-platformcall-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-platformcall-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-platformcall-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.19 - 2026-05-04 - Restore Apply Dry Run

### 📝 Summary

- `0.30.19` adds a no-mutation restore apply dry-run so operators can inspect the ordered restore operations before any DFX upload, load, reinstall, or verification command is enabled.
- `0.30.19` also adds first-class auth metrics so operators can see session bootstrap, session lifecycle, identity fallback, and attestation verifier outcomes without filtering synthetic `Access` rows.
- `0.30.19` also adds replay metrics so operators can see root capability replay checks, reservations, cached decode, commits, and aborts without adding caller or request-id cardinality.

### ➕ Added

- Added `RestoreApplyDryRun`, `RestoreApplyDryRunPhase`, `RestoreApplyDryRunOperation`, and `RestoreApplyOperationKind` for the structured dry-run operation plan.
- Added `canic restore apply --plan <file> [--status <file>] --dry-run [--out <file>]`, which reads a restore plan plus optional restore status and writes ordered operations without mutating targets.
- Added fail-closed restore apply validation so a supplied restore status must match the plan backup ID, source identity, topology hash, member count, and phase count.
- Added `MetricsKind::Auth` rows with `[surface, operation, outcome, reason]` labels for session bootstrap, session lifecycle, identity fallback, and attestation verifier visibility.
- Added `MetricsKind::Replay` rows with `[operation, outcome, reason]` labels for root capability replay checks, reservation, cached decode, commit, and abort visibility.

```bash
canic restore apply \
  --plan restore-plan.json \
  --status restore-status.json \
  --dry-run \
  --out restore-apply-dry-run.json
```

### 📚 Documentation

- Documented `canic restore apply --dry-run` in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Aligned top-level `canic help` usage with the dry-run apply command.
- Documented the auth metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the replay metric row shape and fixed label set in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added restore apply dry-run coverage for ordered upload, load, reinstall, and member verification operations.
- Added CLI coverage requiring `--dry-run` before restore apply can run.
- Added backup and CLI coverage rejecting restore apply dry-runs when the supplied status file does not match the plan.
- Added auth metric coverage for stable `[surface, operation, outcome, reason]` labels and count accumulation.
- Added metrics query coverage showing auth rows are sorted before pagination.
- Added public facade coverage for querying the new `Auth` metric family through `canic::api::metrics::MetricsQuery`.
- Added replay metric coverage for stable `[operation, outcome, reason]` labels, count accumulation, and sorted pagination.
- Added public facade coverage for querying the new `Replay` metric family through `canic::api::metrics::MetricsQuery`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0319-apply-backup4 cargo test -p canic-backup apply_dry_run -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0319-apply-cli2 cargo test -p canic-cli restore_apply -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0319-backup-full2 cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0319-cli-full2 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0319-clippy6 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-auth-metrics-test cargo test -p canic-core auth_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-auth-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-auth-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-auth-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-auth-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-metrics-test cargo test -p canic-core replay_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-workflow-test cargo test -p canic-core replay -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-metrics-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-replay-metrics-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.18 - 2026-05-04 - Restore Readiness Gate

### 📝 Summary

- `0.30.18` adds restore-readiness gates to backup preflight and restore planning, plus initial restore status output, so automation can write the standard report, plan, and status artifacts before restore execution.
- `0.30.18` also adds feature-gated sharding metrics so sharding-enabled canisters can see shard assignment decisions, shard creation, key assignment, and startup shard bootstrap progress through `canic_metrics`.
- `0.30.18` also records low-level runtime canister snapshot and restore management calls through the existing `CanisterOps` metric family.
- `0.30.18` also adds delegated-auth token verification outcome metrics so operators can see started, completed, and typed failed verification attempts without adding caller or token subject cardinality.

### ➕ Added

- Added `canic backup preflight --require-restore-ready`, which checks the generated `restore_ready` flag after writing `manifest-validation.json`, `backup-status.json`, `backup-inspection.json`, `backup-provenance.json`, `backup-integrity.json`, `restore-plan.json`, and `preflight-summary.json`.
- Added `canic restore plan --require-restore-ready`, which checks the generated `readiness_summary.ready` flag after writing the restore plan.
- Added `canic restore status --plan <file> [--out <file>]`, which reads a restore plan and emits the initial no-mutation restore status with every member marked `planned`.
- Added `restore-status.json` to `canic backup preflight` bundles and `restore_status_path` to `preflight-summary.json`.
- Added `RestoreStatus`, `RestoreStatusPhase`, `RestoreStatusMember`, and `RestoreMemberState` so restore execution can build on a stable status artifact.
- Added structured `RestoreNotReady` CLI errors that include stable restore readiness reason strings from the generated preflight summary or restore plan.

```bash
canic restore plan \
  --backup-dir backups/<run-id> \
  --mapping restore-map.json \
  --out restore-plan.json \
  --require-verified \
  --require-restore-ready
```

```bash
canic restore status \
  --plan restore-plan.json \
  --out restore-status.json
```

- Added `MetricsKind::Sharding` rows with `[operation, outcome, reason]` labels behind the `sharding` feature for shard placement visibility.
- Added sharding counters for assignment, plan decisions, shard creation, key assignment, empty-active bootstrap, and configured startup shard bootstrap.
- Added fixed sharding reasons for already-assigned keys, existing shard capacity, creation allowed, pool capacity, no free slots, disabled sharding, satisfied startup targets, invalid state, management calls, and policy denials.
- Added core management-canister wrappers for `take_canister_snapshot` and `load_canister_snapshot`.
- Added `CanisterOps` snapshot and restore emissions with `unscoped` role labels for low-level management calls that do not know the configured role.
- Added `DelegatedAuth` outcome rows with `[operation, outcome, reason]` labels for delegated-token verification progress and bounded failure reasons.

### 📚 Documentation

- Documented `--require-restore-ready` for backup preflight and restore planning in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Documented `canic restore status` and the preflight `restore-status.json` artifact in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Aligned top-level `canic help` usage with the new restore-readiness flags.
- Documented the sharding metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the low-level `CanisterOps` snapshot and restore emissions in [docs/metrics.md](../metrics.md).
- Documented the delegated-auth authority and outcome row shapes in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added backup preflight coverage showing `--require-restore-ready` writes `preflight-summary.json` before returning the restore-readiness error.
- Added restore planning coverage showing `--require-restore-ready` writes `restore-plan.json` before returning the restore-readiness error.
- Added positive backup preflight and restore planning coverage showing `--require-restore-ready` accepts fully populated restore metadata.
- Added restore status coverage for plan-derived status fields and per-member `planned` states.
- Added backup preflight coverage for `restore-status.json` and `restore_status_path`.
- Added top-level CLI usage coverage for the restore-readiness flags.
- Added sharding metric coverage for stable `[operation, outcome, reason]` labels and count accumulation.
- Added metrics query coverage showing sharding rows are sorted before pagination.
- Added public facade coverage for querying the feature-gated `Sharding` metric family through `canic::api::metrics::MetricsQuery`.
- Added canister operation metric coverage for unscoped snapshot and restore rows.
- Added delegated-auth metric coverage for stable authority rows plus verification started, completed, and failed outcome rows.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-preflight cargo test -p canic-cli backup_preflight_ -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-restore cargo test -p canic-cli restore::tests -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-cli-usage cargo test -p canic-cli usage_lists_restore_readiness_gates -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-ready-positive cargo test -p canic-cli require_restore_ready -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-restore-status cargo test -p canic-backup restore_status_starts_all_members_as_planned -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-restore-status-cli cargo test -p canic-cli restore_status -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-preflight-status cargo test -p canic-cli backup_preflight_ -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-backup-full cargo test -p canic-backup -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-cli-full4 cargo test -p canic-cli -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0318-clippy7 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-default-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-feature-check cargo check -p canic-core --features sharding`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-core-test2 cargo test -p canic-core --features sharding sharding_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-query-test2 cargo test -p canic-core --features sharding page_sorts_sharding_metric_family -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-facade-test cargo test -p canic --features sharding --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-clippy2 cargo clippy -p canic-core -p canic --features sharding --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-sharding-default-clippy cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-snapshot-metrics-check2 cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-snapshot-metrics-test2 cargo test -p canic-core canister_ops_metrics_accumulate -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-snapshot-metrics-clippy cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-delegated-auth-metrics-test cargo test -p canic-core delegated_auth -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-delegated-auth-metrics-check cargo check -p canic-core`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-delegated-auth-metrics-clippy cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-delegated-auth-metrics-alltest cargo test -p canic-core metrics -- --nocapture`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

---

## 0.30.17 - 2026-05-04 - Restore Readiness And Runtime Metrics

### 📝 Summary

- `0.30.17` makes restore dry-run, backup preflight, and snapshot journal output expose explicit mapping, journal operation metrics, restore operation, snapshot provenance, readiness, and reason fields so automation can gate restore workflows without inferring readiness from per-member details.
- `0.30.17` also adds cascade metrics so operators can see state/topology propagation fanout, local apply, route resolution, child-send failures, empty snapshots, no-route leaves, and partial fanout failures through `canic_metrics`.
- `0.30.17` also adds pool metrics so operators can see reusable-canister creation, import, recycle, reset, ready selection, and reset scheduler decisions through `canic_metrics`.
- `0.30.17` also adds scaling metrics so operators can see worker creation policy decisions, startup warmup progress, worker creation calls, and registry updates through `canic_metrics`.
- `0.30.17` also adds directory metrics so operators can see keyed placement resolution, claims, create/finalize work, stale repair, cleanup, recovery, and direct binding through `canic_metrics`.

### ➕ Added

- Added `RestoreSnapshotSummary` with module hash, wasm hash, code version, and checksum coverage counts plus all-member readiness booleans.
- Added `RestoreReadinessSummary` with a single restore readiness flag and stable reason strings.
- Added `mapping_supplied` and `all_sources_mapped` to `RestoreIdentitySummary`.
- Added restore snapshot provenance counts and readiness booleans to `preflight-summary.json`.
- Added `restore_ready` and `restore_readiness_reasons` to `preflight-summary.json`.
- Added `restore_mapping_supplied` and `restore_all_sources_mapped` to `preflight-summary.json`.
- Added `journal_operation_metrics` to `preflight-summary.json`.
- Added snapshot download journal `operation_metrics` counters for target count, snapshot create, snapshot download, checksum verification, and artifact finalization progress.
- Added `RestoreOperationSummary` with planned snapshot load, code reinstall, verification check, and phase counts.
- Added restore operation counts to `preflight-summary.json`.
- Added `verification_required` and `all_members_have_checks` to `RestoreVerificationSummary`.
- Added `restore_verification_required` and `restore_all_members_have_checks` to `preflight-summary.json`.
- Added `MetricsKind::Cascade` rows with `[operation, snapshot, outcome, reason]` labels for state/topology propagation visibility.
- Added cascade counters for root fanout, non-root fanout, local apply, route resolution, and child sends.
- Added fixed cascade reasons for empty snapshots, no-route leaves, partial fanout failures, send failures, invalid state, management calls, and policy denials.
- Added `MetricsKind::Pool` rows with `[operation, outcome, reason]` labels for reusable-canister pool visibility.
- Added pool counters for empty-canister creation, immediate import, queued import, recycle, reset, ready selection, and scheduler lifecycle decisions.
- Added fixed pool reasons for empty selection, concurrent scheduler runs, failed-entry requeues, already-present skips, local non-importability, registered-subnet rejection, missing entries, invalid state, management calls, and policy denials.
- Added `MetricsKind::Scaling` rows with `[operation, outcome, reason]` labels for scaling policy planning and worker lifecycle visibility.
- Added scaling counters for configured startup warmup, per-pool warmup, create planning, worker creation, and worker registry updates.
- Added fixed scaling reasons for below-minimum spawning, max-worker skips, within-bounds skips, disabled scaling, satisfied startup targets, missing worker plans, invalid state, management calls, and policy denials.
- Added `MetricsKind::Directory` rows with `[operation, outcome, reason]` labels for keyed directory placement visibility.
- Added directory counters for resolve, recover, bind, classify, claim, create-instance, finalize, stale repair, stale cleanup, and abandoned-child recycle paths.
- Added fixed directory reasons for bound entries, fresh/current pending entries, claimed keys, lost claims, missing entries, released stale claims, repairable stale provisional children, cleanup-required stale entries, invalid child targets, registry misses, role mismatches, invalid state, management calls, and policy denials.

### 📚 Documentation

- Documented the snapshot journal operation metrics plus preflight journal metrics, restore operation, provenance, readiness reason, and verification readiness fields in [crates/canic-cli/README.md](../../crates/canic-cli/README.md).
- Documented the cascade metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the pool metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the scaling metric row shape and fixed label set in [docs/metrics.md](../metrics.md).
- Documented the directory metric row shape and fixed label set in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added restore planning coverage for the new snapshot provenance summary fields and readiness booleans.
- Added restore planning coverage for the new readiness summary and reason strings.
- Added restore planning coverage for explicit mapping summary fields.
- Added preflight summary coverage for the new restore snapshot provenance fields, readiness booleans, and reason strings.
- Added preflight summary coverage for explicit restore mapping fields.
- Added preflight summary coverage for journal operation metrics.
- Added snapshot download journal coverage for operation metric counters.
- Added restore planning coverage for the new operation summary fields.
- Added preflight summary coverage for restore operation counts.
- Added restore planning coverage for the new verification readiness fields.
- Added preflight summary coverage for the new restore verification readiness fields.
- Added cascade metric coverage for stable `[operation, snapshot, outcome, reason]` labels and count accumulation.
- Added public facade coverage for querying the new `Cascade` metric family through `canic::api::metrics::MetricsQuery`.
- Added metrics query coverage showing cascade rows are sorted before pagination.
- Added pool metric coverage for stable `[operation, outcome, reason]` labels and count accumulation.
- Added public facade coverage for querying the new `Pool` metric family through `canic::api::metrics::MetricsQuery`.
- Added metrics query coverage showing pool rows are sorted before pagination.
- Added scaling metric coverage for stable `[operation, outcome, reason]` labels and count accumulation.
- Added public facade coverage for querying the new `Scaling` metric family through `canic::api::metrics::MetricsQuery`.
- Added metrics query coverage showing scaling rows are sorted before pagination.
- Added directory metric coverage for stable `[operation, outcome, reason]` labels and count accumulation.
- Added public facade coverage for querying the new `Directory` metric family through `canic::api::metrics::MetricsQuery`.
- Added metrics query coverage showing directory rows are sorted before pagination.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-restore5 cargo test -p canic-backup restore -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-preflight10 cargo test -p canic-cli backup_preflight_writes_standard_reports -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-journal cargo test -p canic-backup journal -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-snapshot cargo test -p canic-cli download_snapshots_writes_manifest_and_durable_journal -- --nocapture`.
- Ran `cargo check -p canic-backup -p canic-cli`.
- Ran `cargo test -p canic-backup operation_summary -- --nocapture`.
- Ran `cargo test -p canic-cli backup_preflight_writes_standard_reports -- --nocapture`.
- Ran `cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-core-cascade cargo test -p canic-core cascade_metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-core-query cargo test -p canic-core page_sorts_new_multi_label_metric_families_before_paginating -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-facade cargo test -p canic new_metric_families_are_public_facade_usable -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-clippy11 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0317-core-clippy cargo clippy -p canic-core -p canic --all-targets -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-core pool_metrics -- --nocapture`.
- Ran `cargo test -p canic-core page_sorts_new_multi_label_metric_families -- --nocapture`.
- Ran `cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `cargo check -p canic-core`.
- Ran `cargo test -p canic-core scaling_metrics -- --nocapture`.
- Ran `cargo test -p canic-core page_sorts_new_multi_label_metric_families -- --nocapture`.
- Ran `cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.
- Ran `cargo check -p canic-core`.
- Ran `cargo test -p canic-core directory_metrics -- --nocapture`.
- Ran `cargo test -p canic-core workflow::placement::directory -- --nocapture`.
- Ran `cargo test -p canic-core page_sorts_new_multi_label_metric_families -- --nocapture`.
- Ran `cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic --test metrics_facade --all-targets -- -D warnings`.

---

## 0.30.16 - 2026-05-04 - Canister And Wasm Store Metrics

### 📝 Summary

- `0.30.16` adds canister operation and wasm-store metrics so operators can see fleet lifecycle progress, create allocation source, propagation failures, module-source resolution, bootstrap chunk sync, managed publication, chunk cache hits, and store publish failures through `canic_metrics`.

### ➕ Added

- Added `MetricsKind::CanisterOps` rows with `[operation, role, outcome, reason]` labels for fleet canister operation visibility.
- Added `MetricsKind::WasmStore` rows with `[operation, source, outcome, reason]` labels for wasm-store source and publication visibility.
- Added canister operation counters for create, install, upgrade, and delete workflows.
- Added create allocation counters for new canister allocation, pool reuse, and pool top-up failures.
- Added root bootstrap create-skip counters for already-present roles and roles without staged wasm.
- Added fixed operation outcome and reason labels so operators can distinguish successful, skipped, allocation, propagation, topology, missing-wasm, and management-call paths.
- Added module-source resolution counters for embedded sources, registered resolvers, bootstrap store manifests, and live store manifests.
- Added bootstrap chunk-sync counters for all-cached, cache-miss upload, management-call, and hash-mismatch paths.
- Added managed publication counters for store prepare, chunk publish, chunk upload/cache-hit, manifest promotion, exact-release reuse, and capacity-triggered rollover.

### 📚 Documentation

- Documented the canister operation and wasm-store metric row shapes and fixed label sets in [docs/metrics.md](../metrics.md).

### 🧪 Testing

- Added canister operation metric coverage for stable `[operation, role, outcome, reason]` labels and count accumulation.
- Added `CanisterOpsMetricsApi` facade coverage.
- Added wasm-store metric coverage for stable `[operation, source, outcome, reason]` labels and count accumulation.
- Added `WasmStoreMetricsApi` facade coverage.
- Added lifecycle metric coverage for stable `[phase, role, stage, outcome]` labels and count accumulation.
- Added `LifecycleMetricsApi` facade coverage for runtime and bootstrap metric rows.
- Added public facade coverage for querying the new `CanisterOps` and `WasmStore` metric families through `canic::api::metrics::MetricsQuery`.
- Added metrics query coverage showing new multi-label metric families are sorted before pagination.
- Ran `cargo test -p canic-core canister_ops -- --nocapture`.
- Ran `cargo check -p canic-core -p canic-control-plane`.
- Ran `cargo clippy -p canic-core -p canic-control-plane --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0316-metrics2 cargo test -p canic-core metrics -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0316-facade cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0316-query cargo test -p canic-core page_sorts_new_multi_label_metric_families -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0316-check cargo check -p canic-core -p canic-control-plane`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0316-workflow-clippy3 cargo clippy -p canic-core -p canic-control-plane --all-targets -- -D warnings`.

---

## 0.30.15 - 2026-05-04 - Query Perf, Restore Ordering, And Lifecycle Metrics

### 📝 Summary

- `0.30.15` makes restore dry-run output clearer with identity, verification, and topology ordering summaries, makes query-side instruction audit probes explicit with typed same-call samples, and adds lifecycle metrics so operators can see init, post-upgrade, and bootstrap progress through `canic_metrics`.

### ➕ Added

- Added `MetricsKind::Lifecycle` rows with `[phase, role, stage, outcome]` labels for runtime seeding and async bootstrap progress.
- Added lifecycle metric outcomes for scheduled, started, completed, failed, waiting, and skipped states.
- Added core lifecycle metric recording for root and non-root init/post-upgrade runtime seeding.
- Added bootstrap metric recording for non-root bootstrap timers and root bootstrap scheduling/execution.
- Added `identity_summary` to restore plans with fixed, relocatable, mapped, in-place, and remapped member counts.
- Added `verification_summary` to restore plans with fleet check, member check group, member check, members-with-checks, and total check counts.
- Added `QueryPerfSample<T>` for same-call query probe responses that carry the query value and local instruction counter together.
- Added `MetricsQuery::sample_query(value)` so audit-only query probes can report `local_instructions` without relying on query-side perf-table writes that are not committed after the query returns.
- Added `ordering_summary`, per-member `phase_order`, and per-member `ordering_dependency` fields to restore plans so dry-runs show whether topology parent dependencies were satisfied inside the same group or by an earlier group.
- Added restore ordering counts to `preflight-summary.json` so automation can inspect restore phase and dependency shape without parsing the full restore plan.
- Added restore identity and verification counts to `preflight-summary.json` so automation can inspect dry-run restore scope without parsing the full restore plan.

```rust
Ok(MetricsQuery::sample_query(EnvQuery::snapshot()))
```

### 🔧 Changed

- Updated the internal audit leaf, root, and scaling query probes to return `QueryPerfSample<T>` instead of ad hoc `(value, perf_counter)` tuples.
- Instruction audit query rows now treat a zero `QueryPerfSample::local_instructions` value as unobservable, preserving `PARTIAL` visibility instead of silently recording a zero-cost query sample.
- Restore planning now rejects manifests whose restore groups would place a child before its topology parent.
- Restore verification summaries now expand role-level member check groups once per matching member, so dry-run check totals match the work the restore verifier will perform.

### 🧪 Testing

- Lifecycle metric implementation was not tested at user request.
- Added restore planning coverage for identity summaries in both in-place and mapped restore plans, plus verification summary counts.
- Added unit coverage for `MetricsQuery::sample_query`.
- Added instruction-audit support coverage for query sample count normalization.
- Added restore planning coverage for same-group parent ordering metadata, earlier-group parent dependency metadata, and child-before-parent restore group rejection.
- Added restore planning coverage for per-member expansion of role-level verification checks.
- Added preflight summary coverage for restore ordering counts.
- Added preflight summary coverage for restore identity and verification counts.
- Added facade coverage for `canic::api::metrics::MetricsQuery` and `QueryPerfSample<T>`.
- Ran `cargo test -p canic --test metrics_facade -- --nocapture`.
- Ran `cargo test -p canic-core metrics -- --nocapture`.
- Ran `cargo test -p canic-tests --test instruction_audit query_perf_counts -- --nocapture`.
- Ran `cargo test -p canic-backup -p canic-cli restore -- --nocapture`.
- Ran `cargo test -p canic-backup restore -- --nocapture`.
- Ran `cargo test -p canic-cli backup_preflight_writes_standard_reports -- --nocapture`.
- Ran `cargo check -p audit_leaf_probe -p audit_root_probe -p audit_scaling_probe`.
- Ran `cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --test metrics_facade -p canic-core -p canic-tests -p audit_leaf_probe -p audit_root_probe -p audit_scaling_probe --all-targets -- -D warnings`.

---

## 0.30.14 - 2026-05-04 - Backup Unit Topology Validation

### 📝 Summary

- `0.30.14` makes backup unit and verification boundaries harder to misdeclare by validating whole-fleet/subtree membership and verification role references against the manifest topology, rejecting ambiguous unit/filter declarations, then reporting the validated boundary metadata in manifest summaries.

### 🔧 Changed

- Whole-fleet backup units now fail manifest validation unless their role list covers every fleet role.
- Backup units now fail manifest validation when unit IDs, role entries, or dependency entries are duplicated.
- Manifest validation now fails when a fleet role is not covered by any backup unit.
- Verification plans now fail manifest validation when fleet checks, member check groups, or nested role filters reference unknown roles.
- Verification plans now fail manifest validation when member check groups repeat a role.
- Fleet member verification checks now fail manifest validation when their role filters reference unknown roles.
- Verification checks now fail manifest validation when a role filter is repeated.
- Subtree-rooted backup units now fail manifest validation unless the selected members form one connected subtree.
- Subtree-rooted backup units now fail manifest validation when they include an ancestor but omit one of its manifest descendants.
- `canic manifest validate` and preflight `manifest-validation.json` now report backup unit counts, kind counts, consistency mode, topology hash inputs, and per-unit topology validation metadata.

### 🧪 Testing

- Added manifest validation coverage for duplicate unit IDs, duplicate unit roles, duplicate dependencies, missing role coverage, unknown verification roles, duplicate verification role filters, duplicate member verification groups, whole-fleet role coverage, disconnected subtree units, subtree units that omit descendants, and enriched manifest validation summaries.

---

## 0.30.13 - 2026-05-04 - Skipped Patch Number

### 📝 Summary

- `0.30.13` was accidentally skipped during patch publishing; no release was cut for this patch number.

---

## 0.30.12 - 2026-05-04 - Backup Provenance Reports

### 📝 Summary

- `0.30.12` adds a backup provenance report for audit workflows and makes verification fail closed when manifest and journal topology receipts drift.

### ➕ Added

- Added `BackupLayout::provenance()` for a manifest-plus-journal provenance projection that records tool/source metadata, backup ID agreement, topology receipts, consistency units, and per-member snapshot hashes/version hints.
- Added `canic backup provenance --dir <backup-dir> [--out <file>] [--require-consistent]` for scriptable provenance export and audit gating.

```bash
canic backup provenance \
  --dir backups/<run-id> \
  --out backup-provenance.json \
  --require-consistent
```

### 🔧 Changed

- `canic backup preflight` now writes `backup-provenance.json` and includes its path in `preflight-summary.json`.
- `preflight-summary.json` now includes compact inspection/provenance status strings plus topology mismatch and backup unit counts for automation.
- `canic backup provenance --require-consistent` now writes the report first, then fails if manifest and journal backup IDs or topology receipts do not match.
- `BackupLayout::inspect()` now reports topology receipt mismatches, and `BackupLayout::verify_integrity()` rejects mismatched or missing journal topology receipts.

### 🧪 Testing

- Added unit coverage for provenance report construction, topology receipt drift rejection, CLI provenance consistency gating, CLI option parsing, CLI provenance reads, and preflight provenance summary output.

---

## 0.30.11 - 2026-05-04 - Release Surface Refresh

### 📝 Summary

- `0.30.11` refreshes release metadata after the topology/journal inspection patch so installer and setup references point at the live patch release.

### 🔧 Changed

- Updated the workspace version, lockfile package versions, install script default, and README install URLs to `0.30.11`.

### 🧪 Testing

- `0.30.11` was already being published before this detailed changelog split; no additional verification is claimed here.

---

## 0.30.10 - 2026-05-04 - Manifest Journal Path Agreement

### 📝 Summary

- `0.30.10` adds a lightweight backup inspection report, records topology receipts in journals, makes backup integrity verification reject layouts where manifest snapshot provenance and the durable journal disagree about an artifact path, fails snapshot capture if topology changes before the first snapshot is created, and updates runtime constructor hooks for the explicit unsafe `ctor` form.

### ➕ Added

- Added `BackupLayout::inspect()`, which compares manifest and journal metadata without reading artifact bytes.
- Added `canic backup inspect --dir <backup-dir> [--out <file>] [--require-ready]` for operator-readable manifest/journal agreement reports.

### 🔧 Changed

- `BackupLayout::verify_integrity()` now compares each manifest member's `source_snapshot.artifact_path` with the matching journal artifact path before checking the artifact on disk.
- Added a typed `ManifestJournalArtifactPathMismatch` error so preflight failures identify provenance drift separately from missing files or checksum failures.
- `canic backup preflight` now writes `backup-inspection.json` alongside status, integrity, manifest validation, restore plan, and summary reports.
- `canic backup inspect --require-ready` now writes the inspection report and then fails when metadata is not ready for full verification.
- `canic snapshot download` now recomputes the selection topology hash immediately before snapshot creation and fails closed if it differs from the discovery hash.
- `download-journal.json` now records optional `discovery_topology_hash` and `pre_snapshot_topology_hash` receipts, and `backup status` includes them in the resume report.
- Updated `ctor` attributes in memory/runtime bootstrap hooks to pass `unsafe` as the first macro argument for current `ctor` syntax.

```bash
canic backup inspect \
  --dir backups/<run-id> \
  --out backup-inspection.json \
  --require-ready
```

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0310-test5 cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0310-clippy5 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-package-0310-journal-topology cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-ctor-check cargo check -p canic-memory -p canic-core -p canic-control-plane`.
- Ran `git diff --check`.

---

## 0.30.9 - 2026-05-04 - Release Surface Refresh

### 📝 Summary

- `0.30.9` refreshes release metadata after the manifest snapshot checksum patch so installer and setup references point at the live patch release.

### 🔧 Changed

- Updated the workspace version, lockfile package versions, install script default, and README install URLs to `0.30.9`.

### 🧪 Testing

- `0.30.9` was already published before this detailed changelog backfill; no additional verification is claimed here.

---

## 0.30.8 - 2026-05-04 - Manifest Snapshot Checksums

### 📝 Summary

- `0.30.8` records durable artifact checksums in manifest snapshot provenance and verifies that manifest and journal checksums agree before a backup layout is accepted as restore-ready.

### 🔧 Changed

- Added optional `source_snapshot.checksum` provenance to manifest members while keeping older manifests without the field readable.
- `canic snapshot download` now writes each captured artifact checksum into the generated manifest.
- `BackupLayout::verify_integrity()` now rejects layouts where manifest snapshot provenance disagrees with the durable journal checksum.

### 📚 Documentation

- Updated `canic-cli` usage notes to call out checksum-backed manifest provenance for generated backup layouts.

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0308-test cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0308-clippy cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-package-0308 cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `git diff --check`.

---

## 0.30.7 - 2026-05-03 - Backup Preflight Bundle

### 📝 Summary

- `0.30.7` makes `canic snapshot download` produce the canonical manifest, journal, and artifact layout that `backup preflight` expects. It also adds a single no-mutation backup preflight command that writes the standard validation, status, integrity, restore-plan, and summary reports for a backup directory, and keeps changelog examples aligned with the patch that introduced each command or flag.

### ➕ Added

- Added `canic backup preflight --dir <backup-dir> --out-dir <dir> [--mapping <file>]`, which reads the canonical backup layout, requires a complete journal, verifies durable artifact checksums, computes a restore plan, and writes all preflight artifacts into one report directory.
- Added `preflight-summary.json`, a compact readiness summary with the backup ID, source environment, source root, topology hash, readiness flags, member counts, restore-plan member count, and paths to each generated report.

### 🔧 Changed

- `canic snapshot download` now writes `fleet-backup-manifest.json` after a successful non-dry-run capture, so a freshly downloaded backup can be passed directly to `canic backup preflight`.
- Snapshot journals now store artifact paths relative to the backup directory, keeping captured backup layouts portable when archived or moved.

```bash
canic backup preflight \
  --dir backups/<run-id> \
  --out-dir preflight/<run-id> \
  --mapping restore-map.json
```

### 📚 Documentation

- Moved the root changelog command examples directly under the matching patch bullets.
- Kept detailed bash examples inside the specific patch sections where those command forms became available.

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0307-test cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0307-clippy cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-package-0307 cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `git diff --check`.
- Ran changelog placement review for the 0.30 bash examples.

---

## 0.30.6 - 2026-05-03 - Release Surface Refresh

### 📝 Summary

- `0.30.6` refreshes release metadata after the operator reporting patch so installer and setup references point at the live patch release.

### 🔧 Changed

- Updated the workspace version, lockfile package versions, install script default, and README install URLs to `0.30.6`.

### 🧪 Testing

- `0.30.6` was already published before this detailed changelog backfill; no additional verification is claimed here.

---

## 0.30.5 - 2026-05-03 - Verified Restore Planning

### 📝 Summary

- `0.30.5` lets manifest validation write report files, backup status fail on incomplete journals, and restore dry-run planning require a verified backup layout, closing the gap between validation summaries, resumability checks, `backup verify`, and operator restore runbooks. It also adds end-to-end coverage for the public Access and Perf metric rows emitted by macro-generated endpoints.

### ➕ Added

- Added `canic restore plan --backup-dir <dir> --require-verified`, which runs the canonical manifest, journal, durable artifact, and SHA-256 checksum verification path before restore planning.
- Added `canic backup status --dir <dir> --require-complete`, which still writes the resumable status report and then returns an error when any artifact is not durable yet.
- Added `canic manifest validate --manifest <file> --out <file>`, so validation summaries can be captured alongside backup status, integrity, and restore plan reports.
- Added CLI coverage for the verified planning path, including the case where `--require-verified` is rejected for direct `--manifest` planning because there is no canonical backup layout to inspect.
- Added PocketIC coverage that verifies a denied signer guard increments `canic_metrics(Access)` and a successful signer endpoint dispatch increments `canic_metrics(Perf)`.

```bash
canic manifest validate \
  --manifest backups/<run-id>/manifest.json \
  --out manifest-validation.json
```

```bash
canic backup status \
  --dir backups/<run-id> \
  --out backup-status.json \
  --require-complete
```

```bash
canic restore plan \
  --backup-dir backups/<run-id> \
  --mapping restore-map.json \
  --out restore-plan.json \
  --require-verified
```

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0305-test cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0305-clippy cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-package-0305 cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `cargo test -p canic-tests --test pic_role_attestation metrics -- --test-threads=1 --nocapture`.
- Ran `cargo clippy -p canic-tests --test pic_role_attestation -- -D warnings`.
- Ran `git diff --check`.

---

## 0.30.4 - 2026-05-03 - Release Surface Refresh

### 📝 Summary

- `0.30.4` refreshes release metadata after the backup integrity patch so installer and setup references point at the live patch release.

### 🔧 Changed

- Updated the workspace version, lockfile package versions, install script default, and README install URLs to `0.30.4`.

### 🧪 Testing

- `0.30.4` was already published before this detailed changelog backfill; no additional verification is claimed here.

---

## 0.30.3 - 2026-05-03 - Backup Integrity Verification

### 📝 Summary

- `0.30.3` adds backup journal status and layout integrity verification so operators can inspect resumability and prove a downloaded backup is complete before using it for restore planning.

### ➕ Added

- Added `BackupLayout::verify_integrity()`, which reads the canonical manifest and download journal, checks matching backup IDs, requires each manifest member to have a durable journal artifact, rejects extra journal artifacts, and verifies artifact SHA-256 checksums.
- Added `DownloadJournal::resume_report()` plus serializable journal status report types that summarize artifact states, completion status, pending artifact count, and next resume actions.
- Added serializable `BackupIntegrityReport` and `ArtifactIntegrityReport` output types for audit logs and runbooks, including an explicit `verified` flag on successful integrity reports.
- Added `canic backup status --dir <backup-dir> [--out <file>]`, which emits the resumable journal report as JSON.
- Added `canic backup verify --dir <backup-dir> [--out <file>]`, which emits the integrity report as JSON.
- Extended `canic restore plan` with `--backup-dir <dir>` as an alternative to `--manifest <file>`, so restore planning can read the canonical manifest from a verified backup layout.

```bash
canic backup status \
  --dir backups/<run-id> \
  --out backup-status.json
```

```bash
canic backup verify \
  --dir backups/<run-id> \
  --out backup-integrity.json
```

```bash
canic restore plan \
  --backup-dir backups/<run-id> \
  --mapping restore-map.json \
  --out restore-plan.json
```

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-test cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-test2 cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-test3 cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-test4 cargo test -p canic-backup -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-test5 cargo test -p canic-backup -p canic-cli`.
- Ran `cargo fmt --all`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0303-clippy6 cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-package-0303-status2 cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `git diff --check`.

---

## 0.30.2 - 2026-05-03 - Restore Preflight Tightening

### 📝 Summary

- `0.30.2` tightens restore preflight so generated plans carry the provenance and validation metadata operators need before mutating canisters.

### 🔧 Changed

- Added backup ID, source environment, source root canister, topology hash, and member count to `RestorePlan`, making plan JSON self-identifying when it is copied into an audit or runbook.
- Added identity mode, verification class/checks, source snapshot metadata, and target parent canister references to each `RestorePlanMember`, so dry-run output now shows what will be restored and how it should be verified.
- Reject restore mappings that include source canisters not present in the manifest, catching stale or mistyped mapping files before restore execution.
- Validate backup-unit `roles` and `dependency_closure` entries against the manifest fleet roles, so consistency boundaries cannot silently reference missing members.

### 🧪 Testing

- Ran `CARGO_TARGET_DIR=/tmp/canic-backup-0302-test cargo test -p canic-backup`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-backup-0302-test2 cargo test -p canic-backup`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-cli-0302-test cargo test -p canic-cli`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-cli-0302-test2 cargo test -p canic-cli`.
- Ran `cargo fmt --all`.
- Ran `cargo fmt --all --check`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-0302-clippy cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `git diff --check`.

---

## 0.30.1 - 2026-05-03 - Publish Follow-Through

### 📝 Summary

- `0.30.1` finishes the immediate `0.30` publish follow-through by exposing manifest validation and restore dry-run planning, tightening endpoint metrics cleanup, release ordering, and installer/version surfaces.

### 🔧 Changed

- Updated the workspace version and installer bootstrap references to `0.30.1`, so the curlable setup path and installer README point at the patch release.
- Updated the workspace publish order so `canic-backup` publishes before dependent crates and `canic-cli` publishes with the rest of the public crate set.
- Added `canic manifest validate --manifest <file>`, which reads a fleet backup manifest, runs the manifest contract, and emits a compact JSON validation summary.
- Added `canic restore plan --manifest <file> [--mapping <file>] [--out <file>]`, which reads a fleet backup manifest, validates optional source-to-target mappings, and emits the no-mutation restore plan as JSON.
- Kept `MetricsKind` and internal metric-key ordering explicit with `remain::sorted`, making future metrics additions fail fast when enum order drifts.
- Added [docs/metrics.md](../metrics.md), which documents every `canic_metrics` row shape, value type, principal dimension, and cardinality expectation.

```bash
canic manifest validate \
  --manifest backups/<run-id>/manifest.json
```

```bash
canic restore plan \
  --manifest backups/<run-id>/manifest.json \
  --mapping restore-map.json \
  --out restore-plan.json
```

### 🗑️ Removed

- Removed the remaining endpoint metrics macro hook emissions and compatibility shims after the public endpoint counter backend was removed; endpoint instruction accounting remains available through `canic_metrics(Perf)`.

### 🧪 Testing

- Ran `cargo generate-lockfile`.
- Ran `cargo test -p canic --test install_script_surface -- --test-threads=1 --nocapture`.
- Ran `cargo test -p canic --test protocol_surface -- --test-threads=1 --nocapture`.
- Ran `cargo test -p canic-cli`.
- Ran `cargo test -p canic-backup -p canic-core -p canic-macros`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-cli-0301-clippy cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-backup -p canic-cli -p canic-core -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo package -p canic-backup -p canic-cli --locked --allow-dirty`.
- Ran `cargo fmt --all`.
- Ran `cargo fmt --all --check`.
- Ran `git diff --check`.

---

## 0.30.0 - 2026-05-03 - Fleet Snapshot Backups

### 📝 Summary

- `0.30.0` starts the fleet snapshot backup line with a design-backed backup model, reusable backup primitives, and an operator CLI for capturing canister snapshots from topology-defined units.

### ➕ Added

- Added `canic-backup`, a reusable crate for backup manifests, topology hashes, artifact checksums, resumable journals, and restore dry-run planning.
- Added `canic-cli`, with `canic snapshot download` for downloading snapshots for one canister or a canister plus direct/recursive registry-discovered children.
- Added topology-hash validation so backup manifests can bind discovery and snapshot execution to the same canonical `(pid, parent_pid, role, module_hash)` view.
- Added manifest fields and validation for member verification checks, quiescence strategy names, identity modes, module/wasm provenance, and backup-unit consistency rules.
- Added resumable download journal states: `Created`, `Downloaded`, `ChecksumVerified`, and `Durable`.
- Added restore planning that validates remaps, blocks remapping fixed-identity members, and orders parents before children.

```bash
canic snapshot download \
  --canister <canister-id> \
  --root <root-canister-id> \
  --recursive \
  --out backups/<run-id> \
  --stop-before-snapshot \
  --resume-after-snapshot
```

### 🔧 Changed

- Moved the snapshot/backup design into `docs/design/0.30-canister-snapshots/0.30-design.md` and made the topology, directory, quiescence, restore-ordering, verification, and registry-projection rules explicit.
- Kept endpoint macro compatibility after the public endpoint metrics backend removal by preserving the access-layer hook names.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-backup -p canic-cli`.
- Ran `cargo clippy -p canic-backup -p canic-cli --all-targets -- -D warnings`.
- Ran `cargo package -p canic-backup -p canic-cli --locked`.
- Ran `cargo test -p canic-core -p canic-macros`.
- Ran `cargo clippy -p canic-core -p canic-macros --all-targets -- -D warnings`.
- Ran `PUBLISH_DRY_RUN=1 bash scripts/ci/publish-workspace.sh` and confirmed the full `0.30.0` publish-order crate set is visible on crates.io.
- Ran a local DFX dry-run resolving the root plus six descendants.
- Ran a live local DFX root snapshot download through `canic snapshot download` and verified the durable journal, SHA-256 checksum, downloaded artifacts, and resumed root canister status.
- Ran `make test`.
