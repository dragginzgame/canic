# Canic 0.95 Changelog

## [0.95.7] - 2026-07-21 - Manual Root Refill And Bounded Child Funding

### 📝 Summary

Corrects the Slice D funding boundary: root ICP-to-cycles conversion is an
explicit operator action, while automatic top-up belongs only to nonroot
canisters requesting bounded funding from their parent. The automatic child
owner observes balances at least hourly and cannot poll through a parent
policy or capacity rejection.

### 🔧 Automatic Child Funding

- Keeps one awaited, non-overlapping `cycles:topup` owner only for configured
  nonroot roles. Root and disabled roles retain their lifecycle balance sample
  but execute no recurring top-up callbacks.
- Reduces the maximum balance-observation gap from six hours to one hour. The
  existing headroom formula, doubled observed burn, threshold-per-day floor,
  five-minute funding allowance, five-minute safety margin, and one-minute
  lower bound remain. Operators should size the threshold for that maximum
  gap plus the ten-minute reserve; funding does not replace ingress abuse
  controls or low-cycle alerting.
- After a successful grant, schedules no new funding effect before the greater
  of the one-minute observation floor and the role's configured parent-funding
  cooldown.
- Retries typed transport/ops failures and an in-progress conflict through the
  bounded 1/2/4/8/16/30-minute progression. Cooldown, cumulative-budget,
  capacity, authorization, and configuration rejections stop the automatic
  owner instead of repeatedly calling the parent.
- Retains the parent as the sole abuse-control authority for direct-child
  admission, kill switch, replay exclusion, cost guard, request clamping,
  cumulative child budget, cooldown, and available balance. Automatic child
  funding cannot bypass those checks or trigger root ICP conversion.

### 🗑️ Hard Cuts

- Removes the automatic hub self-refill workflow, hub-only policy evaluator,
  resumable-hub lookup, timer-generated operation identity, and automatic root
  timer branch.
- Removes `topup.icp_refill.min_hub_cycles_before_refill` from the strict
  configuration schema and generated bootstrap model. Existing pre-1.0 config
  must delete that key; there is no alias, ignored-field fallback, or legacy
  parser.
- Keeps the maintained guarded ICP-refill endpoint and operator CLI, including
  dry-run, per-call cap, optional exchange-rate gate, funding kill switch,
  ledger transfer, CMC notification, replay, cost settlement, and durable
  recovery.

### 📚 Documentation

- Refreshes the root README against the maintained toolchain, backup/restore
  authority, lifecycle macro spelling, and public application-timer contract.
- Aligns completed design headers, active auth and lifecycle audit methods,
  audit fingerprints, operational source maps, internal links, and metrics
  table rendering with the current repository layout.
- Marks the root June audit report as frozen historical evidence and removes a
  tracked temporary scratch file.

### 🧪 Testing

- Focused policy tests cover the one-hour maximum, inclusive threshold,
  conservative burn behavior, manual refill gates, and refill recovery.
- Workflow tests prove root selects no automatic owner, nonroot selection
  carries the configured funding cooldown, the one-minute floor remains, and
  policy/capacity exhaustion does not enter automatic retry.
- Configuration, bootstrap rendering, role-contract selection, timer
  inventory, layering, formatting, and strict targeted Clippy remain part of
  this batch's validation.

## [0.95.6] - 2026-07-21 - Cycle Top-Up Safety Authority

### 📝 Summary

Begins Slice D by separating automatic funding from diagnostic cycle-history
writes. Configured nonroot parent funding and root ICP self-refill now share
one configuration-gated, non-overlapping timer owner whose next observation is
derived from balance headroom rather than an inherited hourly interval.

### ⚙️ Scheduling And Funding

- Registers `cycles:topup` only while automatic funding is configured. A
  nonroot `topup` policy selects parent funding; a root requires enabled
  `topup.icp_refill` and now reaches the maintained hub self-refill workflow.
- Executes newly observed work immediately at or below the configured
  threshold, with one minute between completed funding effects if the balance
  remains low. Above it, the conservative burn estimate is the greater of
  twice the latest observed net burn and one configured threshold per day.
- Schedules the projected threshold crossing five minutes before the accepted
  funding allowance plus a further five-minute safety margin, bounded between
  one minute and six hours. Missing, stale, flat, or deposit-affected history
  uses the threshold-per-day floor; observations older than 12 hours are not
  treated as current burn evidence.
- Awaits parent funding or ICP refill inside the common timer callback. Expected
  transport, in-progress, cooldown, or capacity failure retries after 1, 2, 4,
  8, 16, then 30 minutes; policy/configuration contradiction and terminal
  non-resumable root-refill outcomes stop failed.

### 📊 Observation And Retention

- Records one balance sample during lifecycle startup and records pre-effect
  plus post-effect observations around configured funding. Cycle history no
  longer performs a write merely because an hour elapsed.
- Keeps the existing seven-day history window. Each observation removes at
  most 128 expired balance rows and 128 expired top-up-event rows, leaving any
  additional diagnostic cleanup for a later real observation.
- Disabled roles expose `cycles:topup` as `unregistered + idle` with zero
  callbacks. Low-burn configured roles may wait up to six hours; declining
  headroom advances the deadline.

### 🧹 Hard Cuts

- Removes the coupled `cycles:tracking` timer, standard-versus-top-up mode,
  permanent hourly cadence, heap-only funding in-flight flags, and detached
  funding task. The common timer's running state is the sole overlap authority.
- Removes the obsolete `request_in_flight` top-up metric and the unused common
  recurring-timer helper with a distinct initial callback. No alias, fallback,
  or parallel history/top-up scheduler remains.
- The top-up threshold is inclusive: exact-threshold nonroot funding and root
  ICP refill are now due rather than waiting for the balance to fall lower.

### 📡 Contract Notes

- Cycle-history, top-up-event, configuration, Candid, and stable-record shapes
  are unchanged. Runtime timer status now reports `cycles:topup`; diagnostic
  history intentionally has event-based rather than hourly resolution.
- No dependency, stable allocation, or Cargo package version changes are
  present.

### 🧪 Testing

- Pure policy tests cover inclusive threshold behavior, insufficient/stale
  history, deposit and flat-balance fallback, doubled observed burn, and the
  one-minute/six-hour bounds.
- Workflow tests cover the complete capped retry progression and typed
  retry-versus-stop classification. Storage tests cover ordered latest-sample
  lookup and bounded retention.
- Runtime-status and timer-inventory tests cover the current top-up identity;
  the maintained timer PocketIC journey covers disabled-role idle projection.

## [0.95.5] - 2026-07-21 - Deadline-Driven Log Retention

### 📝 Summary

Completes Slice C by giving runtime-log append, count eviction, and age
deletion one ordered storage authority. The permanent ten-minute retention
interval and destructive full-history rewrite are removed; the default
no-age configuration now executes zero log-retention callbacks.

### ⚙️ Scheduling And Retention

- Enforces `max_entry_bytes` and `max_entries` before append returns. A full
  steady-state store evicts only its single oldest row.
- Schedules optional age retention at the exact oldest-entry boundary:
  `created_at + max_age_secs + 1s`. The extra second preserves the maintained
  strict `created_at < now - max_age_secs` behavior.
- Removes at most 256 expired rows per callback and continues through separate
  zero-delay timer messages only while another due row exists. Empty stores
  and configurations without `max_age_secs` remain `unregistered + idle`.
- Stops on local configuration or storage contradiction instead of polling.
  Retention diagnostics print directly rather than appending a new durable log
  that could sustain its own expiry loop.

### 🧹 Hard Cuts

- Replaces the append-only two-memory log and its clear-then-rewrite sweep with
  one ordered stable map at core-runtime allocation 35. Allocations 31 and 32,
  the `log_index` and `log_data` reserved-state rows, the generic retention
  parameter/summary flow, and the ten-minute cadence constant are removed.
- Existing runtime-log history is non-authoritative diagnostic state and is
  not migrated from the retired append-only allocation. No legacy reader,
  fallback, alias, or duplicate retention path remains.
- Lowering the compiled count limit below retained history clears that history
  in one bounded hard cut before accepting the current entry rather than
  performing an unbounded deletion loop.

### 📡 Contract Notes

- The state manifest now declares allocation 35 as the modeled `runtime_log`
  v1 domain with canonical `LogEntryRecord` and `LogEntriesData` identities.
  The Candid log query and log configuration shapes are unchanged.
- No dependency or Cargo package version changes are present.

### 🧪 Testing

- Focused storage tests cover exact count and byte bounds, zero-count clearing,
  timestamp regression rejection, reduced-count hard cuts, strict age
  boundaries, 256-row continuation, and snapshot round trips.
- Role-contract, core state-contract, host manifest, and CLI state tests prove
  allocation 35 is unique, modeled, and rendered instead of the retired
  reserved-memory rows.
- The maintained application-timer PocketIC journey verifies that default log
  retention remains `unregistered + idle` with zero executions.
- Strict all-target `canic-core` Clippy passes with warnings denied.

## [0.95.4] - 2026-07-21 - Placement Acknowledgement Authority

### 📝 Summary

Gives terminal placement-receipt acknowledgement one durable placement-only
index and one pending-only scheduler. Normal retries no longer page unrelated
receipt-backed intents, and empty canisters retain truthful
`unregistered + idle` status without executing acknowledgement callbacks.

### ⚙️ Scheduling And Recovery

- Terminal placement settlement adds its exact operation to the derived index;
  successful root acknowledgement removes both terminal local evidence and its
  index entry through the canonical receipt-backed intent authority.
- Rebuilds the derived index from canonical terminal placement intents before
  timer startup after install or upgrade. Stale derived rows are discarded;
  missing primaries, non-terminal rows, wrong resource ownership, and key/value
  mismatches fail closed with typed storage causes.
- Processes at most 32 successful acknowledgements per callback and continues
  through separate timer messages while more exact work exists. New terminal
  evidence advances an existing retry immediately.
- Retries only root transport/ops failure. The first failed call stops that
  attempt rather than spending cycles against the remainder of a root-wide
  outage; public root rejection and local/index contradictions stop failed.
- Uses deterministic retry delays of 1, 2, 4, 8, 16, then 30 minutes capped.
  Success resets the progression. This permits at most 52 failed calls in the
  first 24 hours and 48 calls per day at the cap, while detecting root recovery
  within at most 30 minutes when no new placement event advances it sooner.

### 🧹 Hard Cuts

- Removes the heap cursor over the shared receipt-backed primary map, the
  cross-consumer filtering pass, the fixed one-minute retry flag, and the
  generic internal receipt-page view. No alternate placement acknowledgement
  scan or retry flow remains.
- Removes the now-unused generic timer retry-result constructor; owner
  workflows continue to return their exact work count and directive.

### 📡 Contract Notes

- Adds core-runtime stable memory allocation 45 for the derived placement
  acknowledgement index. Existing intent, receipt, Candid, configuration, and
  JSON shapes are unchanged; lifecycle reconstruction is the hard-cut path for
  canisters upgraded from the released line.
- No dependency or Cargo package version changes are present.

### 🧪 Testing

- Focused storage and placement tests cover placement-only selection, bounded
  pages, canonical rebuild, corruption rejection, exact terminal removal,
  retry classification, and the complete capped delay progression.
- Role-contract and host state-manifest tests prove allocation 45 is unique,
  selected by the intended roles, and represented by canonical record and
  snapshot identities.
- A maintained scaling PocketIC topology creates initial and manual workers,
  observes real root acknowledgements, and proves the placement owner returns
  to `unregistered + idle` after draining its exact receipts.
- Strict all-target `canic-core` Clippy passes with warnings denied.

## [0.95.3] - 2026-07-20 - Event-Driven Pool Recovery

### 📝 Summary

Removes the root pool's permanent 30-minute maintenance interval. Durable
pending-reset rows now reconstruct one event-driven bounded worker, while an
empty root retains only truthful `unregistered + idle` status and executes no
pool callbacks.

### ⚙️ Scheduling And Recovery

- Hard-cuts the separate `pool:maintenance` timer key and shared pool cadence.
  Lifecycle checks stable pending-reset state once and producers advance the
  existing `pool:pending` owner immediately when they create known work.
- Continues ten-row sweeps through separate timer messages. Terminal progress
  resets retry state; no pending rows stop the timer.
- Treats local-build canister-status unavailability as the sole retryable
  blocked condition. Its deterministic delay progresses through 1, 2, 4, 8,
  16, and 30 minutes, then remains capped at 30 minutes. This probe is absent
  from IC builds, so the retry cannot consume production IC cycles.
- Treats unexpected pool-policy variants as invariant failures and stops with
  typed pool diagnostics. Management reset failures retain their existing
  durable terminal `Failed` evidence rather than remaining in the retry queue.
- Corrects the released finite-intent owner so cleanup storage and deadline
  reconciliation failures stop as invariant failures. The one-minute
  self-retry constant and directive are removed while recoverable intent and
  expiry-index evidence remain intact.

### 📊 Audit Correction

- Direct inspection found that log byte truncation is append-owned but the
  configured entry-count cap is still enforced by the recurring rebuild. The
  log migration is therefore blocked into its own later Slice C batch until
  count and age mutation have one bounded authority; this pool batch does not
  conceal that storage change inside timer work.

### 📡 Contract Notes

- Runtime introspection no longer contains the removed
  `pool:maintenance` row. Root roles retain `pool:pending`, which is
  `unregistered + idle` when no durable reset work exists.
- No stable schema, memory allocation, Candid, configuration, JSON shape,
  dependency, or Cargo package version changes are present.

### 🧪 Testing

- All 926 `canic-core` library tests pass, including deterministic bounded
  retry progression and intent invariant-stop coverage. The timer inventory
  guard and strict all-target core Clippy pass.
- A focused PocketIC root-topology journey installs the maintained root and
  children and proves the empty pool owner has zero executions, no live
  registration, and no surviving maintenance row.

## [0.95.2] - 2026-07-20 - Finite Intent Expiry Authority

### 📝 Summary

Replaces hourly polling and full pending-intent scans with one ordered stable
index containing only finite local reservations. The index owns the exact next
eligible cleanup deadline, is reconstructed during lifecycle restore, and
drives one bounded common-timer process only while expirable work exists.

### ⚙️ Scheduling And Storage

- Adds one derived stable expiry index ordered by cleanup deadline and intent
  identity. Finite reservations enter it transactionally; commit, rollback,
  recovery, and cleanup remove their exact row. TTL-free reservations never
  enter the index.
- Rebuilds the derived index from canonical pending intent state before timers
  start after install or upgrade. Contradictory metadata, pending rows, or
  primary records retain typed invariant causes and fail closed; prior derived
  expiry rows are discarded and reconstructed.
- Schedules the exact first eligible cleanup second, processes at most 32 due
  intents per callback, yields immediate continuation only while more rows are
  already due, and retries storage failures after one minute.
- Stops and reports `unregistered + idle` when the index is empty. Removing an
  earlier reservation reconciles the sole live deadline instead of leaving a
  stale callback armed.
- Routes application local intents and every production cost-guard reservation
  through workflow-owned scheduling. `CostGuardWorkflow` is now the sole
  production bridge from cost accounting mutations to expiry scheduling,
  including control-plane deployment and publication.

### 🧹 Hard Cuts

- Removes the hourly intent-cleanup interval, the pending-store full scan, and
  the misleading helper that called all pending local intents “expirable”.
- Removes the duplicate ops-level cost-guard failure-recovery facade and the
  obsolete `idle` intent metric reason. No alias, fallback, or parallel
  scheduling path remains.

### 📡 Contract Notes

- Adds core-runtime stable memory allocation 44 for the derived expiry index.
  Existing intent records, pending records, totals, metadata, and receipt-backed
  intent schemas are unchanged.
- No public Candid, configuration, JSON, dependency, Cargo package version, or
  receipt-reclamation surface changes in this batch.

### 🧪 Testing

- Unit coverage proves deadline/identity ordering, bounded pages, exact terminal
  removal, TTL-free exclusion, overflow rejection without partial mutation,
  lifecycle reconstruction, corruption rejection, bounded continuation, and
  failure retention of the recoverable row.
- PocketIC proves a finite reservation retains capacity before its deadline,
  survives upgrade, is re-armed from rebuilt authority, and releases capacity
  after expiry. The same journey proves TTL-free work remains reserved without
  keeping cleanup registered.
- The common timer, lifecycle, stable-memory, layering, and sole cost-guard
  workflow ownership guards cover the new boundary.

## [0.95.1] - 2026-07-20 - Common Timer Authority

### 📝 Summary

Replaces the competing guarded-slot and fixed-rate timer mechanics with one
heap-owned scheduler for Canic processes, lifecycle deferrals, and application
timers. Scheduling is request-sequenced and generation-bound, asynchronous
recurrence begins after completion, and live runtime status no longer treats a
historical metric row as an active registration.

### ⚙️ Scheduling

- Routes log retention, cycle tracking, delegated-proof renewal, intent
  cleanup, root-pool maintenance and reset, placement-receipt acknowledgement,
  lifecycle deferral, and application timers through one `TimerWorkflow`.
- Gives every built-in process a closed timer key. Application timers use a
  separately allocated opaque identity and a non-cloneable `TimerHandle`.
- Merges requests made during an active callback without arming a competing
  callback. The earliest requested deadline wins unless a later sequenced
  cancellation suppresses rearming; a still later schedule reenables it.
- Replaces fixed-rate asynchronous intervals with one-shot rearming after each
  completed invocation. Immediate continuation yields through a new timer
  message rather than looping inside the callback.
- Checks request-sequence, generation, application-identity, delay, and
  deadline arithmetic. Exhaustion fails closed without overwriting the current
  registration.
- Removes the pool worker's duplicate in-progress, reschedule, and timer-slot
  state; the common authority now retains work arriving while a reset batch is
  active.

### 🧹 Hard Cuts

- Removes `timer_guarded!`, `timer_interval_guarded!`, `TimerSlot`, guarded
  workflow/ops helpers, and direct public `canic::cdk::timers` access.
- Removes the direct IC interval primitive from production. The sole platform
  owner arms and clears one-shot callbacks only.
- Renames the internal lifecycle facade operation to `defer_lifecycle` and
  routes root, non-root, and generated user-hook deferrals through the common
  workflow.
- Removes the obsolete pool `in_progress` metric reason with the manual guard
  it described. No alias, deprecated wrapper, or fallback path remains.

### 📡 Public And Operator Surfaces

- `timer!` and `timer_interval!` now return the maintained opaque
  `canic::api::timer::TimerHandle`.
- `canic::api::timer::cancel(handle)` consumes that handle and reports whether
  the live timer identity existed. Cancelling a scheduled timer releases its
  task and status entry; cancelling a running interval allows the current
  invocation to finish and suppresses recurrence.
- `timer_interval!` is explicitly an after-completion interval. It does not
  replay missed fixed-rate ticks after a time jump.
- The current runtime-introspection timer row hard-cuts the old aggregate
  `status` and `registered` fields. It now reports independent `registration`
  and `condition`, scheduling mode, enabled configuration, generation, next
  deadline, typed last outcome, bounded work count, and counters explicitly
  named `*_since_runtime_start`.
- Timer metrics retain `[mode, label]` as their bounded key. `count` is the
  execution count and `value_u64` is the latest armed delay in milliseconds;
  changing exact deadlines no longer allocates a metric row per duration.

### 🧪 Testing

- Adds deterministic control tests for duplicate registration, earlier
  replacement, stale generations, schedule-during-run retention, earliest
  request merging, cancellation precedence, reenabling, and checked overflow.
- Adds PocketIC proof that a cancelled application timer never executes, a
  one-shot executes exactly once, and an interval rearms once after completion
  without replaying missed fixed-rate ticks.
- The same PocketIC journey proves completed and cancelled one-shots disappear
  from runtime status while the live interval reports
  `scheduled + active + after_completion`.
- Updates the timer inventory and lifecycle-boundary guards for the single raw
  platform owner and common lifecycle facade.

## [0.95.0] - 2026-07-20 - Timer Authority Slice A

### 📝 Summary

Closes the completed 0.94 backup/restore line at released `v0.94.14` and
activates the accepted 0.95 timer-authority design. The audit inventories every
production timer, lifecycle deferral, public timer form, retry scheduler, and
bounded host wait before scheduler implementation begins.

### 📊 Audit

- Confirms `ops::runtime::timer` is the sole production consumer of direct IC
  timer operations.
- Dispositions all 14 timer/process rows and both bounded host waits.
- Reproduces async interval overlap, stale guarded slots, false live status,
  unnecessary idle wake-ups, unrelated full scans, duplicate mechanical and
  lifecycle flows, and unreachable configured root self-refill.
- Measures the empty-root first-hour baseline at ten background callbacks and
  72,303 timer-callback instructions. Seven log/pool wakes had no
  authoritative work.
- Adds a source inventory guard so a new timer wrapper, bypass, or timed wait
  cannot enter silently.

### 🔧 Decisions

- Retains cancellable public one-shot and after-completion interval forms.
  Unused guarded macros, `TimerSlot`, fixed-rate async recurrence, and public
  raw-CDK timer access are hard-cut in the common-authority slice.
- Freezes event/deadline ownership for log age retention, pool resets, intent
  expiry, placement acknowledgement, and delegated-proof renewal.
- Keeps cycle sampling and automatic top-up independent so disabled top-up is
  unregistered and configured root ICP self-refill becomes reachable through
  its existing workflow.
- Freezes per-owner batch, retry, and cadence bounds without adding a global
  heartbeat or receipt-specific state.

### 🧪 Testing

- Uses the existing root PocketIC harness to measure current callback count and
  instruction cost from the released anchor.
- Runs the targeted source inventory and direct-owner guards.
- Adds no production scheduling, Candid, JSON, stable-state, Cargo-version,
  dependency, backup/restore, receipt-reclamation, or compatibility change.
