# 0.61 Changelog

## 0.61.40 - 2026-06-08 - Release readiness and deployment permit close-out

### Summary

- Fixed control-plane compile failures after lifecycle create became
  permit-required.

- Cleaned up stale-looking 0.61 readiness wording in the replay-protection
  design.

- Kept the slice focused: no CLI, Candid, JSON/output, dependency, or lockfile
  behavior changed.

### Changed

- Added a control-plane deployment helper that reserves, completes, or recovers
  a management-deployment `CostGuardPermit` around lifecycle create.

- Bootstrap auto-create, bootstrap wasm-store creation, and runtime wasm-store
  publication creation now pass the required deployment permit into
  `CanisterLifecycleEvent::Create`.

- `canic-core` control-plane support now exposes the narrow cost-guard and
  replay-policy types needed by the control-plane helper.

- The design's implementation-plan section is now explicitly labeled as the
  historical implementation record used while building 0.61.

- Release readiness now points readers to the acceptance criteria and
  executable replay-policy, hard-cut, and cost-guard gates instead of making
  old branch-slice prose look like fresh work.

- The former "Remaining Cost-Abuse Guardrails" and "Slice G" headings were
  renamed to avoid implying open post-gate implementation slices.

### Notes

- The reserve boundary logs command kind, role, parent, quota subject, and
  payer through the Canic log system without logging wasm bytes or request
  payloads.

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo check --locked -p canic-control-plane --all-targets
cargo test --locked -p canic-control-plane --all-targets -- --nocapture
cargo clippy --locked -p canic-control-plane --all-targets --all-features -- -D warnings
cargo test --locked -p canic-core --test cost_guard_boundary_guard -- --nocapture
cargo test --locked -p canic-core replay_policy --lib -- --nocapture
cargo clippy --locked -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test --locked -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.39 - 2026-06-06 - Release blocker manifest gate

### Summary

- Added one aggregate release-candidate replay manifest gate.

- Kept the slice manifest/test/docs-only: no runtime, CLI, Candid, or output
  behavior changed.

### Changed

- Replay-policy tests now fail if the endpoint manifest, root capability
  command manifest, or pool admin command manifest contains any
  `ReleaseBlocker` entry.

- The aggregate failure message reports blockers with their manifest scope, for
  example `endpoint:<name>`, `root-capability:<variant>`, or
  `pool-admin:<variant>`.

- The 0.61 design now points release-candidate readiness at the executable
  manifest gates instead of relying only on prose blocker lists.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.38 - 2026-06-06 - Durable publish manifest guard

### Summary

- Added executable replay-policy coverage for durable-publication endpoint
  classification.

- Kept the slice manifest-only: no runtime, CLI, Candid, or output behavior
  changed.

### Changed

- Replay-policy tests now derive the expected durable-publish endpoint set from
  the protected wasm-store update method manifest plus the root template
  publication admin methods.

- The guard proves each durable-publish endpoint is an implemented update
  endpoint with monotonic publication policy, `DurablePublish` cost class,
  durable-publish quota metadata, and durable-publish cycle-reserve metadata.

- The same guard fails if an unrelated endpoint drifts into the
  durable-publish cost class.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.37 - 2026-06-06 - Canister upgrade deployment permit

### Summary

- Put actual canister upgrade installs behind a management-deployment
  `CostGuardPermit`.

- Preserved the response-idempotent already-current upgrade path by returning
  before quota or cycle reservation when the target module hash already
  matches.

### Changed

- `CanisterLifecycleEvent::Upgrade` now carries explicit cost context for the
  quota subject, payer, and request time.

- `CanisterLifecycleWorkflow::apply_upgrade` reserves deployment quota and
  cycle budget immediately before management `install_code` upgrade dispatch.

- Upgrade execution logs the deployment cost-guard reservation through the
  CanisterLifecycle topic without logging module bytes or request payloads.

- `ModuleInstallWorkflow` now exposes only permit-required install helpers for
  lifecycle workflow callers.

- The cost-guard boundary integration test now rejects direct lifecycle
  workflow calls to unpermitted module install helpers.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::canister_lifecycle --lib -- --nocapture
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core --test cost_guard_boundary_guard -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.36 - 2026-06-06 - Root provision deployment permit thread

### Summary

- Threaded the root provision deployment `CostGuardPermit` through lifecycle
  creation.

- Tightened management-deployment source guards so provisioning workflows cannot
  call raw management create, cycle top-up, or creation-install helpers without
  permit wrappers.

### Changed

- `CanisterLifecycleEvent::Create` now carries the reserved deployment permit
  from root `ProvisionCanister` execution.

- Provisioning allocation now uses permit-required wrappers for pool top-up and
  fresh canister creation.

- Initial canister install now uses permit-required management install wrappers
  during root provision creation.

- The cost-guard boundary integration test now also pins management-deployment
  permit wrappers and rejects unpermitted provisioning workflow calls.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core --test cost_guard_boundary_guard -- --nocapture
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core workflow::ic::provision --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.35 - 2026-06-06 - Threshold ECDSA permit boundary guard

### Summary

- Tightened the threshold-ECDSA signing cost-guard boundary.

- Added executable source guards for cost-guard permit construction and
  expensive-adapter permit requirements.

### Changed

- `EcdsaOps::sign_bytes` now requires a `CostGuardPermit` for both
  `auth-crypto` and non-`auth-crypto` builds.

- Prepared auth signing wrappers now pass their existing cost permits through
  to the lower ECDSA signing adapter.

- Added a `canic-core` integration guard proving `CostGuardPermit` construction
  remains private, ECDSA signing calls stay inside prepared auth signing ops,
  and ICP refill value-transfer adapters continue to require permits.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core --test cost_guard_boundary_guard -- --nocapture
cargo test -p canic-core ops::auth --lib -- --nocapture
cargo test -p canic-core api::auth --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.34 - 2026-06-06 - ICP refill value-transfer permit boundary

### Summary

- Tightened the ICP refill value-transfer cost-guard boundary.

- This implements the cost-guard permit requirement in
  `docs/design/0.61-replay-protection/0.61-design.md` for the shared-replay
  ICP refill ledger transfer and CMC notify path.

### Changed

- `IcpRefillOps::icrc1_transfer` now requires a `CostGuardPermit` before it can
  call the ledger transfer adapter.

- `IcpRefillOps::notify_top_up` now requires a `CostGuardPermit` before it can
  call the CMC notify adapter.

- ICP refill workflow now requires the reserved value-transfer permit after
  cost-guard reservation and before marking or executing each refill external
  effect.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.33 - 2026-06-06 - Shared pending replay receipt quotas

### Summary

- Added shared pending replay receipt quotas for fresh replay-protected
  operations.

- This implements the receipt-quota requirement in
  `docs/design/0.61-replay-protection/0.61-design.md` at the shared
  `reserve_or_replay_receipt` boundary.

### Changed

- Fresh shared replay receipt reservation now rejects with `ResourceExhausted`
  when the actor already has 64 pending replay receipts.

- Fresh shared replay receipt reservation now rejects with `ResourceExhausted`
  when the command kind already has 512 pending replay receipts.

- Pending quota counts non-expired `Reserved`, `ExternalEffectInFlight`, and
  `RecoveryRequired` receipts. Expired, committed, and terminal-failed receipts
  do not consume pending quota.

- Existing committed replay receipts still return their cached response before
  current pending quota checks.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::replay::receipt --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.32 - 2026-06-06 - CLI pending operation log

### Summary

- Added write-before-send pending operation logging for generated manual ICP
  refill operation IDs.

- This implements the high-value CLI persistence requirement in
  `docs/design/0.61-replay-protection/0.61-design.md` for the current
  `canic cycles convert` replay-protected CLI path.

### Changed

- Live `canic cycles convert` canister mode now writes generated operation IDs
  to the project-local pending operation log before sending the canister update:

```text
.canic/operations/pending.json
```

- A matching `pending_send` record is reused when the operator retries the same
  generated-ID command after a CLI crash or uncertain send.

- A successful CLI return marks the matching pending record `completed`; failed
  or interrupted sends leave the record pending for safe retry diagnostics.

- Non-JSON output now reports `operation_id_source=pending_log` when a local
  pending record supplies the operation ID. JSON output shape remains
  unchanged and continues to include `operation_id`.

### Notes

- No CLI commands, flags, or JSON report shapes change in this patch.

- Supplying `--operation-id <hex64>` bypasses the generated-ID pending log and
  continues to use the operator-provided ID directly.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-cli cycles::convert --lib -- --nocapture
cargo clippy -p canic-cli --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.31 - 2026-06-06 - CLI generated operation ID visibility

### Summary

- Made CLI-generated replay operation IDs visible for manual ICP refill
  triggers.

- This closes the operator usability gap where non-JSON `canic cycles convert`
  output carried the generated ID only inside the rendered Candid payload or
  JSON report.

### Changed

- `canic cycles convert` canister mode now tracks whether the replay
  `operation_id` was provided by the operator or generated by the CLI.

- When the CLI generates an operation ID and output is not JSON, dry-run output
  and live output before the endpoint call include:

```text
operation_id=<hex64>
operation_id_source=generated
```

- JSON output is unchanged and continues to include `operation_id`.

- Removed the global `used_underscore_binding` Clippy allow while keeping
  `missing_panics_doc` allowed.

- Renamed macro-visible test canister endpoint arguments so delegated-token and
  internal-call payloads use normal parameter names and are explicitly
  discarded in the function body.

### Notes

- No CLI commands, flags, or JSON report shapes change in this patch.

- Supplying `--operation-id <hex64>` continues to suppress the generated-ID
  notice.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-cli cycles::convert --lib -- --nocapture
cargo clippy -p delegation_signer_stub -p project_hub_stub -p project_instance_stub -p runtime_probe --all-targets --all-features -- -D warnings
cargo clippy -p canic-cli --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.30 - 2026-06-06 - Operation ID hard-cut error code

### Summary

- Normalized the public hard-cut error for replay-sensitive requests that omit
  their client operation ID.

- This pins the `OperationIdRequired` boundary described by
  `docs/design/0.61-replay-protection/0.61-design.md` instead of returning a
  generic invalid-input code.

### Changed

- Added public `ErrorCode::OperationIdRequired` and an `Error` helper with the
  stable message `operation_id is required for this command`.

- Delegation-proof replay metadata, delegated-token issue/mint replay
  metadata, pool `CreateEmpty` replay metadata, and root capability
  `MissingReplayMetadata` errors now use `OperationIdRequired` when metadata is
  absent.

- Zero or oversized replay TTL values continue to return `InvalidInput`; replay
  conflicts continue to return `Conflict`.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core workflow::rpc --lib -- --nocapture
```

## 0.61.29 - 2026-06-06 - Replay receipt stable-shape guards

### Summary

- Added stable replay receipt coverage for the upgrade-shape requirements in
  `docs/design/0.61-replay-protection/0.61-design.md`.

- The tests exercise the stable CBOR record boundary used by replay receipts
  rather than only the in-memory receipt model.

### Changed

- Added coverage proving committed replay receipts preserve status, response
  schema, response bytes, and recorded external-effect data through stable
  record round-trip.

- Added coverage proving pending `Reserved` receipts and
  `RecoveryRequired(ExternalEffectStatusUnknown)` receipts preserve their
  status and effect metadata through stable record round-trip.

- Added coverage proving unsupported replay receipt schema versions return a
  controlled decode error instead of panicking or being accepted.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core storage::stable::replay --lib -- --nocapture
```

## 0.61.28 - 2026-06-06 - Delegated-auth hard-cut source guard

### Summary

- Added executable guard coverage for the delegated-auth hard cut from
  `docs/design/0.61-replay-protection/0.61-design.md`.

- The guard scans live `canic-core` runtime source, not historical docs, so old
  verifier-local token-use replay machinery cannot be reintroduced unnoticed.

### Changed

- Added an integration test that fails if removed delegated-token update
  consumption symbols return under `crates/canic-core/src`.

- The guard also fails if the removed stable `auth/token_uses.rs` module name or
  module declaration reappears.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core --test delegated_auth_hard_cut_guard -- --nocapture
```

## 0.61.27 - 2026-06-06 - Delegated-token mint replay decision coverage

### Summary

- Added deterministic auth API coverage for the delegated-token mint replay
  decisions required by `docs/design/0.61-replay-protection/0.61-design.md`.

- The tests focus on the shared replay receipt boundary so mint replay
  behavior can be proven without fake threshold-ECDSA adapter injection.

### Changed

- Added mint replay tests proving committed `auth.mint_token.v1` receipts
  decode and return the cached `DelegatedToken` response.

- Added mint replay tests proving the same operation ID rejects different
  actors and different authoritative mint payloads.

- Added mint replay tests proving an in-progress duplicate maps to a public
  conflict while the stored receipt remains `Reserved` with no external effect
  recorded.

- Extracted token-signing cost guard request construction into one `AuthApi`
  helper so production code and tests share the same command kind, quota
  subject, payer, quota window, and cycle reserve shape.

- Added signing quota coverage proving the delegated-token mint signing guard
  can reject a fresh signing operation before any signing adapter is reachable.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core api::auth --lib -- --nocapture
```

## 0.61.26 - 2026-06-06 - Delegated-token mint wrapper manifest coverage

### Summary

- Closed the delegated-token mint wrapper manifest gap from
  `docs/design/0.61-replay-protection/0.61-design.md`.

- Fleet/test `*_issue_token` wrappers that call `AuthApi::mint_token` are now
  explicit replay-policy entries.

### Changed

- Added `signer_issue_token` and `user_shard_issue_token` to
  `ENDPOINT_REPLAY_POLICY_MANIFEST` as implemented
  `ReplayProtected(auth.mint_token.v1)` update endpoints.

- Both entries declare `CostClass::ThresholdEcdsaSign`, signing quota policy,
  and signing cycle-reserve policy.

- Added a replay-policy regression test that scans `canisters/` and `fleets/`
  Rust sources for `#[canic_update]` functions calling `AuthApi::mint_token`
  and fails if a wrapper is missing from the manifest.

### Notes

- This is manifest/test coverage for wrappers that already delegate to the
  replay/cost-guarded mint path landed in `0.61.25`.

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
```

## 0.61.25 - 2026-06-06 - Delegated-token mint replay guard

### Summary

- Started delegated-token mint replay hardening from
  `docs/design/0.61-replay-protection/0.61-design.md`.

- Public delegated-token issue and mint requests now require caller-provided
  replay metadata instead of relying on token nonces or server-generated
  operation IDs.

### Changed

- Added optional replay metadata to `DelegatedTokenIssueRequest` and
  `DelegatedTokenMintRequest`; the public API rejects missing or invalid
  metadata before token signing.

- `AuthApi::issue_token` now reserves a shared receipt with command kind
  `auth.issue_token.v1`, hashes the authoritative proof/token payload without
  metadata, and returns committed `DelegatedToken` responses on duplicate
  replays.

- `AuthApi::mint_token` now reserves a shared receipt with command kind
  `auth.mint_token.v1`, requests the root proof with the same operation
  metadata, and commits the final shard token response under the mint command.

- Shard token signing now uses a prepared-token boundary. Callers must reserve
  a `CostGuardPermit`, mark `ThresholdEcdsaSign(DelegatedToken)`, and then sign
  the prepared token through `sign_prepared_delegated_token`.

- Active auth contract and recurring audit docs now describe delegated-token
  verification as TTL-bounded bearer-token verification; replay-sensitive
  mutations must use domain operation receipts.

- The 0.61 design now requires `Topic::Auth` logs for delegated-token replay
  reservations, committed replay returns, blocked replay decisions, signing
  effect marks, recovery-required signing failures, and response commits. The
  live mint/issue path emits those logs without token, proof, signature, or
  receipt response bytes.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core ops::auth --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-testing-internal --lib -- --nocapture
cargo check -p canister_user_shard -p delegation_signer_stub -p delegation_root_stub -p sharding_root_stub
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
bash scripts/ci/run-auth-trust-chain-guards.sh
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.24 - 2026-06-06 - Root ProvisionCanister deployment guard

### Summary

- Graduated root capability `ProvisionCanister` from command-level release
  blocker to implemented replay-protected management-deployment behavior.

- `canic_response_capability_v1` is now marked implemented because every root
  capability command in the command manifest has replay policy coverage.

### Changed

- `ProvisionCanister` execution now resolves the parent, checks that the parent
  is registered, and reads the configured initial-cycle target before crossing
  the replay external-effect boundary.

- Fresh provisioning now reserves a `CostGuardPermit` with
  `CostClass::ManagementDeployment` before lifecycle create/install work.

- The guard uses command kind `root.provision.v1`, the requesting caller as
  quota subject, the root canister as payer, a 60-second quota window, max 10
  operations per window, the configured initial-cycle amount as the cycle
  reservation, and a 1 TC minimum remaining cycle balance.

- The provisioning path now marks
  `ExternalEffectDescriptor::ManagementCreateCanister` before pool allocation,
  management create, pool top-up, controller update, install, registry, or
  topology propagation work can proceed.

- Post-boundary provisioning failures recover the in-flight cycle reservation
  and preserve the replay receipt as
  `RecoveryRequired(ExternalEffectStatusUnknown)`.

- Successful provisioning completes the deployment guard and returns the new
  canister principal through the existing root replay commit flow.

- `ROOT_CAPABILITY_COMMAND_REPLAY_POLICY_MANIFEST` now marks
  `ProvisionCanister` implemented with deployment quota/reserve policy, and the
  endpoint-level blocker regression test now expects no endpoint release
  blockers.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.23 - 2026-06-06 - Root RequestCycles value-transfer guard

### Summary

- Graduated root capability `RequestCycles` from command-level release blocker
  to implemented replay-protected value-transfer behavior.

- `ProvisionCanister` is now the only remaining root capability command
  blocker, so `canic_response_capability_v1` remains blocked only through that
  command.

### Changed

- Added `MgmtOps::deposit_cycles_with_permit` so cycles transfer execution has
  the same permit-bearing boundary shape as guarded management creation.

- `RequestCycles` execution now reserves a `CostGuardPermit` with
  `CostClass::ValueTransfer` after authorization and before the
  `deposit_cycles` management await.

- The guard uses command kind `root.request_cycles.v1`, the requesting child as
  quota subject, the root canister as payer, a 60-second quota window, max 60
  operations per window, the approved transfer amount as the cycle reservation,
  and a 1_000_000_000 minimum remaining cycle balance.

- The cycles transfer path now marks
  `ExternalEffectDescriptor::ManagementCall { method: "deposit_cycles" }`
  before the management await.

- If the transfer await returns an infrastructure error, the cost guard is
  recovered and the replay receipt is preserved as
  `RecoveryRequired(ExternalEffectStatusUnknown)`.

- Successful transfers record the funding ledger, complete the value-transfer
  guard, and then commit the cached cycles response through the existing replay
  flow.

- `ROOT_CAPABILITY_COMMAND_REPLAY_POLICY_MANIFEST` now marks `RequestCycles`
  implemented with value-transfer quota/reserve policy. The command-level
  blocker regression test now leaves only `ProvisionCanister`.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.22 - 2026-06-06 - Root capability command manifest split

### Summary

- Split the remaining root capability RPC blocker into a per-command replay
  policy manifest.

- `canic_response_capability_v1` remains an endpoint release blocker, but the
  remaining work is now narrowed to `ProvisionCanister` and `RequestCycles`.

### Changed

- Added `RootCapabilityCommandReplayPolicy` and
  `ROOT_CAPABILITY_COMMAND_REPLAY_POLICY_MANIFEST`.

- `canic_response_capability_v1` is now represented as
  `CommandDispatch(root.capability_rpc.v1, root.capability.command_manifest.v1)`
  instead of a single replay-protected deployment command.

- Root capability command variants are now classified independently:
  `UpgradeCanister`, `RecycleCanister`, `IssueRoleAttestation`, and
  `IssueInternalInvocationProof` are implemented replay-protected commands.

- `ProvisionCanister` remains blocked until root provisioning records a
  management-deployment cost barrier and external-effect/recovery boundary
  before create/install work can produce a second durable result.

- `RequestCycles` remains blocked until root cycles funding records a
  value-transfer cost barrier and external-effect/recovery boundary before the
  cycles transfer await.

- Manifest tests now fail if a `RootCapabilityCommand` variant is added without
  a replay-policy entry, if costed root capability commands omit quota/reserve
  policy, or if the command-level blocker list drifts.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.21 - 2026-06-06 - ICP refill value-transfer guard graduation

### Summary

- Graduated `canic_icp_refill` from release blocker to implemented
  replay-protected value-transfer behavior.

- The only remaining endpoint release blocker is
  `canic_response_capability_v1`.

### Changed

- ICP refill now reserves a `CostGuardPermit` with `CostClass::ValueTransfer`
  before the first ledger transfer or CMC notify external effect for a fresh
  replay attempt.

- The guard uses command kind `icp.refill.v1`, the replay actor as quota
  subject, the current canister as payer, a 60-second quota window, max 60
  operations per window, and a 1_000_000_000 cycle reservation/minimum.

- Terminal committed refill responses complete the value-transfer guard.

- Resumable outcomes, recovery-required outcomes, and response-commit failures
  recover the guard so quota is counted while the in-flight cycle reservation
  is released.

- `ENDPOINT_REPLAY_POLICY_MANIFEST` now marks `canic_icp_refill` as
  implemented with value-transfer quota/reserve policy, and blocker tests now
  leave only `canic_response_capability_v1`.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.20 - 2026-06-06 - ICP refill external-effect receipt marking

### Summary

- Marked ICP refill ledger transfer and CMC notify boundaries in shared replay
  receipts before the external awaits.

- `canic_icp_refill` remains a release blocker until value-transfer quota and
  reserve enforcement is wired into the refill path.

### Changed

- Fresh manual refill execution now carries the shared `ReplayReceiptToken`
  through record advancement, ledger transfer, and CMC notify.

- Ledger transfer now marks `ExternalEffectDescriptor::IcpTransfer` before the
  `icrc1_transfer` await.

- CMC `notify_top_up` now marks `ExternalEffectDescriptor::ManagementCall`
  before the CMC notify await.

- Transport or infrastructure failures after either marked external-effect
  boundary now preserve the shared receipt as
  `RecoveryRequired(ExternalEffectStatusUnknown)`.

- Known retryable application-level outcomes, such as retryable ledger or CMC
  responses, still leave the refill business record resumable and discard the
  temporary uncommitted receipt.

- Added Canic runtime logs under `Topic::Cycles` for fresh receipt
  reservations, committed replay returns, replay conflicts, external-effect
  marking, terminal commits, resumable receipt aborts, and recovery-required
  outcomes.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This patch intentionally does not graduate `canic_icp_refill` in the replay
  manifest. The remaining manifest blocker is value-transfer cost policy
  enforcement for the ICP refill path.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.19 - 2026-06-06 - ICP refill shared replay reservation decisions

### Summary

- Wired fresh ICP refill execution through shared replay receipt reservation
  decisions.

- `canic_icp_refill` remains a release blocker until refill transfer and notify
  effects are marked in the shared receipt before external calls.

### Changed

- `execute_manual_refill` now reserves a shared replay receipt before creating
  or advancing a fresh refill record.

- Committed ICP refill replay receipts now decode and return the cached
  `IcpRefillResponse` instead of re-entering the refill transfer/notify path.

- Actor mismatch, payload mismatch, in-progress, expired, recovery-required,
  and terminal-failed shared receipt decisions now map to public conflict
  errors for ICP refill requests.

- Terminal refill records commit their response into the shared replay receipt.

- Resumable refill records, such as requested transfer state or retryable
  transfer/notify failures, abort the temporary shared receipt so existing
  retry behavior remains unchanged until external-effect marking and recovery
  semantics are added.

- The 0.61 replay-protection design now requires Canic runtime logs for ICP
  refill replay reservation decisions, replay conflicts, committed replay
  returns, resumable receipt aborts, transfer/notify external-effect marking,
  terminal commits, retryable outcomes, and recovery-required outcomes.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This patch intentionally does not graduate `canic_icp_refill` in the replay
  manifest. Remaining work is to mark ledger transfer and CMC notify effects
  in flight before the external calls, preserve uncertain outcomes as recovery
  required, and then reclassify the endpoint when the full replay lifecycle is
  covered.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
```

## 0.61.18 - 2026-06-06 - ICP refill shared replay identity foundation

### Summary

- Started the ICP refill migration toward shared replay receipts by defining
  the refill replay identity inputs and constructing the shared reserve input
  in the manual-refill path.

- `canic_icp_refill` remains a release blocker until refill execution reserves,
  marks, and commits shared replay receipts around transfer/notify progress.

### Changed

- Added the static ICP refill replay command kind `icp.refill.v1`.

- Added helpers that map `IcpRefillRequest.operation_id` into the shared
  `OperationId` type, derive the direct-caller replay actor, and build the
  canonical refill payload hash with `ReplayPayloadHasher`.

- `execute_manual_refill` now builds a `ReplayReceiptReserveInput` from those
  shared identity fields and uses its `OperationId` bytes for the existing
  refill record lookup/create path.

- The refill payload hash now has tests proving it excludes `operation_id`
  while binding replay actor, source canister, source subaccount, target
  canister, amount, and refill mode.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This patch intentionally does not graduate `canic_icp_refill` in the replay
  manifest. The remaining work is to wire live refill execution onto shared
  receipt reservation decisions, external-effect marking, response/terminal
  commits, and retry decisions while preserving the refill business record for
  transfer and notify recovery metadata.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::ic::icp_refill --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.17 - 2026-06-06 - Canister upgrade manifest graduation

### Summary

- `canic_canister_upgrade` is now recorded as an implemented
  response-idempotent endpoint.

- The remaining endpoint release blockers are `canic_icp_refill` and
  `canic_response_capability_v1`.

### Changed

- Added a costed response-idempotent replay-policy helper for endpoints that do
  not require caller-provided operation IDs but can still reach a costed
  management or transfer path.

- `ENDPOINT_REPLAY_POLICY_MANIFEST` now records `canic_canister_upgrade` as
  `ResponseIdempotent(management.canister_upgrade.v1)` with implementation
  status `Implemented`, cost class `ManagementDeployment`, and deployment
  quota/reserve policy.

- Added a manifest regression test proving `canic_canister_upgrade` remains
  implemented, response-idempotent, and tied to deployment quota/reserve
  policy.

- Added upgrade-planning tests proving a matching installed target module hash
  makes repeated upgrade requests no-ops, while missing or different hashes
  still request an upgrade.

- Added upgrade RPC dispatch tests proving `UpgradeCanisterRpc` carries replay
  metadata into the root request DTO and rejects non-upgrade response variants.

- Updated the release-blocker regression test so only `canic_icp_refill` and
  `canic_response_capability_v1` remain pinned as endpoint blockers.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This patch does not change upgrade runtime behavior. It graduates the
  manifest entry based on the existing upgrade planner, which skips the
  management upgrade path once the target canister already reports the approved
  target module hash.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core domain::policy::upgrade --lib -- --nocapture
cargo test -p canic-core ops::rpc::request --lib -- --nocapture
```

## 0.61.16 - 2026-06-06 - Pool admin endpoint manifest graduation

### Summary

- The endpoint-level `canic_pool_admin` replay policy entry is no longer a
  release blocker.

- Pool admin is now represented as an implemented command-dispatch endpoint
  whose safety proof comes from the per-variant pool admin command manifest.

### Changed

- Added `ReplayPolicy::CommandDispatch` so dispatcher endpoints can point to a
  command manifest instead of pretending every command uses one endpoint-level
  replay shape.

- `ENDPOINT_REPLAY_POLICY_MANIFEST` now records `canic_pool_admin` as
  `CommandDispatch(pool.admin.v1, pool.admin.command_manifest.v1)` with
  implementation status `Implemented`.

- Added a regression test proving `canic_pool_admin` remains an implemented
  command-dispatch endpoint with deployment quota/reserve policy.

- Added a regression test proving the pool admin endpoint cannot remain
  implemented if any `PoolAdminCommand` manifest entry regresses to
  `ReleaseBlocker`.

- Added a release-blocker regression test that pins the remaining endpoint
  slices to `canic_canister_upgrade`, `canic_icp_refill`, and
  `canic_response_capability_v1`.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This patch does not change pool runtime behavior. It graduates the
  endpoint-level manifest entry now that `CreateEmpty`, `Recycle`,
  `ImportImmediate`, and `ImportQueued` each have implemented command-level
  replay classifications and tests.

- The remaining manifest blockers are larger runtime slices and should stay
  separate: canister upgrade, ICP refill, and root capability RPC.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
```

## 0.61.15 - 2026-06-06 - Pool Recycle replay proof

### Summary

- Pool `Recycle` is now recorded as an implemented response-idempotent pool
  admin command.

- Duplicate recycle requests stop at the existing pending-reset or ready pool
  entry before the management reset path.

### Changed

- `POOL_ADMIN_COMMAND_REPLAY_POLICY_MANIFEST` now marks `Recycle` as
  `ResponseIdempotent(pool.recycle.ensure_v1)` with implementation status
  `Implemented`.

- Recycle now removes the target from the subnet registry and records a
  metadata-preserving pending-reset pool entry before calling the management
  reset path.

- Successful recycle preserves the original registry role, parent, and module
  hash when the canister becomes ready in the pool.

- Failed immediate reset leaves the pending-reset pool entry in place and
  schedules pool reset recovery, so retries do not re-enter the destructive
  reset path for the same canister.

- Added workflow tests proving duplicate recycle detection for pending-reset
  entries and registry metadata preservation during recycle preparation.

- Added a manifest regression test proving `Recycle` remains implemented,
  response-idempotent, and tied to the deployment quota/reserve policy.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
```

## 0.61.14 - 2026-06-06 - Pool ImportImmediate replay proof

### Summary

- Pool `ImportImmediate` is now recorded as an implemented response-idempotent
  pool admin command.

- Duplicate immediate imports stop at the existing pool entry once the first
  request has marked the canister pending reset or ready.

- ICP-backed `canic` commands now check the selected `icp-cli` executable
  before running wrapped ICP operations.

### Changed

- `POOL_ADMIN_COMMAND_REPLAY_POLICY_MANIFEST` now marks `ImportImmediate` as
  `ResponseIdempotent(pool.import_immediate.ensure_v1)` with implementation
  status `Implemented`.

- Added a manifest regression test proving `ImportImmediate` remains
  implemented, response-idempotent, and tied to the deployment quota/reserve
  policy.

- Expanded pool workflow coverage so `ImportImmediate` checks both ready and
  pending-reset pool entries before the reset path. The pending-reset test
  proves duplicate retries keep a single pool record and preserve
  `PendingReset`.

- Added an `icp-cli` compatibility guard in the shared host command runner.
  Supported versions are `>=0.3.0, <0.4.0`; missing or incompatible executables
  now report the discovered version, required range, pinned `0.3.0` install
  hint, and `--icp <path>` override path before any ICP-backed operation runs.

- Routed install ping and release-set staging calls through the guarded ICP
  execution path so those direct command sites cannot bypass compatibility
  checks.

- `canic medic` and `canic status` now report ICP CLI compatibility instead of
  only checking whether `icp --version` returns output.

- Backup, cycles, replica, snapshot, token, installed-deployment, and restore
  error mapping now preserve the compatibility diagnostic instead of degrading
  it into an unrelated subcommand failure.

- The CI prerequisite check now rejects an installed `icp` that does not match
  the pinned `CANIC_ICP_CLI_VERSION`, defaulting to `0.3.0`.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch. The visible CLI behavior change is earlier failure with a specific
  `icp-cli` compatibility diagnostic for ICP-backed commands.

- Pool `Recycle` remains the only explicit pool admin variant release blocker.
  It can still cross a management reset before removing the subnet-registry
  entry, so its repeat behavior needs a separate proof or replay receipt slice.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-host --lib icp::tests
cargo test -p canic-host --lib
cargo test -p canic-cli --lib
cargo test -p canic --test changelog_governance
cargo fmt --all -- --check
git diff --check
cargo clippy -p canic-host --lib -- -D warnings
cargo clippy -p canic-cli --lib -- -D warnings
bash -n scripts/ci/require_icp.sh scripts/ci/install-icp-cli.sh
```

## 0.61.13 - 2026-06-05 - Attestation replay policy and ICP CLI 0.3 cleanup

### Summary

- `canic_attestation_key_set` is now recorded as an implemented
  snapshot-convergent endpoint in the replay policy manifest.

- Local dev setup and CI now install `icp` from the official `icp-cli` `0.3.0`
  installer instead of the legacy npm `@icp-sdk/icp-cli` package.

### Changed

- Reclassified `canic_attestation_key_set` from a
  `ReplayProtected(auth.attestation_key_set.v1)` release blocker with signing
  quota/reserve policy to `SnapshotConvergent(auth.attestation_key_set.v1)`
  implemented with cost class `None`.

- Added a manifest regression test proving the endpoint does not declare
  signing quota or cycle-reserve policy.

- Added `CANIC_ICP_CLI_VERSION`, defaulting to `0.3.0`, at the Makefile and
  dev-installer boundary.

- Added `scripts/ci/install-icp-cli.sh` as the shared installer helper for the
  official `icp-cli` GitHub release installer. The helper installs into
  Cargo's bin directory and registers that directory through `$GITHUB_PATH`
  when running in GitHub Actions.

- `make install-dev` and `make update-dev` now route `icp` installation through
  the shared installer helper.

- The shared dev installer still installs `ic-wasm` with npm, but it no longer
  installs npm `@icp-sdk/icp-cli`.

- The shared dev installer removes the legacy user-local npm `icp` wrapper when
  it points at `@icp-sdk/icp-cli`, preventing `$HOME/.local/bin/icp` from
  shadowing `$HOME/.cargo/bin/icp`.

- CI now uses the same official `icp` installer and keeps npm scoped to
  `ic-wasm`.

- `IcpCli::command()` and `IcpCli::command_in(...)` now pass
  `--project-root-override <path>` whenever Canic already has an explicit ICP
  project root. The subprocess still runs with that directory as `current_dir`,
  but ICP CLI 0.3 also receives the root through its native common parameter.

- Local replica command construction now honors an `IcpCli` environment by
  rendering `icp network <action> -e <environment>`. The default no-target
  behavior remains `icp network <action> local`, and direct network contexts
  still render the network name argument.

- Added regression tests for rooted command rendering and environment-backed
  local replica command rendering. Updated the install-root command argument
  assertion to include the explicit project-root override.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The endpoint can refresh cached root attestation public-key material, but it
  uses the ECDSA public-key query path rather than threshold signing. It does
  not issue proof material and does not need operation-id replay receipts.

- The checked `icp 0.3.0` surfaces still expose the command families Canic
  wraps today: `canister call/status/top-up/snapshot`, `network
  start/status/stop/ping`, `project show`, `environment list`, `cycles`, and
  `token`.

- Root-owned legacy `icp` binaries outside the user-local cleanup path must be
  removed separately by the machine owner if they exist on another workstation.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
bash -n scripts/dev/install_dev.sh scripts/ci/require_icp.sh scripts/ci/install-icp-cli.sh
bash scripts/ci/install-icp-cli.sh 0.3.0
make -n install-dev update-dev
actionlint .github/workflows/ci.yml
icp --version
icp canister call --help
icp canister status --help
icp canister top-up --help
icp canister snapshot create --help
icp canister snapshot download --help
icp canister snapshot upload --help
icp canister snapshot restore --help
icp network start --help
icp network status --help
icp --project-root-override /home/adam/projects/canic environment list
icp --project-root-override /home/adam/projects/canic project show
ic-wasm --version
which -a icp
cargo test -p canic-host icp -- --nocapture
cargo test -p canic-cli --lib icp -- --nocapture
cargo clippy -p canic-host --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
cargo test -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.61.12 - 2026-06-05 - Canister status read-only replay policy

### Summary

- `canic_canister_status` is now recorded as an implemented update-shaped
  read-only endpoint in the replay policy manifest.

### Changed

- Reclassified `canic_canister_status` from
  `ReplayProtected(management.canister_status.v1)` release blocker with
  deployment quota/reserve policy to `QueryOrReadOnly` implemented with cost
  class `None`.

- Added a manifest regression test proving `canic_canister_status` remains
  read-only and does not declare quota or cycle-reserve policy.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The endpoint is update-shaped because it reads management-canister status,
  but it does not mutate Canic state and does not perform deployment, signing,
  value-transfer, or publication effects.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.11 - 2026-06-05 - Pool ImportQueued convergence proof

### Summary

- Pool `ImportQueued` is now recorded as an implemented snapshot-convergent
  pool admin command.

- Repeated queued imports converge on one pending-reset pool entry per
  canister and do not duplicate queue records.

### Changed

- `POOL_ADMIN_COMMAND_REPLAY_POLICY_MANIFEST` now marks `ImportQueued` as
  `SnapshotConvergent(pool.import_queued.ensure_v1)` with implementation
  status `Implemented` and cost class `None`.

- Added a focused workflow test proving duplicate PIDs in the same
  `ImportQueued` request and a repeated request leave exactly one pool entry.
  The first request records one add and one skip; the repeated request records
  all skips.

- Split the queued-import implementation behind an internal authorized helper
  so production still performs authorization, admissibility checks, metrics,
  scheduling, and IC timestamps, while the unit test can exercise the pure
  state convergence without native-test IC calls.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- Pool `Recycle` and `ImportImmediate` remain explicit release blockers because
  they can still cross management reset effects.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.10 - 2026-06-05 - Root auth-material replay recovery

### Summary

- Root role-attestation and internal-invocation proof issuance now preserve
  replay receipts when a threshold-ECDSA signing boundary has been crossed and
  the result becomes uncertain.

- `canic_request_role_attestation` and
  `canic_request_internal_invocation_proof` are now marked implemented in the
  replay policy manifest.

### Changed

- Split auth-material signing into prepare/sign phases. Preparation resolves
  the attestation key, warms the cached public key, and computes the message
  hash before cost reservation and before the external signing boundary.

- Prepared role-attestation and internal-invocation proof signing still require
  `CostGuardPermit`.

- Root auth-material execution now marks a `ThresholdEcdsaSign` external effect
  on the shared replay receipt immediately before guarded signing.

- Guarded signing failures recover the cycle reservation and mark the replay
  receipt `RecoveryRequired(ExternalEffectStatusUnknown)`.

- Cost-completion failures and post-execution replay commit failures mark the
  replay receipt `RecoveryRequired(ResponseCommitFailed)`.

- Generic root replay abort now removes only receipts that are still
  `Reserved`. Receipts already marked in-flight or recovery-required survive
  the generic execution-error path.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The remaining replay manifest blockers are outside these two auth-material
  signing endpoints.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core ops::auth --lib -- --nocapture
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.9 - 2026-06-05 - Root auth-material signing cost guard

### Summary

- Root role-attestation and internal-invocation proof signing now require a
  `CostGuardPermit`.

- Fresh root auth-material signing reserves signing quota and an in-flight
  cycle budget before threshold ECDSA.

### Changed

- `AuthOps::sign_role_attestation` and
  `AuthOps::sign_internal_invocation_proof` now require a `CostGuardPermit`,
  preventing those root auth-material signing paths from being called without
  crossing the shared cost guard boundary.

- `RootResponseWorkflow` execution reserves `ThresholdEcdsaSign` cost guard
  slots for `root.issue_role_attestation.v1` and
  `root.issue_internal_invocation_proof.v1`.

- Signing failures recover the cycle reservation; successful signatures
  complete the quota and reservation.

- Added focused tests for low-cycle reserve rejection and successful
  auth-material signing reservation completion.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The role-attestation and internal-invocation proof manifest entries remain
  release blockers because generic root capability execution still aborts fresh
  replay on execution error. A later slice must mark or recover post-ECDSA
  failures before those endpoints are fully implemented.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::rpc::request::handler::execute --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core ops::auth --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.8 - 2026-06-05 - Root replay cross-variant guard

### Summary

- Tightened shared root replay receipt handling so a caller cannot reuse the
  same request id across different root capability command kinds.

- Root replay now checks for existing non-expired receipts with the same replay
  actor and operation id before evaluating the current command kind. If such a
  receipt belongs to another command kind, the new request is rejected as a
  replay conflict instead of being treated as a fresh operation.

### Changed

- Added a replay receipt store query for receipts matching actor and operation
  id while excluding the current command kind.

- The root replay workflow uses that query before normal same-command replay
  evaluation. Live cross-command matches return duplicate conflict; expired-only
  cross-command matches preserve the expired replay decision.

- Added a non-reserving existing-replay probe for auth-first root capability
  paths, so those paths can return cached/conflicting replay state before
  authorization while still leaving truly fresh requests unreserved until after
  authorization succeeds.

- Added tests for cross-variant root request-id reuse and for the stable-store
  cross-command receipt query.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- This strengthens the root capability replay invariant introduced earlier in
  0.61 without changing committed same-command replay behavior.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::replay::guard --lib -- --nocapture
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core storage::stable::replay --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.7 - 2026-06-05 - Pool admin variant replay inventory

### Summary

- Added command-level replay policy coverage for every `PoolAdminCommand`
  variant under the `canic_pool_admin` endpoint.

- The new pool admin command manifest records the chosen replay class,
  implementation status, cost class, quota policy, and cycle-reserve policy
  for `CreateEmpty`, `Recycle`, `ImportImmediate`, and `ImportQueued`.

- `ImportImmediate` now returns success before admissibility probing or
  management reset when the target canister is already present in the pool.
  This makes a successful immediate import retry harmless for the common
  already-committed case.

### Changed

- Added `POOL_ADMIN_COMMAND_REPLAY_POLICY_MANIFEST` to
  `canic-core::replay_policy`.

- `CreateEmpty` is recorded as implemented and replay-protected with
  `pool.create_empty.v1`.

- `Recycle`, `ImportImmediate`, and `ImportQueued` are recorded with explicit
  response-idempotent ensure-style command kinds, but remain release blockers
  until their replay receipts or stronger idempotence guards are implemented.

- Added tests that parse `PoolAdminCommand` from the DTO source and fail when a
  variant is added without a command-level replay policy entry.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- `canic_pool_admin` remains an endpoint-level release blocker. This patch
  narrows the remaining work by making the non-CreateEmpty variant status
  explicit and test-enforced.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.6 - 2026-06-05 - Pool CreateEmpty replay guard

### Summary

- Continued Slice D from
  `docs/design/0.61-replay-protection/0.61-design.md` by moving
  `PoolAdminCommand::CreateEmpty` onto shared replay receipts and deployment
  cost guards.

- `PoolAdminCommand::CreateEmpty` now carries `CreateEmptyPoolRequest`
  metadata. Direct pool create-empty calls must include a 32-byte request id
  and bounded replay TTL before Canic will reserve or execute the operation.

- Fresh create-empty requests reserve a `pool.create_empty.v1` replay receipt,
  reserve deployment quota and an in-flight cycle budget, mark the management
  `create_canister` effect in flight, create the pool canister through a
  `CostGuardPermit`, register the created canister, and commit the created
  principal as the replay response.

- Duplicate requests with the same request id, caller, and payload return the
  originally created pool canister principal without calling management
  `create_canister` again.

### Changed

- Added `CreateEmptyPoolRequest` to the pool DTO boundary with optional
  `RootRequestMetadata`. The workflow rejects missing metadata, zero TTL, and
  TTL values above the 300-second pool replay maximum.

- Added the explicit replay command kind `pool.create_empty.v1`.

- Pool create-empty payload hashing binds the command kind and direct caller,
  while the empty create payload intentionally excludes replay metadata.

- Management create failures after the external-effect boundary now recover
  the cycle reservation and mark the replay receipt `RecoveryRequired` with
  `ExternalEffectStatusUnknown`, so a retry cannot silently create another
  canister for the same operation id.

- Response-encoding failures after management create mark the receipt
  `RecoveryRequired` with `ResponseCommitFailed` and recover the cycle
  reservation before returning the workflow error.

- Added `MgmtOps::create_canister_with_permit` for guarded management create
  paths. The existing ungated management adapter remains for surfaces that have
  not yet moved onto deployment cost guards.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The replay policy manifest still leaves `canic_pool_admin` as a release
  blocker because the endpoint also carries non-CreateEmpty variants. This
  patch only claims the `CreateEmpty` command path.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core workflow::pool --lib -- --nocapture
cargo test -p canic-core ops::cost_guard --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.5 - 2026-06-05 - Delegation signing cost guard

### Summary

- Added the first shared cost-guard foundation for 0.61 costed operations.

- Root delegation-proof signing now reserves signing quota and a durable
  in-flight cycle budget before crossing the threshold ECDSA signing boundary.

- `AuthOps::sign_prepared_delegation_proof` now requires a `CostGuardPermit`,
  so the root signing path cannot call threshold ECDSA through that adapter
  without first crossing the guard.

- The replay policy manifest now marks `canic_request_delegation` as
  implemented. Other costed endpoint entries remain release blockers until
  their replay/cost-guard paths are wired.

### Changed

- Added `ops::cost_guard`, backed by the existing durable intent store.
  The guard records:
  - a per-command, per-subject quota reservation in a deterministic time
    bucket;
  - a per-cost-class, per-payer cycle reservation that counts outstanding
    in-flight work against the local liquid-cycle balance.

- Fresh delegation proof requests reserve cost state only after replay preflight
  returns a fresh receipt. Committed replay responses still return the stored
  proof without consuming signing quota or checking current cycle reserve.

- Delegation proof signing uses a 60-second quota window, allows 60 fresh
  signing operations per shard/caller per window, and reserves
  `1_000_000_000` cycles while requiring `1_000_000_000` cycles to remain
  after the reservation.

- Signing failures after the cost barrier consume the signing quota and release
  the in-flight cycle reservation through recovery handling. Cheap validation
  failures before the cost barrier abort the replay receipt and do not consume
  quota.

### Notes

- No CLI commands, flags, output columns, or JSON report shapes change in this
  patch.

- The cost-guard module is intentionally small in this slice. It establishes
  the unforgeable permit and durable quota/reserve mechanics needed by later
  deployment and token-signing guard work.

- Pool `CreateEmpty`, root role/internal proof signing, delegated-token minting,
  and broader deployment/value-transfer cost guards remain later 0.61 work.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::cost_guard --lib -- --nocapture
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
```

## 0.61.4 - 2026-06-04 - Delegation proof replay receipts

### Summary

- Continued the 0.61 replay-protection line by moving root delegation-proof
  issuance onto shared replay receipts.

- `DelegationProofIssueRequest` now carries optional root replay metadata.
  Shard-side delegation requests populate it before calling root, while root
  rejects direct issuance requests that omit metadata or provide an invalid
  replay TTL.

- Root delegation-proof issuance now reserves a command-kind-scoped receipt
  before threshold ECDSA signing, returns committed proof bytes for duplicate
  request ids, and reports conflicts for actor, payload, in-flight, expired, or
  recovery-required receipts.

- The delegated-proof signing path is split into prepare and sign phases so the
  API can mark the threshold ECDSA external effect in flight before crossing
  the signing boundary.

- Added cached NNS data-center inspection:
  `canic nns data-center list`,
  `canic nns data-center info <data-center-prefix>`, and
  `canic nns data-center refresh`.

- Data-center rows are derived from the shared mainnet registry relation
  inventory used by nodes and node operators. The adapter follows subnet
  membership, node records, node-operator records, and
  `data_center_record_<id>` values once, then projects data-center ownership,
  region, GPS, node-operator counts, node-provider counts, and assigned-node
  counts from that relation graph.

### Changed

- Added the explicit replay command kind
  `auth.issue_delegation_proof.v1`.

- Delegation replay payload hashing binds the root caller, requested shard,
  scopes, audience, and certificate TTL, while intentionally excluding replay
  metadata from the authoritative payload hash.

- Committed delegation proofs are Candid-encoded with response schema version
  `1` before being stored in the shared receipt map. Duplicate requests with
  the same request id, actor, and payload decode and return the stored proof.

- Shared receipt terminal transitions now preserve any already-recorded
  external-effect descriptor when a receipt moves from in-flight to committed,
  terminal-failed, or recovery-required.

- The legacy direct `AuthOps::sign_delegation_proof` wrapper was removed.
  Root issuance now uses the prepare/sign split directly.

- Renamed the internal NNS node/operator registry traversal to a shared
  relation inventory so node, node-operator, node-provider, and data-center
  views do not repeat registry graph walking.

- Kept data-center inspection on the same NNS ownership boundary:
  `canic-ic-registry` owns registry traversal and protobuf decoding,
  `canic-host` owns cache/report/text shaping, and `canic-cli` owns the Clap
  surface.

### CLI Surface

```text
canic nns data-center refresh
canic nns data-center list
canic nns data-center list --verbose
canic nns data-center info <data-center-prefix>
canic nns data-center list --format json
```

Data-center JSON output uses schema version `1` reports with registry
provenance and per-data-center fields:

```text
data_center_id
region
owner
latitude
longitude
node_operator_count
node_provider_count
node_count
```

### Notes

- `canic nns data-center list` and `info` are cache-first and populate
  `.canic/data-center/ic/data-centers.json` on a first cache miss. `refresh`
  force-refreshes through `.canic/data-center/ic/refresh.lock` and atomic cache
  replacement.

- Data-center counts are assigned mainnet subnet nodes, node operators, and
  distinct node providers that reference each data center. The list is derived
  from data centers referenced by active node-operator records; unreferenced
  registry data-center records are not enumerated in this slice.

- This patch does not add the signing cost guard, quota permit, or cycle
  reserve policy. Those remain a later 0.61 blocker before the replay manifest
  can mark delegation-proof issuance fully releasable.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic-ic-registry data_center -- --nocapture
cargo test -p canic-host nns_data_center --lib -- --nocapture
cargo test -p canic-cli --lib nns -- --nocapture
cargo test -p canic-cli --lib command_family_help_returns_ok -- --nocapture
```

## 0.61.3 - 2026-06-04 - Root replay receipt migration

### Summary

- Continued Slice B from
  `docs/design/0.61-replay-protection/0.61-design.md` by moving root RPC replay
  checks, reservations, commits, aborts, capacity checks, and TTL expiry onto
  the shared replay receipt store.

- Added explicit root replay command kinds for the existing root capability
  surfaces:
  `root.provision.v1`, `root.upgrade.v1`, `root.recycle_canister.v1`,
  `root.request_cycles.v1`, `root.issue_role_attestation.v1`, and
  `root.issue_internal_invocation_proof.v1`.

- Extended shared replay receipts with optional expiry so root replay can keep
  its existing TTL behavior while future non-expiring receipt users can leave
  the field unset.

- Added the first broad NNS inspection foundation command:
  `canic nns registry version`.

- The command queries the canonical mainnet NNS registry canister for the
  latest registry version and prints text or JSON output.

- Added cached NNS node-operator inspection:
  `canic nns node-operator list`,
  `canic nns node-operator info <node-operator-prefix>`, and
  `canic nns node-operator refresh`.

- Node-operator rows are derived from mainnet subnet membership,
  `node_record_<node>`, and `node_operator_record_<operator>` registry values.

- Added cached NNS node inspection:
  `canic nns node list`, `canic nns node info <node-prefix>`, and
  `canic nns node refresh`.

- Node rows are derived from mainnet subnet membership, `node_record_<node>`,
  `node_operator_record_<operator>`, and subnet registry values so each row can
  report node, operator, provider, subnet, subnet kind, and data center.

### Changed

- Root replay preflight now prepares shared receipt tokens, checks receipt
  capacity, explicitly reserves fresh receipts, returns committed receipt
  responses for duplicate requests, and aborts reserved receipts on policy or
  execution failure.

- The non-root cycles replay-first path now uses the same shared receipt-backed
  root replay machinery.

- Removed the active legacy `RootReplayOps` and root replay slot-key module.
  The old `RootReplayRecord` binary encoding remains test-only so its historical
  stable shape stays documented, but root replay no longer reads or writes the
  legacy `canic.core.root_replay.v1` map.

- Shared receipt storage now supports removal, active actor counts, and expired
  receipt purging for root replay capacity and TTL semantics.

- Kept the new NNS registry inspection implementation split by ownership
  boundary: `canic-ic-registry` owns the raw NNS registry query, `canic-host`
  owns the report shape and text rendering, and `canic-cli` owns the Clap
  command surface.

- Kept node-operator inspection on the same NNS ownership boundary:
  `canic-ic-registry` owns registry traversal and protobuf decoding,
  `canic-host` owns cache/report/text shaping, and `canic-cli` owns the Clap
  surface.

- Kept node inspection on the same NNS ownership boundary:
  `canic-ic-registry` owns registry traversal and protobuf decoding,
  `canic-host` owns cache/report/text shaping, and `canic-cli` owns the Clap
  surface.

### CLI Surface

```text
canic nns registry version
canic nns registry version --format json
canic nns registry version --source-endpoint https://icp-api.io
canic --network ic nns registry version
canic nns node refresh
canic nns node list
canic nns node list --verbose
canic nns node info <node-prefix>
canic nns node list --format json
canic nns node-operator refresh
canic nns node-operator list
canic nns node-operator list --verbose
canic nns node-operator info <node-operator-prefix>
canic nns node-operator list --format json
```

Text output includes:

```text
network
registry_canister_id
registry_version
fetched_at
source_endpoint
fetched_by
```

JSON output uses `NnsRegistryVersionReport` with schema version `1`.

Node JSON output uses schema version `1` reports with registry provenance and
per-node fields:

```text
node_principal
node_operator_principal
node_provider_principal
subnet_principal
subnet_kind
data_center_id
```

Node-operator JSON output uses schema version `1` reports with registry
provenance and per-operator fields:

```text
node_operator_principal
node_provider_principal
node_allowance
data_center_id
node_count
```

### Notes

- This finishes the Slice B root replay migration requirement. Delegation-proof
  issuance and pool `CreateEmpty` still need their own replay-protected request
  DTOs and cost guards in later 0.61 slices.

- `canic nns registry version` is a live read and does not write a cache file.
  The value is small and acts as a freshness/provenance probe for later cached
  NNS views.

- The command remains mainnet-only. Local replica NNS registry discovery is not
  implemented in this slice.

- `canic nns node-operator list` and `info` are cache-first and populate
  `.canic/node-operator/ic/operators.json` on a first cache miss. `refresh`
  force-refreshes through `.canic/node-operator/ic/refresh.lock` and atomic
  cache replacement.

- `canic nns node list` and `info` are cache-first and populate
  `.canic/node/ic/nodes.json` on a first cache miss. `refresh` force-refreshes
  through `.canic/node/ic/refresh.lock` and atomic cache replacement.

- Node counts are assigned mainnet subnet nodes for each operator, not spare or
  unassigned node inventory.

- The nested `version` command is intentionally distinct from the CLI
  `--version` flag. `canic nns registry version` performs the registry query,
  while `canic nns registry --version` prints the Canic CLI version.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo test -p canic-core workflow::rpc::request::handler --lib -- --nocapture
cargo test -p canic-core --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
cargo test -p canic-ic-registry node -- --nocapture
cargo test -p canic-ic-registry node_operator -- --nocapture
cargo test -p canic-host nns_node --lib -- --nocapture
cargo test -p canic-host nns_node_operator --lib -- --nocapture
cargo clippy -p canic-ic-registry -p canic-host -p canic-cli --lib -- -D warnings
cargo test -p canic-cli --lib nns -- --nocapture
cargo test -p canic-cli --lib command_family_help_returns_ok -- --nocapture
cargo test -p canic-host nns_registry --lib -- --nocapture
cargo fmt --all -- --check
git diff --check
```

## 0.61.2 - 2026-06-04 - Shared replay receipt store

### Summary

- Continued Slice B from
  `docs/design/0.61-replay-protection/0.61-design.md` by adding the shared
  stable replay receipt store and reserve/replay/commit API.

- Reserved stable memory ID `21` for shared replay receipts under the auth
  memory range and added a dedicated `canic.core.replay_receipts.v1` stable
  map.

- Added the stored receipt record shape for shared replay receipts, including
  command kind, operation ID, replay actor, payload hash schema/version,
  receipt status, committed response bytes, and external-effect descriptor.

- Added mechanical receipt storage ops with command-kind-scoped slot-key
  derivation so the same operation ID is isolated per replay-protected command
  kind.

### Changed

- Added `ops::replay::receipt`, the first shared replay receipt service API.
  It can reserve a fresh receipt, classify duplicate requests, return committed
  responses, report actor/payload mismatches, report pending or in-flight
  operations, mark external effects in flight, commit successful responses,
  commit bounded terminal failures, and mark recovery-required receipts.

- Receipt storage encodes records with CBOR at the stable-store boundary while
  converting back into the shared replay model before policy decisions.

- Terminal-failure commits apply the shared
  `MAX_REPLAY_TERMINAL_ERROR_BYTES` bound before storing error payload bytes.

### Notes

- This is still branch-only Slice B foundation work. Existing root RPC replay
  continues to use the legacy root replay store in this patch; migrating root
  replay onto the shared receipt primitive remains required before any 0.61
  release candidate.

- No CLI commands, flags, or output formats change in this patch.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo test -p canic-core --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
cargo fmt --all -- --check
git diff --check
```

## 0.61.1 - 2026-06-04 - Shared replay model foundation and NNS node-provider enrichment

### Summary

- Started Slice B from
  `docs/design/0.61-replay-protection/0.61-design.md` by adding the shared
  replay model that later command migrations will use.

- Added the core replay vocabulary:
  `OperationId`, `CommandKind`, `ReplayActor`, `ReplayReceiptKey`,
  `ReplayReceipt`, `ReplayReceiptStatus`, `ExternalEffectDescriptor`,
  `EcdsaPurpose`, and common replay error classifications.

- Added versioned replay payload hashing support through
  `REPLAY_PAYLOAD_HASH_SCHEMA_VERSION` and `ReplayPayloadHasher`.

- Added bounded terminal-error bytes with
  `MAX_REPLAY_TERMINAL_ERROR_BYTES = 4096` so later terminal-failure receipts
  cannot persist unbounded error payloads.

- Enriched `canic nns node-provider list` and
  `canic nns node-provider info` with registry-derived assigned-node counts.

- Node counts are derived by following mainnet subnet membership through
  `node_record_<node>` and `node_operator_record_<operator>` registry values to
  each node provider principal.

- Made `canic nns node-provider list` and `info` cache-first. On the first
  cache miss they populate `.canic/node-provider/ic/providers.json`; subsequent
  reads use the cached enriched report.

- Added `canic nns node-provider refresh` to force-refresh the native NNS
  governance/registry-backed cache, with `--dry-run`, `--output`, and refresh
  lock handling matching the subnet metadata surface.

### Changed

- The existing root replay guard and key derivation now accept the shared
  32-byte `OperationId` type instead of raw `[u8; 32]` request IDs at the
  replay-core boundary.

- Root RPC replay behavior is otherwise preserved in this slice. The existing
  root DTO metadata still carries `[u8; 32]` and is converted to `OperationId`
  before entering the replay guard.

- Added minimal local protobuf decoding for NNS registry `NodeRecord` and
  `NodeOperatorRecord`.

- Added bounded concurrent registry reads in `canic-ic-registry` so provider
  enrichment does not perform every node/operator lookup sequentially.

- Added registry provenance to node-provider host reports:
  `registry_canister_id` and `registry_version`.

- `canic nns node-provider list --verbose` now includes the registry version
  alongside full provider principals and reward-account detail.

- Node-provider refresh writes through `.canic/node-provider/ic/refresh.lock`
  and replaces `providers.json` atomically, so repeated `list`/`info` calls no
  longer redo the expensive node/operator registry enrichment pass.

- Factored the shared host refresh-lock, atomic cache-write, refresh-output,
  and positive-duration parsing helpers used by the cached NNS subnet and
  node-provider surfaces.

### Notes

- This is a foundation slice. It does not yet migrate stable root replay
  records to the final shared receipt shape, and it does not yet add replay
  metadata to delegation-proof issuance or pool creation requests.

- The count is assigned mainnet subnet nodes, not a spare-node or unassigned
  inventory count.

- The native NNS governance and registry sources still do not expose a
  canonical human-readable provider name. Dashboard pages expose display names,
  but this slice intentionally keeps the data source IC-native and does not add
  a dashboard HTTP dependency. The JSON `name` field remains nullable for
  schema room, but text output does not render a `NAME` column.

### Testing

Focused validation for this patch includes:

```text
cargo test -p canic-core ops::replay --lib -- --nocapture
cargo test -p canic-core --lib -- --nocapture
cargo clippy -p canic-core --all-targets --all-features -- -D warnings
cargo test -p canic --test changelog_governance -- --nocapture
cargo test -p canic-ic-registry node_provider -- --nocapture
cargo test -p canic-host subnet_catalog -- --nocapture
cargo test -p canic-host nns_node_provider -- --nocapture
cargo test -p canic-host duration -- --nocapture
cargo test -p canic-cli --lib nns -- --nocapture
cargo clippy -p canic-ic-registry -p canic-host -p canic-cli --all-targets --all-features -- -D warnings
cargo fmt --all -- --check
git diff --check
```

## 0.61.0 - 2026-06-04 - Replay policy inventory and auth hard cut

### Summary

- Started the 0.61 replay-safety hardening line with the Slice A foundation
  from `docs/design/0.61-replay-protection/0.61-design.md`.

- Added a `canic-core::replay_policy` manifest that classifies Canic-emitted
  update endpoints by replay policy, implementation status, cost class, quota
  policy, and cycle-reserve policy.

- Hard-cut verifier-local delegated-token update consumption. Delegated tokens
  are again TTL-bounded bearer credentials; replay-sensitive mutations must use
  domain operation IDs and replay receipts.

- Made `canic_app` set-style commands response-idempotent. Repeating a
  state-setting request now returns success with `changed = false` instead of
  an already-in-state error.

- Added an API-boundary caller/shard guard for local root delegation-proof
  issuance. `canic_request_delegation` now rejects
  `msg_caller() != request.shard_pid` before delegated-token config lookup,
  proof preparation, or threshold ECDSA signing.

### Changed

- Removed the delegated-token consumed-use runtime path from access auth,
  auth ops, auth storage ops, and stable auth state.

- Removed the stable `auth/token_uses` module and the live
  `delegated_token_uses` field from `AuthStateRecord`.

- Added an upgrade-shape test proving old serialized auth state with historical
  consumed-token markers decodes into the new auth state shape while dropping
  that removed field.

- Added manifest tests that compare Canic macro-emitted update endpoints
  against the replay-policy manifest, require unique entries, and require cost
  metadata for costed endpoints.

- Updated active recurring audit templates so delegated auth is described as
  bearer-token verification and replay protection is assigned to domain replay
  receipts.

- Changed the root `canic_app` endpoint response from `Result<(), Error>` to
  `Result<AppCommandResponse, Error>`. `SetStatus` returns
  `Status(SetStateResponse<AppStatus>)`, and `SetCyclesFundingEnabled` returns
  `CyclesFundingEnabled(SetStateResponse<bool>)`; both responses include
  `previous`, `current`, and `changed`.

- Reused the existing `DelegationCallerShardMismatch` wording for the direct
  delegation-proof issuance guard so local root issuance and root-capability
  authorization diagnostics stay aligned.

### Notes

- This is branch-only foundation work. It intentionally does not yet fix pool
  `CreateEmpty` or delegation-proof issuance response replay/caching.

- `ReleaseBlocker` manifest entries identify later 0.61 slices that must land
  before a public 0.61 release candidate.

### Testing

Focused validation for this patch includes:

```text
cargo fmt --all
cargo test -p canic-core access::auth::token --lib -- --nocapture
cargo test -p canic-core api::auth --lib -- --nocapture
cargo test -p canic-core ops::auth::delegated::issue --lib -- --nocapture
cargo test -p canic-core storage::stable::auth --lib -- --nocapture
cargo test -p canic-core ops::storage::state::app --lib -- --nocapture
cargo test -p canic-core replay_policy --lib -- --nocapture
cargo test -p canic-core --lib -- --nocapture
rg -n "consume_update_token_once|consume_delegated_token_use|DelegatedTokenUse|delegated_token_uses|DELEGATED_TOKEN_USE_CAPACITY|token_uses" crates/canic-core/src crates/canic/src crates/canic-macros/src crates/canic-control-plane/src crates/canic-wasm-store/src -g "*.rs"
```
