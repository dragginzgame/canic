# 0.24.x Detailed Changelog

Technical release notes for the `0.24.x` minor line.

## 0.24.7 - 2026-04-05 - PocketIC Attestation Restore Hardening

### 📝 Summary

- `0.24.7` hardens the `pic_role_attestation` PocketIC lane by recovering automatically from dead cached-baseline restores and by bringing the role-attestation capability-path tests back in line with the real child-to-root cycles flow.

### 🩹 Fixed

- Cached `pic_role_attestation` baselines now rebuild automatically when PocketIC snapshot restore fails, so dead local instances stop surfacing as opaque `ConnectionRefused` panics partway through the suite.
- The role-attestation capability-path coverage now uses the real caller split:
  - policy-denial cases still run as `root -> root`
  - cycles capability proof cases now run as `signer -> root`
- That keeps the tests aligned with the current runtime contract, where root cycles authorization resolves and funds real child callers instead of accepting the old root-self shortcut.

### 🧪 Testing

- `cargo test -p canic-core --test pic_role_attestation capability_endpoint_policy_and_structural_paths -- --test-threads=1 --nocapture` passed after the caller-path correction.
- `cargo test -p canic-core --test pic_role_attestation capability_endpoint_role_attestation_proof_paths -- --test-threads=1 --nocapture` passed after switching the role-attestation proof cases to the real `signer -> root` path.
- `cargo test -p canic-core --test pic_role_attestation -- --test-threads=1 --nocapture` passed with `15 passed; 0 failed`.

---

## 0.24.6 - 2026-04-05 - Public Testkit Helper Lift

### 📝 Summary

- `0.24.6` promotes the genuinely reusable PocketIC helpers out of Canic’s unpublished self-test crate and into public `canic-testkit`, so downstream Canic projects get a better host-side test surface without inheriting Canic’s own root-topology or auth-fixture assumptions.

### 🔧 Changed

- Moved the standalone non-root Canic canister PocketIC fixture into `canic-testkit`, so downstream tests can build and install one ordinary `app`/`test`/other non-root Canic canister without copying local fixture glue.
- Added generic PocketIC `install_code` rate-limit retry helpers to `canic-testkit::pic::Pic`, so downstream lifecycle/upgrade tests can reuse the same backoff path instead of re-owning PocketIC-specific retry loops.
- Switched the internal lifecycle-boundary fixture onto the new public `canic-testkit` retry helpers and `Fake` principal generation, which keeps the public/private boundary cleaner and reduces duplication.
- Kept Canic-specific root-topology, attestation, and delegation fixtures in the unpublished `canic-testing-internal` crate, so the public crate still exposes only generic host-side test infrastructure.

### 🧪 Testing

- `cargo clippy -p canic-testkit -p canic-testing-internal -p canic-tests --test standalone_canisters --test lifecycle_boundary -- -D warnings` passed after the public helper promotion.
- `cargo test -p canic-tests --test standalone_canisters -- --nocapture` passed with the standalone `scale_hub` perf probe and standalone bogus-token auth guard using the public `canic-testkit` fixture.
- `cargo test -p canic-tests --test lifecycle_boundary -- --nocapture` passed with the internal lifecycle-boundary fixture using the new public PocketIC retry helpers.

---

## 0.24.5 - 2026-04-05 - Root Harness Tightening

### 📝 Summary

- `0.24.5` keeps the root hierarchy reserved for the tests that genuinely need root-managed structure by moving one more local auth guard check onto the standalone PocketIC lane, sharing the internal delegation fixture plumbing across auth-focused suites, and replacing the last generic reconcile harness path with a named cached root profile.

### 🔧 Changed

- The local bogus-token delegated-auth guard check no longer lives in `delegation_flow`; it now runs in `standalone_canisters` against one standalone `test` canister, so `delegation_flow` is back to being a certified/root-shard suite only.
- Added one unpublished internal delegation helper module in `canic-testing-internal`, so both `delegation_flow` and the ignored instruction audit reuse the same `user_hub -> user_shard -> root delegation` plumbing instead of re-owning that setup in each test file.
- The root harness now exposes a named cached `reconcile small-store` profile for `root_wasm_store_reconcile`, so the specialized `wasm_store` reconcile lane no longer reaches through the generic explicit-role harness escape hatch.
- Removed the unused generic fresh explicit-role root setup helpers and the old `setup_root()` alias from the test harness, leaving only named profile entrypoints for the remaining root-backed suites.

### 🧪 Testing

- `cargo clippy -p canic-testing-internal -p canic-tests --test delegation_flow --test standalone_canisters --test instruction_audit -- -D warnings` passed after the internal delegation helper extraction.
- `cargo test -p canic-tests --test standalone_canisters -- --nocapture` passed with both the standalone `scale_hub` perf probe and the standalone bogus-token auth guard check.
- `cargo test -p canic-tests --test delegation_flow -- --nocapture` passed with the certified/root-shard suite returning to its non-`ic` self-skip behavior.
- `cargo test -p canic-tests --test instruction_audit -- --list` still enumerates cleanly after switching the ignored audit onto the shared internal delegation helper.
- `cargo clippy -p canic-tests --test root_suite --test root_wasm_store_reconcile -- -D warnings` passed with the new named reconcile harness profile.
- `cargo test -p canic-tests --test root_suite root_sharding_cases::user_hub_sharding_profile_stays_minimal_and_can_create_a_shard -- --test-threads=1 --nocapture` passed with the reduced sharding profile still proving runtime behavior.
- `cargo test -p canic-tests --test root_wasm_store_reconcile -- --list` still enumerates the reconcile suite cleanly after moving it onto the named cached root profile.

---

## 0.24.4 - 2026-04-05 - Standalone Test Fixture and Local Tooling Hardening

### 📝 Summary

- `0.24.4` keeps the root hierarchy reserved for the cases that genuinely need root-managed structure, moves standalone `app`, `test`, and `scale_hub` checks onto one shared unpublished PocketIC fixture, keeps heavy internal env/directory queries out of ordinary canister builds, and makes local install/audit flows less fragile when the shared `dfx` state is flaky.

### 🔧 Changed

- Added one shared unpublished standalone non-root PocketIC fixture in `canic-testing-internal`, so tests and audits that only need one ordinary `app`, `test`, or `scale_hub` canister no longer have to boot the full root hierarchy first.
- The local bogus-token guard check in `delegation_flow` now installs one standalone `test` canister directly, and the ignored instruction audit now measures the standalone `app` query floor, standalone `test::test` update floor, and standalone `scale_hub` dry-run query floor without paying for root bootstrap.
- Ordinary canister builds no longer export the heavy internal `canic_env`, `canic_memory_registry`, `canic_app_directory`, and `canic_subnet_directory` queries by default.
- Those four internal introspection queries now compile only when `CANIC_INTERNAL_TEST_ENDPOINTS=1` is set at build time, and the unpublished root PocketIC harness enables that flag automatically so internal topology assertions keep their coverage without bloating shipped leaf surfaces.
- The recurring wasm audit now builds visible canisters through `scripts/app/build.sh` directly instead of failing fast on `dfx ping local`, so a flaky local replica no longer blocks size reruns that only need fresh artifacts.
- The local `canic-install-root` flow now tries one clean `dfx stop` / `dfx start --background --clean --system-canisters` recovery when `dfx ping local` fails, so `make demo-install` and `make test-canisters` stop assuming someone restarted the local replica by hand first.
- PocketIC process locks now record both the owning pid and its `/proc` start tick, so stale-lock cleanup can distinguish a dead owner from later pid reuse instead of leaving bad lock directories behind.

### 🧪 Testing

- `cargo test -p canic-tests --test delegation_flow -- --test-threads=1 --nocapture` still passed after keeping sharding only where delegated-auth flows really need it and moving the bogus-token case onto the standalone `test` fixture.
- `cargo test -p canic-tests --test delegation_flow authenticated_guard_rejects_bogus_token_on_local -- --nocapture` passed with the standalone `test` canister fixture instead of the cached root topology.
- `cargo test -p canic-tests --test standalone_canisters -- --nocapture` passed with a standalone `scale_hub` perf-probe check, proving the dry-run scaling query no longer needs the root hierarchy either.
- `cargo test -p canic-tests --test instruction_audit -- --list` still enumerates the audit scenarios cleanly after moving the standalone `app` query floor and standalone `test::test` floor out of the root hierarchy.
- `cargo clippy -p canic-testing-internal -p canic-tests --all-targets -- -D warnings` passed with the new shared standalone fixture.
- A normal `bash scripts/app/build.sh minimal` build omits those internal test-only queries from `.dfx/local/canisters/minimal/minimal.did`, while `CANIC_INTERNAL_TEST_ENDPOINTS=1 bash scripts/app/build.sh minimal` restores them for test-only builds.
- `cargo test -p canic-testkit process_lock_tests -- --nocapture` passed with coverage for legacy pid-only lock files and pid-reuse detection through `/proc` start ticks.
- `cargo test -p canic-installer --lib install_root -- --nocapture` passed with coverage for the new local replica auto-start command shape and opt-out parsing.
- `bash scripts/ci/wasm-audit-report.sh` completed successfully and wrote the retained `2026-04-05` follow-up report without requiring a healthy local replica first.

---

## 0.24.3 - 2026-04-05 - Sharding Runtime Fold-Back

### 📝 Summary

- `0.24.3` folds the optional sharding lane back into `canic-core`, removes the standalone `canic-sharding-runtime` crate and its extra `xxhash-rust` dependency, keeps facade users on the same `canic` `sharding` feature while simplifying the workspace/runtime boundary, and narrows the internal root test surface so hierarchy-heavy suites only pay for the roles they actually exercise.

### 🔧 Changed

- Moved sharding API, workflow, policy, view, and mapper code into feature-gated `canic-core` placement modules, so the optional lane is enforced by the core crate's own `sharding` feature instead of a separate workspace crate.
- `canic` now forwards `features = ["sharding"]` to `canic-core`, so facade users keep the same feature flag and `canic::api::canister::placement::ShardingApi` import path.
- The HRW shard scorer now uses the existing `sha2` dependency instead of `xxhash-rust`, which removes one more sharding-only dependency from the workspace and keeps the hash stack aligned with the rest of the runtime.
- `canister_minimal`'s normal wasm dependency tree still stays free of sharding baggage after the fold-back because the sharding code and dependencies remain Cargo-feature gated inside `canic-core`.
- The internal root harness now exposes explicit `topology`, `scaling`, and `sharding` profiles, so tests can opt into the smallest cached baseline that matches the behavior they are actually covering.
- The root hierarchy coverage is now one comprehensive topology test plus a separate scaling case, while replay, delegation, and instruction-audit suites select the lightest profile they need instead of reusing the heaviest shared hierarchy by default.
- Root-directory assertions now match the targeted cascade model: root remains authoritative, child directory views may be partial, and children still must resolve themselves through entries that root owns.

### 🗑️ Removed

- Removed the `canic-sharding-runtime` crate from the workspace, publish order, downstream packaging checks, and contributor-facing crate inventory.

### ⚠️ Breaking

- Direct consumers of `canic-sharding-runtime` must move off that crate in `0.24.3`; the supported sharding surface now comes from `canic` or feature-gated `canic-core`.

### 🧭 Migration Notes

- Downstreams already using `canic = { features = ["sharding"] }` do not need a feature-flag change.
- Downstreams importing `canic_sharding_runtime::api::ShardingApi` should switch to `canic::api::canister::placement::ShardingApi` or `canic_core::api::placement::sharding::ShardingApi`.
- Future HRW-based shard selection for the same `partition_key` and pool state may differ from earlier releases because the score hash changed from `xxh3` to `sha2`, but existing persisted assignments remain unchanged.

### 🧪 Testing

- Focused `cargo build` and `cargo clippy -D warnings` passed for `canic-core` with `sharding`, `canic`, `canister_user_hub`, and `canister_scale_hub`.
- `cargo test -p canic-core --features sharding --test pic_sharding_bootstrap -- --nocapture` passed.
- `cargo tree -p canister_minimal --target wasm32-unknown-unknown -e normal` no longer shows `canic-sharding-runtime` or `xxhash-rust`.
- `cargo test -p canic-tests --test root_suite -- --test-threads=1 --nocapture` passed with the narrowed hierarchy profiles.
- `cargo test -p canic-tests --test root_suite root_reference_topology_is_consistent -- --test-threads=1 --nocapture` passed with the root PocketIC harness rebuilding its baseline under `--cfg canic_enable_internal_test_endpoints`.

---

## 0.24.2 - 2026-04-05 - Root Capability Replay Follow-Through

### 📝 Summary

- `0.24.2` follows the first root auth reductions by reusing cached root response attestations, carrying cycles authorization through replay/capability execution, trimming replay and registry response work, and keeping only the retained final instruction audit output.

### 🔧 Changed

- Root now reuses still-valid outbound response attestations for repeated RPCs to the same audience instead of reissuing the same signed role attestation on every call.
- Root and non-root `RequestCycles` replay/capability execution now carry an already-authorized grant from preflight into execution, while root keeps resolving children from the authoritative subnet registry and non-root parents keep using their local direct-child cache.
- Replay caches now store canonical response bytes directly and decode them through `canic_memory::serialize`, so replay hits no longer pay an extra Candid decode/encode layer just to rebuild the cached response shape.
- Subnet registry responses now iterate the stable registry directly instead of exporting a whole snapshot first, which trims the read path and keeps the response mapper focused on entry projection.
- The dated `2026-04-05` rerun confirms the same direction: sampled `root::canic_request_delegation` drops from `3205866` local instructions in `instruction-footprint-20` to `2274445`, and `root::canic_response_capability_v1` `cycles-request` drops from `612625` in the retained `instruction-footprint-29` run to `607069`.
- In that `2026-04-05` rerun, `prepare_call` is `295880`, `decode_response` is `253330`, `cache_root_verifier_keys` is `351755`, `preflight` is `114771`, and `execute_capability` is `252602`, so both the delegated-auth and request-cycles paths now have stable stage-level numbers with a fresh dated comparison point.
- The recurring instruction audit now explicitly treats query rows as same-call probe-backed samples because query-side perf rows do not persist in shared state, which keeps query lanes comparable instead of reading back misleading zeroes from `canic_metrics`.
- Removed the redundant intermediate `instruction-footprint-19` through `instruction-footprint-28` generated outputs and the stale `tmp-debug` audit artifact so the audit surface keeps one retained final report instead of a confusing same-snapshot run pile.

---

## 0.24.1 - 2026-04-04 - Root Auth Key Prewarm

### 📝 Summary

- `0.24.1` follows up the first delegation-path replay/storage reductions by cutting cold root auth work, removing a redundant signer fanout, and tightening the root-local verifier cache path, so the first live delegated-auth request is materially cheaper without changing behavior.

### 🔧 Changed

- Warmed the root delegation and attestation key caches during the reference root canister's `canic_setup` hook through the public auth facade, so the cold root key fetch cost moves out of the first live delegated-auth request path without changing runtime behavior.
- In the current instruction audit, that drops sampled `root::canic_request_delegation` from `4356980` local instructions in `instruction-footprint-17` to `3934327` in `instruction-footprint-18`.
- In the same path, `issue_proof` drops from `882945` to `504032`.
- Stopped root from pushing the newly issued delegation proof back to the requesting signer shard, and instead let the shard store the returned proof locally through the existing delegated-auth setup path.
- In the current instruction audit, that drops sampled `root::canic_request_delegation` again from `3934327` in `instruction-footprint-18` to `3247900` in `instruction-footprint-19`, while cutting `prepare_call` from `645625` to `297555` and `decode_response` from `513391` to `256172`.
- Collapsed the root-local verifier cache path from two `DelegationState` writes into one combined auth-state commit, so a fresh delegated-auth issuance no longer rewrites the same stable auth record twice in succession just to store the shard key and verifier proof.
- In the current instruction audit, that trims sampled `root::canic_request_delegation` one more time from `3247900` in `instruction-footprint-19` to `3205866` in `instruction-footprint-20`, with `cache_root_verifier_keys` dropping from `404812` to `357119`.

---

## 0.24.0 - 2026-04-04 - Shared Runtime Reduction and Test Boundary Cleanup

### 📝 Summary

- `0.24.0` opens the new minor line after the accidental version bump without changing the current engineering direction: the next work still centers on shrinking the shared wasm floor, keeping public `canic-testkit` support separate from Canic-only self-test harness code, and hardening release-facing surfaces such as the curlable installer path.

### 🔧 Changed

- [CHANGELOG.md](/home/adam/projects/canic/CHANGELOG.md) now opens the `0.24.x` minor line.
- `0.24.x` is the new home for the current follow-through work rather than a broad reset or topology redesign.
- Switched shared `CandidType` source comments from `///` to `//` across the shared DTO and spec files, so Candid runtime docs stop inflating release wasm without changing the exported endpoint surface.
- In the current `minimal` release artifact, that reduces the shrunk wasm from `1764427` to `1741732` bytes and the `.wasm.gz` artifact from `498796` to `486798` bytes.
- Expanded the recurring instruction audit so it now measures real scaling, sharding, replay/cycles, delegated-auth issuance, and verifier-confirmation update paths alongside the local-only query perf probes, with stage-level checkpoint deltas captured for the sampled update flows where internal checkpoints exist.
- Reworked staged chunk storage so chunk bytes now live in a stable payload vector behind a small key-to-slot index instead of sitting directly in the stable chunk BTreeMap value path.
- In the current instruction audit, that drops the sampled `root::canic_template_publish_chunk_admin` single-chunk path from `9713784` local instructions in `instruction-footprint-12` to `390788` in `instruction-footprint-13`, with the old `chunk_store_insert` stage falling from `9518067` to `194978`.
- Replaced replay-store `RootReplayRecord` CBOR persistence with a direct byte layout and swapped replay payload hashing from canonical Candid encode to a structured field hash, so the sampled `root::canic_request_delegation` path drops from `5516827` local instructions in `instruction-footprint-15` to `4356980` in `instruction-footprint-17`.
- In the same delegation path, `commit_replay` drops from `1151412` to `993079` local instructions and `prepare_replay_input` drops from `779044` to `137129`, which means replay prep is no longer the dominant stage inside delegated auth issuance.

### 🧭 Migration Notes

- This minor bump does not signal a new API model by itself.
- The practical focus remains shared-runtime wasm reduction, public/internal testing-boundary cleanup, and release-surface correctness.
