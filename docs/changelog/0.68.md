# 0.68 Changelog

## 0.68.17 - 2026-06-18 - Root proof provisioning hard cut cleanup

### 🗑️ Removed

- Removes the legacy single-proof root delegation prepare/get endpoint pair
  from the root endpoint macro and public protocol re-exports.

- Removes the old single-proof delegation request/prepare/get DTOs, auth API
  adapters, runtime replay workflow, replay response helpers, and pending proof
  cache.

- Removes test helpers and the test-only user-shard composite query wrapper
  that exercised issuer-to-root nested root proof retrieval.

### 🔧 Changed

- Switches the instruction audit delegated-auth lane from the removed
  single-proof prepare endpoint to `canic_prepare_delegation_proof_batch`.

- Updates active authentication docs, delegated-signature contract docs, and
  the 0.68 design note so the supported provisioning sequence is only root
  batch prepare, direct root batch get, and root batch install.

- Drops unused pending batch refresh metadata that was written but never read;
  refresh timing remains in prepare responses and signer active-proof state.

### 🧪 Testing

- Pins the protocol surface so the legacy single-proof root delegation
  endpoints stay absent while batch provisioning DTOs still round-trip through
  Candid.

- Revalidates the root batch provisioning unit path, replay policy, protocol
  surface, core/facade compile, canic-test helpers, and the sharding PocketIC
  root provisioning suite after the cleanup.

## 0.68.16 - 2026-06-18 - Root provisioning MVP regression closeout

### 🧪 Testing

- Adds PocketIC coverage for the old issuer-to-root nested retrieval path,
  proving it fails with `RootDataCertificateUnavailable` after issuer
  authorization succeeds rather than passing for an unrelated ACL reason.

- Adds PocketIC coverage proving signer-local delegated-token issuance still
  succeeds after an active root delegation proof is installed and the root
  canister is made unavailable before proof expiry.

- Adds a test-only user-shard composite query wrapper for the nested retrieval
  regression and extends the test root issuer TTL enough to cover the
  root-unavailable scenario through PocketIC install-code cooldown.

## 0.68.15 - 2026-06-18 - Root retrieval certificate-context error

### ➕ Added

- Adds the stable `RootDataCertificateUnavailable` public error code for root
  delegation proof retrieval attempts that cannot access root certified query
  context.

- Mirrors the new error code in the checked-in wasm-store Candid sidecar so the
  public error enum remains protocol-visible.

### 🔧 Changed

- Maps the root canister-signature proof assembly failure produced when
  `get_signature_as_cbor` cannot access `data_certificate()` to the typed
  public error instead of leaving it as a generic root proof invalid/internal
  error.

- Keeps unrelated root canister-signature CBOR failures classified as invalid
  proof errors, so only the direct-root-query certificate-context regression
  receives the new typed code.

### 🧪 Testing

- Adds unit coverage for the public error mapping and the root
  canister-signature CBOR error classifier.

- Revalidates the root batch provisioning PocketIC path after the error
  classification change.

## 0.68.14 - 2026-06-18 - Root batch lifecycle regression coverage

### 🧪 Testing

- Extends PocketIC root batch provisioning coverage so an installed active root
  delegation proof transitions to `RefreshNeeded` at `refresh_after_ns` and
  `Expired` at proof expiry.

- Proves signer-local delegated-token prepare stops after the active proof
  expires.

- Adds partial batch install failure and retry coverage: an available signer is
  installed once and then reported as `AlreadyInstalled`, while an unavailable
  signer reports `CallFailed` on both the first attempt and retry.

## 0.68.13 - 2026-06-18 - Controller-only provisioning MVP closeout

### 🔧 Changed

- Finalizes the 0.68 root proof provisioning MVP around controller-only root
  batch authorization while preserving provisioner ACL as the documented later
  automation target.

- Makes root delegation proof batch prepare request-id keyed and idempotent:
  exact request-id retries with identical provision entries return the original
  batch metadata, while conflicting request-id reuse is rejected.

- Keeps root out of the normal delegated-token hot path after installation by
  proving signer-local delegated-token issuance from an active root delegation
  proof.

### 🧪 Testing

- Adds unit coverage for exact batch prepare request-id replay and conflicting
  request-id reuse.

- Extends PocketIC root batch provisioning coverage through signer-local
  delegated-token prepare/get after active proof installation.

## 0.68.12 - 2026-06-17 - Root batch install broadcast

### ➕ Added

- Implements the MVP root delegation proof batch install path.

- Adds a runtime auth provisioning workflow that validates each submitted
  retrieved proof against pending root batch metadata before broadcasting the
  proof to the named signer canister.

- Adds root-side install preflight for issuer binding, canonical certificate
  hash matching, pending metadata matching, retrieval/certificate expiry, and
  already-installed idempotency.

### 🔧 Changed

- Changes `canic_install_delegation_proof_batch` from the deferred unavailable
  endpoint into an async root update that returns per-signer install outcomes.

- Marks pending batch entries installed only after the signer install endpoint
  accepts the proof, so repeated root install calls return `AlreadyInstalled`
  for successful prior installs.

### 🧪 Testing

- Adds unit coverage for batch install preflight success, proof mismatch,
  expired pending metadata, and already-installed idempotency.

- Adds workflow coverage proving invalid locally checked proofs are not
  broadcast to signers.

- Adds PocketIC coverage for root prepare -> direct root query retrieval ->
  root batch install -> signer active-proof status, including repeated install
  idempotency.

## 0.68.11 - 2026-06-17 - Root batch direct query retrieval

### ➕ Added

- Implements the MVP direct root query retrieval path for prepared root
  delegation proof batches.

- Resolves pending batch metadata by batch id, issuer principal, and
  certificate hash, then finalizes each prepared delegation certificate with
  the root canister-signature proof assembled from the direct root query
  context.

- Returns `RootDelegationProofBatchGetResponse` values with per-issuer
  `DelegationProof` payloads in request order.

### 🔧 Changed

- Changes `canic_get_delegation_proof_batch` from an unavailable root endpoint
  into the direct-query retrieval half of the 0.68 provisioning flow.

- Keeps batch install deferred in this patch; `.11` only completes prepare ->
  direct root query retrieval.

### 🧪 Testing

- Adds batch retrieval coverage for empty get requests, missing pending
  metadata, expired retrieval windows, successful proof finalization, and
  request-order preservation.

- Keeps root batch prepare/get tests running together so pending metadata
  produced by prepare remains consumable by direct root query retrieval.

## 0.68.10 - 2026-06-17 - Root batch prepare metadata

### ➕ Added

- Implements the MVP root delegation proof batch prepare response path.

- Validates batch request metadata, uses the request id as the batch id, builds
  delegation certificates, computes certificate hashes, prepares root
  canister-signature leaves, and returns per-issuer expiry and refresh
  metadata.

- Caches pending batch metadata keyed by batch id, issuer principal, and
  certificate hash so the direct root query retrieval slice can use the
  prepared material.

### 🔧 Changed

- Changes `canic_prepare_delegation_proof_batch` from an unavailable stub after
  preflight into a root-local prepare operation that returns the MVP batch
  metadata response.

- Keeps batch retrieval and batch install unavailable until the direct root
  query and signer broadcast slices land.

### 🧪 Testing

- Adds batch prepare coverage for missing metadata, empty issuer batches,
  invalid metadata TTL, registered-issuer success metadata, and persisted
  issuer policy preflight.

- Keeps the root-signature creation feature build checked so the production
  root leaf preparation path stays compilable.

## 0.68.9 - 2026-06-17 - Root issuer registry preflight

### 🔧 Changed

- Changes root delegation proof batch prepare preflight to resolve issuer
  policy from persisted root auth state instead of building a permissive
  request-scoped policy from the incoming batch entry.

- Adds root auth-state records for issuer policy and provisioner ACL state,
  with migration-safe serde defaults on the stable auth-state record.

- Adds an ops-layer mapper between root issuer storage records and the pure
  root issuer policy model so policy code remains independent of storage
  schema details.

### 🧪 Testing

- Adds batch prepare preflight coverage for registered issuers, unregistered
  issuers, disabled issuers, TTLs above issuer policy, and grants outside the
  issuer policy.

- Adds direct auth-state round-trip coverage for root issuer policy mapping.

## 0.68.8 - 2026-06-17 - Root issuer policy validation

### ➕ Added

- Adds a pure root delegation proof issuer policy validator for the 0.68 batch
  prepare path.

- Validates issuer registration, enabled state, issuer-principal binding,
  allowed audiences, allowed role grants, certificate TTL, and refresh timing.

- Derives `expires_at_ns` and `refresh_after_ns` from the validated issuer
  policy so later batch prepare metadata can reuse deterministic policy
  decisions.

### 🔧 Changed

- Wires the root batch prepare API stub through the new policy preflight before
  returning the existing unavailable error, keeping the protocol surface gated
  while policy validation lands.

### 🧪 Testing

- Adds focused unit coverage for accepted issuer policy, unregistered and
  disabled issuers, issuer mismatch, disallowed audience/grants, invalid TTL,
  and invalid refresh policy.

- Adds batch prepare preflight coverage for valid request shape and TTL
  rejection.

## 0.68.7 - 2026-06-17 - Root provisioning hard cut

### ⚠️ Breaking

- Hard-cuts issuer-initiated root delegation proof provisioning. Non-root
  callers now fail locally when using the old self-provisioning prepare helper
  and must move to the 0.68 root provisioning flow.

- Removes the stale issuer-to-root root-delegation RPC prepare helper and
  request-metadata shim that existed only to drive the unsupported nested
  retrieval path.

### ➕ Added

- Adds root delegation proof batch DTO and protocol surface for prepare, direct
  query retrieval, and install.

- Wires the new batch root endpoints to explicit unavailable API stubs until
  the 0.68 backend policy, storage, and broadcast implementation lands.

- Adds signer-local active delegation proof status with missing, valid,
  refresh-needed, and expired states plus non-secret operational metadata for
  provisioners.

### 🧪 Testing

- Adds focused auth regression coverage for the hard-cut issuer prepare path.

- Pins the new root batch provisioning and signer active-proof status protocol
  surfaces in the public protocol tests.

- Adds unit coverage for signer active-proof status lifecycle classification
  and keeps replay-policy coverage aligned with the new endpoints.

## 0.68.6 - 2026-06-17 - Provisioning design reservation and gate coverage

### 🥾 Governance

- Reserves the 0.68 design line for root delegation proof provisioning repair
  and moves blob-storage protocol planning to 0.69.

- Moves blob-storage billing and Cashier planning to 0.70 so the billing line
  remains explicitly downstream of a completed 0.69 gateway protocol
  inventory.

- Keeps both blob-storage gateway and Cashier implementation blocked behind
  executable inventory gates until the required external protocol evidence is
  recorded.

### 📚 Documentation

- Adds the 0.68 root delegation proof provisioning design as the active 0.68
  design focus.

- Realigns blob-storage protocol and billing design references, contract
  wording, and previous 0.68 changelog entries around the 0.69 and 0.70
  planning split.

### 🧪 Testing

- Expands gateway inventory-gate regression coverage for feature metadata,
  source paths, gateway method literals, public API/model type names, missing
  method sections, incomplete method evidence, unresolved method fields,
  missing Toko compatibility notes, and synthetic complete-inventory success.

- Expands Cashier inventory-gate regression coverage for feature metadata,
  source paths, method literals, billing endpoint literals, public billing type
  names, missing required method sections, incomplete required-method evidence,
  unresolved required-method fields, unresolved optional-method fields, and
  synthetic complete-inventory success.

## 0.68.5 - 2026-06-17 - Cashier inventory gate

### 🥾 Governance

- Adds a dedicated Cashier protocol inventory scaffold for the future 0.70
  blob-storage billing line while keeping billing implementation blocked.

- Requires source provenance, generated or deployed Candid, method modes,
  request/response DTOs, result/error behavior, cycle attachment requirements,
  balance units, gateway-principal list behavior, and optional-method
  classification before Cashier wrappers or billing endpoints can merge.

- Adds an executable Cashier inventory gate that rejects blob-storage billing
  feature metadata, Cashier source/module paths, Cashier method literals,
  billing endpoint literals, and public Cashier/billing API/model names while
  the inventory remains incomplete.

- Wires the Cashier gate into GitHub CI and local `make test`,
  `make test-wasm`, and `make test-bump` paths alongside the 0.69 gateway
  inventory gate.

### 📚 Documentation

- Updates the 0.70 blob-storage billing design to record the executable
  Cashier inventory gate and its complete-inventory validation behavior.

### 🧪 Testing

- Expands inventory-gate integration coverage to exercise the Cashier gate
  using temporary workspaces and the real shell guard.

- Covers incomplete-inventory pass-through for design-only work, rejection of
  premature Cashier method surfaces, rejection of complete inventories with
  unresolved optional-method fields, rejection of complete inventories missing a
  required Cashier method, and acceptance of a synthetic complete inventory.

## 0.68.4 - 2026-06-17 - Blob-storage provenance gate hardening

### 🥾 Governance

- Captures local Toko blob/asset compatibility evidence in the 0.69
  blob-storage inventory while keeping the inventory incomplete and
  implementation-blocked.

- Records that current Toko asset state is chunk-upload asset/chunk state, not
  proven Caffeine immutable object-storage gateway state.

- Records that no Toko pending-deletion queue, gateway-principal state,
  gateway-confirmation lifecycle, or canonical whole-blob root hash mapping was
  found in the local source evidence.

- Treats the Toko `BlobRootHash` mapping as an explicit blocker until gateway
  protocol evidence, an accepted empty-state path, bulk-registration path, or
  external mapping path is recorded.

- Propagates the same blocker into the 0.70 blob-storage billing design so
  Cashier sync and funding remain design-only until the 0.69 gateway inventory
  is complete.

- Extends the inventory gate to reject premature blob-storage billing and
  Cashier implementation surfaces while the gateway protocol inventory remains
  incomplete.

### 📚 Documentation

- Updates the 0.69 blob-storage design to make the Toko compatibility evidence
  and `BlobRootHash` mapping blocker explicit.

- Updates the 0.70 blob-storage billing design to require the completed 0.69
  inventory, Toko compatibility notes, and proven mapping before billing
  implementation can merge.

### 🧪 Testing

- Adds focused integration coverage for the inventory gate using temporary
  workspaces and the real shell guard.

- Covers the design-only incomplete-inventory pass case, premature billing
  surface rejection, and rejection of a fake complete inventory that still has
  unresolved Toko fields.

## 0.68.3 - 2026-06-17 - Blob-storage inventory gate

### 🥾 Governance

- Adds a CI guard for the 0.69 blob-storage protocol inventory gate. While the
  inventory remains incomplete, the guard rejects blob-storage feature metadata,
  source/module paths, `_immutableObjectStorage*` gateway method literals, and
  public blob-storage API/model names outside the design and inventory docs.

- Requires a future completed inventory to have all six gateway method sections
  individually marked complete with no remaining `TBD` fields, plus complete
  Toko compatibility notes, before the implementation gate unlocks.

- Wires the guard into GitHub CI before release-validation and Rust validation
  steps so premature protocol implementation fails before broad build/test work.

- Wires the same guard into local `make test`, `make test-wasm`, and
  `make test-bump` paths so maintainer-side release preparation also enforces
  the inventory gate.

### 📚 Documentation

- Records the executable guard in the 0.69 blob-storage design and protocol
  inventory while preserving the current implementation-blocked status.

### 🧪 Testing

- Validates the guard against the current incomplete inventory state, where no
  implementation files are allowed yet.

## 0.68.2 - 2026-06-17 - Accidental version-only release

### 📝 Summary

- Records the accidental `0.68.2` release artifact so the published patch
  number has an explicit ledger entry.

- Contains no code, API, storage, protocol, operator-output, or behavior
  changes beyond release metadata and install-version references.

### 🧭 Migration Notes

- No migration is required. Consumers can treat `0.68.2` as equivalent to
  `0.68.1` for runtime behavior.

## 0.68.1 - 2026-06-17 - Delegated-token public issuance hardening

### 🩹 Fixed

- Hardens `canic_prepare_delegated_token` so the public prepare path rejects
  caller-supplied subjects that do not match the update caller before replay
  state is reserved.

- Limits open delegated-token self-issuance to login/session scopes:
  `session` and the existing `verify` scope used by current test/demo
  authenticated endpoint flows.

- Blocks public self-granting of privileged or application-specific scopes
  such as `read`, `write`, `admin`, and custom admin labels even when those
  scopes are present in the installed delegation cert.

### 🧭 Migration Notes

- Existing subnet-wide login remains supported when issuers request only
  `session`/`verify` grants across the target roles and relying canisters
  enforce resource ownership or app-specific permissions locally.

- Workflows that previously used delegated-token grants as the direct source
  of write/admin authority need a caller-authorized issuer path or verifier
  local authorization state before those privileged scopes can be issued.

### 📚 Documentation

- Documents the public delegated-token prepare endpoint as a login/session
  materialization surface rather than a general-purpose role grant issuer.

### 🧪 Testing

- Adds pure policy coverage for allowed login scopes, rejected subject
  mismatch, and rejected privileged/custom scopes.

- Adds workflow-level coverage that subject mismatch and privileged self-grant
  requests fail before replay reservation.

## 0.68.0 - 2026-06-17 - Backup cleanup closeout

### 📝 Summary

- Completes the backup cleanup closeout without changing backup layout formats,
  serialized journal or manifest shapes, backup plan construction behavior,
  selector semantics, generated operation order, runner behavior, preflight
  semantics, or public backup APIs.

- Splits broad `canic-backup` journal, manifest validation, persistence,
  runner operation, plan preflight, and plan builder internals into focused
  child modules.

### 🧹 Cleanup

- Converts the download journal module into a small facade with focused child
  modules for durable journal data shapes, resume reporting, and validation.

- Splits manifest validation into focused modules for scalar validation,
  section-local validation, and cross-section deployment/verification binding.

- Splits persistence into dedicated modules for `BackupLayout`, typed
  persistence errors, JSON atomic read/write helpers, and integrity checking.

- Splits persistence integrity into focused artifact, execution-journal,
  topology-binding, path-resolution, and report modules.

- Splits runner operation execution into focused modules for operation
  dispatch/control calls, artifact lifecycle work, download-journal helpers,
  and runner artifact path validation.

- Splits plan preflight handling into focused modules for typed request
  builders, authority receipt application, and execution preflight receipt
  validation.

- Converts the backup plan builder into a focused directory module for
  selector resolution, registry target expansion, target depth calculation, and
  operation phase generation.

- Updates persistence tests to import fixture types explicitly after the
  persistence facade stopped carrying incidental parent-module imports.

### 📊 Audit

- Keeps all extracted code inside existing `canic-backup` ownership
  boundaries; `canic_backup::journal`, `canic_backup::persistence`,
  `BackupLayout`, `PersistenceError`, `BackupPlan` preflight methods,
  `build_backup_plan`, `resolve_backup_selector`, `BackupPlanBuildInput`, and
  runner entry points remain available through the same public call sites.

- Preserves backup JSON contracts, validation error behavior, preflight receipt
  checks, execution journal integrity checks, durable artifact verification,
  finalized manifest generation, backup selector validation, and backup
  operation ordering.

### 🧪 Testing

- Revalidated focused `canic-backup` journal, manifest, persistence, runner,
  and plan coverage after the production module splits.

- Revalidated full `canic-backup` library coverage before closing out the
  cleanup line.

- Revalidated `canic-backup` compilation, `canic-backup` clippy with all
  targets and warnings denied, formatting, changelog governance, and whitespace
  checks during release preparation.
