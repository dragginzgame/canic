# 0.50 Changelog

## 0.50.3 - 2026-05-30 - standalone and leaf-only reporting

### Added

- Added standalone-profile fixture coverage proving compile-only standalone
  roles remain declared-only and do not synthesize topology attachment.
- Added leaf-only fixture coverage for authority-sensitive observed roles.

### Changed

- Leaf-only adoption reports no longer recommend declaring observed
  authority-sensitive roles such as `governance`. The roles remain visible in
  the report with a warning that the leaf-only profile leaves them external.

### Safety

- The standalone fixture asserts `topology_state = unattached`,
  `declared_only_roles = 1`, and `managed_configured_roles = 0`.
- The leaf-only fixture asserts that authority-sensitive observations do not
  produce `canic fleet role declare ...` recommendations.

### Testing

- `cargo test -p canic-host adoption --lib --locked`

## 0.50.2 - 2026-05-30 - brownfield and partial fixtures

### Added

- Added adoption report regression fixtures for brownfield and partial
  onboarding shapes.
- Added focused fixture coverage for externally controlled observed roles,
  observed-only canisters without role evidence, and declared-only
  compile-only roles.

### Safety

- The fixtures assert that adoption reports keep observed resources passive:
  name-free observations do not invent declaration actions, user/external
  controller evidence stays separate from managed configuration, and suggested
  mutating actions remain unsupported by the adoption surface.

### Testing

- `cargo test -p canic-host adoption --lib --locked`

## 0.50.1 - 2026-05-30 - CLI adoption report surface

### Added

- Added the read-only fleet-scoped adoption report command:
  ```text
  canic fleet adoption report <fleet> --profile <profile>
  ```
- Added text output by default, experimental JSON output through
  `--format json`, and explicit report artifact writing through
  `--output <path>`.
- The CLI report command reads the selected fleet config and calls the passive
  host adoption report builder. It does not update config, package manifests,
  topology, deployment truth, artifacts, deployments, or controllers.

### Testing

- `cargo test -p canic-cli fleets --lib --locked`
- `cargo test -p canic-cli --lib --locked`
- `cargo test -p canic-host adoption --lib --locked`
- `cargo clippy -p canic-cli -p canic-host --all-targets --locked -- -D warnings`
- `cargo test -p canic --test changelog_governance --locked`
- `cargo test -p canic --test workspace_manifest --locked`
- `cargo fmt --all --check`
- `git diff --check`

## 0.50.0 - 2026-05-30 - passive adoption report model

### Added

- Added host-side adoption profile and report model types:
  - `AdoptionProfileV1`
  - `AdoptionReportV1`
  - `AdoptionRoleFindingV1`
  - `AdoptionObservedCanisterFindingV1`
  - `AdoptionRecommendationV1`
- Added the first passive adoption report builder:
  ```rust
  adoption_report_from_config_source(...)
  ```
  The builder reads supplied config, package metadata, deployment inventory,
  and artifact manifest evidence, then returns a report. It does not read from
  or write to the filesystem.

### Safety

- Role classification preserves the 0.49 lifecycle split:
  declared-only roles remain visible, attached roles remain explicit, and
  report generation does not make declared-only roles deployable.
- Adoption reports distinguish managed configuration from live authority.
  User-controlled or externally controlled observed canisters are classified
  separately and do not become safely manageable merely because names or roles
  match.
- Recommendations are non-executed report items. Suggested mutating actions are
  marked as unsupported by the `0.50.0` adoption surface.
- The report model includes `attached-unobserved`, `observed-only`,
  `external-controller-required`, `imported-pool-candidate`, and
  `evidence-conflict` classifications for brownfield onboarding.

### Testing

- `cargo test -p canic-host adoption --lib --locked`
- `cargo test -p canic-host --lib --locked`
- `cargo clippy -p canic-host --all-targets --locked -- -D warnings`
- `cargo test -p canic --test changelog_governance --locked`
- `cargo test -p canic --test workspace_manifest --locked`
- `cargo fmt --all --check`
- `git diff --check`
