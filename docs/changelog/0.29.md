## 0.29.9 - 2026-05-01 - Idle Timer Drain Cleanup

### 📝 Summary

- `0.29.9` removes high idle instruction drain from role-attestation key refresh and log-retention timers.

### 🔧 Changed

- Role-attestation key cache setup now runs once at startup instead of polling root every 10 minutes, matching the `0.29` delegated-auth design.

### 🩹 Fixed

- Log retention now returns immediately when the default no-age policy cannot drop anything, avoiding a full stable-log scan on every idle retention tick.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-core attestation -- --nocapture`.
- Ran `cargo test -p canic-core`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.

---

## 0.29.8 - 2026-05-01 - Delegated Upload Auth Guard

### 📝 Summary

- `0.29.8` fixes authenticated upload calls so large non-token endpoint payloads no longer trip the delegated-token guard.

### 🩹 Fixed

- Delegated-token guards now decode only the first Candid argument as `DelegatedToken` with a bounded decoder budget, instead of rejecting based on the full ingress byte length.
- Large trailing arguments, such as image chunks, are left for the endpoint decoder and no longer count as delegated-token payload size.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-core delegated_token_decode -- --nocapture`.
- Ran `cargo test -p canic-core`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.

---

## 0.29.7 - 2026-04-30 - Standards Metadata Fix

### 📝 Summary

- `0.29.7` fixes the `canic_standards` endpoint so macro-generated Canic canisters report their own crate metadata instead of always reporting `canic-core`.

### 🩹 Fixed

- `canic_standards` now reports metadata from the crate exporting the endpoint, so downstream canisters and the built-in `canic-wasm-store` identify themselves correctly.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo test -p canic --test standards_metadata -- --nocapture`.
- Ran `cargo clippy -p canic-core -p canic --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.29.6 - 2026-04-29 - Stable Auth Storage Cleanup

### 📝 Summary

- `0.29.6` removes the last delegated-auth stable key cache that no longer belongs in the hard-cut design and aligns the active topology lookup names around `AppIndex` and `SubnetIndex`.

### 🔧 Changed

- Delegated signer startup now checks signer key material without describing the step as a persisted prewarm.
- Root proof construction resolves shard public keys directly when it needs to embed signed shard-key material in a delegation certificate.
- Active README, test helpers, snapshot labels, and topology progress logs now say `AppIndex` / `SubnetIndex` for lookup indexes instead of using old directory wording.
- Index mappers now use `record_to_input` / `input_to_record`, matching the DTO boundary they actually cross.

### 🗑️ Removed

- Removed `ShardPublicKeyRecord` and the `AuthStateRecord.shard_public_keys` stable field.
- Removed shard public-key cache accessors and the unused shard-key cache warm path from auth ops.

### 📊 Audit

- Rechecked stable storage records and memory IDs for old delegated-auth proof caches, proof history, `AppDirectory` / `SubnetDirectory`, and stale shard-key cache structures.
- Confirmed remaining `Directory*` stable structures belong to keyed placement-directory state, not app/subnet lookup indexes.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo check -p canic-core --all-targets`.
- Ran `cargo check -p canic-macros -p canic -p canic-reference-support -p canic-testing-internal -p canic-tests`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core auth -- --nocapture`.
- Ran `git diff --check`.

---

## 0.29.5 - 2026-04-29 - Shim Removal Follow-Through

### 📝 Summary

- `0.29.5` removes the remaining non-current shim surfaces around authenticated endpoints, config naming, installer aliases, and testkit process-lock parsing.

### 🔧 Changed

- Authenticated endpoint guards now require `DelegatedToken` as the first argument type. Repo test canisters expose that current token type directly instead of using a placeholder argument.
- Current config uses only `app_index` and `subnet_index`; the old `app_directory` and `subnet_directory` parse aliases are gone.
- The installer binary surface now exposes `canic-install-root` only for the thin-root install path.
- Added a normal PocketIC regression that confirms root publishes delegated auth root key material into `SubnetState` and a child verifier accepts a delegated token through the cascaded state path.
- Removed the deleted `canic-tests --test delegation_flow` target from the workspace test runner.
- Role-attestation cache startup now follows `auth.role_attestation_cache` directly instead of being gated by delegated-token auth being enabled.
- Per-canister auth config now uses the neutral `[...canisters.<role>.auth]` table with explicit `delegated_token_signer` and `role_attestation_cache` fields.
- Renamed the reference canister support package from `canic-internal` to `canic-reference-support`, with Rust imports now using `canic_reference_support`.
- Renamed the endpoint macro package from `canic-dsl-macros` to `canic-macros`, matching its actual role as Canic's proc-macro crate.
- Renamed broad auth identifiers so current code says `AuthApi`, `AuthOps`, `AuthState`, `AuthStateOps`, `AuthOpsError`, `VerifiedDelegatedToken`, and `DelegatedAuthMetrics` instead of carrying delegated-token-only or old state names.
- Renamed role-attestation startup identifiers to `canic_role_attestation_refresh`, `RoleAttestationKeyRefreshWorkflow`, and `with_role_attestation_refresh`, matching the refresh workflow they actually control.
- The packaged downstream installer verification fixture now uses `app_index` / `subnet_index`, matching the current config contract.
- Non-root lifecycle now uses one init/post-upgrade path with an explicit role-attestation refresh flag, removing the duplicated `with_role_attestation_refresh` lifecycle wrappers.

### 🗑️ Removed

- Removed `Reserved` as an authenticated endpoint placeholder, old exported endpoint macro aliases, `__canic_load_config!`, the `app_directory` / `subnet_directory` config aliases, and the `canic-install-reference-topology` binary alias.
- Removed the pid-only PocketIC process-lock owner parser fallback; current owner records must use the structured `pid=` format.
- Removed the stale `canic_delegated_auth_signer` build cfg. Signer key material is prewarmed through runtime config, not a separate compile-time lifecycle branch.
- Removed the misleading per-canister `delegated_auth` table name.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `git diff --check`.
- Ran `cargo check -p canic-macros -p canic-installer`.
- Ran `cargo check -p canic-reference-support -p canic-testing-internal -p canic-tests`.
- Ran `cargo check -p canic-core --all-targets`.
- Ran `cargo check -p canic -p canic-installer -p canister_user_shard -p project_instance_stub -p canister_test -p delegation_signer_stub -p project_hub_stub`.
- Ran `cargo clippy -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core auth -- --nocapture`.
- Ran `cargo clippy -p canic-testkit --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core config -- --nocapture`.
- Ran `cargo test -p canic-testkit process_lock -- --nocapture`.
- Ran `cargo test -p canic-macros authenticated -- --nocapture`.
- Ran `cargo test -p canic --test install_script_surface -- --nocapture`.
- Ran `cargo test -p canic-tests delegated_token_verification_uses_cascaded_subnet_state_root_key -- --nocapture`.
- Ran `bash -n scripts/ci/verify-packaged-downstream-installer.sh`.

---

## 0.29.4 - 2026-04-29 - Delegated Auth Cohesion

### 📝 Summary

- `0.29.4` tightens the final delegated-auth shape so the implementation, trust model, and 0.29 design notes describe one coherent hard-cut system.

### 🔧 Changed

- Removed the misleading verifier-side `expected_shard_key_hash` input from the pure delegated-token verifier. Verification now reflects the actual hard-cut model: root signs the embedded shard public key, the certificate carries the public-key hash, and runtime checks deterministic `ShardKeyBinding` metadata.
- Updated the `0.29` delegated-auth design doc so shard-key validation is described as signed shard-key binding, not verifier-side shard-key derivation.
- Refreshed the root README and installer README so the setup path, repository layout, delegated-auth description, and thin-root install flow are shorter and match the current implementation.
- Added `docs/architecture/authentication.md` as the canonical current auth design handoff for delegated tokens, delegated sessions, role attestations, stable key caches, config boundaries, removed legacy concepts, and expected failure modes.
- Added `SubnetState.auth.delegated_root_public_key` as the subnet-scoped local source for delegated root trust material. Verifier canisters now read cascaded root keys directly from `SubnetState` instead of fetching them from authenticated guards.
- Cleaned the remaining delegated-auth lifecycle wording so root publishes auth material, signers warm key material, and tests no longer refer to removed proof-cache state.

### 🩹 Fixed

- Fixed a trust-model ambiguity where runtime verification passed `cert.shard_key_hash` back as the expected shard key hash. That check was tautological and made the verifier look like it had shard-key authority it does not own.
- Fixed the cold authenticated-query contract gap by removing verifier-side root-key fetch-on-verify and the duplicated stable auth root-key cache. Delegated-token verification now reads local `SubnetState` only and fails as a Canic auth error when the cascaded root key is unavailable.

### 📊 Audit

- Rechecked the delegated-auth surface for leftover proof-cache, V2 naming, root-key fallback, and verifier proof-miss concepts.
- Rechecked stable key-cache identity, role-attestation key refresh, canonical encoding, TTL boundaries, and session expiry behavior against the hard-cut design.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `git diff --check`.
- Ran `cargo check -p canic-core`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core subnet_state -- --nocapture`.
- Ran `cargo test -p canic-core auth -- --nocapture`.
- Ran `cargo check -p canic -p canic-core -p delegation_root_stub -p sharding_root_stub`.

---

## 0.29.3 - 2026-04-29 - Delegated Auth Name Cleanup

### 📝 Summary

- `0.29.3` finishes the hard-cut cleanup by making delegated auth the single public token model. Temporary migration suffixes are gone from DTOs, Rust APIs, endpoint names, and the internal auth implementation module.

### 🔧 Changed

- Renamed public delegated-auth DTOs to their final unsuffixed names, including `DelegatedToken`, `DelegationProof`, and `DelegationAudience`.
- Renamed root delegation request wiring to the final `canic_request_delegation` / `CANIC_REQUEST_DELEGATION` surface.
- Moved the self-validating auth implementation out of the temporary versioned module path and collapsed internal helper names to the unsuffixed delegated-auth model.
- Made stable auth key caches identity-bound by configured ECDSA key name and public-key hash, so config key changes force an immediate refetch instead of reusing stale root, shard, or attestation keys.
- Added explicit key identity to `AttestationKey` so role-attestation key-set refresh preserves root-provided key names and hashes.
- Removed the stale verifier-proof check-cfg declaration from the facade build script.
- Updated the delegated-signature contract document so it describes self-validating tokens instead of verifier-local proof matching.

### 🩹 Fixed

- Fixed stale stable key reuse after `auth.delegated_tokens.ecdsa_key_name` or `auth.role_attestation.ecdsa_key_name` changes. Identity-less key-cache records are invalid state in the hard-cut schema.

### 🗑️ Removed

- Removed duplicate helper names with temporary migration suffixes.
- Removed the remaining unused proof-cache validation errors from delegated-token ops.

### ⚠️ Breaking

- Downstream callers must use the unsuffixed delegated-auth DTO and API names. The token wire format still carries `version = 2` internally; this patch only removes the temporary migration naming.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-core key_state -- --nocapture`.
- Ran `cargo test -p canic-core auth -- --nocapture`.
- Ran `cargo check --workspace`.
- Ran `cargo clippy --workspace --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core auth::delegated -- --nocapture`.
- Ran `cargo test -p canic-macros authenticated -- --nocapture`.

---

## 0.29.2 - 2026-04-29 - Delegated Auth Hard Cut

### 📝 Summary

- `0.29.2` completes the hard break from proof-cache based delegated auth to self-validating delegated tokens. Delegated auth verification no longer depends on verifier-local proof state, proof fanout, or topology-time catch-up.

### 🔧 Changed

- Changed endpoint guards to current-shape delegated-token decoding. Authenticated endpoints now accept `DelegatedToken` directly or use `Reserved` while the guard decodes the first argument as delegated auth.
- Renamed the old verifier-style per-canister flag to the current role-attestation key-cache behavior.
- Changed signer lifecycle prewarm to warm local shard signing key material only. Startup no longer requests or stores delegation proofs.
- Removed `auth.delegated_tokens.proof_cache` from config and bootstrap rendering because proof-cache sizing is no longer part of delegated-auth correctness.

### 🗑️ Removed

- Removed V1 delegated-token DTOs, root `IssueDelegation` capability routing, proof-cache stable storage, verifier proof-install endpoints, proof-fanout metrics, admin repair DTOs, and V1-only PocketIC suites.
- Removed creation-time verifier proof catch-up from topology state propagation. New canisters no longer receive cached proofs during placement/state propagation.
- Removed old local/dev proof-material endpoints and signer-proof status helpers from the demo/test canisters.

### ⚠️ Breaking

- V1 delegated tokens are no longer accepted. Callers must mint and send `DelegatedToken`.
- The old proof distribution contract is gone: there is no verifier-local proof presence check, no proof-cache tuning config, no creation-time catch-up, and no repair fanout endpoint.

### 📚 Documentation

- Updated the implementation status tracker so current-shape guards, signer lifecycle prewarm, root-owned TTL policy, topology catch-up removal, and V1 API/proof-fanout removal are marked complete.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `cargo check --workspace`.
- Ran `cargo clippy --workspace --all-targets -- -D warnings`.
- Ran `cargo test -p canic-core auth::delegated -- --nocapture`.
- Ran `cargo test -p canic-macros authenticated -- --nocapture`.
- Ran `git diff --check`.

---

## 0.29.1 - 2026-04-28 - Delegated Auth Primitives

### 📝 Summary

- `0.29.1` expands Delegated Auth from the initial design and canonical data model into pure issuance, minting, verification, trust-policy primitives, internal root signing, internal shard signing, internal verifier validation, explicit API helpers, an explicit root delegation endpoint, signer-facing mint helpers, root-key pull-on-verify, current-shape guard validation, delegated signer lifecycle prewarm, root-owned TTL policy enforcement, and the first proof-sync removal from topology propagation.

### ➕ Added

- Added delegated auth audience and certificate policy helpers for role/principal subset checks, verifier membership, single-role hash binding, root principal binding, TTL enforcement, and shard public-key hash validation.
- Added root-key trust-anchor resolution through one explicit trusted root public key.
- Added a pure delegated verifier path that validates self-contained tokens without local proof lookup.
- Added pure root-side delegated auth delegation proof issuance with canonical cert-hash signing, role-hash binding, and TTL policy enforcement.
- Added an internal root-side delegated auth signing hook that prepares the canonical certificate, binds the configured IC threshold-ECDSA key and shard derivation path, signs the prepared cert hash, and returns a self-contained `DelegationProof` without verifier fanout.
- Added pure shard-side delegated token minting with embedded proof preservation, cert-hash binding, audience/scope attenuation, TTL checks, and injected shard signing.
- Added an internal shard-side delegated auth signing hook that prepares canonical delegated token claims, signs the prepared claims hash with the local shard threshold-ECDSA derivation path, and returns a self-contained `DelegatedToken`.
- Added an internal verifier-side delegated auth validation hook that checks local shard key binding, resolves the local root trust anchor from the key cache, applies local role membership, and delegates cryptographic validation to the pure delegated verifier without proof-cache lookup.
- Added explicit `AuthApi` helpers for local root proof issuance, signer token issuance from an explicit proof, and verifier validation.
- Added the explicit root `canic_request_delegation` endpoint and matching protocol constant so callers can request self-contained `DelegationProof` values.
- Added a remote `AuthApi::request_delegation` helper for signer-to-root proof requests.
- Added `DelegatedTokenMintRequest` and `AuthApi::mint_token`, which request a root proof and immediately mint a self-contained delegated token without using local proof-cache state.
- Added local/dev delegated signer endpoints on the user-shard demo and delegation signer stub for exercising the one-call delegated auth mint path.
- Added delegated verifier root-key pull-on-verify. When a delegated token reaches a guarded endpoint and the verifier has no matching root key cached, Canic fetches the expected root threshold-ECDSA public key by configured key name and root derivation path, then rejects the token if the fetched key hash does not match the certificate.
- Added current-shape delegated-token decoding in endpoint guards. Guard validation uses the self-contained verifier path and does not look up local delegation proofs.
- Added macro support for `DelegatedToken` and Candid `Reserved` as authenticated endpoint first-argument types, then moved repo-local protected token endpoints to `Reserved` so guarded methods can hide token wire details from endpoint signatures.

### 🔧 Changed

- Moved certificate/token TTL policy ownership fully to root-side config. `DelegationProofIssueRequest` now carries the requested certificate TTL only; root derives policy ceilings from `auth.delegated_tokens.max_ttl_secs`.
- Changed signer lifecycle prewarm to warm local shard signing key material only. Startup no longer requests or stores a delegation proof.

### 🗑️ Removed

- Removed creation-time verifier proof catch-up from topology state propagation. New canisters no longer receive root-cached V1 proofs as part of placement/state propagation; delegated token acceptance is independent of creation order.

### 📚 Documentation

- Updated the implementation status tracker so pure delegated auth issue, mint, verify, internal root signing, internal shard signing, internal verifier validation, explicit API helpers, the root delegation endpoint, remote proof request helper, signer-facing mint helper, local/dev signer endpoints, root-key pull-on-verify, current-shape guard validation, repo-local endpoint surface migration, delegated signer lifecycle prewarm, root-owned TTL policy, and topology catch-up removal are marked complete.

### 🥾 Governance

- Documented that automated agents may inspect Git state but must not run `git add`, `git commit`, or `git push`; staging, committing, and pushing are human-owned actions.

### 🧪 Testing

- Added coverage for claims-level canonical audience/scope normalization, exact token TTL boundary handling, `Roles` claims under `RolesOrPrincipals` certificates, nonce behavior, and signed scope tampering.
- Ran `cargo fmt --all`.
- Ran `cargo test -p canic-macros authenticated -- --nocapture`.
- Ran `cargo test -p canic-core auth -- --nocapture`.
- Ran `cargo test -p canic-core runtime_auth -- --nocapture`.
- Ran `cargo clippy -p canic-macros --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p canic --all-targets -- -D warnings`.
- Ran `cargo clippy -p canister_user_shard --all-targets -- -D warnings`.
- Ran `cargo clippy -p canister_test --all-targets -- -D warnings`.
- Ran `cargo clippy -p delegation_signer_stub --all-targets -- -D warnings`.
- Ran `cargo clippy -p project_hub_stub --all-targets -- -D warnings`.
- Ran `cargo clippy -p project_instance_stub --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.29.0 - 2026-04-28 - Delegated Auth Design

### 📝 Summary

- `0.29.0` starts the Delegated Auth line with a hard-cut design for self-validating delegated tokens that do not depend on verifier-local proof caches.

### ➕ Added

- Added auth wire types for `DelegationCert`, `DelegationProof`, `DelegatedTokenClaims`, `DelegatedToken`, root key trust material, shard key binding, and role/principal audiences.
- Added `CanicAuthCanonical` hashing for certificates, token claims, root-key certificate payloads, and role hashes with deterministic sorting, deduplication, domain separation, and ASCII role/scope validation.

### 📚 Documentation

- Added the Delegated Auth design:
  [docs/design/0.29-delegated-auth/0.29-design.md](../design/0.29-delegated-auth/0.29-design.md)
- Added the matching implementation status tracker:
  [docs/design/0.29-delegated-auth/0.29-status.md](../design/0.29-delegated-auth/0.29-status.md)

### ⚠️ Breaking

- Defines delegated auth as a hard-cut replacement for proof-cache based delegated auth. The target model removes verifier proof replication, proof fanout, topology catch-up, and proof-cache equality from the authentication correctness path.

### 🧭 Migration Notes

- `0.29.2` completes the hard cut; there is no V1 acceptance window in this line.

### 🧪 Testing

- Ran `cargo fmt --all`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-029-auth-test cargo test -p canic-core auth::delegated -- --nocapture`.
- Ran `CARGO_TARGET_DIR=/tmp/canic-029-auth-test cargo clippy -p canic-core --all-targets -- -D warnings`.
- Ran `git diff --check`.
