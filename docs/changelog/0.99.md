# Canic 0.99 Changelog

## [0.99.11] - 2026-07-24 - Canonical Fleet Activation Status

### ✨ Added

- Every maintained runtime bundle now emits the exact controller-only
  `canic_fleet_activation_status` query. The endpoint is shared by root,
  non-root and built-in Wasm-store Canisters and has one protocol constant and
  one checked Candid surface.
- The status path reads the sole protected ID-21 record through
  `api → workflow → ops → storage` and returns the exact activation identity,
  phase and role-valid evidence. The root may project its source cascade and
  current credential manifests; non-roots may project only applied cascade
  evidence and the accepted credential-generation reference.

### 🔒 Recovery

- Missing protected state and contradictory role/evidence combinations return
  typed failures. `Active` without both cascade and credential evidence,
  non-root source manifests, root applied evidence, duplicate or unbound
  credential generations, and a missing current manifest cannot be projected
  as successful recovery state.
- This is an observation-only prerequisite for the live install handoff. It
  does not initialize ID 21, activate a Canister, start timers, deliver
  credentials or make a `Prepared` Canister operational.

### 🧪 Testing

- Focused core tests cover absent state, exact `Prepared` identity, complete
  root `Active` projection and contradictory protected records. Facade tests
  prove the shared emitter and protocol constant, while protocol tests parse
  the checked Wasm-store DID, pin the named response shapes and enforce the
  controller-query guard.
- Public Candid adds only the designed activation-status query and its named
  response types. No package version, dependency, lockfile, stable-memory,
  JSON, CLI or live installation behavior changes.

## [0.99.10] - 2026-07-24 - Verified Root Installation

### 🩹 Fixed

- Root installation now hashes the exact raw Wasm before mutation, queries the
  installed Canister through the maintained ICP status client after command
  success, and requires the returned 32-byte module identity to match. Missing,
  malformed or different module evidence preserves a typed failure and stops
  the install before bootstrap, readiness or installed-state publication.
- The phase-operation boundary now distinguishes attempted evidence from an
  observed postcondition. A successful operation returns the exact durable
  receipt path; a post-effect observation failure records the attempted facts
  as inconclusive rather than labeling them observed.
- The root-install receipt records the exact root principal plus canonical
  expected and observed module hashes, then passes through the existing strict
  Fleet-activation receipt admission before later activation work begins.
  There is still one receipt writer, one status client and one journal
  admission path.

### 🧪 Testing

- Focused host tests prove exact prefixed module-hash acceptance, missing and
  mismatched observation rejection, durable receipt-path return, root-principal
  projection, canonical receipt re-admission and typed phase failure behavior.
- Strict Clippy passes for the changed host library and tests. No package
  version, dependency, lockfile, Candid, stable-memory, JSON schema, endpoint
  or CLI contract changes. The public host error surface adds only the typed
  module-verification cause now produced by the maintained install workflow.

## [0.99.9] - 2026-07-24 - Protected Fleet Activation Owner

### 🔧 Changed

- Qualified Canister builds now validate and embed the exact
  `CANIC_RELEASE_BUILD_ID` supplied by the pre-build release plan. Runtime
  admission has one typed ops path for reading that baked identity; a missing
  or non-canonical value fails closed instead of becoming an ambient default.
- Every managed runtime role now selects the new
  `canic.core.fleet_activation.v1` allocation at stable-memory ID 21. The role
  catalog, allocation registry, state descriptors, audit manifest and built-in
  Wasm-store contract agree on that single owner.
- The protected store contains either no record or exactly one bounded
  `FleetActivationRecord`; there is no persisted `Unbound` variant or second
  Fleet binding. Its current schema includes the frozen `Prepared`/`Active`,
  cascade and credential-manifest shapes required by the 0.99 design.
- The exact `CurrentRootInstallIdentity` Candid record carries
  `FleetBinding`, install ID, release-build ID and optional expected module
  hash. Admission compares the supplied and embedded build identities before
  normalizing `install_id` to the sole internal `operation_id`, then prepares
  an empty-evidence `Prepared` record. Existing protected state cannot be
  overwritten.

### 🧪 Testing

- Focused core tests cover the root-input Candid round trip with canonical text
  IDs, embedded build-ID parsing, supplied/embedded mismatch rejection,
  normalization, stable-record encoding, absent-before-init state, one-time
  `Prepared` commitment and no-write failure behavior.
- Role-contract and state-manifest tests prove ID 21 is unique, described and
  selected by every runtime role, including the built-in Wasm store.
- Root lifecycle, the start macro and the maintained host installer are not
  switched in this foundation batch. The old root init path remains live until
  one later atomic batch can change both sides without publishing an
  uninstallable Wasm. No package version, dependency, lockfile, endpoint,
  exported Canister Candid, CLI or live installation behavior changes here.

## [0.99.8] - 2026-07-24 - Root-Install Receipt Authority

### 🔧 Changed

- The fresh-install journal now admits its exact sequence-one
  `RootInstalled` state. The transition revalidates the sequence-zero
  authority under the same network/Fleet lock, atomically replaces it and
  re-reads the durable result. A stale source journal or conflicting receipt
  hash fails closed.
- Root-install receipt admission reads a regular no-follow file and requires
  the canonical durable JSON encoding of one completed successful
  `install_root` phase. The exact root principal must agree with receipt
  identity, and canonical expected and observed module hashes must match.
- `RootInstalled` stores the raw SHA-256 of those exact receipt bytes.
  Repeating the transition with the same durable receipt is idempotent;
  changing the receipt is rejected. `CanistersPrepared` and later phases
  remain reserved and unencodable.

### 🧪 Testing

- Sixteen focused host tests cover canonical receipt hashing, root and module
  evidence, principal and module mismatch rejection, non-canonical JSON,
  missing and symlinked receipts, monotonic publication, exact retry,
  stale-journal conflict and reserved-phase rejection in addition to the
  existing planning/discovery matrix.
- The maintained installer is deliberately not wired yet: it still uses the
  pre-cut root init contract and does not produce verified post-install module
  evidence. No package version, dependency, lockfile, Candid, public JSON,
  stable-memory, endpoint, CLI or live installation behavior changes here.

## [0.99.7] - 2026-07-24 - Single-Owner Activation Admission

### 🔧 Changed

- Fresh-install planning now holds one close-on-exec kernel lock per canonical
  network and Fleet name. The lock serializes discovery and identity
  allocation but is not persisted recovery truth; the canonical journal
  remains the only durable activation authority.
- Discovery scans the canonical network subtree in deterministic path order
  and validates every published journal before selecting a Fleet. Unsafe,
  non-canonical, corrupt or path-mismatched entries, competing Fleet-name
  authorities and duplicate Fleet-ID claims fail closed with typed causes.
- An exact App and finalized-release-build match resumes the existing
  sequence-zero journal without allocating a new Fleet or operation identity.
  App and release-build contradictions are rejected. A directory left before
  `journal.cbor` publication is inert and does not become authority.

### 🧪 Testing

- Twelve focused host tests now cover exact idempotent resume, two-thread
  create concurrency, App and release-build contradictions, duplicate name and
  Fleet-ID authority, incomplete attempts, invalid and symlinked directories,
  canonical durability, collision safety, corruption, phase legality and
  no-follow journal reads.
- No Cargo package version, dependency, lockfile, CBOR shape, Candid, JSON,
  stable-memory, endpoint, CLI or live installation behavior changes in this
  batch.

## [0.99.6] - 2026-07-24 - Planned Fleet-Activation Journal Authority

### ✨ Added

- The passive `canic-core` activation shapes now give the host and later
  Canister runtime one named `FleetActivationIdentity` and
  `FleetActivationHostRecord`; no host-only shadow identity was introduced.
- A private host journal owner canonically encodes the accepted
  `FleetInstallActivationJournal` field order and declaration-order phase
  discriminants. Its `Planned` record binds canonical network, generated Fleet
  ID, generated operation ID, App, Fleet name, release-build ID, exact
  release-build-plan hash and release-set manifest digest.

### 🔒 Recovery

- Planning re-reads the durable finalized `ReleaseBuildPlanRecord` and rejects
  changed, missing or merely planned evidence before generating identities.
  Journal publication is atomic, create-new and no-follow under
  `.canic/recovery/fleet-install-activations/<network-id>/<fleet-id>/<operation-id>/journal.cbor`.
- The reader re-encodes exact canonical CBOR and checks network, Fleet and
  operation path identity. Malformed, non-canonical, symlinked, path-mismatched
  or conflicting evidence fails closed.
- Only sequence-zero `Planned` with empty post-mutation evidence is admitted.
  `RootInstalled` and later discriminants are reserved but rejected until
  their live transition and reconciliation logic is implemented; the current
  install workflow does not yet publish this journal.
- Release-build nonce generation and Fleet/operation identity generation now
  share one private exact OS-entropy reader rather than duplicating platform
  random-read loops.

### 🧪 Testing

- Six focused host tests prove canonical durable round trips, finalized-build
  admission, create-new collision safety, corruption and non-canonical
  rejection, all three path-identity bindings, phase legality and no-follow
  symlink rejection.
- No Cargo package version, dependency, lockfile, Candid, JSON, stable-memory,
  endpoint, CLI or live installation behavior changes in this batch.

## [0.99.5] - 2026-07-24 - Fresh-Install Fleet/App Identity

### ⚠️ Breaking

- Fresh installation now requires `canic install <app> <fleet>`. Both
  identities are required operands: App selects source configuration and Fleet
  names the installed target. The removed one-argument and `--app` forms have
  no alias or inferred fallback.
- `InstallRootOptions` now carries required `fleet_name` and optional
  `expected_app` fields. The misleading optional `deployment_name` and
  `expected_fleet` fields are removed from this Fleet-install boundary.

### 🔧 Changed

- Fresh install resolves `apps/<app>/canic.toml`, verifies its exact
  `[app].name`, and independently validates the operator-facing Fleet name.
  The Fleet name owns deployment-truth and installed-target lookup while the
  App identity owns source configuration.
- Install output and recovery guidance name the separate Fleet and source App
  explicitly. Config-selection guidance emits only the maintained two-identity
  command.

### 🧪 Testing

- Focused CLI tests prove `--app` is required, distinct Fleet/App values are
  retained, and current usage is documented. Host tests prove a Fleet label
  can differ from its validated source App without either identity being
  inferred from the other.
- No Cargo package version, lockfile, dependency, Candid, JSON, stable-memory,
  endpoint, release-build, Fleet-catalog or activation-journal contract changes
  in this batch.

## [0.99.4] - 2026-07-24 - Durable Release-Build Identity

### ✨ Added

- One `ReleaseBuildPlanRecord` now owns the 32-byte cryptographic nonce,
  domain-separated `ReleaseBuildId` and monotonic `Planned` or `Finalized`
  state at
  `.canic/release-builds/<release-build-id>/plan.cbor`.
- Release-build records use the design's exact fixed-array canonical CBOR.
  Their identity is recomputed from the nonce on every read, and finalized
  records expose the exact domain-separated `release_build_plan_hash` needed
  by later fresh-install recovery.

### 🔧 Changed

- A normal complete install creates and syncs its release-build plan before
  compiling any selected Wasm. Every child Cargo build, including nested
  bootstrap builds, receives the same exact planned ID through a host-owned
  build context; ambient values are removed.
- The Canic build macro validates the canonical release-build ID and retains
  it in selected Wasms. After all exact artifacts exist, install finalizes the
  plan from the no-follow release-set manifest bytes before resolving or
  creating the root Canister.
- Planning uses the operating system cryptographic random source. Publication
  is create-new and collision-safe; finalization is atomic, one-way and
  file-locked across competing host processes, and idempotent only for the
  same exact manifest digest.

### ⚠️ Breaking

- `WorkspaceBuildContext` now carries optional `release_build_id` explicitly.
  `None` means an unqualified development artifact; normal source Fleet
  install selects only an ID backed by the durable release-build plan.

### 🧪 Testing

- Focused core tests prove the exact non-circular ID derivation and canonical
  text. Host tests prove canonical planned bytes, path/nonce/ID agreement,
  exact final hashes, idempotent finalization, conflicting-finalization
  rejection and fail-closed malformed, non-canonical and symlink evidence.
- The complete `canic-host` library suite passes with 863 tests. No Cargo
  package version, lockfile, Candid, JSON, stable-memory, endpoint, Fleet
  catalog or activation-journal contract changes in this batch. Supplied
  deployment plans are not yet admitted as 0.99 release-build authority.

## [0.99.3] - 2026-07-23 - Fleet Identity and Catalog Read Authority

### ✨ Added

- `FleetId` is an immutable 32-byte identity with one exact 64-character
  lowercase hexadecimal text and JSON representation. `FleetName` is a
  separately validated operator label, while `FleetKey` qualifies the ID by
  canonical network and `FleetBinding` binds it to one source App.
- Fleet catalog JSON rows carry the exact canonical network, generated Fleet
  ID, Fleet name, App, environment provenance, deployment time, root principal
  and root-verification state. Schema, network, ordering, name, ID, environment
  and principal contradictions fail closed.

### ⚠️ Breaking

- `canic deploy inspect catalog list|inspect` reads only
  `.canic/networks/<canonical-network-id>/fleets/catalog.json` after resolving
  the selected environment profile. The removed
  `.canic/<environment>/deployments/*.json` catalog path is never scanned and
  no fallback or compatibility report remains.
- The public catalog JSON projection is `FleetCatalogReportV1` with
  `canonical_network_id`, `environment` and Fleet identity rows. The old
  deployment/fleet-template fields, per-entry warning projection and
  `DeploymentCatalog*` Rust API are removed.

### 🔧 Changed

- A missing network catalog is one empty Fleet catalog. Malformed, unknown,
  non-canonical, cross-network, duplicate, unsorted, symlinked or special-file
  authority rejects rather than being downgraded to a warning.
- Network enrollment and Fleet catalog reads share one no-follow regular-file
  reader, removing the duplicate host filesystem safety flow.
- App and Fleet labels now enforce the same existing 40-byte canonical-name
  limit in addition to the established filesystem-safe character rule.

### 🧪 Testing

- Focused identity tests prove Fleet ID text parsing, Fleet-name admission and
  network-qualified App binding. Host tests prove alias convergence, strict
  catalog validation, missing-catalog behavior, rejection of symlinked
  authority and the absence of a legacy deployment-state fallback.
- Focused catalog CLI tests cover parsing, help, Fleet JSON output and command
  dispatch. Formatting, strict affected-package Clippy and the relevant
  governance checks pass.
- No Cargo package version, dependency, lockfile, Candid, stable-memory,
  runtime endpoint, catalog writer or activation-journal contract changes in
  this batch. Fleet ID generation and durable commitment remain owned by the
  later activation-journal work.

## [0.99.2] - 2026-07-23 - Canonical Network Trust Authority

### ✨ Added

- `CanonicalNetworkId` is one domain-separated 32-byte trust identity with an
  exact 64-character lowercase hexadecimal text and JSON form. Public callers
  can derive it only from Canic's pinned public-IC DER anchor or a validated
  enrolled DER trust anchor.
- `canic network enroll <environment> --root-key <der-file> --fingerprint
  <64-lowercase-hex>` is the sole non-public enrollment command. It accepts
  only a regular, non-symlink DER file and verifies the exact SHA-256
  fingerprint before any durable write.

### 🔧 Changed

- Non-public authority is written to
  `.canic/networks/<canonical-network-id>/trust/root-key.der` and
  `enrollment.json` before
  `.canic/environment-profiles/<environment>/network.json` becomes visible.
  The profile contains only the canonical ID and is revalidated against the
  exact authority on every resolution.
- Exact re-enrollment and recovery after an authority-only partial publication
  are idempotent. Conflicting profile bindings, changed anchors, incomplete or
  contradictory authorities, special files, symlinks, and non-canonical
  fingerprints fail closed. No trust-on-first-use, anchor rotation, alias, or
  compatibility path exists.
- Environment labels remain connectivity/provenance selectors. Named staging
  and production profiles backed by `ic` resolve to the same compiled
  public-IC identity; aliases enrolled with the same non-public anchor share
  one network authority.
- The canonical durable writer now supports create-new publication with
  synced missing parents and rejects symlinked parent directories. Enrollment
  reuses that owner rather than adding another atomic-write flow.

### 🧪 Testing

- Focused core identity tests cover domain separation, canonical text, strict
  parsing, and pinned/enrolled DER equivalence; host document tests prove the
  exact JSON projection.
- Host tests cover successful enrollment and resolution, exact idempotency,
  partial-publication recovery, local and public aliases, fingerprint mismatch,
  non-canonical fingerprints, changed anchors, missing authority, public-IC
  enrollment rejection, symlinks, special files, and symlinked output parents.
- Focused CLI parsing, help, version, rendering, formatting, diff hygiene, and
  strict all-target Clippy pass for `canic-core`, `canic-host`, and
  `canic-cli`.
- No Cargo package version, production dependency, Candid, stable-memory,
  runtime endpoint, Fleet ID, install-state, or deployment JSON contract
  changes in this batch. The new host enrollment/profile JSON documents and
  CLI command are intentional 0.99 surfaces.

## [0.99.1] - 2026-07-23 - App Source Surfaces

### ⚠️ Breaking

- The maintained source tree is `apps/<app>/`; `fleets/` is removed and no
  discovery fallback searches both locations.
- Canister package metadata uses `[package.metadata.canic] app = "<app>"`.
  The old `fleet` key is not read.
- Source commands are `canic app ...`, and artifact builds take
  `canic build <app> <role>`. The old `canic fleet ...` command family is
  removed without an alias.

### 🔧 Changed

- App identity now owns config discovery and snapshots, source role
  declaration/attachment/rename, scaffolding, role-package validation,
  build-time environment labels, artifact provenance, adoption reports, and
  source-oriented evidence selectors.
- The root workspace, demo/test Apps, standalone canisters, role-contract
  fixtures, build scripts, inventory guards, packaged-downstream checks, and
  active source documentation all use the canonical App path and metadata
  vocabulary.
- Evidence targets distinguish `app` from `fleet`; App adoption and build
  provenance emit App identity, and project evidence policies can select it
  independently. Live install/deployment/catalog behavior remains unchanged
  for the later Fleet live-surface slice.
- The private evidence-gate result stores its two report payloads indirectly
  after the additional App selector increased their stack footprint. Report
  JSON and CLI output are unchanged.

### 🧪 Testing

- Focused App CLI, list, Medic, evidence, config discovery, release-set,
  build-provenance, adoption, role-contract, workspace-manifest,
  reference-surface, timer/receipt inventory, and blob-storage inventory
  checks pass. The complete `canic-cli` library suite passes 608 tests with
  only its explicitly disposable-environment recovery test ignored.
- Formatting, diff hygiene, and strict all-target Clippy pass for the affected
  `canic-core`, `canic`, `canic-host`, and `canic-cli` packages.
- No Cargo package version, dependency, lockfile, Candid, stable-memory, or
  live deployment contract changed. Source paths, package metadata, Rust
  source models, CLI commands, and source/evidence JSON intentionally break.

## [0.99.0] - 2026-07-23 - Reinstall-Only Minimum App Config Hard Cut

### ⚠️ Breaking

- 0.99 is fresh-install/reinstall-only. It does not upgrade or migrate 0.98
  Canister stable state, installed-state trees, deployment catalogs, or
  authorization records.
- The top-level `app_index` configuration field is removed. App-declared
  Fleet service roles now use:

  ```toml
  [services.fleet]
  roles = ["project_hub"]
  ```
- `[fleet].name` and `FleetConfig` are removed. Every maintained config now
  declares its required immutable source identity as `[app].name`.
- The source-topology type and reserved slot are hard-cut from
  `SubnetRole::PRIME` / `subnets.prime` to
  `SubnetSlotId::DEFAULT` / `subnets.default`.
- Keyed placement config is `binding`; the old `directory` config key and
  `DirectoryConfig` type are removed.

### 🔧 Changed

- `AppId`, `AppRoleRef` and `AppConfigSnapshot` are the source-config identity
  surfaces. Parsing, validation, build support and auth provenance derive the
  exact App identity from one required field.
- Configuration parsing and validation consume one nested Fleet-service
  declaration and one keyed placement binding while preserving current
  one-member placement and runtime Directory behavior.
- Generated compiled configuration, checked-in canister/Fleet configs,
  scaffolding, host role mutation, verbose config diagnostics, CI fixtures,
  and active operator guidance emit the current App/default/binding shape.
- Config-derived diagnostic sources use `app_config`; no `fleet_config`
  source label remains in the changed planning and medic paths.
- The 0.99 and 0.100 contracts now derive future work from completed
  fresh-install evidence rather than a nonexistent 0.98 migration receipt.

### 🧪 Testing

- Focused Canic-core configuration/bootstrap tests, Canic-host release-set and
  ICP-config tests, Canic CLI scaffold tests, Canic config/reference-surface
  tests, targeted package checks, formatting and strict Clippy pass for the
  changed packages.
- No Cargo package version, dependency, Candid endpoint label, JSON, CLI
  command, or stable allocation changes. The Rust config API and source TOML
  schema intentionally break; reinstall-only means no old slot value or
  config shape is decoded.
