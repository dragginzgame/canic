## 0.32.6 - 2026-05-08 - Positional Fleet Commands

### 🔧 Changed

- Finished the fleet argument cleanup across the remaining fleet-scoped commands: `canic install`, `canic medic`, and `canic snapshot download` now take the fleet name as a required positional argument.
- Updated command help, scaffold next-step output, operator hints, docs, and local make targets so the CLI consistently teaches positional fleet selection instead of `--fleet <name>`.
- Changed `canic config <name>` so the root role renders `AUTO` as `-`, because auto-create only applies to child canisters.
- Kept network selection as an explicit per-command option with `local` as the implicit network when `--network <name>` is omitted.

### 🧭 CLI Examples

```bash
canic install test
canic medic test --network local
canic snapshot download test --canister <canister-id> --dry-run
```

---

## 0.32.5 - 2026-05-08 - Fleet Layout

### 🧹 Cleanup

- Made `fleets/test/` the dfx/CI-backed reference topology with the full root, app, minimal, sharding, and scaling canister set.
- Kept `fleets/demo/` as a small root-plus-app fleet for quick experiments.
- Moved isolated PocketIC and integration-test fixture canisters from `fleets/test/*` to `canisters/test/*`, preserving package names while keeping `fleets/` reserved for installable fleet directories.
- Updated workspace membership and docs so `canic fleet list` now discovers only actual fleets such as `demo` and `test`.
- Removed the public `canic network` command and the persisted current-network/current-fleet marker files; fleet-scoped commands now take the fleet name as a positional argument and select non-local networks per command with `--network <name>`.
- Changed `canic install`, `canic config`, `canic list`, `canic medic`, and `canic snapshot download` to take the fleet name as a required positional argument, for example `canic config demo`.
- Added `CAPABILITIES`, `AUTO`, and `TOPUP` columns to `canic config <name>` so declared roles show enabled config features, auto-create membership, and configured top-up policy at a glance.
- Added `canic config <name> --verbose` to print indented declared config details under each role, including placement pools, auth flags, non-default randomness, standards, and index/create membership.

### 🧭 Notes

- This does not try to solve multi-fleet `dfx.json` switching. `dfx.json` stays aligned with the richer `test` fleet for now.

---

## 0.32.4 - 2026-05-07 - Script Cleanup

### 🧹 Cleanup

- Removed `scripts/app/reference_canisters.sh` and older redundant fleet command surfaces.
- Removed the public `canic release-set` command; manifest emission and staging are now owned by `canic build` and `canic install` rather than exposed as an operator command family.
- Centralized command help/version handling in the CLI so command modules no longer repeat their own pre-parse checks.
- Tightened nested CLI help/version handling so subcommands such as `canic backup list help`, `canic restore plan help`, and `canic snapshot download --version` exit cleanly, while option values named `version` are parsed as ordinary values.
- Routed local install and root-staging dfx command construction through the shared host `dfx` helpers instead of open-coding `Command::new("dfx")`.
- Made fleet config discovery and confirmed fleet deletion skip symlinks and stay inside the real `fleets/` tree.
- Kept the `CANIC_REFERENCE_CANISTERS` override for focused PocketIC fixture builds.
- Added `scripts/dev/README.md` to mark `scripts/dev/*` as intentional local developer/operator helpers rather than stale CLI leftovers.

### ⚠️ Breaking

- Made role-attestation `audience` required in `RoleAttestationRequest` and `RoleAttestation`; missing audiences now fail at the DTO boundary instead of through a later policy branch.

### 🔧 Changed

- Top-level help now groups commands by context level: global commands, fleet commands, and workspace/file commands.
- Top-level help now includes the CLI package version in the title banner.
- Local command behavior now uses `local` when `--network <name>` is omitted, and operators must pass `--network <name>` for one-command overrides such as `ic`.
- Removed `canic defaults`; fleet-scoped commands now require an explicit fleet argument instead of reading saved current fleet context.
- Replaced separate `canic fleets`, `canic use`, and top-level `canic scaffold` surfaces with `canic fleet list`, `canic fleet create <name>`, and `canic fleet delete <name>`; bare `canic fleet` now renders the fleet command-family help, while list/delete operate on config-defined fleets rather than requiring prior install state.
- `canic fleet create <name>` now creates a root plus simple app canister and configures root to auto-create the app; the next install is `canic install <name>`.
- `canic install` now propagates the resolved network to dfx subprocesses and Rust build scripts, while keeping the implicit default on `local` unless `--network <name>` is passed.
- `canic install`, `canic config`, `canic list`, `canic medic`, and `canic snapshot download` now take the fleet name as their required positional argument.
- `canic config <name>` now shows the selected fleet config declaration, while `canic list <name>` shows the deployed live root registry.
- Moved non-fleet audit and sandbox canisters back under `canisters/`, leaving `fleets/` for config-defined Canic fleet roots discovered by the CLI.
- Replaced stale CI uses of the removed `canic fleet canisters` command with a private config-role helper, keeping prebuild and wasm-audit automation off removed public CLI surfaces.
- `canic manifest validate` now explains that `--manifest <file>` is required and prints command-specific usage when the path is omitted.
- JSON `--out <path>` writers now create missing parent directories before writing reports.

---

## 0.32.3 - 2026-05-07 - Release Bookkeeping

### 🥾 Governance

- Recorded the recovery from a brief git/release bookkeeping hiccup.
- Confirmed there were no deleted tracked files and restored the 0.32 changelog split so `0.32.0`, `0.32.1`, and `0.32.2` remain readable in order.

---

## 0.32.2 - 2026-05-07 - Pre-Commit Guard

### 🥾 Governance

- Added a pre-commit guard that rejects staged files over 20 MiB before slower formatting checks run.
- The guard checks staged paths instead of scanning build output, so local `target/` artifacts do not make normal commits fail.

---

## 0.32.1 - 2026-05-07 - Canic Executable

### 📝 Summary

- `0.32.1` focuses the `canic` executable into a clearer operator surface for installing, inspecting, backing up, and diagnosing fleets.
- Common `canic` commands are easier to discover and run without remembering internal paths or root IDs.

### ➕ Added

- `canic medic` runs read-only setup diagnostics for the selected network, `dfx ping`, saved fleet state, config path, and root `canic_ready` status.
- `canic fleet create <name>` creates `fleets/<name>/canic.toml`, a minimal root canister under `fleets/<name>/root`, and a simple app canister under `fleets/<name>/app`.
- `canic backup list` scans backup directories under `backups/` by default and prints reusable backup paths from validated manifests.
- `canic snapshot download <name>` can use the installed fleet root without requiring the operator to spell out the root canister ID.

### 🔧 Changed

- `canic list` now shows the selected fleet and network above the tree, uses a positional fleet selector, and renders the table with wider spacing plus a header separator.
- `canic snapshot download` now defaults its output directory to `backups/fleet-<name>-YYYYMMDD-HHMMSS`; `--out <dir>` remains available for explicit paths.
- `canic snapshot download <name> --canister <id>` now verifies that the selected canister belongs to that fleet before any snapshot mutation starts.
- `canic fleet create <name>` asks for confirmation before creating files; pass `--yes` for noninteractive setup scripts.
- Top-level `canic help` now displays the installed version and groups commands by multi-fleet, single-fleet, and single-canister scope while still deriving the command list from Clap metadata.
- `canic install help`, `canic list help`, `canic fleet help`, and `canic medic help` now render fuller Clap-backed option descriptions and examples instead of terse one-line usage text.
- Config selection now previews up to six canisters before ellipsising longer install choices.
- New fleet scaffolds and install config discovery now use top-level `fleets/`.

```bash
canic fleet create my_app
canic install my_app
canic snapshot download test
canic backup list
```

### 🧭 Migration Notes

- Use `canic fleet create <name>` for new fleets, then install the generated config with `canic install <name>`.

### 📚 Documentation

- Updated the root README and `canic-cli` README to explain the current `canic` executable surface, show explicit fleet arguments, and document `canic medic`.
- Clarified `canic-cli` as the narrow operator binary surface, with host-side install/build helpers in `canic-host` and backup contracts in `canic-backup`.

### 🧪 Testing

- Added and updated tests for fleet-name validation, install-state rejection, config-choice previews, grouped help, command-level help, `canic fleet create`, `canic medic`, `canic list` formatting, fleet-aware snapshot downloads, and backup listing.

---

## 0.32.0 - 2026-05-07 - Fleet Identity

### 📝 Summary

- `0.32.0` makes fleet identity explicit in `canic.toml` and tightens the first multi-fleet operator flows.

### ⚠️ Breaking

- `canic install` no longer reads `CANIC_FLEET`; install fleet identity must come from `[fleet].name` in the selected config.
- Existing install state that does not contain a fleet name is rejected instead of being displayed as `default`; reinstall from a config that declares `[fleet].name`.

### 🔧 Changed

- `canic list` and top-level help became clearer for multi-fleet operator workflows.

```toml
[fleet]
name = "demo"
```

### 🧭 Migration Notes

Add a fleet identity to every install config, then reinstall the fleet so `.canic/<network>/fleets/<name>.json` is written from the config-owned name.
