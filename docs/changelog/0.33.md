## 0.33.7 - 2026-05-10 - Canic metadata list visibility

### 📝 Summary

- Replaced the separate `canic_canister_version` and `canic_standards`
  endpoints with `canic_metadata`, which reports package name, package
  version, package description, Canic version, and IC canister version in one
  response.
- Renamed the generated discovery bundle cfgs away from the old
  `standards_canic` wording.
- Added a `CANIC` column to `canic list`, populated from parallel
  `canic_metadata` calls and rendered as `-` when older deployments do not
  expose the new endpoint yet.
- Raised local root install funding so new local roots are created with a
  `100.00 TC` post-bootstrap reserve target, and reused local roots are topped
  up before bootstrap resumes and again after readiness if bootstrap work spent
  cycles.
- Moved `snapshot`, `backup`, `manifest`, and `restore` under a dedicated
  `Backup and restore commands` heading in the top-level `canic help` output.
- Fixed local `canic snapshot download <fleet>` target discovery so it uses the
  decoded local replica registry query path instead of trying to parse the ICP
  CLI transport wrapper as registry JSON.
- Fixed snapshot id extraction for real downloads by calling
  `icp canister snapshot create --quiet` and accepting only hex snapshot ids,
  so table units such as `MiB` are never passed to snapshot download.
- Removed `--resume` from fresh snapshot downloads; ICP CLI uses that flag only
  to continue an existing partial download.
- Added the draft 0.34 backup/restore redesign note, capturing the root-stays-up
  and subtree-phased backup model before more code is changed.

### 🧪 Testing

- Verified the metadata and list changes with
  `cargo test -p canic-cli list::tests -- --nocapture`,
  `cargo clippy -p canic-cli --all-targets -- -D warnings`,
  `cargo test -p canic --test canic_metadata -- --nocapture`,
  `cargo clippy -p canic --all-targets -- -D warnings`,
  `cargo clippy -p canic-core --all-targets -- -D warnings`,
  `cargo check -p canic-wasm-store`, `cargo test -p canic-host cycle -- --nocapture`,
  `cargo clippy -p canic-host --all-targets -- -D warnings`,
  `target/debug/canic install demo`, and `target/debug/canic list demo`.
- Verified the snapshot download registry fix with
  `cargo test -p canic-cli snapshot -- --nocapture`,
  `cargo test -p canic-host snapshot_id -- --nocapture`,
  `cargo test -p canic-host snapshot -- --nocapture`,
  `cargo test -p canic-backup discovery -- --nocapture`,
  `cargo test -p canic-backup snapshot -- --nocapture`, and
  `target/debug/canic snapshot download demo --dry-run`.

## 0.33.6 - 2026-05-10 - Candid discovery and audit baseline cleanup

### 📝 Summary

- Added fleet-scoped `canic endpoints` so operators can list Candid method names
  and argument lists from live fleet metadata or local dev `.did` artifacts
  without hand-writing shell parsing.
- Replaced the old explicit Candid export call in Canic canister roots with a
  required trailing `canic::finish!()` macro, so canisters fail to compile if
  the finish marker is missing.
- Removed the old `canic::cdk::export_candid_debug!()` macro instead of keeping
  a compatibility shim.
- Local Canic builds now emit the generated `.did` artifact and embed it as
  public `candid:service` Wasm metadata before gzipping install artifacts, while
  IC builds remove stale `.did` files and do not expose public Candid metadata.
- Moved the `minimal` baseline canister from `fleets/test` to
  `canisters/audit/minimal`, and made `canic status` compare local deployment
  health against bootstrap-required roles instead of every declared config role.
- Removed the stale `minimal` project canister from `icp.yaml` so checked-in ICP
  project canisters stay aligned with the `fleets/test` root subnet.
- Made `--icp <path>` and `--network <name>` top-level-only options; command
  local placement is hard-rejected instead of hidden for compatibility.
- Removed `canic config --from` so config inspection stays focused on the
  declared fleet, replaced `canic list --from` with `canic list --subtree`,
  and removed `canic list --root` so live list inspection always resolves the
  root from named fleet state.
- Removed `canic endpoints --did` and `canic endpoints --role`; endpoint
  discovery now uses live fleet metadata first, infers registered principal
  roles from the fleet registry, and falls back to the known local `.did`
  artifact for the resolved role.
- Removed the `KIND` column from `canic list` and added `CYCLES` as the final
  column for live deployed canister balance inspection.
- Renamed the shared cycle-balance endpoint from
  `canic_canister_cycle_balance` to `canic_cycle_balance`.
- Made `canic list` cycle checks run `icp canister call canic_cycle_balance`
  in parallel, avoiding serial per-canister waits.
- Centralized byte-size and TC cycle formatting through the shared format
  helpers used by list and config output.
- Removed public install overrides: `canic install` now accepts only the fleet
  name, resolves `fleets/<fleet>/canic.toml`, uses the conventional `root`
  canister target, and keeps the readiness timeout internal.
- Added hard fleet identity checks so config discovery rejects duplicate
  `[fleet].name` values, and install rejects a `fleets/<fleet>/canic.toml`
  whose declared fleet name does not match `<fleet>`.

```bash
canic endpoints test app
canic endpoints test scale_hub --json
canic endpoints test tl4x7-vh777-77776-aaacq-cai
```

### 🧪 Testing

- Verified the CLI and host changes with `cargo test -p canic-cli --lib -- --nocapture`,
  `cargo test -p canic-host release_set::tests -- --nocapture`, and focused
  `cargo check`/`cargo clippy` runs for the touched crates.
- Verified the moved baseline with `cargo check -p canister_minimal` and
  `ICP_ENVIRONMENT=local CANIC_WASM_PROFILE=fast cargo run -q -p canic-cli --bin canic -- build minimal`.
- Verified the checked-in ICP/test-fleet surface with
  `cargo test -p canic --test reference_surface -- --nocapture`.
- Verified local status now reports the existing `test` fleet as deployed
  because all bootstrap-required roles are registered.

## 0.33.5 - 2026-05-10 - Core and publication module cleanup

### 📝 Summary

- Refreshed the module-structure audit after the 0.33.4 macro/build and
  publication splits so the report no longer points at stale monolithic files.
- Split core IC management canister infra into focused directory modules for
  lifecycle, cycles, randomness, snapshots, status/settings, and request/response
  types.
- Split root provisioning workflow into focused modules for payload creation,
  index rebuilds, allocation, install, delete, metrics, and create-time registry
  policy.
- Split control-plane release publication into focused modules for catalog
  reconciliation, managed fleet placement, metrics, source release reads, target
  store preparation, chunk upload, and manifest promotion.
- Split the remaining control-plane publication fleet and lifecycle phase files
  into focused modules for store snapshots, placement, binding transitions,
  store creation, inventory snapshots, and retired-store GC/delete flow.
- Split backup/restore runner internals into focused modules for execution,
  preview/recovery, journal IO/locking, status classification, stopped-canister
  preconditions, constants, and public response types.
- Split restore apply-journal internals so the journal root owns state
  transitions while operation/count/error/validation types live in focused
  submodules.
### 📊 Audit

- The old `workflow/ic/provision.rs = 697` and `infra/ic/mgmt.rs = 612`
  hotspots are gone.
- Largest current focused files in those areas are `infra/ic/mgmt/types.rs =
  296`, `infra/ic/mgmt/lifecycle.rs = 186`,
  `workflow/ic/provision/allocation.rs = 193`, and
  `workflow/ic/provision/install.rs = 138`.
- The old `publication/release.rs = 845` hotspot is gone; the largest release
  publication modules are now `release/managed.rs = 275`,
  `release/chunks.rs = 189`, and `release/catalog.rs = 132`.
- The old `publication/fleet.rs = 704` and `publication/lifecycle.rs = 540`
  hotspots are gone; the largest production publication modules are now
  `release/managed.rs = 275`, `lifecycle/binding.rs = 206`,
  `lifecycle/gc.rs = 194`, and `fleet/placement.rs = 191`.
- The old backup restore runner `restore/runner/mod.rs = 1204` hotspot is
  split; the largest runner modules are now `runner/types.rs = 525` and
  `runner/execute.rs = 337`.
- The restore apply journal root is down to `422` lines, with operation/error
  types in `journal/types.rs = 353` and counters in `journal/counts.rs = 109`.
- The refreshed module-structure audit now reports overall structural risk at
  `3/10`, with remaining pressure centered on host operator helpers and backup
  manifest/snapshot planning.

### 🧪 Testing

- Verified the core split with `cargo check -p canic-core`,
  `cargo clippy -p canic-core --all-targets -- -D warnings`, and
  `cargo test -p canic-core --lib -- --nocapture`.
- Verified the publication split with `cargo check -p canic-control-plane`,
  `cargo clippy -p canic-control-plane --all-targets -- -D warnings`, and
  `cargo test -p canic-control-plane --lib -- --nocapture`.
- Re-ran the same control-plane check, clippy, and lib test suite after the
  publication fleet/lifecycle split.
- Verified the backup/restore split with `cargo check -p canic-backup`,
  `cargo clippy -p canic-backup --all-targets -- -D warnings`, and
  `cargo test -p canic-backup --lib -- --nocapture`.

## 0.33.4 - 2026-05-10 - Metrics and facade cleanup

### 📝 Summary

- Kept default metrics enabled for all generated Canic canisters while reducing
  the public metrics selector from fine-grained family variants to tiered
  operator surfaces.
- Collapsed redundant public rows so low-level management-call, provisioning,
  and coarse system counters remain internal instrumentation rather than
  separate `MetricsKind` values.
- Added inferred per-canister metrics profiles so root, wasm-store, hub, and
  leaf canisters compile only the default tiers that make sense for their role,
  with an explicit `metrics.profile = "full"` escape hatch.
- Made the standard pre-1.0 Canic runtime capabilities default on the `canic`
  facade, so managed canister manifests no longer need to choose
  `control-plane`, `auth-crypto`, or `sharding` features manually.

### 🔧 Changed

- `canic_metrics` now accepts tier selectors:
  `Core`, `Placement`, `Platform`, `Runtime`, `Security`, and `Storage`.
- Returned metric rows now use the first label as the concrete metric family,
  such as `canister_ops`, `platform_call`, `auth`, or `wasm_store`.
- Public canister operation rows remain the operator-facing lifecycle view,
  while detailed provisioning workflow counters stay recorded internally.
- Public platform rows retain low-cardinality IC/platform call visibility, while
  detailed management-canister operation counters stay recorded internally.
- The generated `canic_metrics` endpoint now returns an invalid-input error when
  a canister is queried for a metrics tier outside its compiled profile.
- `canic config <fleet>` now shows each role's resolved metrics profile in the
  config table, and `--verbose` expands the compiled metrics tiers for that
  profile.
- Fleet canister manifests and generated scaffold manifests now use plain
  `canic = ...`; role/capability behavior is selected from Canic config and
  build-time cfg output instead of per-canister Cargo feature lists.
- The `canic` facade now defaults to `metrics`, `control-plane`, `sharding`,
  and `auth-crypto` while retaining explicit opt-out through Cargo's normal
  `default-features = false` mechanism.

```rust
canic_metrics(MetricsKind::Core, page)
canic_metrics(MetricsKind::Security, page)
canic_metrics(MetricsKind::Platform, page)
```

```toml
[subnets.prime.canisters.app.metrics]
profile = "full"
```

```bash
canic config demo --verbose
```

```toml
canic = { workspace = true }
```

### 📚 Documentation

- Rewrote the metrics reference around public tiers, family-prefixed labels, and
  the intentionally internal management/provisioning/system counters.

### 🧹 Cleanup

- Split `canic` build support into focused config, metrics, and root bootstrap
  artifact modules after the module-structure audit flagged the combined helper
  file as too broad.
- Split `canic` endpoint macro support into shared, topology, root, nonroot,
  wasm-store, and bundle modules while preserving the exported macro names.
- Split the control-plane wasm-store publication workflow hub into lifecycle
  and release modules, leaving the parent module focused on status/admin
  orchestration.
- Narrowed `canic-host` to `canic-core` plus host data/formatting dependencies,
  while keeping the facade protocol re-exports stable for downstream callers.
- Narrowed `canic-cli` to `canic-core`, `canic-host`, and `canic-backup` for
  live operator code; scaffold templates still generate downstream canister code
  against the public `canic` facade.
- Removed unused direct manifest dependencies from `canic`, `canic-memory`,
  `canic-testing-internal`, and the root test/demo canisters, and added
  `cargo-machete` ignores for confirmed macro/serde-attribute false positives.

## 0.33.3 - 2026-05-09 - CLI parser cleanup

### 📝 Summary

- Cleaned up `canic-cli` command parsing and test structure without changing
  command shapes.
- Moved the remaining command-family routing and option validation toward Clap
  so fewer bespoke parser branches remain in CLI code.

### 🔧 Changed

- `canic-cli` now lets Clap own top-level command dispatch and more parser
  invariants for restore, install, fleet deletion, and backup status/verify
  options, reducing custom validation branches while keeping command shapes
  stable.
- `canic-cli` command families now share a Clap-backed subcommand dispatcher for
  backup, restore, snapshot, fleet, replica, and manifest routing, and snapshot
  download logic now lives outside the root snapshot command module.
- Manifest command routing and required-manifest validation now use the shared
  Clap dispatcher, and list/config role projection now lives in a dedicated
  `list::config` module separate from live registry querying.
- `canic fleet create <name>` now validates the fleet name through Clap's value
  parser instead of a post-parse CLI error branch.
- `canic config -v` is now accepted as the short alias for
  `canic config --verbose`.
- Restore CLI tests are split into option, plan, apply, and runner modules with
  shared fixtures isolated in the restore test root, reducing the largest
  `canic-cli` test file from roughly 1,400 lines to smaller workflow slices.
- Shared fleet config matching and workspace-relative path display helpers now
  live in `canic-host`, reducing duplicated operator filesystem logic in
  `canic-cli`.

### 🧪 Testing

- Verified the `canic-cli` library test suite, focused help smokes for the
  touched command families, `canic-cli`/`canic-host` clippy, and diff hygiene.

## 0.33.2 - 2026-05-09 - Auth and replay audit hardening

### 📝 Summary

- Completed the next recurring auth/capability/replay audit slice with concrete
  remediation instead of leaving medium-risk watchpoints.
- Tightened delegated-token, capability-proof, delegated-session, and root
  replay behavior around replay, expiry, and capacity boundaries.

### 🔧 Changed

- Root capability proof validation now returns a typed proof view used by
  verifier dispatch, logs, and metrics, reducing repeated raw proof-mode
  matching across the capability path.
- Root replay records now persist caller attribution as a hard-cut stable replay
  schema change with no legacy decode shim.
- Delegated session and session-bootstrap binding stores now reject per-subject
  or global capacity exhaustion instead of evicting unrelated subjects, limiting
  the blast radius of many wallet principals bootstrapping sessions for the same
  delegated subject.
- The delegated-session saturation guard keys pressure by `delegated_pid`, not
  by the wallet caller, because the abuse path is many wallet principals holding
  valid bootstrap tokens for the same delegated subject.
- Optional `start!` / `start_root!` `init = { ... }` blocks now execute from a
  zero-delay lifecycle timer and then schedule bootstrap/user continuation
  timers, keeping generated IC hooks restore/schedule-only.
- `IntentId` now lives in `canic-core::ids`, while stable-memory encoding stays
  in storage, so production workflow signatures no longer reference stable
  storage modules for a pure identifier.
- Split core metrics projection tests, directory placement support/tests,
  config schema tests, and intent storage tests out of their production
  modules, reducing the complexity audit residual score from `7/10` to `3/10`
  without changing runtime behavior.

### 🩹 Fixed

- Delegated-token update guards now reject active token replay by consuming each
  token nonce until the token expires. This is a hard-cut auth-state schema
  change with no legacy decode shim.
- Consumed delegated-token state is keyed by
  `(issuer_shard_pid, subject, cert_hash, nonce)`, pruned at token expiry, and
  bounded to fail closed if the active replay marker set reaches capacity.
- Root capability replay reservations now enforce a per-caller active cap before
  the global replay-store cap, preventing one registered caller from filling the
  shared replay table with unique request metadata.
- The replay saturation guard is aimed at registered caller canisters that are
  malicious, compromised, timer-driven, or user-triggered into issuing many
  fresh root capability requests; ordinary users still cannot write directly to
  the root replay store.
- Capability replay metadata and existing root replay records now reject the
  exact expiry boundary (`now >= expires_at`), matching delegated-token and
  consumed-token freshness semantics.
- Removed public `canic::api::auth::AuthApi::verify_token(...)`; delegated
  session bootstrap now uses a private token-material verifier so downstream
  code cannot mistake partial token verification for full endpoint
  authorization.

### 📊 Audit

- Added recurring reports for canonical auth boundary, capability scope
  enforcement, expiry/replay single-use, subject-caller binding, bootstrap
  lifecycle symmetry, layer violations, and capability surface with remediation
  and watchpoints captured in the May audit summaries.
- Refreshed the capability surface audit against `.icp` generated DID output,
  recording default-on `canic_metrics`, absent env/memory observability on
  ordinary builds, and retired delegation proof-install endpoints.
- Refreshed the complexity accretion audit, recording stable RPC/capability
  enum surfaces and then remediating the main metrics and directory placement
  hotspots.
- Added a 0.33 refactor addendum for follow-up design work around config
  validation, IC management facades, provisioning phases, sharding placement,
  and the root request pipeline.
- Started the config validation ownership refactor by moving top-level, fleet,
  app, auth, subnet, and canister validation policy into `config/validation`
  while leaving `config/schema` focused on serde/data shapes.
- Continued the cleanup/audit slice by replacing dead-code, missing-const, and
  unused-async `allow` suppressions with checked `expect` annotations, removing
  stale suppressions that clippy proved were no longer needed.
- Removed Rust `#[path]` module overrides from the cleanup split and normalized
  the touched modules back to ordinary directory-module discovery.
- Reduced capability scope residual risk by routing proof validation, dispatch,
  logs, and metrics through the validated proof view.
- Reduced expiry/replay residual risk by aligning capability metadata and root
  replay cache expiry with the exclusive delegated-token boundary.

### 🧪 Testing

- Verified targeted delegated-token replay, consumed-token pruning/capacity,
  capability proof-mode, auth identity, root replay boundary, and storage helper
  tests.
- Verified `canic` macro tests plus focused PocketIC lifecycle, root
  post-upgrade reconcile, and role-attestation fixture tests for lifecycle
  timer symmetry.
- Verified PocketIC root replay expiry and TTL-above-max smokes.
- Verified `canic` and `canic-core` clippy for all targets.
- Verified the core layering guard, focused request-handler/intent tests, and
  `canic-core` clippy after moving `IntentId` out of stable storage.
- Rebuilt the local public canister roster with `canic build <role>` for the
  capability surface audit and re-counted endpoint, protocol, and DID surfaces.
- Verified complexity audit file/LOC, non-test LOC, large-file,
  enum/reference, and branch-density scans.
- Verified focused metrics projection, directory workflow, directory storage,
  config schema, and intent storage tests after splitting the complexity
  hotspots.
- Verified focused config schema tests after moving validation policy out of
  schema modules.
- Verified workspace clippy after converting lint suppressions to expectations.

## 0.33.1 - 2026-05-09 - Operator status

### 📝 Summary

- Added native Canic commands for local replica lifecycle checks and quick
  project status.
- `canic status` is now the top-level "what is going on?" command for local
  development.

### ➕ Added

- Added `canic replica start`, `canic replica status`, and
  `canic replica stop` as local ICP replica lifecycle wrappers.
- Added `--debug` to the replica lifecycle commands for ICP CLI debug logging.
- Added top-level `canic status` as a quick project summary for local replica
  health, configured fleets, and deployed fleet count.
- `canic status` now shows the underlying ICP CLI version from the selected
  `--icp` executable.

### 🔧 Changed

- Local installs now fund newly created root canisters with explicit
  config-derived ICP CLI `create --cycles` funding and remove the old
  post-create cycle fabrication/top-up phase.
- `make update-dev` now checks npm for the latest ICP CLI and `ic-wasm`
  packages, uses those latest versions for the update run when available, and
  falls back to the repo pins when the lookup is unavailable.

### 📚 Documentation

- Added a current build-artifact vocabulary for `.icp`, `.canic`, generated
  Candid sidecars, Cargo wasm outputs, and Canic `.wasm.gz` artifacts.
- Consolidated non-fleet test, audit, and sandbox canister placement guidance in
  `TESTING.md`.
- Marked old 0.30/0.31 DFX-era backup/restore docs as historical.
- Cleaned up recurring audit definitions for domain-scoped layout, lifecycle
  evidence standards, and delegated-token/root replay freshness checks.

### 🩹 Fixed

- `canic status` now verifies recorded local roots against the running replica
  and reports stale install state instead of counting saved state files as live
  deployments.
- Local `canic status` now reports `partial` when the recorded root is live but
  its deployed registry is missing roles from the selected fleet config.
- `canic list` now shows local `.wasm.gz` artifact sizes when available and
  lists configured roles missing from the deployed root registry.

### 🧭 CLI Examples

```bash
canic replica start
canic replica start --debug
canic replica status
canic replica stop
canic status
canic --network local status
```

### 🧪 Testing

- Verified the new command help, focused status rendering tests, the full
  `canic-cli` library test suite, and the `canic-cli`/`canic-host` clippy
  gates.

## 0.33.0 - 2026-05-08 - dfx -> icp-cli

### 📝 Summary

- Hard-cut Canic from DFX project tooling to ICP CLI project tooling.
- `icp.yaml` is now the project-level canister and environment definition.
- Local build/install state now lives under `.icp`, while Canic install state
  remains under `.canic`.

### ⚠️ Breaking

- Removed `dfx.json` and the DFX-backed host adapter from the active CLI flow.
- Removed active DFX setup scripts and CI requirements; supported automation now
  installs and checks `icp` plus `ic-wasm`.
- Replaced DFX network propagation with `ICP_ENVIRONMENT` for build scripts and
  generated Canic build context.
- Updated operator-facing commands and examples from `--dfx <path>` to
  `--icp <path>` where a CLI override is still needed.

### 🔧 Changed

- `canic install <fleet>` now creates, tops up, installs, stages, resumes, and
  diagnoses through ICP CLI commands.
- `canic list <fleet>` and `canic medic <fleet>` now query the installed fleet
  through the ICP local endpoint and fleet-scoped install state.
- `canic snapshot download` and `canic restore run` now render ICP CLI snapshot
  commands for upload, restore, and resumable snapshot handling.
- `scripts/app/build.sh` adapts Canic builds to ICP CLI script builds by copying
  the uncompressed `.wasm` to `ICP_WASM_OUTPUT_PATH` while preserving Canic's
  `.wasm.gz` release artifacts.
- The default `icp.yaml` defines separate `demo` and `test` environments so the
  small demo fleet and fuller test topology can coexist in one repo.
- The minimal demo fleet and new fleet scaffolds explicitly disable delegated
  token auth unless the fleet config opts in.
- Delegated-token and role-attestation defaults now use ICP CLI's local
  threshold ECDSA key name, `key_1`, so auth-enabled fleets bootstrap on the
  local ICP environment without extra key configuration.
- Local developer setup installs ICP CLI npm tools under a user-writable npm
  prefix by default, so `make install-dev` no longer needs root-owned
  `/usr/local` npm permissions.
- `make update-dev` now uses the same user-writable ICP CLI install path and
  clears stale npm staging directories for interrupted `icp-cli`/`ic-wasm`
  installs before retrying.

### 🗑️ Removed

- Removed active DFX project files and helper scripts from the supported path.
- Removed DFX artifact discovery from Canic build support and testkit helpers.
- Removed stale DFX wording from current operator docs outside historical
  changelog entries.

### 🧭 CLI Examples

```bash
icp environment list
icp build root -e demo
canic install demo
canic --network local list demo
canic medic demo
canic --icp icp restore run --journal restore-apply-journal.json --execute
```

### 🧪 Testing

- Confirmed the local ICP smoke path with `icp 0.2.5` and `ic-wasm 0.9.10`.
- Verified `canic install demo`, `canic config demo`, `canic list demo`, and
  `canic medic demo` against the local ICP environment.
