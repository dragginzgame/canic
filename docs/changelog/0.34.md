## 0.34.7 - 2026-05-12 - Backup domain module cleanup

### 🔧 Changed

- Tightened local replica start/stop plumbing so Canic calls the ICP CLI local
  network path directly instead of relying on broader environment selection.
- Changed `canic install <fleet>` build progress output so the Canic build
  context is printed as one key/value per line above the artifact table,
  without the old `Build artifacts:` label interrupting the table.

### 🧹 Cleanup

- Split the backup CLI command root into focused `create`, `inspect`,
  `labels`, `model`, `reference`, `status`, and `verify` modules while keeping
  command UX and report labels in the CLI crate.
- Split `canic-backup::plan` into plan building, error, validation, preflight,
  and type modules.
- Split `canic-backup::manifest` into manifest types, validation, summary, and
  error modules.
- Split the executable backup runner into root orchestration, operation
  handlers, manifest construction, and support helpers.
- Split restore planning into mapping validation, member resolution, summaries,
  dependency ordering, types, and errors.
- Split the execution journal into journal orchestration, operation validation,
  receipt handling, and shared validation helpers.
- Split snapshot download into target resolution, topology hashing, manifest
  projection, capture, and support helpers.

### 📊 Audit

- Reran the CLI/host/backup boundary audit after the cleanup. The current
  focused boundary risk is `4/10`: installed-fleet resolution is centralized in
  host, and the remaining risk is concentrated in thin CLI live adapters and
  backup-domain file size.
- Added a cleanliness rerun report for the same slice. The follow-up cleanup
  reduced the broad mixed-responsibility roots: `plan/mod.rs` is now a facade,
  `manifest/mod.rs` is now a facade, and `snapshot/mod.rs` and `runner/mod.rs`
  only own top-level orchestration.

### 🧪 Testing

- Verified the backup boundary and module cleanup with `cargo check` for
  `canic-backup`, host/backup/CLI checks, full `canic-backup` and `canic-cli`
  library tests, targeted plan/manifest/runner/restore-plan/execution/snapshot
  tests, dependency-tree inspection, and clippy for `canic-backup`,
  `canic-host`, and `canic-cli`.

## 0.34.6 - 2026-05-12 - CLI boundary cleanup

### 🔧 Changed

- Moved shared ICP response parsing primitives from `canic-cli` to
  `canic-host::response_parse`, including recursive JSON field lookup,
  `response_candid` extraction, Candid record scanning, quoted-string parsing,
  numeric parsing, and cycle-balance parsing. CLI parsers now import the host
  layer directly.
- Kept live subnet registry parsing in `canic-host::registry` and added a
  backup-owned registry input shape in `canic-backup`, so the backup domain
  crate no longer depends on host/operator plumbing while CLI adapters convert
  host-parsed entries at the boundary.
- Added a shared installed-fleet resolver and moved it to
  `canic-host::installed_fleet`; `canic list`, `canic cycles`,
  `canic metrics`, `canic endpoints`, `canic backup create`, and
  `canic status` now share install-state lookup, local replica registry
  preference, ICP CLI fallback, registry parsing, and basic topology
  projection through the host/operator boundary.
- Split `canic endpoints` into command orchestration, Candid parsing,
  transport, rendering, and endpoint model modules.
- Split `canic cycles` into command orchestration, options, cycle response
  parsing, transport/report collection, rendering, and cycle report model
  modules.
- Split `canic metrics` into command orchestration, options, metric response
  parsing, transport/report collection, rendering, and metric report model
  modules.
- Split top-level CLI command catalog/help rendering and global option
  forwarding out of `canic-cli::lib`, keeping the root focused on command
  dispatch and error mapping.
- Replaced the broad `canic-cli::args` helper module with a focused
  `canic-cli::cli` directory: `cli::clap` owns small Clap conveniences,
  `cli::defaults` owns command default values, `cli::help` owns Canic's
  `help`/`version` aliases, and `cli::globals` owns public and internal
  `--icp`/`--network` forwarding.
- Moved shared CLI support helpers under `canic-cli::support`, including
  backup path/timestamp formatting and deployed registry tree traversal, so the
  crate root is mostly command families plus `cli`, `output`, and test glue.
- Split backup CLI command-family help and report rendering into
  `backup::command` and `backup::render`, trimming `backup::mod` back toward
  backup orchestration and domain wiring.
- Changed local `canic status` missing-root output from `stale` to `lost` and
  added CLI/README guidance that the local ICP CLI replica does not persist
  canister state across stop/start; local fleets shown as `lost` should be
  recreated with `canic install <fleet>`.

#### CLI examples

- `canic list test --subtree user_hub`
- `canic endpoints test app --json`
- `canic backup inspect 1`

### 🧪 Testing

- Verified the endpoint/cycles/metrics/top-level CLI splits with focused
  command tests, full `canic-cli` library tests, and `canic-cli` clippy.
- Verified the host-owned response parsing boundary with `canic-host`
  install-root tests, focused CLI list/cycles/metrics parser tests, full
  `canic-cli` library tests, and host+CLI clippy.
- Verified the host-owned registry and installed-fleet resolver boundary with
  focused `canic-host` registry/resolver tests, full `canic-backup` and
  `canic-cli` library tests, `canic-backup` dependency-tree inspection, and
  host+backup+CLI clippy.
- Verified the CLI helper and backup module cleanup with `canic-cli` check,
  focused backup/help tests, full `canic-cli` library tests, and
  `canic-cli` clippy.

## 0.34.5 - 2026-05-11 - Metrics and cycles split

### ⚠️ Breaking

- Changed `canic metrics <fleet>` from cycle-history reporting to live runtime
  telemetry querying. Use `canic cycles <fleet>` for the previous cycle tracker
  summary report.
- Removed install-time local replica autostart and deleted the
  `CANIC_AUTO_START_LOCAL_ICP` opt-out. `canic install <fleet>` now requires
  the requested ICP environment to already be reachable.

### ➕ Added

- Added `canic cycles <fleet>` as the explicit home for fleet-wide cycle
  tracker summaries, keeping the existing `--since`, `--limit`, `--json`, and
  `--out` report behavior.
- Added `canic metrics <fleet>` runtime telemetry queries backed by
  `canic_metrics`, with `--kind`, `--role`, `--canister`, `--nonzero`,
  `--json`, and `--out` options.
- Added stable-memory backed cycle top-up event history beside the cycle
  balance tracker, exposed through `canic_cycle_topups`.

### 🔧 Changed

- Made runtime metrics parsing handle both named JSON metric pages and ICP CLI
  `response_candid` wrappers when Candid metadata is unavailable.
- Shortened canisters without `canic_metrics` support to compact
  `unavailable` rows instead of printing raw ICP error output in the table.
- Renamed the plain `canic cycles` balance-change columns from `DELTA`/`PER_H`
  to `NET`/`NET/H`, and labeled the title as cycle balance history so positive
  rows are clearly net top-up effects rather than consumption.
- Added a `TOPUPS` column to `canic cycles` by joining each canister's cycle
  balance samples with timestamped `canic_cycle_topups` events. The column
  prints transferred cycles first and only adds counts/status when they add
  context, avoiding log searches when explaining positive net cycle movement.
- Changed the `canic cycles` `CURRENT` column to read the live
  `canic_cycle_balance` endpoint instead of reusing the newest historical
  tracker sample. `NET` and `NET/H` still use the sampled history window.
- Changed the plain `canic cycles` table to use the same registry tree
  indentation as `canic list`, and merged sample count, coverage, and coverage
  status into one compact `HISTORY` column.
- Normalized `canic cycles` duration labels through the shared CLI formatter to
  compact human units such as `2h`, `2h 30m`, `1d 3h`, and `2w 4d` instead of
  falling back to raw seconds for non-exact hour/day windows.
- Tightened `canic list` module coloring so a highlighted module hash means
  module drift within the same repeated role, not a normal difference between
  different roles.
- Clarified the local install workflow around two terminals: start the
  foreground replica with `canic replica start`, then run `canic install
  <fleet>` from another terminal.
- Split release automation so `make patch`, `make minor`, and `make major`
  only bump and sync release files, while `make release-stage`,
  `make release-commit`, and `make release-push` own staging, commit/tagging,
  and pushing. Added `make release-patch`, `make release-minor`, and
  `make release-major` for the full one-shot human release path.
- Moved shared registry tree rendering out of `canic list` internals so
  `canic list` and `canic cycles` use one tree walker, and centralized cycle
  balance response parsing for list/cycles output.
- Kept observability memory IDs adjacent while still pre-1.0:
  `CYCLE_TRACKER_ID = 26`, `CYCLE_TOPUP_EVENTS_ID = 27`, and log memories moved
  to `28/29`.
- Restored `canic` facade defaults to metrics-only. Root, wasm-store,
  sharding, and delegated-auth signer roles now opt into `control-plane`,
  `sharding`, or `auth-crypto` explicitly at the canister crate boundary, so
  plain app canisters no longer carry the root/control-plane runtime by
  default.
- Renamed the `canic config` role-capability column from `FEATURES` to `CAPS`
  so it is clearly separated from Cargo feature selection.
- Changed plain `canic endpoints` output to a three-column table with function
  name, call mode, and signature.
- Kept `canic endpoints --json` mode lists faithful to explicit Candid modes.
  Methods without an explicit Candid mode now serialize `modes: []`, while the
  plain table still labels them as `update`.

#### CLI examples

- `canic cycles test --since 1h --limit 5`
- `canic metrics test --kind core --nonzero --role app`
- `canic metrics test --kind runtime --nonzero --json`
- `make release-patch`
- `make release-stage && make release-commit && make release-push`

### 🧪 Testing

- Verified the cycle-history split and runtime metrics parser with focused
  `canic-cli` tests, full `canic-cli` library tests, and clippy.
- Verified the install-flow cleanup with focused `canic-host` install tests and
  clippy.
- Verified release target wiring with Makefile dry-runs for bump-only,
  split-release, and one-shot release targets.
- Reran the wasm footprint audit after narrowing facade defaults. The corrected
  same-day report shows `app` shrunk wasm at `1,735,534` bytes, down `216,987`
  bytes from the broad-default run.
- Smoke-tested `canic cycles test --since 1h --limit 5` and
  `canic metrics test --kind core --nonzero --role app` against the local test
  fleet.

## 0.34.4 - 2026-05-11 - Runtime timer consistency

### 🩹 Fixed

- Fixed non-root cycle-tracker startup so the immediate startup sample only
  records the local cycle balance. The first auto-top-up policy check now runs
  from the delayed workflow timer, preventing cycle requests from being issued
  during IC init mode.

### 🔧 Changed

- Standardized delayed background workflow startup on the shared 30-second
  init delay for log retention, intent cleanup, cycle top-up policy evaluation,
  and pool scheduling.
- Removed the separate pool init-delay constant and routed pool scheduling
  through the shared background workflow init delay.
- Moved the cycle tracker endpoint emitter out of topology wiring and into a
  dedicated `canic_emit_cycle_tracker_endpoints!()` macro, with shared runtime
  and `wasm_store` bundles opting into the standard cycle tracker directly.

### 🧪 Testing

- Rebuilt the CLI and reran the local demo install path to confirm the init-mode
  cycle request regression is gone.

## 0.34.3 - 2026-05-11 - Endpoint JSON and module hash visibility

### ⚠️ Breaking

- Changed `canic endpoints --json` from argument strings to structured endpoint
  DTOs with Candid declarations, endpoint modes, arguments, returns,
  cardinality, and resolved named record/variant shapes. Consumers that parsed
  the old `arguments: string[]` field should switch to the new nested type tree.
- Removed the public `canic build <canister-name>` command. Use fleet-aware
  `canic install <fleet>` for normal operator builds, or `icp build --all` for
  project build-hook execution.
- Removed the unused `source_snapshot.wasm_hash` manifest output and restore
  summary wasm-hash completeness fields. Use `source_snapshot.module_hash` for
  IC-reported module identity and `source_snapshot.checksum` for durable backup
  artifact integrity.

### ➕ Added

- Added structured endpoint type JSON from `canic endpoints --json`, including
  endpoint modes, arguments, returns, cardinality, and resolved named
  record/variant shapes.
- Added module identity results to `canic backup inspect` as
  `MODULE_HASH`/`expected_module_hash`, so restore planning can show the IC code
  identity it expects to operate on.
- Added a live module identity result to `canic list <fleet>`: default output
  shows an 8-character `MODULE` prefix before `CANISTER_ID`, while `--verbose`
  shows the full `MODULE_HASH`; terminals subtly color rows whose module differs
  from the first visible app/service hash.
- Added the first executable `canic backup create <fleet>` path. Without
  `--dry-run`, the command now writes the backup plan and execution journal,
  proves direct operator authority with ICP canister-status preflights, runs the
  ordered stop/snapshot/start/download/verify/finalize phases, and writes the
  final manifest plus durable artifact journal.
- Added `canic metrics <fleet>` for fleet-wide cycle tracker summaries. The
  command queries `canic_cycle_tracker` on every registered canister, compares
  the newest sample with the best available baseline for the requested window,
  and reports partial coverage when the tracker log does not contain a true
  boundary sample.
- Added the standard `canic_cycle_tracker` query to the canonical
  `wasm_store` endpoint bundle and checked-in `wasm_store.did`, so fleet
  metrics can include the store without a role-specific fallback.

#### CLI surface

- `canic backup create <fleet>` now executes by default; pass `--dry-run` to
  keep the previous plan-only behavior.
- `canic backup create` summary output now includes `MODE`, `STATUS`, and
  `EXECUTED` columns alongside the existing fleet, scope, target, operation,
  and output-directory fields.
- `canic backup status` prefers the persisted backup execution journal for
  plan-backed layouts, so in-progress executable backups report execution
  progress instead of falling through to the lower-level artifact journal.
- `canic backup list` can show plan-backed layouts as `dry-run`, `running`,
  `failed`, or `complete` depending on execution-journal and manifest state.
- `canic metrics <fleet>` defaults to `--since 24h` and prints `ROLE`,
  `CANISTER_ID`, `STATUS`, `SAMPLES`, `COVERAGE`, `WINDOW`, `LATEST`, `DELTA`,
  and `PER_H`.
- `canic metrics <fleet> --json` emits the structured fleet report, including
  requested window, generated timestamp, per-canister sample counts, latest and
  baseline timestamps/cycles, delta, hourly rate, coverage status, and per-row
  errors.
- `canic metrics <fleet> --since <duration>` accepts compact durations such as
  `30m`, `1h`, `6h`, `24h`, and `7d`; `--limit <entries>` controls the maximum
  cycle tracker samples fetched per canister.

### 🔧 Changed

- Made plain `canic endpoints` output valid per-method Candid declarations
  instead of compact method-name/argument-only signatures.
- Parsed endpoint interfaces through the Candid parser so aliases, variants,
  records, options, vectors, return values, and query/update modes are available
  without downstream string parsing.
- Preserved registry-reported wasm module hashes in backup topology records and
  dry-run targets so operator-facing backup and restore summaries carry IC code
  identity alongside snapshot artifact checksums.
- Made root installs pass the root wasm module hash into root init so the root
  subnet registry entry can expose module identity without `canic list` falling
  back to slow management-status calls.
- Changed install artifact builds to call the host builder directly instead of
  re-entering the public CLI through `canic build`.
- Changed test/bootstrap wasm-store build helpers and packaged downstream
  verification to call the host artifact builder directly instead of depending
  on an internal `canic build` command.
- Centralized SHA-256 byte, hex, wasm-hash, and hex-decode helpers under
  `canic-cdk::utils::hash`, with the older wasm helper now delegating to that
  shared implementation.
- Kept backup artifact checksums separate from module hashes: module hashes
  identify installed IC code, while checksums verify downloaded snapshot
  artifacts before restore.
- Kept the new live backup execution path honest about authority: until
  root-mediated backup endpoints exist, live `canic backup create` uses direct
  operator control with `CrashConsistent` quiescence instead of claiming
  root-coordinated authority.

### 🧪 Testing

- Verified structured endpoint parsing and JSON rendering with targeted
  `canic-cli` endpoint tests.
- Verified module-hash propagation with `canic-backup` library tests and
  `canic-cli` backup command tests.
- Verified the new backup runner with a fake host executor that drives the full
  plan through manifest finalization and layout integrity verification.
- Verified the metrics duration parser, JSON parser, Candid text parser, and
  partial-window summarization with focused `canic-cli` unit tests.
- Verified the canonical `wasm_store.did` cycle-tracker surface with a
  protocol-surface test.

## 0.34.2 - 2026-05-11 - Root test alignment

### 🧪 Testing

- Updated the root replay unauthorized-caller test to assert that
  root-capability dispatch metrics stay at zero after endpoint auth rejection,
  while allowing the broader security metrics page to expose access-denial
  rows.

## 0.34.1 - 2026-05-11 - Backup reference UX

### 📝 Summary

- Made `canic backup inspect`, `canic backup status`, and
  `canic backup verify` accept either a list row number or `BACKUP_ID` as a
  positional backup reference, while keeping `--dir <dir>` for explicit paths.
- Made `canic backup verify` reject dry-run plan layouts with the typed
  `DryRunNotComplete` error instead of surfacing a missing-manifest filesystem
  error.

### 🧪 Testing

- Verified the backup reference CLI path with
  `cargo test -p canic-cli backup -- --nocapture`,
  `cargo test -p canic-cli command_family_help_returns_ok -- --nocapture`, and
  `cargo clippy -p canic-cli --all-targets -- -D warnings`.
- Smoke-tested `canic backup inspect 1`, `canic backup status 1`,
  `canic backup verify 1`, and `BACKUP_ID`-based inspect/status commands
  against existing local backup dry-run layouts.

## 0.34.0 - 2026-05-10 - Backup plan foundation

### 📝 Summary

- Started the topology-aware backup/restore rework from the 0.34 design by
  adding `canic-backup::plan`.
- Added typed backup plan, target, operation, authority, quiescence, and receipt
  DTOs so backup execution can move away from the old canister-loop shape.
- Split backup plan validation from execution readiness: dry-run/planning can
  carry `Proven`, `Declared`, or `Unknown` authority evidence, while mutating
  execution requires proven control and snapshot read authority for every
  selected target.
- Added target-scoped control and snapshot-read authority preflight receipts
  that can upgrade declared/unknown plans into executable plans only when every
  selected target has proven authority.
- Added typed control and snapshot-read authority preflight request DTOs derived
  from `BackupPlan`, giving future root coordination and host authority adapters
  a stable input contract.
- Added typed topology and quiescence preflight request/receipt DTOs plus
  execution-gate validation for topology drift, target-set changes, policy
  mismatches, and rejected quiescence.
- Added a full execution preflight receipt bundle so future backup execution can
  apply authority receipts and validate topology/quiescence gates through one
  typed boundary.
- Added `preflight_id`, `validated_at`, and `expires_at` to preflight receipts
  and the execution preflight bundle, and made receipt application reject stale,
  not-yet-valid, or cross-preflight evidence.
- Added `canic-backup::execution` with a model-only backup execution journal
  built from `BackupPlan` phases, including preflight acceptance, ordered
  operation transitions, durable operation receipts, retryable failures, resume
  summaries, and `restart_required` tracking after stops.
- Added typed preflight receipt-bundle acceptance to the execution journal so
  mutation cannot be unblocked by a bundle from a different plan.
- Added `BackupLayout` read/write support for
  `backup-execution-journal.json`, keeping phase execution progress separate
  from the existing artifact download journal.
- Added `BackupLayout` read/write support for `backup-plan.json` so future
  backup runners can resume against the exact validated plan instead of
  reconstructing the operation graph.
- Added execution-layout integrity verification that rejects a persisted
  execution journal when its plan/run ids or operation graph no longer match
  the stored `backup-plan.json`.
- Added the first `canic backup create <fleet> --dry-run` CLI path, including
  optional `--subtree <role-or-principal>` planning, installed-fleet registry
  discovery, persisted `backup-plan.json`, persisted
  `backup-execution-journal.json`, and a compact dry-run summary table while
  keeping real mutation disabled.
- Made `canic backup list` include plan-only dry-run directories as
  `STATUS=dry-run`, using the persisted plan id as `BACKUP_ID` and planned
  target count as `MEMBERS`.
- Made `canic backup status --dir <dry-run-dir>` understand dry-run
  `backup-plan.json` plus `backup-execution-journal.json` layouts and report
  execution-journal progress while `--require-complete` still rejects them as
  non-backups.
- Added `canic backup inspect --dir <dry-run-dir>` with table and JSON output
  for plan metadata, selected targets, authority evidence, operation order, and
  execution-journal state.
- Added a `#` column to `canic backup list` so operators can refer to visible
  backup rows by a short ordinal as well as by `BACKUP_ID`.
- Added fail-closed validation for topology scope, root exclusion, selected
  subtree resolution, execution authority, snapshot read authority, and
  preflight-before-mutation ordering.
- Added a registry-backed plan builder that expands explicit subtrees and
  non-root fleet scopes into ordered phases: preflights, top-down stop,
  snapshot creation, bottom-up start, artifact download, verification, and
  manifest finalization.
- Added backup selector resolution for principals and unambiguous roles, with
  fail-closed handling for missing or ambiguous role selectors.
- Kept live `canic snapshot download` behavior unchanged for this slice; the new
  model is a contract for the next CLI/root-coordination implementation work.

### 🧪 Testing

- Verified the new backup plan foundation with
  `cargo test -p canic-backup plan -- --nocapture`,
  `cargo test -p canic-backup persistence -- --nocapture`,
  `cargo test -p canic-backup --lib -- --nocapture`, and
  `cargo clippy -p canic-backup --all-targets -- -D warnings`.
- Verified the backup-create dry-run CLI path with
  `cargo test -p canic-cli backup -- --nocapture`,
  `cargo test -p canic-cli command_family_help_returns_ok -- --nocapture`,
  `cargo test -p canic-cli --lib -- --nocapture`, and
  `cargo clippy -p canic-cli --all-targets -- -D warnings`.
