# 0.67 Changelog

## 0.67.43 - 2026-06-16 - Endpoint macro documentation cleanup

### 📝 Summary

- Continues `canic` endpoint macro cleanup without changing generated endpoint
  bodies, macro names, cfg gates, public Rust call sites, Candid surfaces, or
  runtime behavior.

- Aligns endpoint emitter and bundle modules with the current facade-style
  ownership header pattern so macro module boundaries are easier to scan.

### 🧹 Cleanup

- Replaces the remaining legacy banner and leaf-emitter comments in shared,
  root, non-root, ICP refill, and bundle endpoint macro modules with module
  ownership headers and concise macro documentation.

- Adds the missing parent endpoint module ownership header so child macro
  modules and the module collector follow the same documentation shape.

- Preserves the already-aligned cycle, topology, and wasm-store endpoint macro
  documentation shape.

### 📊 Audit

- Keeps the cleanup in the macro facade layer only; endpoint implementations,
  lifecycle wiring, authorization checks, emitted method names, and payload
  guards remain unchanged.

### 🧪 Testing

- Revalidated the protocol-surface tests that pin endpoint macro composition
  and canonical wasm-store Candid surface.

- Revalidated endpoint macro expansion tests, formatting, changelog governance,
  and whitespace checks during the release preparation.

## 0.67.42 - 2026-06-16 - Replay-policy manifest split

### 📝 Summary

- Continues `canic-core` structural cleanup without changing replay-policy
  classifications, exported manifest constants, public Rust call sites, DTO
  shapes, or runtime behavior.

- Converts the replay-policy inventory into a directory module with a small
  public facade and focused child modules for endpoint rows, pool-admin command
  rows, root-capability command rows, quota labels, shared policy types, and
  tests.

### 🧹 Cleanup

- Splits the endpoint replay-policy manifest away from command-specific
  manifests so endpoint entries, helper constructors, and endpoint policy
  classifications can be reviewed independently.

- Splits pool-admin and root-capability command replay-policy rows into their
  own modules while preserving the existing public manifest constants and
  accessor functions.

- Moves shared replay-policy enums and row structs into a dedicated type module
  and keeps quota/cycle-reserve policy labels in a small internal label module.

- Moves replay-policy tests into a directory module grouped by coverage, cost
  guard, endpoint, pool-admin, and root-capability concerns.

- Adds current module ownership headers and public type documentation for the
  new replay-policy module layout.

### 📊 Audit

- Replaces the former flat replay-policy file with focused child modules while
  leaving `crate::replay_policy::*` exports intact for existing callers.

- Keeps the cleanup inside the policy-inventory surface; replay receipt
  execution, cost guards, storage, workflow orchestration, and endpoint
  authentication remain owned by their existing layers.

### 🧪 Testing

- Revalidated the replay-policy manifest unit tests after the split.

- Revalidated `canic-core` compilation, `canic-core` clippy with warnings
  denied, formatting, changelog governance, and whitespace checks during the
  release preparation.

## 0.67.41 - 2026-06-16 - Core workflow split

### 📝 Summary

- Continues `canic-core` structural cleanup without changing runtime behavior,
  DTO shapes, public Rust call sites, or operator-facing output.

- Splits the pool workflow and ICP refill workflow internals into focused
  modules so creation, import, recycle, reset, replay, value-transfer guarding,
  manual/hub orchestration, and refill state advancement can be reviewed
  independently.

### 🧹 Cleanup

- Splits pool create-empty handling into its own module with replay metadata,
  replay reservation, response encode/decode, cost-guard reservation, and
  create-canister commit helpers colocated with the tests that protect them.

- Splits pool immediate import and queued import into an import module with
  intent reservation, idempotence checks, queued-entry convergence handling,
  and fixed-label intent metrics isolated from the root pool facade.

- Splits pool recycle preparation and metadata-preservation handling into a
  recycle module, and moves the destructive reset path into a reset module.

- Leaves the pool parent module as the small `PoolWorkflow` facade for shared
  state transitions, ready/pending selection, and admin authorization.

- Splits ICP refill replay handling into a replay module covering receipt
  reservation, committed-response replay, payload hashing, external-effect
  marking, recovery-required marking, response encode/decode, and replay logs.

- Splits ICP refill value-transfer cost-guard handling into a cost-guard module
  with the quota constants and permit lifecycle helpers beside the
  value-transfer boundary checks.

- Splits ICP refill execution state advancement into an execution module for
  transfer, notify, retry, terminal error mapping, and direct-child refill
  grant recording.

- Splits ICP refill manual refill and hub self-refill orchestration into
  focused entrypoint modules while keeping the existing `IcpRefillWorkflow`
  associated methods available to callers.

- Updates ICP refill tests to import moved helper boundaries and data shapes
  explicitly instead of depending on incidental parent-module imports.

### 📊 Audit

- Reduces the pool workflow parent from more than 1,100 lines to a small facade
  while keeping existing associated `PoolWorkflow` methods available to current
  callers.

- Reduces the ICP refill parent from more than 1,200 lines to a policy/context
  helper module under 300 lines, with focused manual, hub, replay, cost-guard,
  and execution children.

- Keeps the extracted modules inside the existing workflow layer; conversions,
  storage access, replay receipt operations, cost guards, and platform calls
  continue to use existing ops boundaries.

### 🧪 Testing

- Validated focused pool workflow tests and focused ICP refill workflow tests
  after the module splits.

- Revalidated `canic-core` compilation, `canic-core` clippy with warnings
  denied, formatting, changelog governance, and whitespace checks.

## 0.67.40 - 2026-06-16 - Sharding assignment release

### 📝 Summary

- Adds sharding assignment reclamation through the Canic
  `release_partition_key` Rust API so callers can free a partition key from
  its current pool owner.

- Keeps sharding assignment and release workflow internals separated into
  focused modules while preserving existing assignment behavior.

### ➕ Added

- Adds release handling that validates the current pool owner before
  unassigning a partition key.

- Decrements shard load counters when a release succeeds and records bounded
  release metrics for completed and rejected release attempts.

### 🧹 Cleanup

- Separates sharding assignment and release workflow internals so allocation,
  assignment, release, registry, bootstrap, and query concerns remain
  reviewable by topic.

### 🧪 Testing

- Validated the focused sharding release and assignment coverage, formatting,
  `canic-core` checks, clippy with warnings denied, changelog governance, and
  whitespace checks during the release preparation.

## 0.67.39 - 2026-06-16 - Host path and operation split

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, CLI behavior,
  operator output, JSON shapes, or public host APIs.

- Converts the release-set path helpers and install-root phase-operation
  wrappers into focused directory modules behind the existing facades.

### 🧹 Cleanup

- Splits release-set path handling into workspace-root resolution, canister
  manifest resolution, artifact-root/manifest-path handling, and package
  version loading modules.

- Keeps the existing release-set path helper exports stable while removing the
  monolithic path helper file.

- Splits install-root phase operations into preparation, manifest emission,
  activation/bootstrap, and phase-trait modules.

- Keeps install-root callers importing through the existing operations facade,
  so preparation, activation, staging, and receipt code continue to use the
  same operation names.

### 📊 Audit

- Reduces the release-set path parent to a small export facade, with focused
  child modules for artifacts, manifests, versions, and workspace paths.

- Reduces the install-root operations parent from a large mixed operation file
  to a small export facade, with the largest remaining child dedicated to the
  activation/bootstrap operation wrappers.

### 🧪 Testing

- Revalidated formatting, focused release-set host tests, focused install-root
  host tests, canic-host compilation, host clippy with warnings denied,
  changelog governance, and whitespace checks after the module moves.

## 0.67.38 - 2026-06-16 - Release-set staging split

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, CLI behavior,
  operator output, JSON shapes, or public host APIs.

- Converts the remaining release-set raw config projection, config mutation,
  and staging helpers into focused directory modules.

- Adds forward-looking design notes for blob storage, blob-storage billing,
  and Toko blob-storage backend migration planning.

### 🧹 Cleanup

- Splits release-set config mutation handling into attach, declare, rename,
  and shared support modules while preserving the file-backed mutation
  wrappers and validation behavior.

- Splits release-set raw config projection into fleet identity, labels,
  role catalog/details, and root-subnet helper modules.

- Splits release-set staging into artifact reading, inter-canister calls,
  Candid decode helpers, staged entry orchestration, and progress reporting
  modules.

### 📚 Documentation

- Adds a 0.68 blob-storage design note for active planning of blob identity,
  lifecycle, storage, and operator-facing behavior.

- Adds a 0.69 blob-storage billing design note covering pricing/accounting
  direction for later storage work.

- Adds a 0.70 Toko blob-storage backend migration design note for a future
  backend transition path.

### 🧪 Testing

- Revalidated formatting, focused release-set host tests, canic-host
  compilation, host clippy with warnings denied, changelog governance, and
  whitespace checks after the release-set module moves.

## 0.67.37 - 2026-06-16 - CI disk diagnostics

### 📝 Summary

- Adds CI disk-space diagnostics for GitHub-hosted runner failures without
  changing runtime behavior, CLI behavior, JSON shapes, public APIs, package
  versions, or release flow.

- Adds pinned ShellCheck installation to local developer setup so shell-script
  validation can be reproduced from the same tool-version source as
  actionlint, ICP CLI, and IC query.

- Keeps `icq` reproducibly pinned while making the default install path use
  crates.io instead of an implicit sibling checkout.

### ➕ Added

- Adds a reusable CI disk diagnostic helper that reports filesystem
  availability plus the largest likely disk consumers in the workspace, Rust
  toolchain/cache directories, runner temp directories, and hosted toolcache.

- Wires disk diagnostics into both CI jobs after Rust cache restore, before
  the heaviest Rust validation/test phases, and at job finish via always-run
  reporting steps.

- Adds threshold checks before heavy Rust phases so low runner space fails
  with an explicit diagnostic instead of surfacing later as a compiler
  `No space left on device` write failure.

- Adds a pinned standalone ShellCheck installer and includes ShellCheck in
  `make install-dev` and `make update-dev`.

### 🔧 Changed

- Extends the shared tool-version pins and developer setup messages to cover
  ShellCheck alongside the existing workflow lint and ICP tooling.

- Removes implicit `../ic-query` discovery from the installer. Explicit
  `CANIC_ICQ_PATH`/git overrides still work, but ordinary setup installs the
  pinned crates.io package and warns non-fatally when crates.io has a newer
  `ic-query` release.

- Keeps the CI diagnostic informational by default at job finish, while using
  minimum-free-space checks only before phases that are likely to generate
  large Rust artifacts.

### 🧪 Testing

- Validated shell syntax for the changed developer and CI helper scripts.

- Validated ShellCheck 0.11.0 installation and ran ShellCheck with sourced
  tool-version following against the changed shell scripts.

- Validated GitHub Actions workflow syntax with actionlint.

- Validated Makefile expansion for `install-dev` and `update-dev`.

- Validated pinned `icq` installation from crates.io and the non-fatal newer
  crates.io release warning.

- Ran whitespace validation with `git diff --check`.

## 0.67.36 - 2026-06-15 - Promotion support split

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, CLI behavior,
  JSON shapes, text output, package versions, or public host APIs.

- Converts the shared deployment-truth promotion support registries for error
  types, digest construction, and validation helpers into focused
  directory-module facades, then continues the same cleanup through promotion
  transform/readiness, execution-receipt, and wasm-store catalog internals.

### 🧹 Cleanup

- Splits promotion error definitions by domain so source, policy, readiness,
  identity, wasm-store, materialization, transform, artifact-plan, and
  provenance/receipt errors live in focused children behind the existing
  promotion error facade.

- Splits promotion digest builders by the same domain boundaries, keeping the
  existing public digest exports and promotion-internal helper names stable.

- Splits promotion validation helpers into source, policy, readiness,
  identity, wasm-store, materialization, transform, artifact-plan, and
  provenance/receipt groups while keeping the low-level SHA-256 shape
  predicate in the ensure facade instead of a standalone helper module.

- Splits artifact-promotion execution receipt handling into request
  orchestration, receipt construction, deployment-receipt linkage, and receipt
  validation modules.

- Splits promotion readiness handling into request orchestration, readiness
  construction, blocker/warning finding collection, and validation modules.

- Splits promotion plan transform handling into construction and validation
  modules while preserving the promotion-internal role-field linkage helper
  used by materialization validation.

- Splits wasm-store catalog verification into request orchestration, catalog
  observation construction, blocker assembly, and validation modules.

### 📊 Audit

- Reduces the promotion error parent from 504 lines to a 28-line facade, with
  child modules between 27 and 91 lines.

- Reduces the promotion digest parent from 427 lines to a 27-line facade, with
  child modules between 26 and 107 lines.

- Reduces the promotion ensure parent from 421 lines to a 47-line facade, with
  child modules between 23 and 101 lines.

- Keeps the largest ensure child as the source-specific requirement module,
  because source kind, locator, receipt, and digest-pin rules are one cohesive
  contract.

- Reduces the artifact-promotion execution receipt parent from 320 lines to a
  32-line facade, with child modules between 58 and 193 lines.

- Reduces the promotion readiness parent from 348 lines to a 40-line facade,
  with child modules between 76 and 136 lines.

- Reduces the promotion plan transform parent from 306 lines to a 10-line
  facade, with 155-line construction and 161-line validation children.

- Reduces the wasm-store catalog verification parent from 281 lines to a
  27-line facade, with child modules between 59 and 158 lines.

### 🧪 Testing

- Revalidated canic-host compilation, the focused deployment-truth promotion
  host test suite, and host clippy with warnings denied after the support
  module moves.

## 0.67.35 - 2026-06-15 - Promotion implementation split

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, CLI behavior,
  JSON shapes, text output, package versions, or public host APIs.

- Converts the largest remaining deployment-truth promotion implementation
  files for artifact identity, source-build materialization, wasm-store,
  policy, and provenance-report handling into focused directory-module
  facades.

### 🧹 Cleanup

- Splits promotion artifact-identity handling into report construction, role
  identity helpers, group/key helpers, and validation modules.

- Splits source-build materialization handling into build evidence,
  materialization report construction, transform-link, and validation modules.

- Splits wasm-store promotion handling into staging-derived identity reports
  and catalog verification modules.

- Splits promotion-policy handling into check construction, policy-decision
  logic, and validation modules.

- Splits artifact-promotion provenance reporting into request validation,
  report building, blocker assembly, role provenance attachment, and report
  validation modules.

### 📊 Audit

- Reduces the promotion identity parent from 597 lines to an 18-line facade,
  with child modules between 71 and 240 lines.

- Reduces the promotion materialization parent from 594 lines to a 19-line
  facade, with child modules between 51 and 239 lines.

- Reduces the promotion wasm-store parent from 502 lines to a 10-line facade,
  with focused 228-line identity and 281-line catalog children.

- Reduces the promotion policy parent from 422 lines to a 6-line facade, with
  child modules between 93 and 240 lines.

- Reduces the promotion provenance-report parent from 490 lines to an 8-line
  facade, with child modules between 27 and 165 lines.

### 🧪 Testing

- Revalidated canic-host compilation, the focused deployment-truth promotion
  host test suite, and host clippy with warnings denied after the promotion
  implementation moves.

## 0.67.34 - 2026-06-15 - Deployment-truth model decomposition

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, CLI behavior,
  JSON shapes, text output, package versions, or public host APIs.

- Finishes the current deployment-truth model decomposition by turning the
  remaining large passive model files into directory-module facades with
  focused DTO groups.

### 🧹 Cleanup

- Splits the root deployment-truth model surface into artifact, execution,
  inventory, plan, and safety child modules while preserving the existing
  public re-export surface.

- Splits lifecycle model DTOs into authority, plan, handoff, proposal,
  verification, and completion child modules behind the existing lifecycle
  facade.

- Splits promotion model DTOs into source, policy, materialization, identity,
  plan, provenance, and receipt child modules behind the existing promotion
  facade.

### 📊 Audit

- Reduces the root deployment-truth model parent from 635 lines to a 91-line
  export facade.

- Reduces the lifecycle model parent from 651 lines to a 37-line export
  facade, with child DTO groups between 58 and 157 lines.

- Reduces the promotion model parent from 640 lines to a 37-line export
  facade, with child DTO groups between 47 and 147 lines.

- Keeps the remaining model child modules cohesive and large enough to scan as
  topic groups rather than one-type fragments.

### 🧪 Testing

- Revalidated canic-host compilation, the focused deployment-truth host test
  suite, host clippy with warnings denied, formatting, changelog governance,
  and whitespace checks after the model moves.

## 0.67.33 - 2026-06-15 - Host build, promotion, and install-test split

### 📝 Summary

- Continues canic-host cleanup without changing host behavior, JSON shapes,
  text output, CLI behavior, package versions, or public host APIs.

- Converts build provenance, canister build, deployment-truth promotion,
  deployment comparison, policy-gate tests, and install-root test surfaces into
  focused directory modules.

- Catches up the 0.67 changelog ledger for the already-pushed `.31` and `.32`
  structural cleanup releases before recording this `.33` slice.

### 🧹 Cleanup

- Splits build provenance into artifact, Cargo metadata, envelope, input,
  model, and source helpers behind the existing facade.

- Splits canister build handling into artifact, Candid sidecar, context,
  model, process, and wasm-store helpers behind the existing facade.

- Splits artifact-promotion provenance into report and execution-receipt
  modules, and splits promotion transform handling into plan, readiness,
  source, and evidence modules.

- Splits deployment comparison into report construction, diff collection,
  status/finding assembly, digesting, and validation modules.

- Splits the largest install-root tests into install-truth and root-state
  verification directory modules grouped by behavior.

- Splits policy-gate tests into parser, envelope policy, build provenance
  policy, manifest gate, and schema groups with shared fixtures kept in the
  parent test module.

- Further splits the largest deployment-truth promotion test children into
  readiness blocker/source-build/transform/text/validation groups and
  promotion-plan serialization/evidence/target-lineage/artifact-plan groups.

### 📊 Audit

- Reduces the build provenance parent from roughly 595 lines to a 23-line
  facade, and the canister build parent from roughly 432 lines to a 22-line
  facade.

- Reduces the promotion provenance parent from 806 lines to a 9-line facade,
  the promotion transform parent from 781 lines to a 19-line facade, and the
  deployment comparison parent from 775 lines to a 25-line facade.

- Reduces the install-truth test parent from 1566 lines to a 10-line module
  index, and the root-state verification test parent from 826 lines to a
  7-line module index.

- Reduces the policy-gate test parent from 833 lines to a fixture/module parent
  with focused test children under roughly 160 lines each.

- Reduces the deployment-truth promotion readiness test parent from 671 lines
  to a 7-line module index, and the promotion plan test parent from 656 lines
  to a 6-line module index.

- Keeps the new install-root and promotion test children under roughly 365
  lines each while preserving the existing test intent and source-shape checks.

### 🧪 Testing

- Revalidated focused build provenance, canister build, deployment-truth
  promotion, deployment comparison, install-root, and policy-gate unit-test
  groups after the module moves.

- Revalidated host clippy with warnings denied, formatting, changelog
  governance, and whitespace checks.

## 0.67.32 - 2026-06-15 - Adoption, authority, and ICP split

### 📝 Summary

- Continues canic-host structural cleanup without changing host behavior, CLI
  behavior, JSON shapes, text output, or public host APIs.

- Converts adoption reporting, deployment-truth authority handling, and ICP
  command/query helpers into focused directory-module facades.

### 🧹 Cleanup

- Splits adoption internals into model, evidence, findings, summary,
  recommendations, and report helpers.

- Splits deployment-truth authority internals into plan, profile,
  reconciliation, report, and shared helpers.

- Splits ICP host helpers into canister, command, error, model, replica, run,
  snapshot, version, and test modules.

### 📊 Audit

- Reduces the adoption parent from roughly 1187 lines to a 23-line facade.

- Reduces the deployment-truth authority parent from roughly 877 lines to an
  11-line facade.

- Reduces the ICP parent from roughly 1235 lines to a 31-line facade.

## 0.67.31 - 2026-06-15 - Deployment-truth observation, receipt, and root split

### 📝 Summary

- Continues canic-host deployment-truth cleanup without changing host behavior,
  CLI behavior, JSON shapes, text output, or public host APIs.

- Converts observation, receipt, and root-verification internals into focused
  directory-module facades.

### 🧹 Cleanup

- Splits deployment-truth observation into artifact, config, identity,
  inventory, registry, root, and shared helper modules.

- Splits deployment-truth receipts into artifact, deployment, and authority
  receipt/evidence helpers.

- Splits root verification into digest, error, receipt, report construction,
  report checks, shared helpers, and validation modules.

### 📊 Audit

- Reduces the observation parent from roughly 957 lines to a 21-line facade.

- Reduces the receipt parent from roughly 927 lines to a 15-line facade.

- Reduces the root-verification parent from roughly 956 lines to an 11-line
  facade plus focused report children.

## 0.67.30 - 2026-06-15 - Deployment-truth support and text split

### 📝 Summary

- Continues canic-host deployment-truth cleanup without changing host behavior,
  JSON shapes, text output, CLI behavior, or public host APIs.

- Moves shared deployment-truth test fixtures out of the test parent and into
  focused support modules.

- Converts authority, lifecycle, promotion, and root-verification text
  rendering from large files into directory-module facades with focused
  renderers and shared formatting helpers.

### 🧹 Cleanup

- Splits shared test support into assertion, authority, environment, lifecycle,
  model, promotion, receipt, root-verification, and workspace helper modules.

- Splits authority text rendering into plan, report, evidence, receipt, and
  shared helper modules.

- Splits promotion text rendering into readiness, materialization, policy,
  artifact identity, wasm-store, plan, provenance, execution receipt, and
  shared helper modules.

- Splits lifecycle text rendering into authority, plan, proposal, external
  lifecycle, receipt, consent, verification, completion, and shared helper
  modules.

- Splits root-verification text rendering into report, receipt, and shared
  helper modules.

- Updates the deployment-truth dry-run source-shape audit so it scans the new
  authority, lifecycle, promotion, and root-verification text module trees
  rather than the removed flat files.

### 📊 Audit

- Reduces the deployment-truth test parent to a 27-line module index.

- Reduces the authority text parent from 523 lines to a 10-line facade.

- Reduces the promotion text parent from 919 lines to an 18-line facade.

- Reduces the lifecycle text parent from 807 lines to an 18-line facade.

- Reduces the root-verification text parent from 154 lines to a 6-line facade.

- Keeps the new authority, lifecycle, promotion, and root-verification text
  children under roughly 275 lines each while preserving the existing
  text-rendering public function names.

### 🧪 Testing

- Revalidated the full deployment-truth unit-test group after the support,
  authority text, lifecycle text, promotion text, and root-verification text
  module moves.

- Revalidated host clippy with warnings denied.

## 0.67.29 - 2026-06-15 - Deployment-truth test structure closeout

### 📝 Summary

- Finishes the remaining large standalone deployment-truth unit-test file
  split without changing production code, host behavior, JSON shapes, text
  output, CLI behavior, or public host APIs.

- Converts authority, diff, local observation, execution receipt, and root
  verification test surfaces to directory-module layout.

- Leaves the shared deployment-truth test fixture module intact for a later
  support-helper split, because it owns cross-cutting samples rather than a
  single test concern.

### 🧹 Cleanup

- Splits authority tests into summary, receipt validation, dry-run evidence
  validation, reconciliation, schema, source-shape, findings, and pool
  authority groups.

- Splits deployment diff tests into identity, controller, artifact,
  observation-gap, canister, pool, verifier-readiness, and safety groups.

- Splits local observation tests into inventory, registry/status, artifact
  manifest, and plan-building groups.

- Splits execution receipt tests into preflight, receipt/classification, and
  resume-safety groups.

- Splits root verification tests into report and receipt groups.

### 📊 Audit

- Reduces the authority test parent from roughly 1666 lines to an 8-line module
  index.

- Reduces the diff test parent from roughly 1820 lines to an 8-line module
  index.

- Reduces local observation, execution receipt, and root verification test
  parents to small module indexes while keeping each behavior group under
  roughly 400 lines.

- Leaves only the shared deployment-truth test fixture module above the
  surrounding test-module sizes, with no mixed `foo.rs` plus `foo/` layouts.

### 🧪 Testing

- Revalidated the full deployment-truth unit-test group after the test module
  moves.

- Revalidated host clippy with warnings denied, formatting, and whitespace
  checks.

## 0.67.28 - 2026-06-15 - Deployment-truth test module split

### 📝 Summary

- Reorganizes the largest deployment-truth unit-test files without changing
  production code, host behavior, JSON shapes, text output, CLI behavior, or
  public host APIs.

- Converts the lifecycle and promotion test surfaces to directory-module
  layout so related assertions live beside each other instead of in two
  monolithic files.

- Keeps the existing deployment-truth test module names as small module
  indexes, preserving normal Rust module discovery and avoiding mixed
  `foo.rs` plus `foo/` layouts.

### 🧹 Cleanup

- Splits lifecycle tests into authority-plan, proposal, external lifecycle,
  receipt, consent, verification, completion, and source-shape child modules.

- Splits promotion tests into policy, artifact identity, build materialization,
  plan, provenance, execution receipt, transform validation, artifact source,
  readiness, and wasm-store child modules.

- Updates the lifecycle source-shape guard include paths for the deeper test
  module layout.

### 📊 Audit

- Reduces the lifecycle test parent from roughly 2403 lines to an 8-line module
  index.

- Reduces the promotion test parent from roughly 3801 lines to a 10-line module
  index.

- Leaves the largest new test children around the same bounded size as the
  already-split deployment-truth test modules, with the behavior-heavy
  readiness and verification groups isolated for future focused cleanup.

### 🧪 Testing

- Revalidated the deployment-truth promotion unit-test group after the split.

- Revalidated the deployment-truth lifecycle unit-test group after the split.

- Revalidated host clippy with warnings denied, formatting, and whitespace
  checks.

## 0.67.27 - 2026-06-15 - Lifecycle authority, digest, and error split

### 📝 Summary

- Continues canic-host deployment-truth lifecycle cleanup without changing
  lifecycle behavior, JSON shapes, text output, CLI behavior, or public host
  APIs.

- Further decomposes the lifecycle production modules that remained large
  after the external lifecycle and external upgrade implementation split.

- Keeps lifecycle parent modules as public re-export facades so downstream
  callers continue using the existing deployment-truth lifecycle API.

### 🧹 Cleanup

- Splits lifecycle authority-plan logic into authority report, external
  lifecycle plan, policy, and validation child modules.

- Splits lifecycle digest helpers by owning lifecycle surface instead of
  keeping authority, external lifecycle, and external upgrade hashing together.

- Splits lifecycle error types by owning lifecycle surface so authority,
  external lifecycle, and external upgrade validation failures are isolated
  while preserving existing public exports.

### 📊 Audit

- Reduces the lifecycle authority-plan parent to a small module facade with
  focused implementation children.

- Replaces broad lifecycle digest and error modules with narrower
  authority/external-lifecycle/external-upgrade groups that match the rest of
  the lifecycle directory layout.

- Keeps source-shape guard coverage aligned with the new lifecycle directory
  modules.

### 🧪 Testing

- Revalidated the deployment-truth lifecycle unit-test group after the
  production module split.

- Revalidated the authority dry-run source guard, host package check, host
  clippy with warnings denied, formatting, and whitespace checks.

## 0.67.26 - 2026-06-15 - Deployment-truth lifecycle implementation split

### 📝 Summary

- Continues canic-host deployment-truth lifecycle cleanup without changing
  lifecycle behavior, JSON shapes, text output, CLI behavior, or public host
  APIs.

- Converts the passive external lifecycle and external upgrade implementation
  surfaces to directory-module layout.

- Leaves the parent lifecycle modules as public re-export facades so callers
  keep using the same deployment-truth lifecycle API.

### 🧹 Cleanup

- Splits external lifecycle pending-report, operator-check, handoff, critical
  fix, and validation logic into focused child modules.

- Splits external upgrade receipt, consent evidence, proposal report,
  completion report, validation, and verification logic into focused child
  modules.

- Further decomposes external upgrade verification into check, policy, report,
  and shared helper modules instead of leaving one large verification child.

### 📊 Audit

- Reduces the external upgrade parent from roughly 1654 lines to roughly 37
  lines.

- Reduces the external lifecycle parent from roughly 848 lines to roughly 22
  lines.

- Keeps the largest new lifecycle implementation children under roughly 400
  lines and updates source-scanning tests to cover the new directory modules.

### 🧪 Testing

- Revalidated the deployment-truth lifecycle unit test group after both module
  splits.

- Revalidated the passive authority source guard, host package check, host
  clippy with warnings denied, formatting, changelog governance, and whitespace
  checks.

## 0.67.25 - 2026-06-15 - Deployment-truth model decomposition

### 📝 Summary

- Continues canic-host deployment-truth cleanup without changing deployment
  truth behavior, JSON shapes, text output, or public host APIs.

- Converts the deployment-truth model surface to directory-module layout and
  splits large passive DTO groups into focused child modules.

- Leaves the parent model module as the public re-export facade plus the
  remaining shared plan, inventory, receipt, artifact, observation, and safety
  DTOs.

### 🧹 Cleanup

- Moves promotion, artifact-promotion, materialization, wasm-store identity,
  promotion-policy, promotion-readiness, and staging receipt DTOs into a
  promotion model module.

- Moves lifecycle authority, external lifecycle, external upgrade proposal,
  consent, verification, completion, and handoff DTOs into a lifecycle model
  module.

- Moves authority receipt, dry-run evidence, reconciliation plan, controller
  delta, report, readiness, action, and external-action DTOs into an authority
  model module.

- Moves deployment comparison report, target, diff, and category DTOs into a
  comparison model module.

- Moves deployment root verification request, report, receipt, check, source,
  evidence-status, state-transition, and state DTOs into a root-verification
  model module.

### 📊 Audit

- Reduces the deployment-truth model parent from roughly 2307 lines to roughly
  635 lines.

- Keeps all moved DTOs re-exported through the existing deployment-truth model
  facade so downstream module imports remain stable.

- Updates the lifecycle source-structure test fixture path for the normal Rust
  directory-module layout.

### 🧪 Testing

- Revalidated the deployment-truth unit test suite after each model split.

- Revalidated the touched host package with package check, host clippy with
  warnings denied, formatting, and whitespace checks.

## 0.67.24 - 2026-06-15 - Release-set config projection split

### 📝 Summary

- Finishes the release-set config decomposition started in `0.67.23` without
  changing release-set behavior, file-backed public APIs, test-facing source
  helpers, or operator output.

- Leaves the release-set config parent as the file-backed API wrapper and
  public re-export facade.

- Moves raw-source config projections into their own module so parsing,
  mutation, data shapes, and file IO are separated by ownership.

### 🧹 Cleanup

- Moves configured role kind, lifecycle, capability, auto-create, top-up,
  metrics-profile, detail, fleet-name, controller, pool expectation, release
  role, deployable role, bootstrap role, and local root-create cycle source
  helpers into a projection module.

- Converts the release-set config model, mutation, and projection children to
  normal directory-module layout.

- Keeps model DTOs and TOML mutation helpers in their existing focused child
  modules from the previous patch.

### 📊 Audit

- Reduces the release-set config parent from roughly 834 lines to roughly 219
  lines.

- Keeps the projection module as the remaining behavior-heavy source parser so
  future cleanup can target it separately.

### 🧪 Testing

- Revalidated focused release-set test coverage after the projection move and
  directory-module conversion.

- Revalidated the touched host package with package check, host clippy with
  warnings denied, formatting, and whitespace checks.

## 0.67.23 - 2026-06-15 - Host policy/config decomposition

### 📝 Summary

- Continues canic-host structural cleanup without changing CLI command
  behavior, visible output, JSON shape, release-set behavior, or public host
  APIs.

- Splits policy-gate internals into focused model, validation, evaluation, and
  manifest-gate modules while leaving the parent as the public parse/evaluate
  facade.

- Converts release-set config handling to normal directory-module layout and
  separates config data shapes plus TOML mutation helpers from the remaining
  file-backed wrapper and read-only projection logic.

### 🧹 Cleanup

- Moves policy-gate report, manifest, policy, and error data shapes into a
  model module.

- Moves policy-gate policy validation, policy evaluation, and project evidence
  manifest gate execution into owning modules.

- Moves release-set config DTOs and local bootstrap cycle constants into a
  config model module.

- Moves release-set fleet role declaration, attachment, rename, package
  metadata update, TOML table-header rewriting, and mutation validation helpers
  into a dedicated config mutation module.

### 📊 Audit

- Reduces the policy-gate parent module from the previous catch-all
  implementation to a small facade of roughly 65 lines.

- Reduces the release-set config parent from roughly 1315 lines to roughly 834
  lines while keeping the behavior-heavy read-only projection functions intact
  for a later focused pass.

- Keeps test-facing source helpers available through the existing release-set
  internal boundary so colocated unit tests do not rely on child-module
  internals.

### 🧪 Testing

- Revalidated focused policy-gate and release-set test coverage after the
  module moves.

- Revalidated the touched host package with package check, host clippy with
  warnings denied, formatting, changelog governance, and whitespace checks.

## 0.67.22 - 2026-06-15 - Host query boundary cleanup

### 📝 Summary

- Continues canic-host query-boundary cleanup without changing CLI command
  behavior, visible text output, JSON output shape, or fallback behavior.

- Centralizes shared host-side canister query helpers for readiness,
  subnet-registry JSON, Canic metadata version parsing, and Candid service
  endpoint parsing.

- Keeps CLI modules focused on command options, target resolution, display, and
  command-specific error mapping while moving reusable command/output parsing
  into canic-host.

### 🔧 Changed

- `canic info list` and `canic info medic` readiness checks now delegate to the
  host readiness helper that chooses direct local replica queries for local
  targets and ICP CLI JSON/Candid sidecar calls otherwise.

- Backup and snapshot subnet-registry reads now delegate to the host
  subnet-registry helper, preserving direct local replica preference and ICP CLI
  fallback behavior behind one boundary.

- Canic metadata version lookup now lives in canic-host, so list output no
  longer owns the `canic_metadata` command invocation or response parsing.

- Endpoint Candid service parsing and the structured endpoint data model now
  live in canic-host; the CLI still owns endpoint command options, local/live
  artifact resolution, rendering, and JSON report wrapping.

### 🧹 Cleanup

- Converts the shared response parsing helpers to a directory module with
  focused tests beside the implementation.

- Removes duplicate parser tests from CLI command modules after moving their
  owning parser logic into canic-host.

- Moves `candid_parser` dependency ownership from canic-cli to canic-host,
  matching where Candid endpoint parsing now lives.

### 🧪 Testing

- Added focused host coverage for Canic metadata response parsing and Candid
  endpoint parsing.

- Revalidated the touched host and CLI command boundaries with focused unit
  tests, package checks, clippy with warnings denied, formatting, and
  whitespace checks.

## 0.67.21 - 2026-06-14 - Cycle-balance query consolidation

### 📝 Summary

- Centralizes cycle-balance querying in canic-host without changing list,
  cycles, install-root, local-replica, or ICP CLI fallback behavior.

- Gives list, cycles reporting, and local root cycle top-up paths the same
  host-owned query helper for `canic_cycle_balance`.

- Keeps reporting paths tolerant of missing live cycle data while preserving a
  typed error path for install-root readiness and funding checks.

### 🔧 Changed

- Adds a host cycle-balance query helper that prefers decoded direct local
  replica responses for local targets and falls back to ICP CLI output parsing.

- `canic info list` cycle-balance values and `canic cycles` live balance
  samples now use the shared host helper instead of each invoking and parsing
  `canic_cycle_balance` separately.

- Install-root local cycle top-up checks now use the same helper before
  deciding whether to fund the root canister.

### 🧹 Cleanup

- Removes duplicate cycle-balance parser wiring from CLI list/cycles and
  install-root local-cycle handling.

- Keeps the shared parser fallback in canic-host for compatibility with ICP CLI
  JSON/Candid output shapes.

### 🧪 Testing

- Validated through the focused host/CLI query paths in the release slice, plus
  formatting, clippy with warnings denied, changelog governance, and whitespace
  checks.

## 0.67.20 - 2026-06-14 - Replica-query module decomposition

### 📝 Summary

- Decomposes canic-host direct local replica querying without changing CLI
  behavior, local-query behavior, fallback behavior, or public host APIs.

- Splits the shared `replica_query` boundary into focused transport, status,
  and wire-decoding child modules while leaving the parent module as the public
  API and small readiness JSON fallback parser.

- Keeps controller-gated diagnostics on the existing ICP CLI path because the
  current direct local query helper sends anonymous queries.

### 🧹 Cleanup

- Moves anonymous local HTTP/CBOR query mechanics and local endpoint resolution
  into `replica_query::transport`.

- Moves `/api/v2/status` reachability and local root-key parsing into
  `replica_query::status`.

- Moves Candid response decoding, public Canic error decoding, and
  subnet-registry JSON adapter code into `replica_query::wire`.

- Converts the new child modules to directory modules and colocates focused
  tests beside their owning code.

- Reduces the parent `replica_query` module from the pre-split catch-all shape
  to public API wiring and the command-output compatibility parser still used
  by CLI fallback paths.

### 🧪 Testing

- Moved direct-query transport, status, and wire-decoding tests beside their new
  owning modules while leaving parent readiness JSON parser tests in the parent
  test module.

- Validated with focused replica-query tests, host clippy with warnings denied,
  the full host library test suite, canic-cli check, formatting, and whitespace
  checks.

## 0.67.19 - 2026-06-14 - Local subnet-registry diagnostic query cleanup

### 📝 Summary

- Continues canic-host local-query cleanup without changing install behavior,
  readiness behavior, diagnostic fallback behavior, or public host APIs.

- Centralizes direct `canic_subnet_registry` Candid response decoding behind the
  shared replica-query helper used by existing local installed-deployment
  resolution.

- Uses decoded local subnet-registry roles for install-root readiness progress
  diagnostics before falling back to ICP CLI JSON output.

### 🔧 Changed

- `query_subnet_registry_json` and `query_subnet_registry_json_from_root` now
  share one typed subnet-registry response decoder instead of each decoding the
  direct local query response inline.

- Install-root readiness diagnostics now ask replica-query for registry roles on
  local networks and only parse ICP CLI JSON when direct local querying is
  unavailable or not selected.

- The raw diagnostic command dump remains unchanged for bootstrap failure and
  timeout cases.

### 🧹 Cleanup

- Removes another brittle parser from the successful local diagnostic path while
  preserving the compatibility parser for fallback CLI output.

- Keeps role-summary rendering separate from JSON fallback parsing so the typed
  local path does not need to synthesize and reparse a CLI-shaped payload.

### 🧪 Testing

- Added replica-query coverage for decoding direct `canic_subnet_registry`
  response bytes into both ordered roles and the existing CLI-shaped JSON
  adapter.

- Added install-root readiness diagnostics coverage for decoded role rendering
  and fallback JSON role-summary parsing.

- Validated with formatting, host clippy with warnings denied, focused
  replica-query tests, focused readiness diagnostics tests, the full host
  library test suite, changelog governance, and whitespace checks.

## 0.67.18 - 2026-06-14 - Install-root phase and local-query decomposition

### 📝 Summary

- Continues canic-host install-root decomposition without changing install
  behavior, readiness behavior, local cycle funding behavior, or public host
  APIs.

- Moves install preparation, activation phase sequencing, final install-state
  receipt construction, command output, build environment, build-target
  execution, local root-cycle funding, readiness parsing/diagnostics, and
  install timing helpers into focused install-root child modules.

- Keeps local install readiness and local root-cycle checks on the same
  operator-facing fallback path while preferring decoded direct local replica
  queries when a local replica endpoint is available.

### 🔧 Changed

- Local install readiness polling now prefers direct local replica Candid-byte
  queries for `canic_ready` and `canic_bootstrap_status`, falling back to ICP
  CLI JSON/Candid text output when direct local querying is unavailable or the
  target is non-local.

- Local root cycle funding now queries `canic_cycle_balance` through decoded
  direct local replica responses before falling back to the older ICP CLI
  output parser.

- Direct `canic_cycle_balance` decoding uses the public Canic error DTO shape
  so rejected endpoint results surface as structured query failures.

### 🧹 Cleanup

- Leaves the install-root parent focused on top-level install orchestration and
  module wiring.

- Moves phase-specific helpers out of the catch-all command/orchestration path
  while keeping existing test-facing helper access under the install-root test
  module.

- Removes one more brittle local-replica parser dependency from the successful
  local cycle-balance path.

### 📊 Audit

- Reduces the install-root parent from the `0.67.17` baseline to the
  orchestration and API surface that still belongs in the parent module.

- Keeps the new install-root helpers under existing host ownership and normal
  directory-module layout.

### 🧪 Testing

- Added replica-query coverage for decoding `Result<u128, Error>` cycle-balance
  response bytes.

- Validated with formatting, host clippy with warnings denied, focused
  replica-query tests, focused install-root tests, the full host library test
  suite, changelog governance, and whitespace checks.

## 0.67.17 - 2026-06-14 - Install-root registration and truth-check decomposition

### 📝 Summary

- Continues canic-host install-root decomposition without changing install
  behavior, deployment-truth check behavior, or public host APIs.

- Moves deployment registration and registered-root verification into a focused
  install-root child module while preserving the existing public
  `register_deployment_state` and `verify_registered_deployment_root` exports.

- Moves root-canister status/create resolution into a focused child module used
  by install phase operations.

- Moves read-only install truth checks and execution preflight construction into
  a focused child module while preserving the public check/preflight exports.

### 🧹 Cleanup

- Keeps the install-root parent focused on top-level install orchestration,
  phase sequencing, install-state receipt writing, and final state assembly.

- Tightens child-module imports so readiness, current execution, and install
  operations import helpers from their owning modules instead of through parent
  re-exports.

- Updates source-boundary tests to track the moved registration and read-only
  truth-check implementation modules.

### 📊 Audit

- Reduces `install_root/mod.rs` from 1004 lines after `0.67.16` to 529 lines.

- Adds focused child modules for deployment registration/root verification,
  root-canister resolution, and read-only install truth/preflight checks.

### 🧪 Testing

- Validated the focused install-root test group, then reran the full host
  library test suite, host clippy with warnings denied, formatting, changelog
  governance, and whitespace checks.

## 0.67.16 - 2026-06-14 - Install-root operation and plan decomposition

### 📝 Summary

- Continues canic-host install-root decomposition without changing install
  behavior, deployment-truth receipt shape, or public host APIs.

- Moves artifact-promotion execution receipt construction into a focused
  install-root child module.

- Moves install phase operation wrappers and root wasm reinstall execution into
  a focused child module.

- Moves deployment-plan artifact validation, release-set manifest generation,
  and plan artifact path resolution into a focused child module.

- Moves current install execution context, executor-capability checking, and
  deployment-truth gate execution into a focused child module.

### 🧹 Cleanup

- Keeps the install-root parent focused on top-level install orchestration,
  phase sequencing, install-state receipt writing, registration/root
  verification, and read-only truth-check helpers.

- Removes parent import coupling for execution-preflight receipt construction
  and updates install-root source-boundary tests for the moved module layout.

- Keeps all new modules under the existing install-root ownership boundary using
  directory-module layout.

### 📊 Audit

- Reduces `install_root/mod.rs` from 1736 lines after `0.67.15` to 1004 lines.

- Adds focused child modules for artifact-promotion receipts, install
  operations, plan artifact handling, and current execution gate handling.

### 🧪 Testing

- Validated focused install-root deployment-truth coverage, then reran the full
  host library test suite, host clippy with warnings denied, formatting,
  changelog governance, and whitespace checks.

## 0.67.15 - 2026-06-14 - Install-root receipt decomposition

### 📝 Summary

- Continues canic-host install-root decomposition without changing install
  behavior, deployment-truth receipt shape, or public host APIs.

- Moves deployment-truth gate enforcement, text rendering, and gate receipt
  construction into a focused install-root child module.

- Moves execution-preflight receipt writing and evidence formatting into a
  focused install-root child module.

- Moves release-set staging operation evidence and staging receipt conversion
  into a focused install-root child module.

- Moves phase-receipt scope handling, completed-phase receipt construction, and
  role-phase receipt construction into a focused install-root child module.

### 🧹 Cleanup

- Leaves the install-root parent focused more tightly on install orchestration,
  deployment-truth preparation, plan/artifact path resolution, install state
  writes, and root canister resolution.

- Keeps test-facing helper access through the existing install-root test module
  imports while avoiding new public host API surfaces.

### 📊 Audit

- Reduces `install_root/mod.rs` from 2316 lines at the `0.67.14` baseline to
  1736 lines after the gate, preflight, staging, and phase-receipt splits.

- Adds focused child modules for deployment-truth gate handling,
  execution-preflight receipts, release-set staging, and phase receipts.

### 🧪 Testing

- Validated focused install-root deployment-truth coverage, then reran the full
  host library test suite, host clippy with warnings denied, formatting,
  changelog governance, and whitespace checks.

## 0.67.14 - 2026-06-14 - Verifier and safety report decomposition

### 📝 Summary

- Continues the canic-host deployment-truth report decomposition without
  changing deployment diff behavior, safety report output, or public report
  APIs.

- Moves verifier-readiness comparison into a focused report child module.

- Moves safety-report construction, status calculation, resume reasons,
  summaries, and next-action helpers into a focused report child module while
  preserving the existing public `safety_report_from_diff` export.

### 🧹 Cleanup

- Leaves the report parent focused on local check orchestration, plan/inventory
  diff orchestration, deployment identity comparison, and shared diff helpers.

- Keeps receipt-resume callers using the same safety status and resume-reason
  helpers through a report-family scoped re-export.

### 📊 Audit

- Reduces the deployment-truth report parent to roughly 400 lines after the
  verifier-readiness and safety-report helper split.

- Keeps all extracted report helpers under the existing report module family,
  preserving ownership and avoiding new host API surfaces.

### 🧪 Testing

- Validated focused deployment-diff coverage for verifier readiness and safety
  reports, then reran the full host library test suite, host clippy with
  warnings denied, formatting, changelog governance, and whitespace checks.

## 0.67.13 - 2026-06-14 - Controller and digest report decomposition

### 📝 Summary

- Continues canic-host deployment-truth report decomposition without changing
  deployment diff behavior, persisted data shapes, or public host APIs.

- Moves controller-authority and observed-controller reconciliation helpers
  into a focused report child module.

- Moves installed-module-hash comparison and runtime-config digest comparison
  helpers into focused report child modules.

### 🧹 Cleanup

- Removes the parent-report callback previously used by canister comparison for
  controller reconciliation.

- Keeps module-hash lookup/reporting and raw/canonical runtime-config digest
  comparison separate from the main report orchestration.

### 📊 Audit

- Reduces the deployment-truth report parent to roughly 720 lines after the
  controller, module-hash, and config-digest helper splits.

- Narrows the report parent toward orchestration and shared helper ownership,
  leaving the verifier-readiness and safety-report helpers as the remaining
  bounded follow-up clusters.

### 🧪 Testing

- Validated focused deployment-diff coverage for controller authority,
  installed module hashes, and config digest drift, then reran the full host
  library test suite, host clippy with warnings denied, formatting, changelog
  governance, and whitespace checks.

## 0.67.12 - 2026-06-14 - Artifact, canister, and pool report decomposition

### 📝 Summary

- Continues canic-host deployment-truth report decomposition without changing
  deployment diff behavior, persisted data shapes, or public host APIs.

- Moves artifact diff comparison, canister diff comparison, and pool diff
  comparison helpers into focused report child modules.

- Keeps the existing plan/inventory diff entry point and report module public
  API intact.

### 🧹 Cleanup

- Isolates artifact duplicate/conflict/missing/digest checks away from the
  report parent.

- Isolates canister duplicate/conflict/missing/control-class/controller checks
  away from the report parent.

- Isolates pool duplicate/conflict/missing/control-class and cross-surface
  canister/pool role checks away from the report parent.

### 📊 Audit

- Reduces the deployment-truth report parent from the post-0.67.11 shape to
  roughly 1.1k lines by extracting the artifact, canister, and pool comparison
  families.

- Preserves the existing shared duplicate-evidence and conflicting-assignment
  helper model for the new report child modules.

### 🧪 Testing

- Validated focused deployment-diff coverage for artifact, canister, and pool
  cases, then reran the full host library test suite, host clippy with warnings
  denied, formatting, changelog governance, and whitespace checks.

## 0.67.11 - 2026-06-14 - Deployment-truth text and report decomposition

### 📝 Summary

- Decomposes deployment-truth text rendering without changing operator output,
  persisted data shapes, or public text exports.

- Moves lifecycle, external-upgrade, promotion, authority, comparison,
  execution-preflight, and deployment-root verification text renderers into
  focused child modules.

- Starts deployment-truth report decomposition by isolating receipt-aware
  resume comparison and root-subnet evidence checks behind focused child
  modules.

### 🧹 Cleanup

- Leaves the deployment-truth text parent as a small facade and shared
  formatting-helper module.

- Keeps the report parent responsible for the primary plan/inventory diff and
  safety-report construction while moving bounded receipt-resume and root-subnet
  evidence helper clusters out of the parent.

- Preserves the existing public host API and the existing test-visible
  root-subnet evidence hooks.

### 📊 Audit

- Reduces the deployment-truth text parent to roughly 80 lines after moving all
  renderer families into child modules.

- Reduces the deployment-truth report parent by extracting the receipt-resume
  and root-subnet evidence clusters while leaving the broader diff comparison
  logic in place for follow-up cleanup.

- Extends the deployment-truth source audit so the new text child modules remain
  covered by the controller-mutation primitive guard.

### 🧪 Testing

- Validated the focused text renderer, receipt-resume, root-subnet evidence,
  and deployment-diff test areas, then reran the full host library test suite,
  host clippy with warnings denied, formatting, changelog governance, and
  whitespace checks.

## 0.67.10 - 2026-06-14 - Lifecycle and install-root helper decomposition

### 📝 Summary

- Completes the current host deployment-truth lifecycle decomposition without
  changing operator behavior, persisted data shapes, or public lifecycle
  exports.

- Moves external-upgrade receipt, consent, verification, completion, and
  proposal report construction and validation into a focused lifecycle child
  module.

- Starts the next install-root decomposition pass by isolating command/build,
  root-verification, and receipt IO helpers behind focused child modules.

### 🧹 Cleanup

- Leaves `deployment_truth/lifecycle/mod.rs` as a small public re-export module
  after extracting the external-upgrade report cluster.

- Moves install-root ICP command construction, build-environment setup,
  local-cycle funding, timing-summary rendering, root-verification
  state/receipt helpers, and deployment-truth receipt pathing/writing out of
  the install-root parent module.

- Keeps the existing public host API intact for CLI callers and tests.

### 📊 Audit

- Reduces the lifecycle parent module to a small re-export surface and reduces
  `install_root/mod.rs` from roughly 3.0k lines to roughly 2.3k lines.

- Updates the June change-friction carry-forward notes so the remaining large
  host pressure is narrowed to install-root truth orchestration plus
  deployment-truth report/text modules.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.9 - 2026-06-14 - Lifecycle pending and handoff decomposition

### 📝 Summary

- Continues the host deployment-truth lifecycle decomposition without changing
  operator behavior, persisted data shapes, or public lifecycle exports.

- Moves external lifecycle pending, check, handoff, and critical-fix report
  construction and validation into a focused lifecycle child module.

- Keeps the existing lifecycle API re-export surface intact for callers.

### 🧹 Cleanup

- Isolates pending action projection, operator check summaries, handoff action
  construction, critical-fix residual exposure summaries, and their validators
  away from the lifecycle parent module.

- Leaves the external-upgrade receipt, consent, verification, completion, and
  proposal report cluster as the remaining lifecycle parent follow-up.

### 📊 Audit

- Reduces the lifecycle parent module from roughly 2.5k lines to roughly 1.7k
  lines after extracting the pending/check/handoff/critical-fix cluster.

- Updates the June change-friction carry-forward notes so the remaining
  lifecycle pressure is narrowed to external-upgrade reports, with
  install-root/report-family pressure still open.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.8 - 2026-06-14 - Lifecycle authority-plan decomposition

### 📝 Summary

- Continues the host deployment-truth lifecycle decomposition without changing
  operator behavior, persisted data shapes, or public lifecycle exports.

- Moves lifecycle authority-report and external lifecycle-plan construction and
  validation into a focused lifecycle child module.

- Keeps the existing lifecycle API re-export surface intact for callers.

### 🧹 Cleanup

- Isolates lifecycle authority classification, controller/consent derivation,
  observed expected lookup, lifecycle subject construction, role-upgrade
  projection, and subject uniqueness validation away from the lifecycle parent
  module.

- Leaves the remaining external upgrade receipt, consent, verification,
  proposal, pending, handoff, and critical-fix report families as follow-up
  lifecycle split candidates.

### 📊 Audit

- Reduces the lifecycle parent module from roughly 3.2k lines to roughly 2.5k
  lines after extracting the authority-plan cluster.

- Updates the June change-friction carry-forward notes so lifecycle
  authority-plan pressure is recorded as reduced, with remaining lifecycle
  report-family and install-root pressure still open.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.7 - 2026-06-14 - Promotion identity and plan decomposition

### 📝 Summary

- Continues the host deployment-truth promotion decomposition without changing
  operator behavior, persisted data shapes, or public promotion exports.

- Moves artifact identity reports, wasm-store identity/catalog helpers,
  transform/readiness construction and validation, and artifact promotion
  plan/target-lineage logic into focused promotion child modules.

- Keeps the existing promotion API re-export surface intact for callers.

### 🧹 Cleanup

- Isolates artifact identity reporting, wasm-store publication identity,
  promotion transform readiness, and artifact promotion plan construction away
  from the promotion parent module.

- Leaves the promotion parent as a small public re-export module after the
  provenance, execution-receipt, materialization, identity, wasm-store,
  transform/readiness, and artifact-plan clusters were extracted.

### 📊 Audit

- Reduces the promotion parent module to roughly 70 lines after extracting the
  remaining promotion helper clusters.

- Updates the June change-friction carry-forward notes so promotion pressure is
  recorded as reduced, with lifecycle and install-root/report-family pressure
  still open for follow-up cleanup.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.6 - 2026-06-13 - Promotion materialization decomposition

### 📝 Summary

- Continues the host deployment-truth promotion decomposition without changing
  operator behavior, persisted data shapes, or public promotion exports.

- Moves source-build materialization evidence construction/validation,
  materialization identity report construction/validation, output-group
  validation, and transform materialization-link validation into a focused
  promotion child module.

- Keeps the existing promotion API re-export surface intact for callers.

### 🧹 Cleanup

- Isolates source-build materialization evidence, build recipe/input/result
  validation, materialization report grouping, and transform link checks away
  from the promotion parent module.

- Leaves the remaining promotion transform/readiness and wasm-store clusters
  as separate follow-up candidates rather than mixing domains into this
  release.

### 📊 Audit

- Reduces the promotion parent module to roughly 1.8k lines after extracting
  the materialization cluster.

- Updates the June change-friction carry-forward notes so promotion
  provenance, execution receipt, and materialization helper extraction are
  recorded as completed follow-ups, with lifecycle and install-root pressure
  still open.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.5 - 2026-06-13 - Promotion provenance decomposition

### 📝 Summary

- Continues the host deployment-truth decomposition from `0.67.4` without
  changing operator behavior, persisted data shapes, or public
  deployment-truth exports.

- Moves artifact-promotion provenance report construction/validation and
  execution-receipt construction/validation into a focused promotion child
  module.

- Keeps the existing promotion API re-export surface intact for callers.

### 🧹 Cleanup

- Isolates provenance blocker aggregation, wasm-store/catalog/materialization
  linkage, role execution-receipt mapping, and provenance/receipt validators
  away from the promotion parent module.

- Leaves promotion transform, readiness, wasm-store, and materialization helper
  clusters in the parent for follow-up slices instead of mixing multiple
  domains into this release.

### 📊 Audit

- Reduces the promotion parent module to roughly 2.3k lines after extracting
  the provenance and execution-receipt cluster.

- Records the new promotion split in the June change-friction carry-forward
  notes, with lifecycle, install-root, and remaining report-family pressure
  still open.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.4 - 2026-06-13 - Deployment-truth module decomposition

### 📝 Summary

- Continues the June 13 host change-friction cleanup by reducing the largest
  deployment-truth lifecycle and promotion modules without changing operator
  behavior, persisted data shapes, or public deployment-truth exports.

- Converts lifecycle and promotion into directory modules so focused internals
  can live next to their owning feature area instead of as flat sibling files.

- Keeps the existing deployment-truth API re-export surface intact for callers.

### 🧹 Cleanup

- Moves lifecycle error types and digest serialization helpers into focused
  lifecycle child modules.

- Moves promotion request types, error types, digest helpers, identity
  grouping helpers, guard helpers, and policy check construction/validation
  into focused promotion child modules.

- Updates source-audit tests that intentionally inspect deployment-truth source
  files so they follow the new lifecycle module path.

### 📊 Audit

- Reduces the remaining lifecycle parent module to roughly 3.2k lines after
  extracting error and digest support.

- Reduces the remaining promotion parent module to roughly 3.1k lines after
  extracting request, error, digest, identity, policy, and guard support.

- Leaves promotion provenance/execution receipt helpers as the next large
  follow-up cluster rather than mixing that higher-coupling split into this
  release.

### 🧪 Testing

- Validated the host library test suite, host clippy with warnings denied,
  formatting, changelog governance, and whitespace checks.

## 0.67.3 - 2026-06-13 - Registry projection cleanup

### 📝 Summary

- Closes the workflow/API registry-record projection follow-up from the June 13
  change-friction audit.

- Keeps raw subnet registry record reads inside ops-owned modules, while
  workflow and endpoint-boundary code use semantic projections for role,
  parent, registration, and unregister behavior.

- Leaves runtime behavior and persisted registry data shapes unchanged.

### 🧹 Cleanup

- Replaces workflow and endpoint-boundary uses of raw subnet registry records
  with `role_parent`, `is_registered`, pool registration metadata, and
  semantic unregister helpers.

- Removes the dead raw-record removal façade from subnet registry ops after
  callers moved to narrower helpers.

- Updates the June audit summaries so future handoffs no longer carry the
  registry-record projection follow-up as open.

### 🧪 Testing

- Validated the full `canic-core` library test suite, `canic-core` clippy with
  warnings denied, changelog governance, formatting, whitespace checks, and
  source scans confirming raw registry access remains ops-owned.

## 0.67.2 - 2026-06-13 - CLI and audit cleanup

### 📝 Summary

- Finishes the immediate post-helper-split cleanup around Canic's operator
  surface by keeping the `canic-cli` README focused on Canic-owned commands
  and describing external helper tools generically.

- Refreshes the pinned external helper tooling used by local setup and CI for
  the 0.67.2 release line.

- Records the June 13 recurring audit pass for audience-target binding,
  capability-scope enforcement, and change friction.

- Fixes the explicit pool recycle workflow/storage boundary leak found by the
  change-friction audit by moving pool registration metadata projection behind
  `ops::storage::pool`.

### 📊 Audit

- Audience-target binding passed against the current delegated-token,
  role-attestation, and structural capability surfaces with a 3/10 risk index.

- Capability-scope enforcement passed against delegated-token verification,
  root capability envelope validation, authorization/replay ordering, and
  endpoint-boundary rejection with a 3/10 risk index.

- Change friction passed after the pool recycle cleanup. The final risk index
  is 4/10, with host deployment-truth decomposition and broader registry-record
  projection work left as follow-up watchpoints.

### 🧹 Cleanup

- Removes stale long-form NNS command examples from the `canic-cli` README so
  Canic documentation does not duplicate the helper tool's command surface.

- Keeps top-level CLI help coverage focused on currently owned Canic command
  families.

- Routes pool recycle metadata preservation through `PoolRegistrationMetadata`
  before pending-reset and ready pool registration, preserving role, parent,
  and module hash without naming the canister storage record in workflow code.

### 🧪 Testing

- Validated the focused pool workflow tests, `canic-core` clippy, formatting,
  and whitespace checks after the pool boundary cleanup.

- Retained the host and CLI library test validation recorded by the June 13
  audit reports.

## 0.67.1 - 2026-06-13 - Host audit and test layout cleanup

### 📝 Summary

- Audits `canic-host` after the `icq` split and removes stale host module
  wiring that no longer had active source ownership.

- Reorganizes large host unit-test files into focused sibling and directory
  modules so deployment truth, install root, release set, adoption, registry,
  configuration, and artifact-support coverage can be reviewed by topic.

- Leaves the CLI surface, runtime behavior, persisted data shapes, and external
  tooling contract unchanged.

### 📊 Audit

- Confirms `canic-host` no longer carries inline `mod tests { ... }` blocks
  under `src`.

- Keeps the release-set and adoption test groups below their previous
  monolithic size by splitting role, profile, authority, evidence, artifact,
  path, mutation, and serialization scenarios into focused modules.

- Removes the orphan cache-file module wiring left behind by earlier host
  cleanup.

### 🧪 Testing

- Maintains the existing host unit-test coverage while moving tests into
  smaller module groups; no tests were intentionally removed or weakened.

## 0.67.0 - 2026-06-13 - IC query extraction

### Summary

- Moves the NNS query command surface from `canic nns ...` into the external
  `ic-query` crate with the `icq nns ...` executable path.

- Moves the live NNS registry adapter and query/report modules out of
  `canic-host`.

- Folds the standalone `canic-subnet-catalog` package into `ic-query`, leaving
  `ic-query` as the standalone publishable `0.0.1` crate for IC registry query
  tooling.

- Changes mainnet root-auth subnet evidence collection so Canic executes `icq`
  instead of linking subnet-catalog code into the host.

- Adds `icq` to the pinned external tooling surface: local setup and CI install
  and verify the `ic-query` CLI alongside `icp` before Canic checks run.

### Operator Surface

- `canic nns ...` no longer owns the public NNS metadata query surface.

- `icq nns ...` is the executable path for NNS registry, subnet, node,
  node-provider, node-operator, data-center, and topology query/report
  commands.

- Canic root-auth evidence workflows now depend on the installed `icq` binary
  for mainnet subnet evidence.
