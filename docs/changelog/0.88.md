# Canic 0.88 Changelog

## [0.88.2] - 2026-07-13 - Typed Fleet-Config Failures

### 📝 Summary

0.88.2 completes the bounded 0.88 line by giving fleet configuration one typed
failure boundary. Parse, I/O, validation, mutation, package-metadata,
role-contract, and rollback failures remain inspectable until command and host
decision-making is complete.

Successful projections, serialized TOML, fleet mutations, rollback behavior,
CLI rendering, and exit behavior are unchanged.

### ⚠️ Breaking

- Public host fleet-config APIs now return `FleetConfigError` instead of boxed
  dynamic errors. This is a pre-1.0 hard cut with no compatibility conversion
  or legacy return path.
- Downstream Rust callers that name the previous boxed return type must adopt
  the typed error or erase it explicitly at their own outer boundary. Config
  files, persisted data, and command usage require no migration.

### 🔧 Changed

- I/O failures retain a bounded operation, path, and original source error.
- Core configuration and TOML failures retain their operation and original
  parser source.
- Invalid input, missing declarations, fleet mismatches, mutation conflicts,
  package-metadata failures, and role-contract rejection have bounded typed
  variants.
- Build, fleet, and scaffold commands retain the typed failure through their
  direct CLI boundaries instead of converting it to text.

### 🩹 Fixed

- A failed package-manifest update followed by a failed fleet-config rollback
  now preserves both causes as typed data; neither failure replaces or embeds
  the other only in rendered text.
- Configuration callers no longer need rendered-message parsing to distinguish
  validation, conflict, parse, or filesystem failures.

### 🧹 Cleanup

- Boxed dynamic-error signatures and string-built error returns are removed
  from the fleet-config owner.
- All projections use one local core-config parsing boundary.
- Redundant host root-topology rejection is removed because validated core
  configuration already owns that invariant.
- No global host error system, compatibility wrapper, or generic error-code
  framework is introduced.

### 🧪 Testing

- Focused boundary tests prove typed I/O and core sources survive, invalid
  inputs and conflicts are classified structurally, existing rollback still
  restores bytes, and rollback failure retains both causes.
- Thirty release-set projection and mutation tests preserve successful values,
  serialized TOML, topology validation, and role-contract output.
- Ninety-five focused build, fleet, and scaffold tests preserve direct CLI
  behavior. Targeted package Clippy, formatting, layering, changelog
  governance, and diff hygiene pass.
- Full workspace, PocketIC, deployment, and release suites were not run under
  the targeted testing policy.

## [0.88.1] - 2026-07-13 - Publication-Atomic CLI Output

### 📝 Summary

0.88.1 prevents CLI report and evidence destinations from exposing truncated
or partially serialized files. The existing host durable byte writer now owns
both atomic replacement and atomic create-new publication through one private
commit engine.

Shared CLI JSON and text helpers serialize complete bytes before entering that
boundary. `canic deploy plan --out` uses the explicit no-clobber operation and
retains its existing missing-parent, JSON, newline, diagnostic, and exit-code
contracts.

### 🔧 Changed

- Replacing output stages complete bytes in a unique sibling, synchronizes the
  staged file, atomically replaces the destination, and synchronizes the final
  parent directory.
- Create-new output uses the same staging path with atomic no-replace
  publication rather than an existence check followed by a replacing rename.
- Shared output creates missing parent hierarchies one level at a time and
  synchronizes each new directory and the parent entry that introduced it.
- Platforms unable to provide the required atomic publication and
  directory-sync semantics fail explicitly instead of using a weaker writer.

### 🩹 Fixed

- JSON serialization failures leave existing destination bytes unchanged and
  do not create a requested parent hierarchy.
- Failures before publication leave replace destinations unchanged and
  create-new destinations absent.
- A durability failure after publication may return an error with the complete
  new file visible, but final paths never contain partial bytes.
- Handled staging, write, sync, and publication failures remove their sibling
  temporary files.

### 🧹 Cleanup

- The three shared CLI file helpers no longer write final destinations
  directly.
- Deployment-plan output no longer opens and incrementally writes its final
  create-new destination.
- The replacing and create-new entry points expose no public mode or options
  abstraction; their staging and publication engine remains private to host
  durable I/O.
- Scaffold files, the cycles pending-operation log, backup persistence, and
  subsystem-owned host persistence retain their existing owners.

### 🧪 Testing

- Nine focused host tests cover nested parent persistence, successful replace
  and create-new output, atomic no-clobber races, staging and sync failures,
  actual replacement failure, cleanup, and post-publication durability errors.
- Three focused CLI output tests cover all shared helpers and prove partial
  serialization cannot mutate an existing destination or create missing
  parents.
- Focused deployment-plan tests preserve JSON-only output, trailing newline,
  create-new rejection, missing-parent behavior, and exit code.
- Targeted host and CLI test-target Clippy passes with warnings denied.
  Formatting, layering guards, dependency hygiene, changelog governance, and
  diff hygiene pass.
- Full workspace, PocketIC, deployment, and release suites were not run under
  the targeted testing policy.

## [0.88.0] - 2026-07-13 - Durable Backup Artifact Publication

### 📝 Summary

0.88.0 makes snapshot artifact-directory publication genuinely durable. Both
direct snapshot capture and planned backup finalization use one backup-owned
commit operation that synchronizes the complete artifact tree, publishes it
without replacing an existing destination, and synchronizes the owning
directory before the journal reports durable state.

An interruption after publication but before the durable journal transition
can be resumed safely. Recovery accepts only the journal-bound canonical
artifact after reverifying its checksum and resynchronizing its tree; unrelated
or unverifiable destinations continue to fail closed.

### 🔧 Changed

- Artifact traversal opens entries without following symlinks and accepts only
  regular files and directories.
- Regular files are synchronized before nested directories, the temporary
  artifact root, and finally the owning parent directory.
- Publication uses an atomic no-replace operation rather than an
  existence-check followed by an ordinary rename.
- Platforms that cannot provide the required publication and directory-sync
  semantics fail explicitly instead of using a weaker fallback.
- The existing directory-checksum algorithm can consume already-open file
  descriptors, preserving its hashes and serialized contracts while recovery
  avoids reopening inspected entries by path.

### 🩹 Fixed

- A crash after the final rename no longer leaves the backup permanently
  blocked by its own complete artifact.
- A failed final-parent synchronization leaves a complete, verifiable artifact
  that can be safely resynchronized on retry.
- A failed durable journal write exposes neither `ArtifactState::Durable` nor
  an incremented artifact-finalization completion metric in memory.
- Planned finalization rejects any artifact state below `ChecksumVerified`
  before attempting publication.

### 🧹 Cleanup

- Both previous artifact-finalization writers are removed. No compatibility
  writer, filesystem framework, or alternate recovery path remains.
- Directory commit policy remains private to backup persistence and is not
  shared with the separate 0.88 single-file output slice.

### 🧪 Testing

- Focused commit tests cover successful publication, exact recovery, symlink
  and unsupported-entry rejection, atomic no-replace races, and failures at
  every file, directory, publication, and final-parent persistence boundary.
- Focused direct-capture tests preserve successful output and prove destination
  conflicts and journal-write failures cannot report durable completion.
- Focused runner tests prove exact post-publication recovery and checksum
  rejection without replaying completed snapshot commands.
- Targeted `canic-backup` Clippy passes with warnings denied. Formatting,
  changelog governance, dependency hygiene, layering guards, and diff hygiene
  pass.
- Full workspace, PocketIC, deployment, and release suites were not run under
  the targeted testing policy.
