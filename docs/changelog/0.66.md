# 0.66 Changelog

## 0.66.7 - 2026-06-12 - Canister-signature helper naming cleanup

### Summary

- Renames root canister-signature seed/domain helpers from `root_sig_*` to
  `root_canister_sig_*`, avoiding confusion with the removed `root_sig`
  delegated-token proof field.

- Renames issuer canister-signature seed/domain/hash helpers from
  `issuer_sig_*` to `issuer_canister_sig_*`, keeping root and issuer proof
  helper naming symmetrical.

- Renames private root and issuer signature-map owner helpers/statics to make
  the canister-signature certified-data ownership explicit.

### Testing

```text
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo test --locked -p canic-core ops::auth::root_canister_sig --lib -- --nocapture
cargo test --locked -p canic-core ops::auth::issuer_canister_sig --lib -- --nocapture
cargo test --locked -p canic-core ops::auth::delegated::issue --lib -- --nocapture
cargo test --locked -p canic-core auth --lib -- --nocapture
cargo clippy --locked -p canic-core --lib -- -D warnings
cargo test --locked -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.66.6 - 2026-06-12 - Delegated-token prepare terminology cleanup

### Summary

- Removes the unused `canic_delegated_token_verifier` build cfg declaration and
  emission. Delegated-token verifier behavior is now enforced by runtime config,
  not by a compile-time endpoint-bundle cfg.

- Renames the internal delegated-token issuer preparation module and typed
  errors/input from stale `mint` terminology to `prepare` terminology, matching
  the active issuer prepare/get canister-signature flow.

- Normalizes active delegated-auth documentation around issuer token issuance
  terminology while leaving explicit removed `mint_token` references intact.

### Testing

```text
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo test --locked -p canic-core ops::auth::delegated::prepare --lib -- --nocapture
cargo test --locked -p canic build_support --lib -- --nocapture
cargo test --locked -p canic --test protocol_surface -- --nocapture
cargo clippy --locked -p canic-core --lib -- -D warnings
cargo clippy --locked -p canic --lib -- -D warnings
cargo test --locked -p canic --test changelog_governance -- --nocapture
git diff --check
```

## 0.66.5 - 2026-06-12 - Minimal metrics baseline split

### Summary

- Makes the `canic` build macro emit `canic_disable_bundle_metrics` when the
  facade is compiled without the `metrics` feature, so no-default-features
  canisters do not accidentally carry the metrics endpoint bundle.

- Keeps `canisters/audit/minimal` as the no-default-features runtime floor and
  adds `canisters/audit/minimal_metrics` as the explicit metrics-enabled
  comparison baseline for audit and footprint work.

- Renames the manual local sandbox from `sandbox_minimal` to `sandbox_blank`,
  keeping “minimal” reserved for the measured audit baseline.

- Records the intended feature/config contract for audit baselines: Cargo
  features decide which runtime families are compiled, while `canic.toml`
  decides the role-scoped endpoint/config surface. Missing auth support fails
  at runtime startup, but extra Cargo features remain a valid over-provisioning
  concern for wasm footprint audits.

### Testing

```text
cargo check --locked -p canister_minimal -p canister_minimal_metrics -p canister_sandbox_blank
cargo check --locked --target wasm32-unknown-unknown -p canister_minimal -p canister_minimal_metrics -p canister_sandbox_blank
cargo tree --locked --target wasm32-unknown-unknown -p canister_minimal -e features | rg "canic feature|canic-core feature|canic v"
cargo tree --locked --target wasm32-unknown-unknown -p canister_minimal_metrics -e features | rg "canic feature|canic-core feature|canic v"
cargo check --locked -p canic -p canic-host
cargo test --locked -p canic build_support --lib -- --nocapture
cargo test --locked -p canic-host release_set --lib -- --nocapture
cargo test --locked -p canic-tests --test root_wasm_store_reconcile -- --test-threads=1 --nocapture
make fmt-check
cargo test --locked -p canic --test workspace_manifest -- --nocapture
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --locked -p canic --test changelog_governance -- --nocapture
cargo test --locked -p canic --test protocol_surface -- --nocapture
git diff --check
```

## 0.66.4 - 2026-06-12 - Metrics facade validation fix

### Summary

- Exposes the existing runtime memory bootstrap helper through the public
  `canic::api::runtime::MemoryRuntimeApi` facade so integration tests and
  downstream callers do not need internal paths for explicit runtime bootstrap.

- Bootstraps the stable-memory registry in direct metrics facade integration
  tests before paging metric families that read stable-memory-backed ICP refill
  records.

- Restores `cargo test --workspace --all-features` after the 0.66.3 validation
  pass exposed the metrics facade bootstrap gap.

### Testing

```text
cargo test --locked -p canic --test metrics_facade
cargo test --workspace --all-features
```

## 0.66.3 - 2026-06-12 - Strict verifier closeout

### Summary

- Requires an effective raw IC root key for every delegated-auth verifier
  network. Mainnet still requires the configured known mainnet key; local,
  PocketIC, and testnet builds may inject the IC system root key once during
  startup so protected endpoint verification never falls back to runtime
  root-key discovery.

- Enforces `delegated_token_verifier = true` at delegated-token verification
  execution time, before proof verification work, so issuer-only and
  role-attestation-only canisters cannot accidentally verify delegated tokens.

- Records ignored `.icp/local` Candid sidecars as local build artifacts that are
  excluded from release closeout evidence unless regenerated from the current
  checked-in exports.

- Pins the workspace `time` dependency at `0.3.47`, avoiding the `0.3.48`
  `ic-agent`/`tracing-subscriber` conflict while moving off the vulnerable
  `0.3.41` line.

### Testing

```text
cargo tree -i time --locked
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo clippy --locked -p canic-core --lib -- -D warnings
cargo test --locked -p canic-core auth --lib
cargo test --locked -p canic --test protocol_surface
cargo test --locked -p canic --test changelog_governance
cargo build --locked -p canic-wasm-store --target wasm32-unknown-unknown
cargo check --locked -p canic-host
cargo test --locked -p canic-host release_set
cargo test --locked -p canic-tests --test root_suite delegated_token_verification_uses_self_contained_root_proof -- --nocapture
cargo test --locked -p canic-tests --test pic_role_attestation role_attestation_verification_paths -- --nocapture
git diff --check
```

`cargo test --workspace --all-features` still fails in the unrelated
`canic --test metrics_facade` stable-memory bootstrap path.

## 0.66.2 - 2026-06-12 - Closeout audit baseline

### Summary

- Publishes the 0.66 package surface for the post-0.65 closeout audit line.

- Leaves 0.66 scoped to validation, reconciliation, and cleanup of the 0.65
  zero-management-ECDSA auth epoch rather than new auth protocol work.

### Testing

```text
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo clippy --locked -p canic-core --lib -- -D warnings
cargo test --locked -p canic-core auth --lib
cargo test --locked -p canic --test protocol_surface
cargo clippy --workspace --all-targets --all-features -- -D warnings
git diff --check
```

## 0.66.1 - 2026-06-12 - Delegated-auth trust-anchor closeout

### Summary

- Enforces the verifier trust-anchor contract for delegated-token and
  role-attestation canister-signature verification by pairing the configured
  network label with the effective raw IC root public key.

- Requires `network = "mainnet"` to use the known mainnet raw IC root key and
  fail closed when `ic_root_public_key_raw_hex` is missing or mismatched.

- Requires every verifier network, including `local`, `pocketic`, and
  `testnet`, to provide an explicit `ic_root_public_key_raw_hex`; non-mainnet
  modes reject accidental use of the mainnet root key.

- Keeps protected endpoint verification local and self-contained; no root,
  issuer, management-canister, or threshold-ECDSA call was added to verifier
  hot paths.

- Updates the checked-in wasm-store Candid sidecar so
  `DelegatedToken.issuer_proof` uses the distinct `IssuerProof` type name.

- Refreshes closeout wording for root and issuer `"sig"` certified-data
  ownership, removes stale issuer-runtime dead-code wording, and documents the
  no-op async verifier wrappers as endpoint/runtime shape only.

### Testing

```text
cargo fmt --all -- --check
cargo check --locked -p canic-core -p canic
cargo clippy --locked -p canic-core --lib -- -D warnings
cargo test --locked -p canic-core auth --lib
cargo test --locked -p canic-core delegated_tokens_ --lib
cargo test --locked -p canic --test protocol_surface
git diff --check
```

## 0.66.0 - 2026-06-12 - 0.65 audit, testing, and fixing line

### Summary

- Opens 0.66 as the stabilization line for the 0.65 zero-management-ECDSA
  normal-auth epoch.

- Moves the existing endpoint perf-observability design from the 0.66 slot to
  `docs/design/0.67-perf-observability/0.67-design.md`.

- Adds `docs/design/0.66-audit-test-fix-0.65/0.66-design.md` as the active
  0.66 planning document.

- Scopes 0.66 to audit, testing, release validation, and conservative fixes for
  the 0.65 auth hard cut.

- Explicitly keeps 0.66 out of new auth protocol families, compatibility
  lanes, one-shot issuance, management-canister ECDSA in normal auth, and
  endpoint perf-observability feature work.

- Records that 0.66 may remove stale code and tests when they only preserve
  intentionally retired designs.

### Testing

```text
rg -n '0\.66-perf|0\.66 Tentative Design: Endpoint Perf|Release line: 0\.66 candidate|Proposed 0\.66 Direction|first 0\.66 slice' docs/design docs/status/current.md CHANGELOG.md docs/changelog/0.65.md
rg -n '0\.66|0\.67' docs CHANGELOG.md Cargo.toml crates canisters fleets scripts Makefile -g '*.md' -g '*.toml' -g '*.rs' -g '*.did' -g '*.sh' -g 'Makefile'
rg -n '[ \t]+$' docs/design/0.66-audit-test-fix-0.65/0.66-design.md
git diff --check
```
