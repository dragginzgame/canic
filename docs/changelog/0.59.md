# 0.59 Changelog

## 0.59.7 - 2026-06-03 - Report sentinel cleanup

### 📝 Summary

- Kept instruction-footprint report output unchanged while making repeated
  report status and baseline sentinel labels explicit in code.

### 🧹 Cleanup

- Centralized dynamic report status labels (`PASS`, `PARTIAL`, `BLOCKED`) and
  the missing-baseline sentinel (`N/A`) in the report support module.

- Added focused baseline sentinel coverage so missing baselines and missing
  baseline rows keep rendering as `N/A`.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.6 - 2026-06-03 - Estimate flag diagnostic cleanup

### 📝 Summary

- Kept estimate behavior unchanged while making direct environment-driven
  estimate flag errors match the flag type.

### 🧹 Cleanup

- Split boolean estimate flag parsing from positive integer source parsing, so
  invalid `CANIC_INSTRUCTION_AUDIT_ESTIMATE_EXECUTION_CYCLES` values report a
  boolean flag error instead of a positive-integer error.

- Added focused parser coverage for accepted boolean values and rejected
  non-boolean values.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.5 - 2026-06-03 - Estimate markdown contract cleanup

### 📝 Summary

- Kept the offline estimate artifact behavior unchanged while pinning the
  markdown report wording for the optional estimate section.

### 🧹 Cleanup

- Moved the estimate markdown section title, instructions-only label, and table
  header into named report constants.

- Added focused report-rendering coverage proving reports omit the estimate
  section when no rows have estimates and render the required instructions-only
  estimate label when estimates are present.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.4 - 2026-06-03 - Estimate contract label cleanup

### 📝 Summary

- Kept the offline estimate artifact shape unchanged while making the remaining
  JSON contract labels explicit in the report support code.

### 🧹 Cleanup

- Replaced scattered raw estimate label strings for `kind`, `charge_model`,
  `subnet_source`, `source_meaning`, and `rate_source` with named constants.

- Left the focused serialization/rate tests pinned to literal values so the
  public JSON contract still fails loudly if one of those labels changes.

- Expanded estimate-object serialization coverage for the table-backed path
  and the explicit-rate override path, including source labels, rate labels,
  formula version, override flags, and node-count table-rate metadata.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.3 - 2026-06-03 - Estimate omitted-cost contract cleanup

### 📝 Summary

- Kept the 0.59 offline estimate object behavior unchanged while removing one
  repeated allocation in the report support layer.

### 🧹 Cleanup

- Moved the fixed `execution_cycle_estimate.omitted_costs` list to a single
  static slice and reused it for every estimate row.

- Added focused serialization coverage proving the pinned omitted-cost list
  still appears in the JSON estimate object.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.2 - 2026-06-03 - Quiet ICP refill macro release gate

### 📝 Summary

- Restored CI `RUSTUP_TOOLCHAIN` propagation for nested wasm builds and
  removed the expected red compiler diagnostic from the release-gate doctest
  lane while preserving coverage of the ICP-refill endpoint macro's
  missing-guard contract.

### 🩹 Fixed

- Restored `RUSTUP_TOOLCHAIN` through `$GITHUB_ENV` in the MSRV, checks, and
  tag-check jobs. This keeps nested Cargo builds, including the
  `delegation_root_stub` bootstrap `wasm_store` build, on the same toolchain
  that has `wasm32-unknown-unknown` installed while avoiding invalid
  job-level `${{ env.* }}` expressions.

- Replaced the guard-required `compile_fail` doctest for
  `canic_emit_icp_refill_endpoints!` with ordinary unit coverage of the macro's
  missing-guard `compile_error!` branch. The macro still rejects calls without
  `guard = <access expression>`, but `make patch` no longer prints the expected
  compiler failure as red output.

### 📚 Documentation

- Updated the 0.59 design doc with the `0.59.1` / `0.59.2` follow-up contract:
  validation hardening and release-gate output hygiene are acceptable
  `0.59.x` cleanup, while NNS/catalog lookup, public cycles profiling
  commands, and broader bill reconstruction remain out of scope.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  actionlint
  cargo fmt --all
  cargo test -p canic --features icp-refill --doc -- --test-threads=1 --nocapture
  cargo test -p canic --features icp-refill icp_refill_endpoint_macro -- --nocapture
  cargo clippy -p canic --features icp-refill --all-targets
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.1 - 2026-06-03 - Estimate input and workflow lint cleanup

### 📝 Summary

- Tightened the 0.59 estimate input contract across both the shell wrapper and
  the Rust report path. Estimate source inputs now require estimate mode
  instead of being ignored when the report generator is invoked directly.

- Fixed the GitHub Actions workflow validation failure caused by using the
  top-level `env` context inside job-level `env` expressions, and made the
  local developer setup path explicitly carry the pinned actionlint version and
  install directory.

### 🩹 Fixed

- Removed invalid job-level `RUSTUP_TOOLCHAIN: ${{ env.* }}` assignments from
  the CI workflow. The wasm target install steps now use the top-level shell
  environment variables directly.

- Added Rust-side estimate option validation for direct environment-driven
  report generation, matching the shell wrapper rule that
  `--estimate-node-count` and `--cycles-per-billion-instructions` require
  `--estimate-execution-cycles`.

### 🔧 Changed

- `make install-dev` and `make update-dev` now pass the pinned
  `ACTIONLINT_VERSION` and `ACTIONLINT_INSTALL_DIR` through to the shared dev
  installer, keeping the local actionlint setup explicit at the Makefile
  boundary.

### 🧪 Testing

- Validated the cleanup with:

  ```text
  actionlint
  actionlint .github/workflows/ci.yml
  actionlint -version
  make -n install-dev update-dev
  bash -n scripts/dev/install_dev.sh
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  cargo check --workspace
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

## 0.59.0 - 2026-06-03 - Instruction accounting and offline estimates

### 📝 Summary

- Started the Slice 59 instruction-accounting line with a hard terminology
  boundary: measured runtime rows remain instruction rows, while cycle language
  is reserved for real cycle balances/funding/refill surfaces or explicit
  estimates.

- Added opt-in host/test-side execution-cycle estimate decoration for the
  instruction-footprint audit. Estimates require an explicit node-count or
  explicit rate source and do not perform NNS, catalog, or network lookup.

### ➕ Added

- Added the optional `execution_cycle_estimate` sibling object on normalized
  instruction-audit rows. The object records estimate schema version, charge
  model, local instructions, counter ID, sample origin, decimal-string cycle
  estimate/rate values, formula version, rate source, and omitted billed
  components.

- Added pinned offline estimate support for `13` and `34` node assumptions,
  plus the explicit-rate override path for custom or future pricing inputs.

- Added `scripts/ci/instruction-audit-report.sh` estimate flags:

  ```text
  --estimate-execution-cycles
  --estimate-node-count <13|34>
  --cycles-per-billion-instructions <cycles>
  ```

### 🔧 Changed

- Updated instruction-footprint report artifacts to record
  `performance_counter(1)`, `counter_id = 1`, measured unit
  `local_instructions`, and counter semantics in `method.json` and report
  preambles.

- Changed normalized row `sample_origin` from a generic derived label to the
  message-kind scope: `update`, `query`, or `composite_query`.

- Updated endpoint matrix artifacts and markdown output to include
  `sample_origin`, so update, query, and composite-query samples are not
  compared as if they shared identical counter semantics.

### 📚 Documentation

- Added the 0.59 design for instruction accounting and offline cost estimates.

- Updated the recurring instruction-footprint audit definition with the
  counter model, opt-in estimate contract, supported offline rate table, and
  forbidden 0.59 NNS/catalog estimate fields.

### 🧪 Testing

- Validated the release slice with:

  ```text
  cargo fmt --all
  cargo test -p canic-tests --test instruction_audit -- --nocapture
  cargo clippy -p canic-tests --test instruction_audit
  bash scripts/ci/instruction-audit-report.sh --help
  bash scripts/ci/instruction-audit-report.sh --estimate-execution-cycles
  bash scripts/ci/instruction-audit-report.sh --estimate-node-count 13
  bash scripts/ci/instruction-audit-report.sh --estimate-execution-cycles --estimate-node-count 0
  cargo check --workspace
  cargo test -p canic --test changelog_governance -- --nocapture
  git diff --check
  ```

  The three invalid `instruction-audit-report.sh` invocations are expected to
  exit with status `2`; they prove estimate mode rejects missing sources,
  source flags without estimate mode, and zero-valued node counts before report
  artifact creation.
