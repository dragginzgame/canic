# canic-backup

Host-side manifest and orchestration primitives for Canic fleet backup and
restore workflows.

The crate starts with the fleet backup manifest contract. Runtime discovery,
DFX snapshot execution, restore planning, and verification orchestration will be
added as separate modules as the 0.30 backup work lands.
