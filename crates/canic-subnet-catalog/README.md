# canic-subnet-catalog

Pure cached IC subnet catalog model and resolver for Canic host tools.

This crate owns JSON schema validation and principal-byte routing resolution.
It deliberately does not contain live registry transport, protobuf decoding, or
agent code.
