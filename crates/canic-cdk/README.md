# canic-cdk

Canic's lightweight wrapper around `ic-cdk` types and helpers used across the stack.

This crate belongs to the Canic workspace. For installation and usage details, see `../../README.md`.
