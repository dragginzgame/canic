# canic-testing-internal

Workspace-only internal test support for Canic self-tests.

This crate is intentionally unpublished. It owns Canic-specific PocketIC
fixtures and root-topology setup that should not expand the public
`canic-testkit` API surface.
