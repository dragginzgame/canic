diff --git a/crates/canic-core/src/storage/stable/auth/mod.rs b/crates/canic-core/src/storage/stable/auth/mod.rs
index 2a851f0ae..e03f61d92 100644
--- a/crates/canic-core/src/storage/stable/auth/mod.rs
+++ b/crates/canic-core/src/storage/stable/auth/mod.rs
@@ -1,10 +1,4 @@
-use crate::impl_storable_unbounded;
-use crate::{
-    cdk::structures::{DefaultMemoryImpl, cell::Cell, memory::VirtualMemory},
-    eager_static,
-    role_contract::allocation::memory::auth::AUTH_STATE_ID,
-    storage::prelude::*,
-};
+use crate::storage::prelude::*;
 use std::cell::RefCell;
 
 mod records;
@@ -22,15 +16,22 @@ pub use records::{
     RootIssuerRenewalTemplateRecord, RootProofRecord,
 };
 
-eager_static! {
-    pub(super) static AUTH_STATE: RefCell<Cell<AuthStateRecord, VirtualMemory<DefaultMemoryImpl>>> =
-        RefCell::new(Cell::init(
-            crate::ic_memory_key!(authority = CANIC_CORE_MEMORY_AUTHORITY, key = "canic.core.auth.state.v1", ty = AuthState, id = AUTH_STATE_ID),
-            AuthStateRecord::default(),
-        ));
+pub(super) struct HeapAuthStateCell(AuthStateRecord);
+
+impl HeapAuthStateCell {
+    fn get(&self) -> &AuthStateRecord {
+        &self.0
+    }
+
+    fn set(&mut self, record: AuthStateRecord) {
+        self.0 = record;
+    }
 }
 
-impl_storable_unbounded!(AuthStateRecord);
+thread_local! {
+    pub(super) static AUTH_STATE: RefCell<HeapAuthStateCell> =
+        RefCell::new(HeapAuthStateCell(AuthStateRecord::default()));
+}
 
 ///
 /// AuthState
