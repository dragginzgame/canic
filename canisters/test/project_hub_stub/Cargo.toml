[package]
name = "project_hub_stub"
edition = { workspace = true }
rust-version = { workspace = true }
version = { workspace = true }
publish = false

[package.metadata.cargo-machete]
ignored = ["ic-cdk"]

[package.metadata.canic]
fleet = "project_hub_stub"
role = "project_hub"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = { workspace = true }
canic = { workspace = true, features = ["auth-delegated-token-verify", "auth-root-canister-sig-verify", "metrics"] }
ic-cdk = { workspace = true }

[build-dependencies]
canic = { workspace = true, features = [] }

[lints]
workspace = true
