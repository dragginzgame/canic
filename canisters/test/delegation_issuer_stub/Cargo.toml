[package]
name = "delegation_issuer_stub"
edition = { workspace = true }
version = { workspace = true }
publish = false

[package.metadata.cargo-machete]
# `candid::Deserialize` expands against the destination crate's direct `serde`
# dependency.
ignored = ["serde"]

[package.metadata.canic]
app = "delegation_root_stub"
role = "issuer"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = { workspace = true }
canic = { workspace = true, features = ["auth-delegated-token-verify", "auth-issuer-canister-sig-create", "auth-root-canister-sig-verify", "internal-test-fixtures"] }
ic-cdk = { workspace = true }
serde = { workspace = true }

[build-dependencies]
canic = { workspace = true, features = [] }

[lints]
workspace = true
