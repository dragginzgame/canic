[package]
name = "delegation_root_stub"
edition = { workspace = true }
version = { workspace = true }
publish = false

[package.metadata.canic]
fleet = "delegation_root_stub"
role = "root"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = { workspace = true }
canic = { workspace = true, features = ["auth-chain-key-root-sign", "auth-delegated-token-verify", "auth-root-canister-sig-create", "control-plane"] }
canic-control-plane = { workspace = true, features = [
    "root-control-plane",
    "wasm-store-canister",
] }
ic-cdk = { workspace = true }
sha2 = { workspace = true }

[build-dependencies]
canic = { workspace = true }

[lints]
workspace = true
