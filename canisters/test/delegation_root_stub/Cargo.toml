[package]
name = "delegation_root_stub"
edition = { workspace = true }
version = { workspace = true }
publish = false

[package.metadata.canic]
fleet = "delegation_root_stub"
role = "root"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = { workspace = true }
canic = { workspace = true, features = ["auth-chain-key-root-sign", "auth-delegated-token-verify", "auth-root-canister-sig-create", "auth-root-canister-sig-verify", "control-plane", "metrics"] }
ic-cdk = { workspace = true }
sha2 = { workspace = true }

[build-dependencies]
canic = { workspace = true, features = [] }

[lints]
workspace = true
