[package]
name = "delegation_root_stub"
edition = { workspace = true }
version = { workspace = true }
publish = false

[package.metadata.canic]
app = "delegation_root_stub"
role = "root"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = { workspace = true }
canic = { workspace = true, features = ["auth-chain-key-root-sign", "auth-root-canister-sig-create", "control-plane"] }
ic-cdk = { workspace = true }
ic-timers = { workspace = true }

[build-dependencies]
canic = { workspace = true, features = [] }

[lints]
workspace = true
