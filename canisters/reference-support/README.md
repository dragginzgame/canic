# canic-reference-support

Workspace-only helpers and fixtures used by the Canic reference canisters and
integration scaffolding.

This crate is intentionally stored under `canisters/` because it is reference
app support code, not product runtime published from `crates/`.
