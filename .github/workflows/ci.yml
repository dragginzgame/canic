name: CI

env:
  CANIC_MSRV: 1.91.0
  CANIC_INTERNAL_TOOLCHAIN: 1.95.0
  POCKET_IC_VERSION: 13.0.0

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:

jobs:
  msrv:
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.CANIC_MSRV }}
      - name: Install wasm target
        run: rustup target add --toolchain ${{ env.CANIC_MSRV }} wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - name: Check workspace MSRV
        run: cargo check --workspace --locked

  checks:
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.CANIC_INTERNAL_TOOLCHAIN }}
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install fmt tools
        run: cargo install cargo-sort cargo-sort-derives --locked
      - name: Install wasm target
        run: rustup target add wasm32-unknown-unknown
      - name: Install canister build tools
        run: cargo install --locked candid-extractor ic-wasm
      - name: Install dfx
        uses: dfinity/setup-dfx@main
      - name: Check dfx
        run: dfx --version
      - name: Start local dfx
        run: dfx start --background --clean --system-canisters
      - name: Cache PocketIC server
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/pocket-ic-server-${{ env.POCKET_IC_VERSION }}
          key: pocket-ic-${{ env.POCKET_IC_VERSION }}-${{ runner.os }}
      - name: Install PocketIC server
        shell: bash
        run: |
          BIN="$(bash scripts/ci/install-pocketic.sh "$POCKET_IC_VERSION")"
          echo "POCKET_IC_BIN=$BIN" >> "$GITHUB_ENV"
      - name: Build canister wasm artifacts
        shell: bash
        run: bash scripts/ci/build-ci-wasm-artifacts.sh
      - name: Layering guards
        shell: bash
        run: bash scripts/ci/run-layering-guards.sh
      - name: Forbidden crypto API guards
        shell: bash
        run: bash scripts/ci/run-forbidden-crypto-guards.sh
      - run: make fmt-check
      - run: make clippy
      - run: make test-unit
      - name: Build examples (default)
        run: cargo build -p canic --examples --locked

  tag_checks:
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          clean: true
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.CANIC_INTERNAL_TOOLCHAIN }}
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install fmt tools
        run: cargo install cargo-sort cargo-sort-derives --locked
      - name: Install wasm target
        run: rustup target add wasm32-unknown-unknown
      - name: Install canister build tools
        run: cargo install --locked candid-extractor ic-wasm
      - name: Install dfx
        uses: dfinity/setup-dfx@main
      - name: Check dfx
        run: dfx --version
      - name: Start local dfx
        run: dfx start --background --clean --system-canisters
      - name: Cache PocketIC server
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/pocket-ic-server-${{ env.POCKET_IC_VERSION }}
          key: pocket-ic-${{ env.POCKET_IC_VERSION }}-${{ runner.os }}
      - name: Install PocketIC server
        shell: bash
        run: |
          BIN="$(bash scripts/ci/install-pocketic.sh "$POCKET_IC_VERSION")"
          echo "POCKET_IC_BIN=$BIN" >> "$GITHUB_ENV"
      - name: Build canister wasm artifacts
        shell: bash
        run: bash scripts/ci/build-ci-wasm-artifacts.sh
      - name: Forbidden crypto API guards
        shell: bash
        run: bash scripts/ci/run-forbidden-crypto-guards.sh
      - run: make fmt-check
      - run: make clippy
      - run: make test-unit
      - run: cargo build --release --workspace --locked
