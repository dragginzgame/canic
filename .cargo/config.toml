[build]
# Keep target outputs sandbox-friendly and on the workspace filesystem.
target-dir = "target/local"

[env]
# Keep Cargo's temp dir on the same filesystem as target-dir to avoid cross-device link errors.
CARGO_TARGET_TMPDIR = { value = "target/local/tmp", relative = true }
