disallowed-methods = [
  # Avoid bypassing ops-level observability by calling raw cdk APIs directly.
  { path = "canic_core::cdk::call::Call::unbounded_wait", reason = "Use `canic_core::ops::call::Call` so ICC calls are captured in metrics." },
  { path = "canic::cdk::call::Call::unbounded_wait", reason = "Use `canic::core::ops::call::Call` so ICC calls are captured in metrics." },

  { path = "canic_core::cdk::timers::set_timer", reason = "Use `canic_core::ops::timer::TimerOps::set` so scheduled timers are captured in metrics." },
  { path = "canic::cdk::timers::set_timer", reason = "Use `canic::core::ops::timer::TimerOps::set` so scheduled timers are captured in metrics." },

  { path = "canic_core::cdk::timers::set_timer_interval", reason = "Use `canic_core::ops::timer::TimerOps::set_interval` so scheduled timers are captured in metrics." },
  { path = "canic::cdk::timers::set_timer_interval", reason = "Use `canic::core::ops::timer::TimerOps::set_interval` so scheduled timers are captured in metrics." },

  { path = "canic_core::cdk::mgmt::create_canister_with_extra_cycles", reason = "Use `canic_core::ops::ic::create_canister` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::create_canister_with_extra_cycles", reason = "Use `canic::core::ops::ic::create_canister` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::canister_status", reason = "Use `canic_core::ops::ic::canister_status` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::canister_status", reason = "Use `canic::core::ops::ic::canister_status` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::deposit_cycles", reason = "Use `canic_core::ops::ic::deposit_cycles` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::deposit_cycles", reason = "Use `canic::core::ops::ic::deposit_cycles` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::install_code", reason = "Use `canic_core::ops::ic::install_code` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::install_code", reason = "Use `canic::core::ops::ic::install_code` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::uninstall_code", reason = "Use `canic_core::ops::ic::uninstall_code` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::uninstall_code", reason = "Use `canic::core::ops::ic::uninstall_code` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::delete_canister", reason = "Use `canic_core::ops::ic::delete_canister` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::delete_canister", reason = "Use `canic::core::ops::ic::delete_canister` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::update_settings", reason = "Use `canic_core::ops::ic::update_settings` so mgmt calls are captured in metrics." },
  { path = "canic::cdk::mgmt::update_settings", reason = "Use `canic::core::ops::ic::update_settings` so mgmt calls are captured in metrics." },

  { path = "canic_core::cdk::mgmt::http_request", reason = "Use `canic_core::ops::http` helpers so outcalls are captured in metrics." },
  { path = "canic::cdk::mgmt::http_request", reason = "Use `canic::core::ops::http` helpers so outcalls are captured in metrics." },

  # Same guardrails for direct SDK imports (bypasses `canic::cdk`).
  { path = "ic_cdk::call::Call::unbounded_wait", reason = "Use `canic_core::ops::call::Call` so ICC calls are captured in metrics." },
  { path = "ic_cdk::management_canister::create_canister_with_extra_cycles", reason = "Use `canic_core::ops::ic::create_canister` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::canister_status", reason = "Use `canic_core::ops::ic::canister_status` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::deposit_cycles", reason = "Use `canic_core::ops::ic::deposit_cycles` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::install_code", reason = "Use `canic_core::ops::ic::install_code` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::uninstall_code", reason = "Use `canic_core::ops::ic::uninstall_code` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::delete_canister", reason = "Use `canic_core::ops::ic::delete_canister` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::update_settings", reason = "Use `canic_core::ops::ic::update_settings` so mgmt calls are captured in metrics." },
  { path = "ic_cdk::management_canister::http_request", reason = "Use `canic_core::ops::http` helpers so outcalls are captured in metrics." },

  { path = "ic_cdk_timers::set_timer", reason = "Use `canic_core::ops::timer::TimerOps::set` so scheduled timers are captured in metrics." },
  { path = "ic_cdk_timers::set_timer_interval", reason = "Use `canic_core::ops::timer::TimerOps::set_interval` so scheduled timers are captured in metrics." },
]
